あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-31) です。
旧 mjc-main (= 旧 mjc-main-20260504-30) から Loop 1 完了 + Loop 2 worker 起動失敗の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 21900+ 行 / 1.86MB+ あるので `tail -350 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~22:10 JST] mjc-main-20260504-30 状況メモがあります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~22:10 JST 時点)

## 現在の git 状態
- branch: main, ahead 150 (origin/main から先行、本セッション +1 commit のみ)
- working tree: docs/handoff/* と docs/worker-prompts/* untracked 多数
- 旧 mjc-main-20260504-30 が canonical 名 mjc-main を持ち、本セッションで Loop 1 + 1 SESSION SUMMARY を完了:
  1. `67be552` test(secret_store): SecretKey enum の Debug 出力 / Copy trait / kebab-case format 軸で混合 application 拡張 3 軸 (Loop 1, +3 件 → 563 passed) — 「混合 application」パターン 3 種混合へ拡張
  2. AGENT_LOG.md 末尾に SESSION SUMMARY 追記

## 現在の品質状態
- cargo test (lib): **563 passed / 0 failed** (前 560 → +3 件)
- cargo clippy default lint: **警告ゼロ**
- cargo clippy --lib -- -D warnings: **警告ゼロ** (Loop 1 で維持)
- npm run build (tsc + vite build): 本セッションで frontend 触れていないため変更なし

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-30、handoff 直前)
- mjc-main-20260504-31 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260504-31` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-31 mjc-main` を実行 (前 mjc-main-25/26/27/28/29/30 でも初回確認が必要だった precedent あり、冪等)。
5. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-30) を `tail -250 AGENT_LOG.md` で読み、確立パターン + 重要発見 + 次ループ候補を確認**
6. **最初の Loop は Loop 2 引継ぎ (apple_speech.rs)**: prompt 作成済 = `docs/worker-prompts/mjc-worker-apple-speech-meta-symmetric-tests-20260504-30-2.txt`
7. **ただし Loop 2 worker 起動問題 (claude プロセスが起動した形跡なし、pane 完全空白) を最初に切り分ける**
   - 切り分け手順: 小さい prompt (例: 1KB 程度) で `bash scripts/claude-agent-start-worker.sh mjc-worker-test-startup-31 /tmp/test-prompt.txt` を試し、claude が正常起動するか確認
   - 起動成功なら既存の apple_speech prompt を再試行
   - 起動失敗なら原因切り分け: claude binary / API rate limit / quota issue / tmux PATH 解決問題
8. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本 Loop 1)

#### 1. 「対称的補強の 3 軸構造」パターン (10 連続セッション目で application)
- 全セッション (mjc-main-22/23/24/25/26/27/28/29/30) で全 25 ループに application

#### 2. 「混合 application」パターン (3 種混合へ拡張)
- 系譜: 2 種混合 (フィールド+型, mjc-main-29 Loop 1) → 3 種混合 (Debug+trait+format, mjc-main-30 Loop 1)
- 1 ループで 3 つの異なる軸 (異なる抽象レベル) を束ねる効率パターン
- application 候補: 副作用境界を持つ enum 中心ファイル / 機密ロジックを含むモジュール

#### 3. 「Debug 軸補強」パターン新規 application 開始
- enum の `#[derive(Debug)]` 自動派生による「フィールドなし enum は variant 名のみ出力」契約の executable specification 化
- mjc-main-30 Loop 1 で SecretKey enum に application
- 後続候補: 他の `#[derive(Debug)]` 派生型
  - apple_speech.rs AppleSpeechEngine (unit struct) ← Loop 2 引継ぎ prompt 作成済
  - session.rs SessionSegment / Session (struct with fields) ← Loop 3 候補
  - transcription.rs の 6 type ← Loop 候補

#### 4. 「format 不変条件 application」パターン (拡張追従型 test 設計)
- for ループで全 variant 走査 → 将来 variant 追加時の format 規則違反を自動検知
- mjc-main-30 Loop 1 T10 (kebab-case 規則 全 variant 適用)

#### 5. 「対称軸補強パターンの 5 特殊形」 (型/経路/フィールド/層/メタ) は mjc-main-29 で出揃った
- 本 Loop 1 は「混合度」軸を新たに開拓 (5 特殊形と相補的)

### 重要な技術的注意点 (本セッション分のみ抜粋)

#### 1. handoff サマリのファイル名誤認 (mjc-main-29 SUMMARY)
- mjc-main-29 SUMMARY が示した「browser_detection.rs / commands.rs」は **実在しない**
- 実際は app_detection.rs (2263 行) / lib.rs (396 行) の誤認
- **後続セッション (あなた含む) は候補ファイル名を必ず実在確認**

#### 2. 全候補ファイルの密度分析手法 (本 Loop 1 で確立)
- `wc -l <file>` + `grep -c '^\s*#\[test\]' <file>` + `grep -cE '^\s*pub (async )?fn' <file>` で test/pub_fn 比率算出
- secret_store.rs (0.6 test/fn) を最低密度として特定
- 後続セッションも同手法で候補絞り込み可

#### 3. Keychain 副作用境界制約下での純粋ロジック軸 application (本 Loop 1 で実証)
- 「Keychain 実通信禁止」制約下で `#[derive]` 由来 trait 実装と enum format 規則 = 純粋ロジック軸だけで 3 軸を確保
- 副作用境界を持つ他モジュール (権限/認可) でも同手法 application 可能

#### 4. **worker 起動失敗の切り分け課題 (Loop 2、最重要)**
- `bash scripts/claude-agent-start-worker.sh mjc-worker-apple-speech-meta-symmetric-tests-20260504-30-2 docs/worker-prompts/mjc-worker-apple-speech-meta-symmetric-tests-20260504-30-2.txt` を 2 回実行したが、tmux session は作成されるが claude プロセスが見えない / pane 完全空白 / ログ 0 byte / ~5 分待っても変化なし
- claude binary は v2.1.126 動作確認済
- prompt サイズ (10122 byte) は Loop 1 (9763 byte) と僅差
- **後継セッションは小さい prompt で worker 起動テストを最初に行うことを推奨**:
  - 例えば `/tmp/test-prompt.txt` に「`echo hello && exit 0`」のような 100 byte 程度の最小 prompt を書き、`bash scripts/claude-agent-start-worker.sh mjc-worker-test-startup /tmp/test-prompt.txt` で起動 → 30 秒以内に出力があれば worker 起動 OK
  - もし起動 OK なら apple_speech prompt の特定箇所が原因 = prompt を分割して切り分け
  - もし起動 NG なら別の原因 (claude binary / API rate limit / tmux 環境)

#### 5. AGENT_LOG.md 構造の問題 (継承、優先度低)
- AGENT_LOG.md 全体は **21900+ 行** で構造的に整理が必要 = 別 Loop 候補 (低優先)

## 次ループ候補 (優先順位順、本セッションで未着手)

### 最優先 = Loop 2 引継ぎ: apple_speech.rs (314 行 / 9 test / 3 pub_fn = 3.0、prompt 作成済)
- prompt: `docs/worker-prompts/mjc-worker-apple-speech-meta-symmetric-tests-20260504-30-2.txt`
- 内容: メタ対称軸補強パターン 3 軸
  - T10 = `apple_speech_engine_new_returns_exact_japanese_error_message_on_non_macos_build` (cfg Linux only, error message 完全一致)
  - T11 = `language_to_locale_returns_values_always_in_bcp47_pattern_for_supported_and_fallback_inputs` (BCP-47 format 不変条件)
  - T12 = `apple_speech_engine_debug_output_uses_struct_name_without_internal_field_leakage` (unit struct Debug 軸)
- worker 起動が成功すれば即時 application 可
- worker 起動失敗が続くなら別ファイル (session.rs 等) へ切替

### Loop 3 候補: session.rs (503 行 / 18 test / 5 pub_fn)
- SessionSegment / Session 両方とも `#[derive(Debug, Clone, Serialize, Deserialize)]` 派生
- 「Debug 軸補強」を struct with fields に拡張 application 候補
- T1 = SessionSegment Debug 出力でフィールド名 (text / start_ms / end_ms) が含まれる
- T2 = Session Debug 出力でフィールド名 (id / started_at / title / segments / ended_at) が含まれる
- T3 = Debug 派生 + Clone 派生の同時保護 (clone 後 Debug 出力が同一)

### B 候補: transcription.rs (2257 行 / 58 test / 21 pub_fn = 2.8、関数別密度に偏り想定)
- 6 つの type で `#[derive(Debug)]` 派生 (l.19, 27, 42, 50, 64, 1041) = Debug 軸補強候補
- 大規模ファイル = Read 全体禁止、grep で関数別に密度確認必須

### audio.rs / system_audio.rs / markdown.rs (補強候補限定的)

### S 候補継続 (settings.rs 残関数、規模 M, 統合候補あり)
- `default_output_directory` / `update_settings` / `check_*_permission` の境界
- `MEETING_INACTIVE_THRESHOLD = 600s` の settings 統合 (mjc-main-20 SUMMARY 既明示、未消化)

### F-Loop6 (継承、規模 M, 価値中) = タイマースレッド shutdown 対応 (mjc-main-21 由来、未着手)

### Realtime/Whisper 系 (規模 S-M、複数ファイル候補)
- cloud_whisper.rs, openai_realtime.rs, elevenlabs_realtime.rs の error path 残境界

### K (継承). clippy::pedantic の選択的有効化 (規模 L, 非優先)

## 検証制約 (再掲)
- cmake あり → cargo test 563 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: **コミットメッセージはファイル経由が安全** (fish shell parse error 回避):
  - `/tmp/mjc-commit-msg-loopN.txt` に **Read → Write の 2 ステップ** でメッセージを書く (初回も既存ファイル想定で先に Read 必須、touch でファイル作成後に Read)
  - `git add <files> && git commit -F /tmp/mjc-commit-msg-loopN.txt`
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-31 mjc-main` (現セッション名が `mjc-main` でない場合のみ実行)
- worker 完走確認: **agent-tail-output.sh より直接 cat の方が確実** (mjc-main-26 Loop 2 教訓)
  - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/logs/agent/<session>.txt | tail -50`
- worker 起動 health check: 小さい prompt で `bash scripts/claude-agent-start-worker.sh mjc-worker-test-startup /tmp/test-prompt.txt` を最初に試行 (Loop 2 起動問題切り分け用)

## worker prompt 必須要素 (10 連続セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」** = 末尾追記の場所を視覚的に学習させる
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」** を明記
3. **「先頭は絶対に触らない」** を明記
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照** を明記
7. **「行末の空白文字 (trailing whitespace) 禁止」** を明記

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (mjc-main-30 Loop 1 ~10 分達成、worker 起動問題で本セッション全体時間 ~30 分超過)
- worker 1 件 ≒ 1 コミット
- 累積 worker 完走 75/75 (mjc-main-30 Loop 1 まで、Loop 2 は起動失敗で完走未達)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる
- 旧 mjc-main-30 では Loop 1 完了時点 ~70-75% で予防的ハンドオフ判断 (Loop 2 worker 起動問題で連続失敗するなら context を消費せず handoff したほうが効率的)

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge も本セッション中ゼロ。

## 累積成果 (本セッション)
- **テスト 560 → 563 passed** (+3 件、Loop 1 のみ)
- **コミット 1 件 + 本 SUMMARY 1 件**
- **clippy --lib -D warnings ゼロ維持** (default + -D warnings)
- **「混合 application」パターン 2 種混合 → 3 種混合へ拡張**
- **「Debug 軸補強」パターン新規 application 開始** (Loop 1 で SecretKey enum)
- **canonical 名移譲完了** (mjc-main-20260504-30 → mjc-main, 5 セッション連続適用)
- **累積 worker 完走 75/75** (100% 維持)
- **Loop 2 worker 起動失敗 → 後継セッションへ切り分け委譲**

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -350 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260504-31` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-31 mjc-main` を実行 (冪等)。
4. **worker 起動 health check**: 小さい prompt で `bash scripts/claude-agent-start-worker.sh mjc-worker-test-startup-31 /tmp/test-prompt.txt` を試行
   - 30 秒以内に出力があれば worker 起動 OK → 既存 apple_speech prompt 再試行
   - 起動失敗なら別の原因切り分け
5. **Loop 2 を再開**: `docs/worker-prompts/mjc-worker-apple-speech-meta-symmetric-tests-20260504-30-2.txt` を再起動 (worker 起動 OK 確認後)
6. 待機モード禁止。final answer で停止せず、改善ループを継続する。
7. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
8. **worker 完走確認は cat <log_file> | tail -50 を直接使う** (agent-tail-output.sh より確実)
