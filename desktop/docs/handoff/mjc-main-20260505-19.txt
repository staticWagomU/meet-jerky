あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-20) です。
旧 mjc-main (= mjc-main-20260505-19) から **2 ループ完走 + transcription-refactor-plan.md 更新 (Loop 37 = docs 軸 = Loop 35 + 36 反映で 60% 里程標突破を docs 化、~3 分) + transcription_types.rs 関連テスト 10 件移動 (Loop 38 = test 移動軸 = TranscriptionSegment 4 + TranscriptionSource 3 + ModelInfo 3 = ~426 行 一括 = Loop 35 15 件 290 行 precedent 1.4 倍規模、~8 分) + transcription.rs **75% 縮小達成** (元 2999 → 749 行 = -426 行/-2250 行累計、本セッションで 60.8% から 75.0% に +14.2 pt 進展、**75% 里程標突破**) + 「2 ループ + 早期 handoff」precedent 連続 16 セッション目達成 + worker 完走 2/2 (累計 138/138 100% 維持) + harness silent fail mitigation pattern 連続 62 ループ実証達成 + canonical 移譲後 scripts/* M 再出現せず観測 (連続 13 セッション目で結論超強化) + sonnet worker の自律 Tidy First 質的高さ連続 16 セッション目で実証 (Loop 37 で docs 更新 + AGENT_LOG.md hash 反映 を 2-commit precedent 標準パターンで自走、Loop 38 で 10 件 ~426 行 一括移動 + use 文整理 + 不要 import 削除 + cargo test/clippy/fmt 自走) + コミット周期 平均 ~5.5 分で目標 15 分以内大幅達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-4 完了 + Phase 3-B 完全完了 + Whereby 抽出 ✅ 完了 + GoToMeeting 抽出 ✅ 完了 + TranscriptionLoopConfig struct 移動 ✅ 完了 + transcription_error_payload tests 移動 ✅ 完了 = mjc-main-20260505-19 Loop 37 で 60.8% 縮小まで反映済、本セッション Loop 38 (75% 突破) は未反映 = Loop 41+ 候補 G で更新推奨)**
- AGENT_LOG.md は ~26,400 行 / ~2.2MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~08:05 JST] mjc-main-20260505-19 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4**: 「stdin redirect 起動」precedent 創出 + 「2 ループ + 早期 handoff」precedent 強化 + Whereby URL 検知 base + Phase 2-A 着手
- **mjc-main-20260505-5**: 「stdin redirect 化を script に焼き付け」 = harness 本体改善 + 「ファイル参照型 handoff prompt」精度継承 + Phase 2-B ModelManager 抽出完了
- **mjc-main-20260505-6**: Phase 2-A WhisperLocal 抽出 + Audio resampling 統合、harness 衛生 commit (`bfb9846` PATH inner shell escape = 永続的解決)
- **mjc-main-20260505-7**: Phase 2-A 完全完了 (WhisperStream) + Whereby blacklist marketing pages 4 件追加 (variety pivot)
- **mjc-main-20260505-8**: Phase 3 TranscriptionManager + Phase 4-A emit_segments 抽出 (Loop 15 + 16)
- **mjc-main-20260505-9**: Phase 4-B error_payload 5 関数抽出 + GoToMeeting URL 検知 (Loop 17 + 18)
- **mjc-main-20260505-10**: Phase 4-C panic_guard + Phase 4-D run_transcription_loop = Phase 4 全体完了
- **mjc-main-20260505-11**: Phase 3-B 前段準備 (Loop 21) + GoToMeeting legacy URL 検知 (Loop 22)
- **mjc-main-20260505-12**: Phase 3-B 続き = list_models + is_model_downloaded 抽出 + download_model 抽出 = transcription.rs ~32.2% 縮小
- **mjc-main-20260505-13**: Phase 3-B 続き = validate + parse + 17 tests 移動 (Loop 25) + GoToMeeting アプリ launcher 検知 (Loop 26) = ~42.9% 縮小
- **mjc-main-20260505-14**: Phase 3-B (Tauri commands) **完全完了** = stop_transcription + start_transcription + PendingTranscriptionStream 抽出 = ~48.8% 縮小達成
- **mjc-main-20260505-15**: app_detection.rs Webex モジュール抽出 (Loop 29) + transcription-refactor-plan.md 更新 (Loop 30)
- **mjc-main-20260505-16**: app_detection.rs Whereby モジュール抽出 (Loop 31) + resample_audio テスト 4 件 audio_utils.rs 移動 (Loop 32) = ~50.3% 縮小達成 (50% 里程標突破)
- **mjc-main-20260505-17**: TranscriptionLoopConfig struct を transcription_worker_loop.rs に移動 (Loop 33) + transcription-refactor-plan.md 更新 (Loop 34) = ~50.5% 縮小
- **mjc-main-20260505-18**: transcription_error_payload 関連テスト 15 件移動 (Loop 35) + app_detection.rs GoToMeeting モジュール抽出 (Loop 36) = ~60.8% 縮小達成 (60% 里程標突破)
- **mjc-main-20260505-19 (本セッション)**: **transcription-refactor-plan.md 更新 (Loop 37 = docs 軸、Loop 35 + 36 反映)** + **transcription_types.rs 関連テスト 10 件移動 (Loop 38 = test 移動軸 = TranscriptionSegment 4 + TranscriptionSource 3 + ModelInfo 3 = ~426 行)** = transcription.rs **75.0% 縮小達成** (75% 里程標突破)

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **本セッション Loop 38 = 「TranscriptionSegment + TranscriptionSource + ModelInfo の 10 件バンドル判断」 = 単独 3 ループ分割 (sweep 3 件) より 1 ループバンドル (sweep 1 件) が variety 規則的に圧倒的有利と批判判断**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「Whereby に戻って 2 連続にしない」** (Loop 14 の sweep 化しない約束を継承、最低 Loop 36+ 程度の間隔)
- **「GoToMeeting に戻って 2 連続にしない」** (Loop 22 + 26 + 36 = 4-10 ループ間隔。次回再訪は最低 Loop 39+ 程度の間隔)
- **「app_detection.rs サービス別抽出 = sweep 化しない」** (Webex 29 + Whereby 31 + GoToMeeting 36 で 3 件、Zoom/Teams 抽出は Loop 40+ 推奨 = sweep 警告ライン超過)
- **「テスト移動軸の sweep 化しない」** (Loop 32 + Loop 35 + Loop 38 で 3 ループ間隔ずつ、Loop 39 で続行は連続違反警告 = 別軸必須)
- **「struct 移動軸の sweep 化しない」** (Loop 33 = 1 件目、Loop 39+ で続行は 6 ループ間隔 = OK だが対象限定的)
- **「docs 軸の sweep 化しない」** (Loop 30 + 34 + 37 = 3-4 ループ間隔、Loop 39 で実施は 2 ループ間隔 = 連続違反警告境界、Loop 40+ 推奨)

# 引き継ぎサマリ (2026-05-05 ~08:05 JST 時点)

## 現在の git 状態
- branch: main, ahead ~254 (origin/main から先行、本セッション +5 commit = d6626ed + 7f31ec0 + 64fe9cd + 9f11c86 + handoff chore)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-19) の commit:
  1. `d6626ed` docs(transcription-refactor-plan): Loop 35 transcription_error_payload tests 移動 + Loop 36 GoToMeeting 抽出を反映 (Loop 37, ~3 分)
  2. `7f31ec0` chore(agent-log): Loop 37 commit hash d6626ed を AGENT_LOG.md に記入
  3. `64fe9cd` refactor(transcription): transcription_types 関連テスト 10 件 (Segment 4 + Source 3 + ModelInfo 3) を transcription_types.rs に移動 (Loop 38, ~8 分)
  4. `9f11c86` chore(agent-log): Loop 38 commit hash 64fe9cd を AGENT_LOG.md に記入
  5. `<chore commit>` chore(agent-log): mjc-main (mjc-main-20260505-19) の 2 ループ実績 SESSION SUMMARY (本ハンドオフ commit、起動時に最新 hash を確認)

## 現在の品質状態
- cargo test (lib): **700 passed / 0 failed** (本セッション Loop 37 + 38 で件数不変 = 振る舞い不変保証、Loop 38 完走後の cargo test --lib で実数確認済)
- cargo clippy --lib --tests -- -D warnings: **警告ゼロ**
- cargo fmt --check: OK
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK (escape 済み形式、本セッションでは触れていない)

## 完成した Loop 37 (transcription-refactor-plan.md 更新 = docs 軸 variety pivot)
- 反映対象: mjc-main-20260505-18 Loop 35 (transcription_error_payload tests 15 件移動 -311 行) + Loop 36 (GoToMeeting 抽出 -47 行)
- 進捗サマリ更新 (50.5% → 60.8%)、累計削減 1486 → 1175 行
- 新サブセクション 2 件追加: GoToMeeting 抽出 ✅ 完了 (Loop 36 = 1904e04) + transcription_error_payload tests 移動 ✅ 完了 (Loop 35 = dde8175)
- L153 文章書き換え: Webex/Whereby/GoToMeeting 完了、Zoom/Teams 残

## 完成した Loop 38 (transcription_types.rs 関連テスト 10 件移動 = test 移動軸)
- transcription.rs 1175 → 749 行 (-426 行 = 10 件削除 + use 文整理 + ModelInfo unused import 削除)
- transcription_types.rs 41 → 471 行 (+430 行 = mod tests 新設 + 10 件 + use super::*)
- 移動内容:
  - ブロック A (TranscriptionSegment 4 件): test_transcription_segment_serialization / transcription_segment_debug_output_contains_struct_name_all_six_field_names_and_values / transcription_segment_debug_output_equals_after_clone_for_some_and_none_variants / transcription_segment_serde_serializes_with_camel_case_field_names_and_skips_none_options
  - ブロック B (TranscriptionSource 3 件): transcription_source_debug_output_contains_each_variant_name_per_variant / transcription_source_copy_trait_keeps_original_usable_after_assignment / transcription_source_serde_serializes_each_variant_with_snake_case_value
  - ブロック C (ModelInfo 3 件): model_info_debug_output_contains_struct_name_and_all_four_field_names / model_info_clone_produces_independent_copy_for_string_fields_and_size_mb / model_info_serialize_uses_camel_case_for_all_four_fields

## 累計 transcription.rs 削減 (Phase 1〜4 完了 + Phase 3-B 完全完了 + Loop 33 struct 移動 + Loop 35 + 38 test 移動)
- 元 2999 行 → 現在 **749 行** = **~75.0% 縮小** (~2250 行削減) = **「75% 里程標突破」**
- 残存 749 行のうち多くは worker_loop / Whisper エンジン関連 helper / 互換 re-export 6 種

## 累計 app_detection.rs 削減
- 元 3356 行 (Loop 29 開始時) → 現在 3184 行 (-172 行累計、Webex/Whereby/GoToMeeting 抽出による、Loop 38 では未変更)

## harness 衛生事象 (本セッションで観測継続 = 連続 13 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-19 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 前任 mjc-main-20260505-7 〜 18 の連続 12 セッション目観測結論を **連続 13 セッション目で追認** = 結論超強化、永続的解決確定 (`bfb9846` PATH inner shell escape が永続的解決)
- 後継観測ポイント: 新たな harness 動作変化があれば観測継続

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-19、handoff 直前)
- mjc-main-20260505-20 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読 = mjc-main-20260505-19 Loop 37 で 60.8% まで反映済、本セッション Loop 38 (75% 突破) は未反映 = Loop 41+ 候補 G で更新推奨**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-20` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-20 mjc-main` を実行 (前 33 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (本セッション + 前 12 セッションで連続 13 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 14 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-19) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 37 + 38 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 5 ループ Loop 34 (docs) → 35 (test) → 36 (extraction) → 37 (docs) → 38 (test)、直近 docs 連続 0 (Loop 37 で 1 件、Loop 38 別軸)、extraction 連続 0、test 移動連続 1 (Loop 38 のみ)、struct 移動連続 0、Loop 39 で test 移動軸続行は連続違反警告 = 別軸必須**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 39 開始時点)

直近 5 ループ: Loop 34 (docs) → 35 (test) → 36 (extraction) → 37 (docs) → 38 (test)。
- **直近 docs 連続 = 0** (Loop 37 = 1 件、Loop 38 で別軸 pivot 済) - Loop 39 で実施は 2 ループ間隔 = 連続違反警告境界、Loop 40+ 推奨
- **直近 extraction 連続 = 0** (Loop 36 のみ、Loop 37/38 で別軸) - app_detection.rs サービス別抽出は sweep 警告超過 = Loop 40+ 推奨
- **直近 test 移動連続 = 1** (Loop 38 のみ、Loop 37 は docs) - **Loop 39 で続行は連続違反警告 = 別軸必須**
- **直近 struct 移動連続 = 0** (Loop 33 で実施、6 ループ間隔) - Loop 39 で OK だが対象限定的

### 候補 A (推奨 Loop 39): GoToMeeting TLD 拡張 (`app.goto.eu` / `app.goto.co.uk` 等) 調査 + 実装
- priority 2 直接寄与 (会議検知の網羅性向上)
- service detection 軸 = 直近 5 ループ全てと別軸 (純粋 variety pivot)
- URL 仕様調査が前段必要、ただし既存 is_goto_app_meeting_url の正規表現 + Read で base 拡張可能
- WebSearch を控えめに使う、既存実装の host 判定パターン拡張
- 規模 SS-S (3-5 関数 + 5-10 host pattern 追加 + tests)
- 1 ループ完結見込み

### 候補 B (Loop 39+): Discord stage / Slack Huddle 検知
- priority 2 直接寄与
- service detection 軸 = 直近 5 ループ全てと別軸
- URL pattern 調査が前段必要 = 1 ループ完結が不確実
- Discord stage = `discord.com/channels/<server-id>/<stage-id>` 形式? 要検証
- Slack Huddle = `app.slack.com/client/<workspace>/<channel-id>` 形式? 要検証

### 候補 C (Loop 39+): struct 移動軸続編 (TranscriptionStateHandle / WhisperStream の locality 改善)
- transcription.rs L26 で `pub use crate::transcription_manager::TranscriptionStateHandle;`
- transcription.rs L20 で `pub use crate::transcription_whisper_stream::WhisperStream;`
- これらは既に satellite ファイルに集約済 = 互換 re-export 維持のみ = struct 単独移動の余地は限定的
- 別 struct (例: PendingTranscriptionStream / RequestedTranscriptionSources) が transcription.rs 残存中なら候補
- 要 grep 確認

### 候補 D (Loop 40+): app_detection.rs Zoom 抽出
- sweep 警告超過 (Webex 29 + Whereby 31 + GoToMeeting 36 + Zoom 40 = 4 件目)
- Loop 40 で Zoom 抽出 = 4 ループ間隔、別軸を 2 ループ挟んだ後 (Loop 39 別軸 + Loop 40 Zoom)
- 別軸 pivot 確実な後の Loop 40+ 推奨

### 候補 E (Loop 41+): app_detection.rs Microsoft Teams 抽出
- D と同じ理由で Loop 41+ 推奨

### 候補 F (Loop 39+): WhisperStream / TranscriptionStream 関連テスト移動
- transcription.rs L102+ の test_whisper_stream_* / test_stream_lifecycle_* / test_stream_config_* / test_feed_empty_* / test_ensure_engine_*
- 移動先: transcription_whisper_stream.rs / transcription_traits.rs
- 規模 M (~30-60 件のテスト)、1 ループ完結が不確実
- **test 移動軸 = Loop 38 と連続違反警告** = Loop 39 では候補から除外推奨

### 候補 G (Loop 41+): docs 軸 = transcription-refactor-plan.md 再更新 (Loop 38 反映)
- Loop 37 で更新済 → Loop 38 反映が必要 (75% 里程標突破、749 行)
- ただし Loop 37 から 2 ループ間隔 = 連続違反警告境界
- Loop 41+ 推奨 (1 ループ間隔開けて)

### 候補 H: frontend 軸 pivot (主観的判断のリスクあり、慎重に選ぶ)
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり
- 浅 grep で具体的な不足を 1 つ特定してから worker 発注、主観的探索は避ける

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記との矛盾)

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- Webex sweep precedent 19 セッション維持を破る、Loop 42+ 程度まで間隔を空ける推奨

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 110+ untracked
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md ~26,400 行の archive 戦略
- 未着手、未消化

## 検証制約 (再掲)
- cmake あり → cargo test 700 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-20 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 62 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 M なら ~210-380 秒、規模 S 一括 426 行は ~480 秒程度 = Loop 38 実例)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - 規模極小タスクの場合: worker が agent-commit.sh で自走 commit する場合あり (本セッション Loop 37 + 38 とも自走 commit + commit hash 反映 chore commit も自走、`git log --oneline -5` で確認推奨)
  - **本セッション Loop 38 例**: 10 件 ~426 行 一括移動を ~8 分で完走 = sonnet worker の Tidy First 検証深さ実例 (Loop 17/19/20/24/25/28/29/31/32/33/35/36 precedent 学習、連続 16 セッション目)

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける** (4209 bytes でも too long エラー precedent あり)
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本セッションの起動 prompt と同パターン、連続 16 セッション目)

## worker prompt 必須要素 (62 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 37 = ~3 分、Loop 38 = ~8 分、平均 ~5.5 分で **目標 15 分以内大幅達成**)
- worker 1 件 ≒ 1 commit + commit hash 反映 chore commit 1 件 (本セッションは Loop 37 + Loop 38 ともに worker 自走で 4 commit、SUMMARY chore で +1 commit = 計 5 commit)
- 累積 worker 完走 138/138 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで早期ハンドオフ判断 = 引き継ぎ時の context (大量必読ドキュメント + grep + diff) を考慮した予防的判断 = 「2 ループ + 早期 handoff」precedent **連続 16 セッション目達成**

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中ゼロ (worker 完走が早く、待機時間が短かったため)。

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読 = 本セッション Loop 38 (75% 突破) は未反映、Loop 41+ 候補 G で更新推奨)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-20` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-20 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (本セッション + 前 12 セッションで連続 13 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 14 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「Whereby に戻って 2 連続にしない」「GoToMeeting に戻って 2 連続にしない」「app_detection.rs サービス別抽出 sweep 警告超過 = Loop 40+ で別軸 pivot 必須」「テスト移動軸の sweep 化しない = Loop 39 で続行は連続違反警告」「struct 移動軸の sweep 化しない」「docs 軸の sweep 化しない = Loop 39 で実施は 2 ループ間隔 = 連続違反警告境界」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「直近 docs 連続 0、extraction 連続 0、test 移動連続 1、struct 移動連続 0、Loop 39 で test 移動続行は連続違反警告 = 別軸必須」を踏まえ、Loop 39 で **GoToMeeting TLD 拡張** (service detection 軸 = 直近 5 ループ全てと別軸 = 純粋 variety pivot、規模 SS-S、確実性中) が候補 A だが **批判的に再評価**。代替案 = **Discord stage / Slack Huddle 検知** (URL 調査必要)、**struct 移動軸続編** (PendingTranscriptionStream 等が transcription.rs 残存中か grep)、**frontend 軸** (主観性リスク)。Loop 40 以降は variety pivot 必須なら別軸を選ぶ。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 62 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-380 秒、規模極小なら ~30-90 秒、規模 S 一括 426 行は ~480 秒、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 16 セッション目) で書く
