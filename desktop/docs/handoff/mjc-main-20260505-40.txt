あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-40) です。
旧 mjc-main (= mjc-main-20260505-39) から **2 ループ完走 + Loop 78 = 検知拡張 Q6 Resolved 化 = window title 日本語 localization 対応 Phase 2 英語 only + Phase 5 日本語追加の段階導入方針 ~3-4 分 + メイン批判判断 連続 30 セッション目達成 = handoff K1 を Section 5.5 既記載と整合確認 → Resolved Block 明示化に範囲限定 + AGENTS.md priority 2 直接寄与 + variety pivot で構造分離 4 連続到達回避 + Loop 79 = AGENT_LOG.md archive 戦略 plan 起こし = docs/architecture/agent-log-archive-plan.md (新規 140 行) 作成 = Section 1-7 構成 + 切り出し基準 3 option 比較 = session ベース推奨 + Phase 1-4 段階導入方針 + Open Questions Q1-Q4 ~4-5 分 + メイン批判判断 連続 31 セッション目達成 = handoff K2 を 3 option 比較 + Phase 完了基準機械化 + 実施別途承認の明示で objectivity 確保 + AGENTS.md L46-L52 自律改善方針寄与 + harness 衛生軸新軸導入 = 多軸分散維持 + 振る舞い不変 705 件 + worker 完走 2/2 累計 179/179 100% + 2 ループ + 早期 handoff precedent 連続 35 セッション目達成 + worker 自律 2-commit pattern 連続 20 ループ目達成 + 直近 4 ループ軸分散 = 構造分離 2 + 機能拡張/docs 1 + harness 衛生 1 = 警告境界 4 連続から十分離れた多軸分散 + 検知拡張 Q5 + Q6 Resolved 累計 2 件達成 = Phase 2 着手前準備の継続** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成 = transcription.rs 削除済 = 該当軸は今後アクション不要)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 Resolved + Q6 Resolved = mjc-main-20260505-39 Loop 78 で更新済、Q1/Q2/Q3/Q4/Q7/Q8 = Open のまま、Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (mjc-main-20260505-39 Loop 79 新規作成、Phase 1-4 段階導入方針、実施は別途ユーザー直伝指示要)**
- AGENT_LOG.md は ~30,500+ 行 / ~2.7MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-39 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 38**: Phase 1-6 + サービス別抽出シリーズ + 各種 test 移動 + struct 移動 + Phase 6 第 1-5 歩 + 最終 + app_detection scope シリーズ + 大型 rust file 責務分離 7 file 目達成 + 検知拡張 plan + Phase 1 + Q5 Resolved
- **mjc-main-20260505-39 (本セッション)**: **Loop 78 = 検知拡張 Q6 Resolved 化 (variety pivot 機能拡張/docs 軸) + Loop 79 = AGENT_LOG.md archive 戦略 plan 起こし (harness 衛生軸新軸導入) + メイン批判判断 連続 30-31 セッション目達成 + 多軸分散維持 (構造分離 2 + 機能拡張/docs 1 + harness 衛生 1)**

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価**
- **本セッション Loop 78-79 = pivot 後 2 連続 = Loop 80 は構造分離復帰または harness 衛生続行が自由度高い、ただし 80-82 で 3 連続到達なら 4 連続目で variety pivot 必要**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ 6 件完了」「transcription.rs リファクタ完全完遂」「大型 rust file 責務分離 = 7 file 目達成」「テスト移動軸 sweep 化警告超過状態」「docs 軸 = detection-extension-plan.md + Q5 + Q6 Resolved = sweep 化禁止」「検知拡張 Phase 1 + Q5 + Q6 完了 = Phase 2 は実機要件 Q1/Q2 未解決のため着手不可」「AGENT_LOG.md archive plan 起こし完了 = Phase 1 着手はユーザー直伝指示要」**

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~354 (origin/main から先行、本セッション +5 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-39) の commit:
  1. `079267f` docs(architecture): Q6 を Resolved 化 (Loop 78)
  2. `316ee9d` chore(agent-log): Loop 78 hash chore
  3. `ab85457` docs(architecture): AGENT_LOG.md archive 戦略 plan 起こし (Loop 79)
  4. `fc3c2cc` chore(agent-log): Loop 79 hash chore
  5. `f4f4b3a` chore(agent-log): SESSION SUMMARY (Loop 78-79 + handoff 判断)

## 現在の品質状態
- cargo test (lib): **705 passed / 0 failed** (件数完全不変、本セッション 2 ループとも docs only で維持)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ (前任維持、本セッション rust 触らず)
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK

## 大型 rust file 責務分離実績 (handoff 候補 X 始動以降の累計、本セッションは触れていない)

- audio.rs: 1011 行 → 949 行 (-62 行) + audio_traits.rs (24 行) + audio_sample_helpers.rs (47 行)
- session_store.rs: 1175 → 725 行 (-450 行累計、前任 mjc-main-20260505-38 で -408 行) + session_store_types.rs (19) + session_store_parse.rs (32) + session_store_render.rs (188) + session_store_list.rs (270)
- elevenlabs_realtime.rs / openai_realtime.rs: 各 ~5 行ずつ削減 + realtime_audio_command.rs (12 行)
- session_manager.rs: 1274 → 1245 行 (-29 行) + session_manager_types.rs (17) + session_manager_persist.rs (23) = standalone fn 0 件 = 構造美達成
- 残 (Loop 80+ 候補):
  - **session_store.rs 残 fn = path_for_session / write_session_markdown_to / save_session_markdown = 短小 = 抽出メリット薄い**
  - **elevenlabs+openai WS タスク本体共通化 (規模 M、shared module 設計判断要)** = X4
  - **app_detection.rs / audio.rs 残余地 = sweep 化警告超過状態**
  - **検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可**
  - **session_manager.rs impl method 内部 helper 抽出 = locality 反する可能性 + 抽出メリット 0 grep 確認済** = X5

## 検知拡張 plan 実装実績

- Phase 1 完了 (mjc-main-20260505-36 Loop 72)
- Q5 Resolved (mjc-main-20260505-37 Loop 74)
- **Q6 Resolved (本セッション Loop 78) = window title 日本語 localization 対応 = Phase 2 英語 only + Phase 5 日本語追加の段階導入方針**
- Q1/Q2/Q3/Q4/Q7/Q8 = 実機要件のため Open のまま据え置き
- Phase 2 = 未着手 (Q1/Q2 実機要件未解決)

## harness 衛生計画

- **AGENT_LOG.md archive 戦略 plan = 本セッション Loop 79 で起こし完了** (docs/architecture/agent-log-archive-plan.md, 140 行)
- Phase 1 = Archive Index Header 追加 (規模 SS、~5-10 分)
- Phase 2 = batch-001 切り出し ~25K 行 (規模 S、~10-15 分)
- Phase 1-2 着手はユーザー直伝指示要 (本 plan 内に「実施は別途承認」明記)

## harness 衛生事象 (本セッションで観測継続 = 連続 33 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-39 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 33 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern (refactor/docs + chore hash 反映)** は本セッション Loop 78/79 とも worker 自律実行 = 連続 20 ループ目 = 「session 跨ぎの暗黙的 precedent 継承」が完全に安定動作

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-39、handoff 直前)
- mjc-main-20260505-40 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全完遂済 = 該当軸は今後アクション不要**、**docs/architecture/detection-extension-plan.md は Phase 1 + Q5 + Q6 Resolved = Phase 2 は実機要件未解決のため着手不可**、**docs/architecture/agent-log-archive-plan.md は新規作成済 = Phase 1 着手はユーザー直伝指示要**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績、SESSION SUMMARY commit を含む)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-40` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-40 mjc-main` を実行 (前 54 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 33 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 34 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-39) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 78-79 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 13 ループで構造分離 8 件 + 機能拡張/docs 4 件 + harness 衛生 1 件 = Loop 80 は構造分離 1 連続 (Loop 76-77 で 2 連続後、Loop 78-79 で pivot 2 連続) からの自然な復帰または harness 衛生 2 連続到達 (= H1) も自由度内**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 80 開始時点)

直近 13 ループ: Loop 67 (session_store struct) → 68 (audio pure helper) → 69 (session_store pure fn) → 70 (realtime AudioCommand 共通化) → 71 (検知拡張 plan docs 新規) → 72 (検知拡張 Phase 1 rust impl) → 73 (session_manager struct extract) → 74 (検知拡張 Q5 docs Resolved) → 75 (session_manager fn extract) → 76 (session_store render extract) → 77 (session_store list extract) → 78 (検知拡張 Q6 docs Resolved) → 79 (AGENT_LOG.md archive plan)。
- 軸 pattern: 構造分離 (67/68/69/70/73/75/76/77 = 8 件) + 機能拡張/docs (71/72/74/78 = 4 件) + harness 衛生 (79 = 1 件) = 8:4:1 多軸分散
- scope 内訳: session_store (4) + audio (1) + realtime (1) + detection (4) + session_manager (2) + harness (1) = 多 scope 分散
- **Loop 80 = 自由度高い**。Loop 78-79 で pivot 2 連続後の構造分離復帰 (X4) が自然 / harness 衛生続行 (H1) もユーザー直伝指示があれば OK / 機能拡張/docs 復帰 (K3) は主観性警告で弱い

### 候補 X (Loop 80 推奨, 構造分離軸復帰 = 1 連続)

#### X4. elevenlabs+openai WS タスク本体共通化 Phase 1 (規模 M、shared module 設計判断要) ← 推奨度高
- 本セッションで grep 実態確認: elevenlabs_realtime.rs (1141 行) と openai_realtime.rs (1096 行) 両方とも依然大型
- WS task 構造: task_handle (L93/L75 spawn) + reader_task (L299/L229) + audio_rx.recv() ループ (L330/L260)
- AudioCommand 共通化は Loop 70 で realtime_audio_command.rs (12 行) で完了済 = 次の階層は task entry 共通化
- 規模 M = 1 ループ厳しい = Phase 1 = task entry skeleton 抽出のみに絞れば可能 (recv loop 内の共通エラーハンドリング or shutdown 処理が候補)
- WS protocol 差異 (handshake / send) は大きいので、共通化スコープを慎重に絞る
- 構造分離軸 1 連続復帰 (Loop 78-79 で機能拡張+harness pivot 後の自然な戻り)
- **メインが grep で 1 候補に絞ってから worker 発注、Phase 分割を明示的に worker prompt に書く**

#### X5. session_manager.rs impl method 内部 helper 抽出 (規模 SS-S、locality 反する可能性) ← skip 推奨
- 本セッションで grep 実態確認: impl SessionManager 内 standalone helper fn は L28 lock() (1 行) のみ
- 抽出メリット 0 = locality 反する判断確定
- 弱い候補 = skip 推奨

### 候補 H (Loop 80 alternative, harness 衛生続行 = 2 連続到達 ユーザー直伝指示要)

#### H1. agent-log-archive-plan.md Phase 1 着手 = AGENT_LOG.md に Archive Index Header 追加 (規模 SS、~5-10 分)
- 本セッション Loop 79 で plan 起こし完了 → Phase 1 着手判断
- ただし plan 内に「実施は別途ユーザー直伝指示要」と明示記載 = 後継メインが grep 確認後に exception 判断要
- harness 衛生軸 2 連続到達 = variety 警告境界より遠い (3 連続未満)

### 候補 K (Loop 80 弱推奨, 機能拡張/docs 軸復帰)

#### K3. 検知拡張 Phase 2 着手判定 plan 追記 (規模 SS、主観性高警告)
- Q1/Q2 実機要件のため Phase 2 着手不可状態
- 代替方針 = 「英語 pattern 推測値で Phase 2 着手 + 実装後 false negative/positive を log 経由で発見」 と「実機検証なしの推測 pattern 実装は禁止 = backlog 化」のいずれかを plan に明示記載
- 主観性高警告: ユーザー方針要 = 単独判断では弱い

### 候補 J (継承、低優先): frontend 軸

- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行)
- TODO/FIXME/any: 直近セッションで grep ゼロ = 主観性高警告
- 浅 grep で具体的不足を 1 つ特定してから worker 発注

### 候補 H' (継承、低優先): 大量 untracked 整理

- 大量 untracked ファイル整理判断 (`docs/handoff/`, `docs/worker-prompts/` に 280+ untracked、ユーザー直伝指示があるまで保留)

### 低優先 (継承)

#### B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
#### A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 705 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-40 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 100 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 SS なら ~3-8 分、規模 M なら ~7-12 分)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - **本セッション Loop 78 例**: docs 1 file 修正 + AGENT_LOG.md エントリ ~30 行 を ~3-4 分で完走 (規模 SS、worker prompt 135 行)
  - **本セッション Loop 79 例**: docs 1 file 新規 140 行 + AGENT_LOG.md ~30 行 を ~4-5 分で完走 (規模 SS、worker prompt 210 行)
  - **新規パターン 連続観測**: worker amend 時の hash 更新漏れに対し、worker 自身で chore commit を作成して補正する自律学習が Loop 60-79 の **20 ループ連続** で観測 = 完全に安定 pattern 化

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 36 セッション目)

## worker prompt 必須要素 (100 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-79 経験から)**:
- worker prompt に「2-commit pattern (refactor/docs commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を **明示的に** 記載 = 20 連続ループで完全安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~135-220 行で短縮効果あり (Loop 67-79 で実証 ~3-8 分完走)、規模 M は ~210-225 行を上限目安に

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 78 = ~3-4 分、Loop 79 = ~4-5 分、平均 ~4 分で **目標 15 分以内大幅達成**)
- worker 1 件 = 1-2 commit (本セッション Loop 78/79 とも 2-commit pattern = refactor/docs + chore hash 反映)
- 累積 worker 完走 179/179 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 35 セッション目達成

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 1 回 (handoff 直後の Loop 78 待機中、適切タイミングで自走中の確認 = 影響なし)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全完遂済 = 該当軸は今後アクション不要、**docs/architecture/detection-extension-plan.md は Phase 1 + Q5 + Q6 Resolved = Phase 2 は実機要件未解決のため着手不可**、**docs/architecture/agent-log-archive-plan.md は新規作成済 = Phase 1 着手はユーザー直伝指示要**)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-40` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-40 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 33 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 34 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「transcription.rs リファクタ完全完遂 = 該当軸完全消滅」「app_detection.rs サービス別抽出シリーズ 6 件完了」「大型 rust file 責務分離 = 7 file 目達成」「test 軸は別ファイル素材を慎重に検討」「docs 軸は detection-extension-plan.md + Q5 + Q6 Resolved = sweep 化注意」「agent-log-archive-plan.md = 新規作成済」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「Loop 78-79 で pivot 2 連続後」を踏まえ、Loop 80 で **(a) 候補 X4 = WS タスク共通化 Phase 1 (構造分離軸復帰、推奨度高、ただし規模 M = grep で 1 候補に絞る批判判断必要) または (b) 候補 H1 = agent-log-archive-plan.md Phase 1 着手 (harness 衛生 2 連続、ユーザー直伝指示要)** のいずれか採用検討。**X5 = skip 推奨 (抽出メリット 0 grep 確認済) / K3 = 主観性高警告 / J = frontend 主観性高警告**。worker prompt は「Loop 60-79 と同じ 2-commit pattern」を明示 + ~135-220 行 (Loop 67-79 precedent) で短縮効果。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 78-79 で実証 = handoff K1 を Section 5.5 既記載と整合確認 → Resolved Block 明示化に範囲限定 + handoff K2 を 3 option 比較 + Phase 完了基準機械化で objectivity 確保 → メイン批判判断 連続 30-31 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 35 セッション目達成、後継は 2 ループ continued または 3 ループ exception 判断を文脈で行う**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**、**worker prompt は ~135-220 行を目安に simpler 化** (Loop 67-79 で短縮効果実証)
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 100 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-720 秒、規模 SS-S なら ~3-8 分、規模 M なら ~7-12 分、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 36 セッション目) で書く
12. **commit hash 更新漏れパターン (Loop 60-79 で 20 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern (refactor/docs commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を明示記載 = 20 連続ループで安定動作

# Loop 80 への期待 (推奨ループ設計)

直近 13 ループで構造分離軸 8 件 + 機能拡張/docs 軸 4 件 + harness 衛生軸 1 件 = Loop 78-79 で pivot 2 連続後 = **Loop 80 で構造分離復帰 (X4) または harness 衛生続行 (H1) のいずれも自由度内**:

- 候補 X4 = WS タスク共通化 規模 M Phase 1 = 1 ループ厳しい場合は Phase 1 を更に分解 = task entry skeleton 抽出のみに絞る
- 候補 H1 = agent-log-archive-plan.md Phase 1 = AGENT_LOG.md に Archive Index Header 追加 = 規模 SS = ユーザー直伝指示要
- 候補 X5 = session_manager 内部抽出 = 抽出メリット 0 grep 確認済 = skip 推奨
- 候補 K3 = 検知拡張 Phase 2 着手判定 plan 追記 = 主観性高警告
- 候補 J = frontend = 主観性高警告継承
- 候補 H' = 大量 untracked 整理 (ユーザー直伝指示要)

メインは grep で 1 候補に絞る → worker prompt 作成 (~135-220 行 simpler 目安) → 起動 = 通常の自律改善ループへ復帰。

**最有力候補 X4 (WS タスク共通化 Phase 1) を採用する場合**: elevenlabs_realtime.rs L75-L260 / openai_realtime.rs L93-L330 を Read で精読し、共通 pattern 部分 (recv loop の error handling, shutdown 処理) を 1 fn にスライスできるか grep で実態確認。Phase 1 = 共通 trait + impl 抽出のみで残し、各 WS protocol 部は touch しない方針推奨。
