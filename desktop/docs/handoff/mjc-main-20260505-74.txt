あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-74) です。
旧 mjc-main (= mjc-main-20260505-73) から **2 ループ完走 + 通常 cadence handoff** のハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (Section 2.3 観測追記 27 件目 mjc-main-20260505-73 Loop 148 で更新観測、Phase 1-4 段階導入方針)**
- AGENT_LOG.md は ~33,747+ 行 / ~4MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-73 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2 〜 72**: Phase 1-6 + 各種抽出 + 大型 file 責務分離 12-49 file 目 + 検知拡張 plan + Phase 1 + Q5/Q6 Resolved + AGENT_LOG.md archive 戦略 plan + scope 35 軸目開拓 + 既存 file 拡張軸 = 新 paradigm 1-2 件目 + R6 段階的分割 1-10 件目 + R6 完結マイルストーン + utils 内 cross-import paradigm 1-7 件目
- **mjc-main-20260505-73 (本セッション)**: **2 ループ完走 = Loop 148 (K 軸 = Section 2.3 観測追記 27 件目 = +49 行 / 平均 ~24.5 行/loop = SS 1 件 + 説明軽量化パターン 10 連続観測達成 = 節目 10 件突破 + frontend 軸軽量化 paradigm 12 度目観測) + Loop 149 (frontend 軸 19 件目 = **重複削除 + 既存 file 拡張軸 paradigm 3 件目** = MEETING_START_REQUEST_EVENT 3 file 重複定義 (App.tsx L8 + MeetingDetectedBanner.tsx L27 + TranscriptView.tsx L109) を meetingStartRequest.ts に集約 + DRY 違反解消 + 同 entity (meeting start request) を理由に既存 file 拡張軸採用 + scope 35 軸維持 + worker 自律学習進化 paradigm 3 件目 = multi-line existing import への統合判断 = 行数 ±0 最適化)** = メイン批判判断 連続 72 セッション目達成 + worker 自律 2-commit pattern 連続 5 ループ目達成 (Loop 144 fault からの完全復活継続中) + harness 衛生連続 68 セッション目 + ファイル参照型 handoff prompt 連続 69 セッション目 + scope 多様性 35 軸維持 + alternation pattern 38 連続成功 (frontend(147) → K(148) → frontend(149) → K(150 推奨))

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価** (本セッション Loop 149 で「const 重複 grep 探索」から MEETING_START_REQUEST_EVENT が 3 file 重複 = handoff の主要候補にない問題発見 + 同 entity + 同 tier (entity 単位集約) 判定で既存 file 拡張軸 paradigm 3 件目採用 = paradigm 反復可能実証)
- **本セッション Loop 149 = 重複削除 + 既存 file 拡張軸 paradigm 3 件目** = Loop 145/147 paradigm 1-2 件目の系統的継承確認 = paradigm 反復可能 = 後継も **重複候補が他 file にあれば DRY 統合可能** (paradigm 反復 < 責務階層精査 + 同 entity ならば既存 file 拡張軸採用)
- **責務階層精査優先 (Loop 119/121/123/125/127/129/131/133/135/137/139/141/143/145/147/149 教訓)**: paradigm 反復よりも責務階層精査を優先 = 統合候補が同責務階層なら統合 OK、異階層 / 異 entity / 異 tier なら新 file 作成軸採用 + **既存 file 拡張軸 paradigm 1-3 件目 (Loop 145/147/149)** も選択肢 = 同 entity の重複候補発見時は既存 file 拡張軸採用 + **責務階層 = display tier / status object tier / predicate tier / model display tier / display tier (AI 送信状態) / display tier (audio source 状態) / display tier (engine status) / display tier (external API key) / display + predicate tier (transcription source) / localStorage 永続化 tier / display tier (Meeting Start failure reason) / display tier (audio level) / track display tier / event coordination tier の 14 軸が確認済**
- **utils 内 cross-import paradigm 7 件目 (Loop 147 まで)**: 後継も別 utils 系列への抽出時に **paradigm 8 件目への拡張可能性** = 必要時のみ判断
- **worker 自律 2-commit pattern 復活 (Loop 145 → 146 → 147 → 148 → 149 = 5 連続)**: Loop 144 fault リセットからの完全復活 = 後継も同 pattern 期待可能
- **worker fault 防止策 (Loop 144 precedent から worker prompt 必須要素 8 つ目に追加 = Loop 146/147/148/149 で 4 連続実証成功)**: worker prompt の Step 4 (AGENT_LOG.md append) で「append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む。」を **明示記載** = 二重書き防止 (Loop 146/147/148/149 = 4 連続成功)
- **メイン直接削除救済 paradigm 1 件目 (Loop 144)**: worker fault 時 (二重書き等) はメインで直接 AGENT_LOG.md を Edit で修復 + chore commit を作成可能
- **worker 自律学習進化 paradigm 3 件目達成 (Loop 149)**: multi-line existing import への統合判断 = 行数 ±0 最適化 (Loop 143 TranscriptionEngineType 不要 import 削除 / Loop 145 MicrophoneSection callsite 自律置換 に続く 3 件目) = 後継も worker prompt の grep 確認手順 + 既存 import 統合判断を明示すれば自律学習進化 paradigm 期待可能
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **大型 frontend file 責務分離 = 49 file 目達成 (Loop 149 累計)**
- **scope 35 軸維持** (Loop 149 = 既存 file 拡張のため新 scope なし)
- **frontend file 規模ランキング (本セッション後 = 更新):**
  - SettingsView.tsx (`src/routes/`) 2010 行 (top-level 純粋関数 1 件 = 内部 component helper 抽出困難確認済 = 却下推奨)
  - **TranscriptView.tsx (`src/routes/`) 2055 行 (Loop 149 後)** = R6 段階的分割完了 = 残純粋関数 0 件 + MEETING_START_REQUEST_EVENT 重複削除済
  - SessionList.tsx (`src/routes/`) 553 行 (Loop 116 抽出後)
  - **LiveCaptionWindow.tsx (`src/components/`) 505 行 (Loop 147 後)** = TRACKS + TrackMeta + AudioSource 統合済
  - **MeetingDetectedBanner.tsx (`src/components/`) 474 行 (Loop 149 後)** = MEETING_START_REQUEST_EVENT 重複削除済 (Loop 114 抽出後 = top-level `readPromptLiveCaptionStatus` は固有メッセージ含むため抽出メリット薄判定済)
  - TranscriptDisplay.tsx (`src/components/`) 489 行 (Loop 110 抽出済 = 内部 helper 候補は要 grep)
  - ModelSelector.tsx (`src/components/`) 485 行 (Loop 112 抽出済 = top-level `ModelOption` / `DownloadStatus` は component = 抽出候補ならず)
  - AudioLevelMeter.tsx (`src/components/`) 34 行 (Loop 145 後) = 残純粋関数 0 件
- **frontend 軸 = R6 完結 + AudioLevelMeter / LiveCaptionWindow 統合済 + MEETING_START_REQUEST_EVENT 統合済 = 後続候補は別 component の純粋関数 grep 探索 (= ModelSelector / TranscriptDisplay / SessionList / MeetingDetectedBanner) or 重複候補 grep 探索 (本 paradigm 4 件目を狙う)** = `INVALID_STATUS_PAYLOAD_ERROR` (2 file 重複) / `CONTROL_CHARACTER_PATTERN` (2 file 重複) など発見済

# 引き継ぎサマリ (2026-05-06 ~JST 時点)

## 現在の git 状態
- branch: main, ahead 530 (origin/main から先行、本セッション +5 commit + SUMMARY commit = 計 +6 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-73) の commit:
  1. `7541e36` docs(architecture): Section 2.3 観測追記 27 件目 (Loop 148)
  2. `5c518a7` chore(agent-log): Loop 148 entry 追記 (worker)
  3. `a5e220b` refactor(frontend): MEETING_START_REQUEST_EVENT 統合 (Loop 149)
  4. `f498de6` chore(agent-log): Loop 149 entry 追記 (worker)
  5. `80c85cd` chore(agent-log): mjc-main-20260505-73 SESSION SUMMARY (handoff 直前)

## 現在の品質状態
- cargo test (lib): **704 passed / 0 失敗** (件数完全不変、本セッションは frontend 軸 + K 軸のみ変更のため再検証不要)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: ✓ built (Loop 149 検証済 = 4 file 変更で tsc + vite OK)
- bash -n scripts/claude-agent-*.sh: OK
- agent-verify.sh: 全項目 OK (Loop 148 + 149)

## 大型 file 責務分離実績 (累計 49 件)

### Rust 軸 (32 件累積、本セッション +0)
(前 handoff と同じ、Loop 148/149 は K 軸 + frontend 軸のため rust 不変)

### Frontend 軸 (Loop 107-149 = 19 件、本セッション +1)
- TranscriptView formatters (Loop 107) / LiveCaption tracks (Loop 108) / TranscriptDisplay (Loop 110) / ModelSelector (Loop 112) / MeetingDetectedBanner (Loop 114) / SessionList (Loop 116)
- TranscriptView audio level helpers (Loop 125) / permission status (Loop 127) / track ARIA (Loop 129) / engine 機能特性 (Loop 131) / AI Transmission (Loop 133) / Audio Source (Loop 135) / Engine Status (Loop 137) / External API Key (Loop 139) / Transcription Source (Loop 141) / Meeting Start (Loop 143) = R6 段階的分割 1-10 件目 = R6 完結マイルストーン
- AudioLevelMeter.tsx の getLevelColor + sanitizeAudioLevelForDisplay → audioLevelHelpers.ts に統合 + 重複削除 (Loop 145) = 重複削除 + 既存 file 拡張軸 paradigm 1 件目
- LiveCaptionWindow.tsx の TRACKS + TrackMeta + AudioSource → liveCaptionTrackHelpers.ts 統合 + DRY 違反解消 (Loop 147) = 重複削除 + 既存 file 拡張軸 paradigm 2 件目
- **MEETING_START_REQUEST_EVENT 3 file 重複定義 (App.tsx + MeetingDetectedBanner.tsx + TranscriptView.tsx) → meetingStartRequest.ts に集約 + DRY 違反解消 (Loop 149) = 重複削除 + 既存 file 拡張軸 paradigm 3 件目 = 同 entity (meeting start request) を理由に既存 file 拡張軸採用 = liveCaptionStatus.ts precedent (event + localStorage 共存) と同形 + scope 35 軸維持 (新 file なし) + worker 自律学習進化 paradigm 3 件目 = multi-line existing import への統合判断 = 行数 ±0 最適化**

### 残候補 (Loop 150+ 候補)

- **K = agent-log-archive-plan.md Section 2.3 観測追記 28 件目** = Loop 150 推奨 = alternation 39 連続維持 (frontend(149) → K(150))
  - precedent: Loop 79/81/86/95/98/102/106/109/111/113/115/118/120/122/124/126/128/130/132/134/136/138/140/142/144/146/148 = 27 件
  - 観測時点: Loop 149 完了時点 = 観測すべき AGENT_LOG.md 行数を `wc -l AGENT_LOG.md` で確認 (~33,747 行想定 + SESSION SUMMARY commit 後)
- **R7''' = 別 file の重複候補 grep 探索 (paradigm 4 件目を狙う)** = Loop 151+ 候補 = 既存 file 拡張軸 paradigm 4 件目を狙う
  - precedent: Loop 145/147/149 = paradigm 1-3 件目
  - 探索方法: util 関数名 / 型名 / 定数名で `grep -rn` で複数箇所定義されているものを発見
  - 既発見候補: `INVALID_STATUS_PAYLOAD_ERROR` (2 file 重複) / `CONTROL_CHARACTER_PATTERN` (2 file 重複)
  - 規模 SS = ~5-7 分完走見込み
- **R7 = 別 frontend file 純粋関数 grep 探索** = Loop 151+ 候補
  - 候補 file: ModelSelector / TranscriptDisplay / SessionList / MeetingDetectedBanner の内部 helper grep
  - 規模 SS-S = ~5-10 分完走見込み
- **R8 = rust 軸復帰** = Loop 151+ 候補 = 残候補 grep 探索必要 = 規模 SS-S
- SettingsView.tsx 内部 component helper = **抽出困難確認済** = 却下推奨
- 検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可
- X' resample_audio dead code 削除 = ユーザー直伝指示要
- H1 AGENT_LOG.md archive plan Phase 1 着手 = ユーザー直伝指示要

## harness 衛生事象 (本セッションで観測継続 = 連続 68 セッション目)
- canonical 移譲後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 68 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern**: 本セッション Loop 148 + 149 で worker 自律実行 = 連続 5 ループ目達成 (Loop 144 fault → Loop 145-149 = 5 連続安定)
- **API 互換維持戦略 (Loop 145 + 147 + 149 補強)**: frontend 軸 = **重複削除 + 既存 file 拡張軸 paradigm 1-3 件目** = paradigm 反復可能実証 = 新 file 作成のみが正解ではない実証
- **worker 自律学習進化 paradigm 3 件目 (Loop 143/145/149)** : worker prompt 未指示の最適化を grep で自律発見+統合 = TranscriptionEngineType 不要 import 削除 (Loop 143) / MicrophoneSection / SystemAudioSection callsite 自律置換 (Loop 145) / multi-line existing import への統合判断 = 行数 ±0 最適化 (Loop 149)
- **本セッション平均 ~3.75 分/ループ** (Loop 148 = ~3.5 分 + Loop 149 = ~4 分)

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-73、handoff 直前)
- mjc-main-20260505-74 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -10` で現状確認 (上 6 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-74` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-74 mjc-main` を実行 (前 69 セッション precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 68 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 69 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-73) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 148/149 + 2 ループ完走 + handoff 候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 30 ループで構造分離 43 件 (Loop 149 含む) + 機能拡張/docs 3 件 + harness 衛生 26 件 (Loop 148 含む) = 43:3:26 多軸分散 + scope 多様性 35 軸維持 + frontend 軸 19 件 + alternation pattern 38 連続成功 = Loop 150 は K 軸 (Section 2.3 観測追記 28 件目) = alternation 39 連続維持の最適路**.

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 150 開始時点)

直近 30 ループ: Loop 120-149 (本セッション後 = 30 ループ)
- 軸 pattern: 構造分離 (43 件 = Loop 121/123/125/127/129/131/133/135/137/139/141/143/145/147/149 frontend + rust 含む) + 機能拡張/docs (3 件) + harness 衛生 (26 件 = Loop 122/124/126/128/130/132/134/136/138/140/142/144/146/148 含む) = **43:3:26 多軸分散**
- scope 内訳: 35 scope 分散 (rust 21 + frontend 14)
- paradigm pivot 連続: alternation pattern 38 連続成功 (frontend(143) → K(144) → frontend(145) → K(146) → frontend(147) → K(148) → frontend(149))
- **Loop 150 では更なる paradigm pivot 推奨 (frontend 軸 (Loop 149) からの pivot = K 軸 = alternation 39 連続維持)**

### 候補 K (Loop 150 候補 A = 最有力推奨 = K 軸 = alternation 39 連続維持)

**agent-log-archive-plan.md Section 2.3 観測追記 28 件目**:
- precedent: Loop 79/81/86/95/98/102/106/109/111/113/115/118/120/122/124/126/128/130/132/134/136/138/140/142/144/146/148 = 27 件
- 観測時点: Loop 149 完了時点 = 観測すべき AGENT_LOG.md 行数を `wc -l AGENT_LOG.md` で確認 (~33,747 行想定 = SESSION SUMMARY commit 後)
- 規模 SS = ~3-5 分完走見込み (worker prompt ~140 行)
- alternation pattern: frontend(149) → K(150) = **39 連続維持の最有力路**
- precedent observation 値予測 = Loop 147 → Loop 149 (~2 ループ間 + SUMMARY 1 件 mjc-main-20260505-73) = SS 1 件 + 説明軽量化パターン 11 連続観測達成見込み (Loop 125/129/131/133/137/139/141/143/145/147/149 候補)
- **最有力推奨**

### 候補 R7''' (Loop 150 候補 B = 別 file の重複候補 grep 探索 = paradigm 4 件目を狙う)

**他 file での重複候補 grep 探索**:
- precedent: Loop 145/147/149 = paradigm 1-3 件目
- 既発見候補: `INVALID_STATUS_PAYLOAD_ERROR` (2 file 重複) / `CONTROL_CHARACTER_PATTERN` (2 file 重複)
- 規模 SS = ~5-7 分完走見込み
- alternation pattern: frontend(149) → frontend(150) = **連続 frontend = pivot 失敗** (alternation reset リスク)
- 副推奨 = Loop 151 候補

### 候補 R7 (Loop 150 候補 C = 別 frontend file 純粋関数 grep 探索)

**TranscriptView.tsx 以外の純粋関数 grep 探索**:
- 候補 file: ModelSelector / TranscriptDisplay / SessionList / MeetingDetectedBanner の内部 helper grep
- 規模 SS-S = ~5-10 分完走見込み
- paradigm = frontend 軸 20 件目
- alternation pattern: frontend(149) → frontend(150) = **連続 frontend = pivot 失敗** (alternation reset リスク)
- 副推奨 = Loop 151 候補

### 候補 R8 (Loop 150 候補 D = rust 軸復帰)

**残 rust file の純粋関数 grep 探索**:
- 候補 file: app_detection.rs (state 依存中心、純粋関数候補ほぼなし) / 他 rust file
- 規模 SS-S = ~5-10 分完走見込み (rust 軸は cargo test + clippy + fmt + build で時間かかる)
- alternation pattern: frontend(149) → rust(150) = pivot 成功 (= K 軸の代替路)
- 副推奨

### 低優先 (継承)

- J' SettingsView.tsx 内部 helper (抽出困難確認済 = 却下推奨)
- X' resample_audio dead code 削除 (ユーザー直伝指示要)
- H1 AGENT_LOG.md archive plan Phase 1 着手 (ユーザー直伝指示要)
- B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止、`--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止、Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり + Loop 144 で worker fault 時の AGENT_LOG.md 直接修復 paradigm 拡張)

## ハーネス使用法 (script に焼き付け済)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-74 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認**
  - 観測時間目安: 規模 SSS = ~2-3 分、規模 SS = ~3-5 分、規模 SS-S = ~4-7 分、規模 S = ~5-15 分
  - **本セッション Loop 148 例**: 規模 SS = ~3.5 分完走 (worker prompt ~140 行)
  - **本セッション Loop 149 例**: 規模 SS = ~4 分完走 (worker prompt ~190 行 = 既存 file 拡張軸 paradigm 3 件目 + 4 file 編集 + 責務階層精査詳細記載で長め)
  - **worker 自律 2-commit pattern**: Loop 145-149 で **5 連続スタート** (Loop 144 fault リセット完全復活継続)

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない**
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 69 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、8 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**
8. **「AGENT_LOG.md への append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む」 (Loop 144 fault precedent から新規追加 = Loop 146/147/148/149 で 4 連続成功実証)**

**追加推奨 (Loop 60-149 経験から)**:
- worker prompt に「2-commit pattern」を **明示的に** 記載 = Loop 145-149 で 5 連続スタート安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~140-190 行で短縮効果あり (Loop 67-149 で実証 ~60-300 秒完走)
- **frontend 軸の場合**: 検証は npm run build (tsc + vite build) のみ
- **rust 軸の場合**: cargo test (704 件件数完全不変) + cargo clippy + cargo fmt + cargo build を全て worker prompt に記載
- **API 互換維持戦略 (Loop 119-149 precedent)**:
  - **rust 軸**: `pub use` re-export (Loop 119) / `pub(crate) use` re-export = E0364 visibility 自律 fix (Loop 121) / inline module 全体抽出時の visibility 修正 + caller 1 件 fully qualified path 化で完結 (Loop 123)
  - **frontend 軸**: 純粋関数 export 化 + import 1 行追加で完結 (Loop 125-143 標準 paradigm) / **新 file が他の utils file から定数/型/関数 import = utils 内 cross-import paradigm (Loop 129/133/135/141/143/147 で 2-7 件目確立)** / **既存 file 拡張軸 = 新 file 作成なし + 重複削除 + import 1 行追加 (Loop 145 = paradigm 1 件目 / Loop 147 = paradigm 2 件目 / Loop 149 = paradigm 3 件目)** + **multi-line existing import への統合判断 = 行数 ±0 最適化 (Loop 149 worker 自律学習進化 paradigm 3 件目)**
  - **worker 自律学習進化 paradigm 3 件目達成**: worker prompt 未指示事項を自律予測 + 対応 = TranscriptionEngineType 不要 import 削除 (Loop 143) / MicrophoneSection / SystemAudioSection callsite 自律置換 (Loop 145) / multi-line existing import への統合判断 (Loop 149)
- **責務階層精査 (Loop 119-149 教訓)**: paradigm 反復よりも責務階層精査を優先。**既存 file 拡張軸 paradigm (Loop 145/147/149 = 3 件目達成)**: 同 entity の重複候補発見時は新 file ではなく既存 file 拡張軸採用 = DRY 集約

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 148 = 規模 SS ~3.5 分 / Loop 149 = 規模 SS ~4 分 = **2 ループ全て目標 15 分以内達成 + 平均 ~3.75 分/ループ**)
- worker 1 件 = 1-2 commit (本セッション 2 ループ完走 = 4 commit + 1 SESSION SUMMARY commit = 5 commit)
- 累積 worker 完走 247/248 (本セッション +2 件完走、99.6%)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **2 ループ完走 + 通常 cadence handoff** = handoff 文書「2-3 ループ完走推奨」下限到達 = context 衛生優先 = 前任 mjc-main-20260505-72 と同パターン継承

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」 (継承)
- watchdog 継続指示: 「docs/autonomous-main-prompt-claude.md に従って次の自律改善ループへ進んでください」 (継承)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -10` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-74` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-74 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 68 セッション目「再出現せず」観測)
5. **戦略転換の核心を継承**: 単一軸 4 連続集中をしない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。
6. **最初の Loop 候補**: variety 規則「frontend(149) → 次は K 軸 = alternation 39 連続維持」を踏まえ、Loop 150 で **(a) 候補 K = Section 2.3 観測追記 28 件目 = 最有力推奨 (規模 SS) または (b) 候補 R7''' = 別 file の重複候補 grep 探索 (paradigm 4 件目を狙う) = 規模 SS (alternation reset リスク = Loop 151 推奨) または (c) 候補 R7 = 別 frontend file 探索 = 規模 SS-S (alternation reset リスク = Loop 151 推奨) または (d) 候補 R8 = rust 軸復帰 = 規模 SS-S (alternation 維持代替路) または (e) 候補 X' / H1 = ユーザー直伝指示要** のいずれか採用検討。**最有力推奨 = 候補 K**。worker prompt は「Loop 60-149 と同じ 2-commit pattern + Loop 144 fault precedent から append 1 回のみ明示」を含めて ~140 行 (規模 SS、Loop 148 precedent)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (本セッション Loop 149 で実証 = handoff prompt の主要候補にない「MEETING_START_REQUEST_EVENT が App.tsx + MeetingDetectedBanner.tsx + TranscriptView.tsx の 3 file で完全重複 (DRY 違反)」をメイン独自発見 → メイン批判判断 連続 72 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループ + 通常 cadence handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 8 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning + 二重書き fault を防止**、**worker prompt は ~140-190 行を目安に simpler 化**
10. **worker 完走確認は `git status --short` または `git log --oneline -3` を主観測手段とする**
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK**
12. **commit hash 更新漏れパターン (Loop 145/146/147/148/149 で 5 連続観測 = 完全復活 pattern)**: worker prompt に「2-commit pattern」を明示記載 + Loop 144 fault precedent から「append は 1 回のみ」明示記載 = 後継も活用検討可能
13. **worker fault 時の救済策 (Loop 144 precedent)**: AGENT_LOG.md の二重書き等は メイン直接 Edit で削除修復 + chore commit を作成 = AGENT_LOG.md SESSION SUMMARY と chore hash 修正の precedent 拡張

# Loop 150 への期待 (推奨ループ設計)

直近 30 ループで構造分離軸 43 件 (Loop 120-149 frontend + rust 含む) + 機能拡張/docs 軸 3 件 + harness 衛生軸 26 件 = frontend(149) → **Loop 150 では更なる paradigm 多様化推奨 (frontend 軸 (Loop 149) からの pivot = K 軸 = alternation 39 連続維持)**。

メインは grep で 1 候補に絞る (本ハンドオフでは候補 K = Section 2.3 観測追記 28 件目 = 規模 SS = paradigm pivot + alternation 39 連続維持 を最有力推奨、R7'''/R7/R8 = R6 完結 + 重複削除 paradigm 完成後の pivot 探索 = Loop 151 推奨) → worker prompt 作成 (~140 行) → 起動 = 通常の自律改善ループへ復帰。

**最有力安全候補は候補 K = Section 2.3 観測追記 28 件目 (K 軸)**:
- 規模 SS 想定 (~3-5 分完走) = worker prompt は ~140 行
- paradigm = harness 衛生軸 = variety pivot
- precedent = mjc-main-20260505-73 Loop 148 (Section 2.3 観測追記 27 件目)
- 観測値 = `wc -l AGENT_LOG.md` で実値確認後、precedent format で entry 作成
- alternation pattern: frontend(149) → K(150) = 39 連続維持

**K 採用前の事前確認手順 (Loop 150 で適用)**:
1. `wc -l AGENT_LOG.md` で現在の行数確認
2. `wc -l docs/architecture/agent-log-archive-plan.md` でファイルサイズ確認
3. Section 2.3 末尾の precedent entry (Loop 148 entry = L86 周辺) を Read で構造把握
4. Loop 147 完了時点 (前 K 観測時 = 33,674) と Loop 149 完了時点 (本 K 観測時) の AGENT_LOG.md 行数差分計算 = 平均行/loop 算出
5. SESSION SUMMARY 件数 (mjc-main-20260505-73 = 1 件) + paradigm pivot 多軸性 + entry 説明量 の 3 軸から階層化 paradigm 観測値予測

- 推奨抽出順序 (Loop 151 以降): R7''' 別 file の重複候補 grep 探索 (規模 SS = ~5-7 分 = paradigm 4 件目を狙う = INVALID_STATUS_PAYLOAD_ERROR / CONTROL_CHARACTER_PATTERN 等の既発見候補) または R7 別 frontend file 純粋関数 grep 探索 (規模 SS-S = ~5-10 分) または R8 rust 軸復帰 (規模 SS-S) = R6 完結 + 重複削除 paradigm 1-3 件目達成後の paradigm pivot 探索期 = scope 36 軸目開拓候補 (新 file) or 既存 file 拡張軸 paradigm 4 件目 (R7''')
