あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-2) です。
旧 mjc-main (= mjc-main-20260505-1) から **3 ループ完走 + 26 連続「Debug 軸補強」のローカル最適化からの完全脱却 + AGENTS.md 優先順位 1〜4 の本質改善 (構造改善 + 会議検知の網羅性) への戦略転換完成 + Cisco Webex 検知の URL classify + window title fallback 二重化完成 + harness silent fail mitigation pattern 連続 26 ループ実証達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 24,100+ 行 / 1.9MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~01:30 JST] mjc-main-20260505-1 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (最重要、後継が継承すべき判断)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

旧 mjc-main-20260505-1 (本セッション) は、前 32 セッションが連続 26 ループで「Debug derive 軸補強テスト追加」だけを繰り返していた実態を批判的評価し、**意図的に逸脱して優先順位 1〜4 の本質改善へ転換**した。

**Debug 軸補強の問題点 (本セッションで明示):**
- AGENTS.md 優先順位 1〜4 (クラッシュ修正 / 会議検知 / 別トラック音声 / 低遅延文字起こし) に **一切寄与しない**
- 「CI で derive Debug の出力契約を保護する」だけ = 実機ユーザー体験への寄与ゼロ
- 失敗しにくく commit しやすい = ローカル最適化に陥っていた典型例

**後継が継承すべき方針:**
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**

# 引き継ぎサマリ (2026-05-05 ~01:30 JST 時点)

## 現在の git 状態
- branch: main, ahead 188 (origin/main から先行、本セッション +4 commit (Loop 1-3 + SESSION SUMMARY))
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 旧 mjc-main-20260505-1 が canonical 名 mjc-main を持ち、本セッションで 3 ループ + 1 SESSION SUMMARY を完了:
  1. `bc7b035` refactor(transcript): TODO コメント 1 行のみの dead module `src-tauri/src/transcript.rs` を Tidy First で除去 (Loop 1、637 件不変、~50 秒)
  2. `5e4e887` feat(app_detection): Cisco Webex (`*.webex.com/meet/<id>`) Personal Room 検知を classify_meeting_url に追加 (Loop 2、+5 件 → 642 passed、~4 分)
  3. `132dbcf` feat(app_detection): classify_meeting_window_title に Webex のブラウザタブ fallback 検知 (`starts_with("Webex Meeting")`) 追加 (Loop 3、+4 件 → 646 passed、~3 分)
  4. AGENT_LOG.md 末尾に SESSION SUMMARY 追記 (本ハンドオフ commit 内)

## 現在の品質状態
- cargo test (lib): **646 passed / 0 failed** (前 637 → +9 件、3 ループ)
- cargo clippy default lint: **警告ゼロ**
- cargo clippy --lib -- -D warnings: **警告ゼロ** (3 ループ全てで維持)
- npm run build (tsc + vite build): 本セッションで frontend 触れていないため変更なし

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-1、handoff 直前)
- mjc-main-20260505-2 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 4 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-2` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-2 mjc-main` を実行 (前 15 セッションで実証済 precedent あり、冪等)。
5. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-1) を `tail -400 AGENT_LOG.md` で読み、戦略転換の核心 + 次ループ候補を確認**
6. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**Debug / Copy / serde / PartialEq 等の派生型補強パターンには絶対に戻らない**。
7. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 次ループ候補 (優先順位順、本セッション戦略転換路線継続)

### 最優先 (AGENTS.md 優先順位 2 = 会議検知の網羅性継続)

#### A. Whereby (`whereby.com`) サービス追加 (規模 S, 価値高)
- URL: `https://whereby.com/<room-name>` または `https://<subdomain>.whereby.com/<room-name>`
- 既存 `is_zoom_host` パターン踏襲で host 判定可能
- **誤検知リスク**: `whereby.com/about`, `whereby.com/pricing` 等の通常ページとの区別。Webex の `/meet/<id>` 明示 prefix がない = 単一 segment 判定だけだと誤検知。**実機/Web で会議室 URL 形式を要調査**。
- 安全側スライス: 既知の特殊 path (`/api/`, `/about`, `/pricing`, `/blog`) を除外する allowlist 形式から始める

#### B. Webex 他 URL 形式追加 (規模 S, 価値中)
- 残形態: `j.php?MTID=...` (旧形式) / `wbxmjs/joinservice/sites/...` (新形式) / `webappng/sites/...` (newer)
- `is_webex_meeting_url` 内コメントで future work として明示済
- **誤検知リスク**: `j.php` は MTID query 必須、その他は prefix 一致で誤検知少ない想定
- 1 ループで 1 形態追加 (3 ループで全網羅)

#### C. Microsoft Teams のブラウザ window title fallback (規模 S, 価値中)
- 既存コメント (line 547-549 付近) で「外部解説と区別不能のため除外」とあるが、特定パターン (`<meeting-title> | Microsoft Teams` 形式の suffix 一致) で誤検知低く検知可能か再検討

### 中優先 (AGENTS.md 優先順位 1 = クラッシュ修正)

#### D. production 内の `unwrap` / `expect` / `panic!` 監査 (規模 M, 価値高)
- 現状 grep で 382 件 (test 含む)、production 抽出は 30-50 件と推定
- 上位 5 件のクラッシュリスク特定 → 1 ファイル (例: `system_audio.rs`) から `Result` / `Option::ok_or` への変換開始
- 変更スコープが大きい場合は調査担当 (haiku) を 1 ループ走らせて **報告書** を作る → 次ループで実装

### 中優先 (構造改善 = transcription.rs 2999 行)

#### E. transcription.rs 構造調査ループ (規模 S, 価値中)
- 調査担当 (haiku) で 1 ターン完結 = 巨大ファイルの分割候補抽出
- 後続ループで段階的に分割 (例: `streaming::*` / `engine_factory::*` 等のサブモジュール抽出)

### 低優先 (harness 衛生)

#### F. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 100+ untracked ファイル
- 既 commit 済 (`-1`, `-2`, `-3`, `-11`, `-20`) と untracked が **非対称放置**
- `.gitignore` 化 vs 全 commit の議論を要する = 1 ループで決めず、**ユーザー直伝指示があるまで保留**

#### G. AGENT_LOG.md 24,100+ 行の archive 戦略
- 末尾 400 行のみ active、それ以前は読まれない
- archive ディレクトリ (`docs/agent-log-archive/`) への分離戦略を検討

## 検証制約 (再掲)
- cmake あり → cargo test 646 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` (本セッションで動作実証、エスケープ問題なし)
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-2 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (mjc-main-31〜38 + 本セッションで実証した mitigation pattern、連続 26 ループ実証)
  - log file (`logs/agent/<session>.txt`) と tmux pane は補助観測手段
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示

## worker prompt 必須要素 (20 連続セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション平均 ~3 分/loop で目標を **5 倍上回る達成**)
- worker 1 件 ≒ 1 コミット
- 累積 worker 完走 102/102 (本セッション +3 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中 2 件 (Loop 2 完走待ち中 + Loop 3 完走待ち中) を「Loop 進行 + commit」で消化済

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-2` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-2 mjc-main` を実行 (冪等)。
4. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。
5. **最初の Loop 候補**: 上記 A (Whereby) / B (Webex 他形式) / C (Teams window title) / D (panic 監査) / E (transcription.rs 構造調査) から選ぶ。誤検知リスクが明確なものは実機/Web 調査ループ (haiku research) を先行。
6. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり)
7. 待機モード禁止。final answer で停止せず、改善ループを継続する。
8. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
9. **worker 完走確認は `git status --short` を主観測手段とする** (連続 26 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-180 秒)
