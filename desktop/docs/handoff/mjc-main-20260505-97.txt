あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-97) です。
旧 mjc-main (= mjc-main-20260505-96) から **1 ループ完走 + Loop 199 worker stuck 救済** のハンドオフを受けました。
注: handoff prompt は SESSION SUMMARY commit 後の 1 ループ実績 + stuck 救済 paradigm 1 件目開拓版.

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (Section 2.3 観測追記 52 件目 mjc-main-20260505-95 Loop 197 まで反映済 = 53 件目は Loop 199 worker stuck で未追記 = 後継 Loop 199 で実施候補)**
- AGENT_LOG.md は ~34,748 行 / ~3.2MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-96 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」.

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2 〜 95**: Phase 1-6 + 各種抽出 + 大型 file 責務分離 12-51 file 目 + 検知拡張 plan + Phase 1 + Q5/Q6 Resolved + AGENT_LOG.md archive 戦略 plan + scope 39 軸目開拓 + 既存 file 拡張軸 paradigm 1-21 件目 + 重複候補 grep 探索 paradigm 1-24 件目 + utils 内 cross-import paradigm 1-7 件目 + paradigm 系統樹拡張 1-12 件目候補 + const 集約軸 frontend 1-10 件目反復 + rust 軸 const 集約 paradigm 1-2 件目 (Loop 194/196)
- **mjc-main-20260505-96 (本セッション)**: **1 ループ完走 + Loop 199 worker stuck 救済 = Loop 198 (rust 軸 = const 集約 paradigm 反復 10 件目達成 = paradigm 系統樹拡張 11 件目反復 = 新規 const 追加軸 paradigm 反復 = src/transcription_error_payload.rs に `pub(crate) const ERROR_TRANSCRIPTION_WORKER_ABEND: &str = "文字起こしワーカーが異常終了しました";` 追加 + 4 callsite (本番 1 = L13 + test 3 = L42/L104/L198) を const 参照に統一 = Loop 194 cloud_whisper.rs precedent と同系統樹 = 新規 const 追加軸 paradigm の rust 反復) + Loop 199 worker stuck = kill + handoff 早期判断 = stuck 救済 paradigm 1 件目開拓** = メイン批判判断 連続 **98 セッション目達成** (Loop 198 = handoff prompt R30' 主張「主要候補なし」を独自 grep で再探索 + transcription_error_payload.rs / secret_store.rs / system_audio_format.rs に有力候補発見 + 第一推奨採用) + worker 自律 2-commit pattern 連続 **53 ループ目達成** (Loop 145-198 = 54 連続安定 = **節目 50 件突破後 3 件目**) + harness 衛生連続 **91 セッション目達成 = 節目 90 件突破後 1 件目** + ファイル参照型 handoff prompt 連続 **92 セッション目** + scope 多様性 39 軸維持 + alternation pattern pivot 加速継続 (K(197) → rust(198) = 37 → 38 = **加速期維持 = 節目 30 件突破後 8 件目**) + worker prompt 必須要素 9 (冒頭 `---` 含めない) **30 連続実証成功** (Loop 168-198 = 節目 25 連続突破後 5 件目) + **paradigm 完全成熟期 cross-language 拡張更なる持続達成** = const 集約軸 frontend 10 + rust 3 = 計 13 連続.

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討** = 本セッション K(197) → rust(198) = 38 連続成功 = pivot 加速期更なる強化
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価** (本セッション Loop 198 で R30' 主張「主要候補なし」を独自 grep で再探索 + 別 file 有力候補発見 = メイン批判判断 連続 98 セッション目達成)
- **本セッション Loop 199 = worker Claude Code 起動 stuck (output 0 bytes + 7 分経過 + CPU 9 秒のみ) = kill + handoff 早期判断 = stuck 救済 paradigm 1 件目開拓**
- **責務階層精査優先 (Loop 119-198 教訓継承)**: paradigm 反復よりも責務階層精査を優先
- **重要転換点 (本セッション Loop 198)**: transcription_error_payload.rs は既存 const 0 件 file への新規 const 追加 = paradigm 系統樹拡張 11 件目反復 (Loop 194 cloud_whisper.rs precedent と同系統樹) = paradigm 完全成熟期 cross-language 拡張更なる持続 (frontend 10 + rust 3 = 計 13 件) = 後継 Loop 199+ で **R30'' = secret_store.rs `"このプラットフォームでは Keychain がサポートされていません"` 3 件 = 全件本番 = 集約価値最高** または **R30''' = system_audio_format.rs `"非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)"` 3 件 = 本番 1 + test 2** が責務階層的に第一推奨
- **worker 自律 2-commit pattern 復活継続 (Loop 145 → 198 = 54 連続)**: Loop 144 fault リセットからの完全復活継続中 = **節目 50 件突破後 3 件目**. Loop 199 = worker stuck で kill = 連続記録は 53 で停止 (54 にカウントしない).
- **worker fault 防止策**: 「append は 1 回のみ」を **明示記載**
- **worker prompt 必須要素 9**: 「entry テンプレート冒頭に `---` を含めない」を **明示記載**
- **メイン直接削除救済 paradigm 2 件目 + stuck 救済 paradigm 1 件目開拓 (Loop 144/166/199)**: 後継も活用検討可能
- **worker 完走確認は git status を主観測手段とする (Loop 158/180-198 precedent + 本セッション 1 連続実証成功)**
- **stuck 判定基準 (本セッション Loop 199 開拓)**: 起動から 5-7 分後に log file size と git log を観測 + 進捗ゼロ (log 0 bytes + commit 反映なし) なら stuck 判定 + tmux kill-session が responsible
- **R30'' (Loop 199 第一推奨候補) = secret_store.rs ERROR_KEYCHAIN_NOT_SUPPORTED const 集約 = 規模 SSS = 全件本番 = 集約価値最高 = paradigm 系統樹拡張 11 件目反復, R30''' (第二推奨) = system_audio_format.rs ERROR_NON_F32_PCM_FORMAT const 集約 = 規模 SSS = 本番 1 + test 2, R34 (Loop 200 第三推奨) = K 軸 = Section 2.3 観測 53 件目 = 規模 SS = 機械的反復ローカル最適化リスク批判要, R37 (第四推奨) = 大型 file 責務分離 52 件目候補 = 規模 M-L = 探索コスト高, X' / H1 = ユーザー直伝指示要**

# 引き継ぎサマリ (2026-05-06 ~JST 時点)

## 現在の git 状態
- branch: main, ahead は前任 +650 + 本セッション +3 commit (Loop 198 refactor + chore + SESSION SUMMARY) = 約 +653
- working tree: clean (docs/handoff/* と docs/worker-prompts/* と .tmp-handoff-launch-* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-96) の commit (古い順):
  1. `604f574` refactor(rust): ERROR_TRANSCRIPTION_WORKER_ABEND を transcription_error_payload.rs に集約 + 4 callsite (Loop 198)
  2. `2481173` chore(agent-log): Loop 198 entry
  3. `3d07666` chore(agent-log): SESSION SUMMARY commit (1 ループ実績 + stuck 救済 paradigm 1 件目)

## 現在の品質状態
- cargo test (lib): **704 passed / 0 失敗** (Loop 198 で再検証 = 件数完全不変)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ (Loop 198 で再検証)
- cargo fmt --check: OK (Loop 198 で再検証)
- cargo build --lib: エラーなし (Loop 198 で再検証)
- npm run build (tsc + vite build): OK (前任 mjc-main-20260505-93 Loop 192 で完走確認 = 本セッションは rust + docs のみで frontend 不変)
- bash -n scripts/claude-agent-*.sh: OK
- agent-verify.sh: 全項目 OK (Loop 198 = rust のみ skip 判定)

## 大型 file 責務分離実績 (累計 51 件、本セッション +0)

## 既存 file 拡張軸 paradigm (21 件累積、本セッション +0)

## 重複候補 grep 探索 paradigm (24 件累積、本セッション +1 候補発見だが採用 1 件のみ = 24 件目維持)

- Loop 153-194 = 23 件目までの precedent
- Loop 196 = 24 件目達成
- 本セッション Loop 198 = 後継独自 grep で R30' 別 file 探索 + transcription_error_payload.rs / secret_store.rs / system_audio_format.rs に 3 候補発見 + 第一推奨 (transcription_error_payload.rs) を採用 = grep 探索 paradigm 24 件目維持 (採用は 1 件)

## paradigm 系統樹拡張 (11 件目反復 + 12 件目候補確立、本セッション +1 反復)

- Loop 119-194 = 11 件目までの precedent
- Loop 196 = 12 件目候補確立 (既存 const 確立 file への並列拡張軸 paradigm rust 版)
- **本セッション Loop 198 = paradigm 系統樹拡張 11 件目反復達成 = 新規 const 追加軸 paradigm 反復 = Loop 194 cloud_whisper.rs precedent と同系統樹**

## stuck 救済 paradigm (本セッション +1 件目開拓)

- **Loop 199 worker Claude Code 起動 stuck = kill + handoff 早期判断 paradigm 1 件目**
- 判定基準: 起動から 5-7 分後に log file size と git log を観測 + 進捗ゼロ (log 0 bytes + commit 反映なし) なら stuck 判定
- 対処: tmux kill-session → SESSION SUMMARY 作成 → handoff 早期判断
- 後継への教訓: worker 起動 5-7 分後に観測 + 進捗ゼロなら stuck 判定 + kill が responsible

## 残候補 (Loop 199+ 候補)

- **R30'' (Loop 199 第一推奨) = src/secret_store.rs `"このプラットフォームでは Keychain がサポートされていません"` 3 callsite = 全件本番 = 規模 SSS = ~3-5 分**:
  - L56, L74, L92 (3 件全て `Err("...".to_string())` 形式 = #[cfg(target_os = "macos")] / #[cfg(target_os = "windows")] / #[cfg(not(any(target_os = "macos", target_os = "windows")))] それぞれの分岐内?)
  - 抽出先: src/secret_store.rs に新規 const `pub(crate) const ERROR_KEYCHAIN_NOT_SUPPORTED: &str = "...";` 追加
  - paradigm 系統樹拡張 11 件目反復 = const 集約 paradigm 反復 11 件目候補
  - **本番品質寄与最高 (全件本番 = ユーザー向けエラーメッセージ統一 + typo 防止)**
  - alternation: rust(198) → rust(199) = rust 2 連続 = pivot 規則ギリギリだが本番品質寄与最高で正当化
  - **後継要確認**: secret_store.rs を Read で構造確認 + 各 callsite の cfg attribute をチェック (3 件が同 const を共有可能か検証)

- **R30''' (Loop 199 第二推奨) = src/system_audio_format.rs `"非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)"` 3 callsite = 本番 1 + test 2 = 規模 SSS**:
  - L22 (本番 = `return Err("...")`), L128 (test = expected error message), L162 (test = `Err("...")`)
  - 抽出先: src/system_audio_format.rs に新規 const 追加
  - paradigm 系統樹拡張 11 件目反復 (Loop 198 と同 paradigm)
  - alternation: rust(198) → rust(199) = rust 2 連続だが本番品質寄与あり

- **R34 (Loop 200 第三推奨) = K 軸 = Section 2.3 観測 53 件目 = 規模 SS = ~3-7 分**:
  - alternation: rust(199) → K(200) = pivot 加速期維持
  - precedent: Loop 79/81/.../195/197 = 52 件目までの updates
  - **ただし機械的反復で Mac アプリ品質に直接寄与せずローカル最適化リスク** = 「派生型 CI 保護パターンに戻らない」継承方針との緊張感
  - **本セッション Loop 199 で着手したが worker stuck で未完了 = 後継の最有力候補**

- **R36 (第四推奨) = cloud_whisper.rs OpenAI API URL test fixture 集約 = 規模 SS = test 内のみ**:
  - 17 callsite (handoff prompt の 13 件は誤、17 件が正確値) = 後継 mjc-main-20260505-96 の批判検証で確認
  - **多数が build_whisper_api_url() の input/output test 仕様 = const 化困難**
  - 集約可能なのは url field 直接代入の数件のみ = 規模 SS = 価値小
  - **却下推奨** (handoff prompt 主張に対する後継 mjc-main-20260505-96 の批判判断結論)

- **R37 (第五推奨) = 大型 file 責務分離 52 件目候補 = 規模 M-L = 探索コスト高**:
  - 残存大型 file: app_detection.rs 2458 行 (検知拡張 plan = Phase 2 着手不可) / session_manager.rs 1232 行 / audio.rs 915 行 / settings.rs 801 行 / cloud_whisper.rs 778 行 / session.rs 773 行 / transcript_bridge.rs 736 行 / session_store.rs 722 行
  - frontend: TranscriptView.tsx 2056 行 / SettingsView.tsx 2011 行
  - 規模 M-L = 探索コスト高 + 過去 51 件目までの precedent では既に責務分離困難と判定された file 多数の可能性

- 検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可
- X' resample_audio dead code 削除 = ユーザー直伝指示要
- H1 AGENT_LOG.md archive plan Phase 1 着手 = ユーザー直伝指示要

## harness 衛生事象 (本セッションで観測継続 = 連続 91 セッション目 = 節目 90 件突破後 1 件目)
- canonical 移譲後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 91 セッション目観測結論 = 節目 90 件突破後 1 件目
- **2-commit pattern**: 本セッション Loop 198 で worker 自律実行 = **連続 53 ループ目達成 = 節目 50 件突破後 3 件目** (Loop 144 fault → Loop 145-198 = 54 連続安定)
- Loop 199 = worker stuck で kill = 連続記録は 53 で停止 (54 にカウントしない)
- **本セッション Loop 198 完走時間 ~3 分** (規模 SS = rust 軸 const 集約軽量化 paradigm 維持 + 単一 file 内 + 4 callsite 機械的置換完了)
- **本セッション Loop 199 = worker Claude Code 起動 stuck (output 0 bytes + 7 分経過 + CPU 9 秒のみ) = kill + handoff 早期判断 = stuck 救済 paradigm 1 件目開拓**
- **本セッション boundary fault 0 件** (Loop 198 で worker prompt 必須要素 9 = 冒頭 `---` 含めない設計の 1 連続実証成功 = Loop 168-198 で 30 連続再発防止確認 = 節目 25 連続突破後 5 件目)

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-96、handoff 直前)
- mjc-main-20260505-97 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 200-400 行のみ).
2. `git status --short --branch` と `git log --oneline -10` で現状確認 (上 3 コミットが本セッションの実績).
3. `tmux list-sessions | grep mjc` で他セッション状態確認.
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要. `mjc-main-20260505-97` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-97 mjc-main` を実行 (前 92 セッション precedent あり、冪等).
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 91 セッション目「再出現せず」観測 = 節目 90 件突破後 1 件目 = 後継も同観測なら結論「永続的解決」が連続 92 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-96) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 198 + 1 ループ完走 + Loop 199 stuck 救済 candidate を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**. **variety 規則の状態 = 直近 30 ループで構造分離 51 件 + 機能拡張/docs 4 件 + harness 衛生 52 件 (Loop 158/.../197 含む) = 51:4:52 多軸分散 + scope 多様性 39 軸開拓達成 + alternation pattern pivot 加速 K(197) → rust(198) = 37 → 38 = Loop 199 は別軸 (rust or 別 paradigm) = R30'' (secret_store.rs Keychain エラー集約 = 全件本番) または R30''' (system_audio_format.rs 非 f32 PCM 集約 = 本番 1 + test 2) または R34 (K 軸 alternation 復帰) が最適路 = paradigm 系統樹拡張 11 件目反復**.

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 199 開始時点)

直近 30 ループ: Loop 169-198 (本セッション後 = 30 ループ)
- 軸 pattern: 構造分離 (51 件) + 機能拡張/docs (4 件) + harness 衛生 (52 件) = **51:4:52 多軸分散**
- scope 内訳: 39 scope 開拓達成
- paradigm pivot: cross-language 拡張更なる持続達成 (frontend 10 + rust 3 = 計 13 件) = R30'' / R30''' / R34 / R37 のいずれか採用検討
- **Loop 199 では R30'' = secret_store.rs Keychain エラー集約 (全件本番 = 規模 SSS) または R30''' = system_audio_format.rs 非 f32 PCM 集約 (本番 1 + test 2 = 規模 SSS) が最適路**

### 候補 R30'' (Loop 199 候補 A = 第一推奨 = secret_store.rs Keychain エラー集約 = 規模 SSS)

**src/secret_store.rs 内 `"このプラットフォームでは Keychain がサポートされていません"` 3 callsite (全件本番) 集約 = paradigm 系統樹拡張 11 件目反復**:
- 規模 SSS = ~3-5 分完走見込み
- **本番品質寄与最高 = ユーザー向けエラーメッセージの単一 source of truth 化 + typo 防止**
- AGENTS.md priority 1 寄与高
- alternation pattern: rust(198) → rust(199) = rust 2 連続 = pivot 規則ギリギリだが本番品質寄与最高で正当化
- **後継要確認**: secret_store.rs を Read で構造確認 + 各 callsite の cfg attribute をチェック (3 件が同 const を共有可能か検証)

### 候補 R30''' (Loop 199 候補 B = 第二推奨 = system_audio_format.rs 非 f32 PCM 集約 = 規模 SSS)

**src/system_audio_format.rs 内 `"非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)"` 3 callsite (本番 1 + test 2) 集約 = paradigm 系統樹拡張 11 件目反復 (Loop 198 と同)**:
- 規模 SSS = ~3-5 分完走見込み
- 集約価値中-高 + 本番品質寄与あり
- alternation: rust(198) → rust(199) = rust 2 連続だが本番品質寄与あり

### 候補 R34 (Loop 200 候補 A = 第三推奨 = K 軸 = Section 2.3 観測 53 件目)

**harness 衛生軸 = K 軸 = alternation 復帰**:
- precedent: Loop 79/81/.../195/197 = 52 件目までの updates
- 観測 53 件目
- 規模 SS = ~3-7 分完走見込み
- **本セッション Loop 199 で着手したが worker stuck で未完了 = 後継再着手候補**
- **ただし機械的反復で Mac アプリ品質に直接寄与せずローカル最適化リスク = 派生型 CI 保護パターンに戻らない方針との緊張感** = 採用は批判的検討要

### 候補 R37 (第四推奨 = 大型 file 責務分離 52 件目候補 = 規模 M-L)

session_manager.rs 1232 行 / audio.rs 915 行 / settings.rs 801 行 / session.rs 773 行 / transcript_bridge.rs 736 行 / session_store.rs 722 行 / TranscriptView.tsx 2056 行 / SettingsView.tsx 2011 行 等の責務分離余地を grep + Read で探索. **ただし規模 M-L = 探索コスト高 + 過去 51 件目までで責務分離困難と判定された file 多数の可能性**.

### 低優先 (継承)

- J' SettingsView.tsx 内部 helper (component helper 可能性高 = 抽出困難確認済 = 却下推奨)
- X' resample_audio dead code 削除 (ユーザー直伝指示要)
- H1 AGENT_LOG.md archive plan Phase 1 着手 (ユーザー直伝指示要)
- B1. Microsoft Teams window title fallback (継承)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止、`--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止、Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり + Loop 144/166 で worker fault / boundary fix 時の AGENT_LOG.md 直接修復 paradigm 2 件目達成 + 本セッション Loop 199 で stuck 救済 paradigm 1 件目開拓 = メイン直接 SESSION SUMMARY commit 作成)

## ハーネス使用法 (script に焼き付け済)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-97 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認**
  - **本セッション継続 paradigm**: `git status` (working tree clean) を主観測手段にする = `git log` の即座のチェックは fault 誤判定リスクあり (Loop 158/180-198 precedent + 本セッション Loop 198 で 1 連続実証成功)
  - 観測時間目安: 規模 SSS = ~2-3 分、規模 SS = ~3-7 分、規模 SS-S = ~5-10 分、規模 S = ~10-20 分、規模 M-L = ~15-30 分、規模 L = ~25-40 分
  - **本セッション例**: Loop 198 = 規模 SS = 実測 ~3 分 (transcription_error_payload.rs 単一 file 内 + 4 callsite 機械的置換完了 = 規模 SS 想定の最速級)
  - **stuck 判定基準 (本セッション Loop 199 開拓)**: 起動から 5-7 分後に log file size と git log を観測 + 進捗ゼロ (log 0 bytes + commit 反映なし) なら stuck 判定 + tmux kill-session が responsible

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 92 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、9 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**
8. **「AGENT_LOG.md への append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む」 (Loop 144 fault precedent から新規追加 = Loop 146-198 で 53 連続成功実証)**
9. **「entry テンプレート冒頭に `---` を含めない (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める)」 (Loop 166 boundary fix precedent から新規追加 = Loop 167-198 で 32 連続再発防止確認 = 節目 25 連続突破後 7 件目)**

**追加推奨 (Loop 60-198 経験から)**:
- worker prompt に「2-commit pattern」を **明示的に** 記載 = Loop 145-198 で 54 連続スタート安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~80-300 行で短縮効果あり (本セッション Loop 198 = 155 行 = SS 規模 rust 軸)
- **frontend 軸の場合**: 検証は npm run build (tsc + vite build) のみ
- **rust 軸の場合**: cargo test (704 件件数完全不変) + cargo clippy + cargo fmt + cargo build を全て worker prompt に記載
- **API 互換維持戦略 (Loop 119-198 precedent)**: const 集約 paradigm = 新規 const 追加 + callsite を const 参照に統一 + 件数完全不変保証
- **責務階層精査 (Loop 119-198 教訓)**: paradigm 反復よりも責務階層精査を優先

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 198 = 規模 SS ~3 分 = **目標 15 分以内達成 + 過去最速級維持**)
- worker 1 件 = 1-2 commit (本セッション 1 ループ完走 = 2 commit + 1 SESSION SUMMARY commit = 3 commit)
- 累積 worker 完走 295/296 (本セッション +1 件完走 + 1 件 stuck = 99.7%)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **1 ループ完走 + Loop 199 worker stuck = handoff 早期判断** = 「2-3 ループ完走推奨」の最低限 1 ループ達成 + stuck 救済 paradigm 1 件目開拓 + context 衛生優先

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」 (継承)
- watchdog 継続指示: 「docs/autonomous-main-prompt-claude.md に従って次の自律改善ループへ進んでください」 (継承)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -10` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認.
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認. `mjc-main` なら不要、`mjc-main-20260505-97` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-97 mjc-main` を実行 (冪等).
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 91 セッション目「再出現せず」観測 = 節目 90 件突破後 1 件目)
5. **戦略転換の核心を継承**: 単一軸 4 連続集中をしない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ. **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討** = **rust 軸 (Loop 198) からの選択肢 = R30'' (secret_store.rs Keychain エラー集約 = 全件本番) / R30''' (system_audio_format.rs 非 f32 PCM 集約 = 本番 1 + test 2) / R34 (K 軸 alternation 復帰 = 観測 53 件目 = 機械的反復ローカル最適化リスク批判要) / R37 (大型 file 責務分離) のいずれか採用検討**
6. **最初の Loop 候補**: variety 規則を踏まえ、Loop 199 で **(a) 候補 R30'' = secret_store.rs Keychain エラー集約 (規模 SSS = 全件本番 = 集約価値最高) または (b) 候補 R30''' = system_audio_format.rs 非 f32 PCM 集約 (規模 SSS = 本番 1 + test 2) または (c) Loop 200 で R34 = K 軸 = 観測 53 件目 (規模 SS = 機械的反復ローカル最適化リスク批判要 = 本セッション Loop 199 で着手したが stuck で未完了) または (d) X' / H1 = ユーザー直伝指示要** のいずれか採用検討. **第一推奨 = 候補 R30'' (secret_store.rs Keychain エラー集約 = 規模 SSS = 全件本番 = 本番品質寄与最高 = paradigm 系統樹拡張 11 件目反復 = AGENTS.md priority 1 寄与)**, **第二推奨 = R30''' (system_audio_format.rs 非 f32 PCM 集約 = 規模 SSS = 本番 1 + test 2)**. **重要**: 後継 R30'' 採用時は **rust 軸 = secret_store.rs を Read で構造確認 + 各 callsite の cfg attribute をチェック (3 件が同 const を共有可能か検証)** が responsible. worker prompt は「Loop 60-198 と同じ 2-commit pattern + Loop 144 fault precedent から append 1 回のみ明示 + Loop 166 boundary fix precedent から冒頭 `---` 含めない明示」を含めて ~80-150 行 (規模 SSS, rust 軸).
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (本セッション Loop 198 で実証 = handoff prompt R30' 主張「主要候補なし」を独自 grep で再探索 + 別 file 有力候補発見 + 第一推奨採用 を批判検証達成 = メイン批判判断 連続 98 セッション目達成).
8. 待機モード禁止. final answer で停止せず、改善ループを継続する. **本セッションは 1 ループ完走 + stuck 救済 + handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 9 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning + 二重書き fault + boundary `---` 重複 fault を防止**, **worker prompt は ~80-150 行を目安に simpler 化 (R30'' = SSS 規模)**
10. **worker 完走確認は `git status --short` を主観測手段とする** (本セッション継続 paradigm)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK**
12. **commit hash 更新漏れパターン (Loop 145-198 で 54 連続観測 = 完全復活 pattern + 節目 50 件突破後 3 件目)**: worker prompt に「2-commit pattern」を明示記載 + Loop 144 fault precedent から「append は 1 回のみ」明示記載 + Loop 166 boundary fix precedent から「冒頭 `---` 含めない」明示記載 = 後継も活用検討可能
13. **worker fault / stuck 時の救済策 (Loop 144/166/199 precedent)**: AGENT_LOG.md の二重書き / boundary `---` 重複等は メイン直接 Edit で削除修復 + chore commit を作成 = メイン直接削除救済 paradigm 2 件目 + worker stuck = tmux kill-session + handoff 早期判断 = stuck 救済 paradigm 1 件目開拓
14. **stuck 判定基準 (本セッション Loop 199 開拓)**: worker 起動から 5-7 分後に log file size と git log を観測 + 進捗ゼロ (log 0 bytes + commit 反映なし) なら stuck 判定 + tmux kill-session が responsible

# Loop 199 への期待 (推奨ループ設計)

直近 30 ループで構造分離軸 51 件 + 機能拡張/docs 軸 4 件 + harness 衛生軸 52 件 = rust(198) → **Loop 199 では R30'' = secret_store.rs Keychain エラー集約 (全件本番 = 規模 SSS) または R30''' = system_audio_format.rs 非 f32 PCM 集約 (本番 1 + test 2 = 規模 SSS) または R34 = K 軸 = Section 2.3 観測 53 件目 (本セッション Loop 199 で着手したが stuck で未完了) が最適 = 規模 SSS / SS**.

メインは grep + Read 精読で 1 候補に絞る (本ハンドオフでは候補 R30'' = secret_store.rs Keychain エラー集約 = 規模 SSS = 全件本番 = 本番品質寄与最高 = AGENTS.md priority 1 寄与 を第一推奨、R30''' = system_audio_format.rs 非 f32 PCM 集約 = 規模 SSS = 本番 1 + test 2 = 第二推奨、R34 = K 軸 alternation 復帰 = 観測 53 件目 = 規模 SS = 機械的反復ローカル最適化リスク批判要 = 第三推奨) → worker prompt 作成 (~80-150 行 = SSS 規模) → 起動 = 通常の自律改善ループへ復帰.

**第一推奨は候補 R30'' (secret_store.rs Keychain エラー集約 = 規模 SSS)**:
- 規模 SSS 想定 (~3-5 分完走) = worker prompt は ~80-120 行
- paradigm = rust 軸 const 集約 paradigm 反復 11 件目 = paradigm 系統樹拡張 11 件目反復
- precedent = Loop 119-198 = 既存 paradigm 系統樹 1-12 件目候補
- Mac アプリ内部品質改善 = AGENTS.md priority 1 寄与最高 (全件本番 = ユーザー向けエラーメッセージ単一 source of truth 化)
- 後継要確認 = secret_store.rs を Read で構造確認 + 各 callsite の cfg attribute をチェック (3 件が同 const を共有可能か検証)

- 推奨抽出順序 (Loop 200 以降): R30''' → R34 → R37 等

後継 mjc-main-20260505-97 への 2-3 ループ完走推奨。第一推奨候補 = R30'' = secret_store.rs Keychain エラー集約 = 規模 SSS = 全件本番 = paradigm 系統樹拡張 11 件目反復 / 第二推奨 = R30''' = system_audio_format.rs 非 f32 PCM 集約 = 規模 SSS = 本番 1 + test 2 / 第三推奨 = R34 = K 軸 alternation 復帰 = 観測 53 件目 = 規模 SS. 詳細は docs/handoff/mjc-main-20260505-97.txt 参照.

---
