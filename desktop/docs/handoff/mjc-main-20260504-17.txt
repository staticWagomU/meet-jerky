あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-17) です。
旧 mjc-main (= 旧 mjc-main-20260504-16) から 3 ループ完了の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 19000+ 行 / 1.7MB+ あるので `tail -350 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~15:30 JST] mjc-main-20260504-16 状況メモ (ループ 1-3 詳細) があります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~15:30 JST 時点)

## 現在の git 状態
- branch: main, ahead 96+ (origin/main から先行)
- working tree: clean (untracked: `docs/worker-prompts/` 増分、`docs/handoff/mjc-main-20260504-7/10/12/13/14/15/16/17.txt`)
- 旧 mjc-main-20260504-16 が canonical 名 mjc-main を持っており、本セッションで 3 ループ + 1 SESSION SUMMARY を完了:
  1. `353da45` feat(audio-source-section): per-source UI badge prop drill (frontend 3 ファイル, +30 -12)
  2. `9eecc11` refactor(audio_event): audio source 識別子 microphone / system_audio を audio_event.rs に集約 (Tidy First, rust 3 ファイル + 1 test)
  3. `ca32ccf` refactor(audio-source-section): drop badge を audio-source-drop-badge 専用 class に分離 (Tidy After, frontend 3 ファイル + App.css 11 行追加)
  4. `ffcf1af` chore(agent-log): SESSION SUMMARY 追記

## 現在の品質状態
- cargo test (lib): **457 passed / 0 failed** (前 456 → +1 件、Loop 2 のみで rust test 追加)
- cargo clippy default lint: **警告ゼロ**
- cargo clippy -D warnings: **警告ゼロ**
- npm run build (tsc + vite build): **通過** (bundle JS 486.47 KB / gzip 137.51 KB / CSS 86.34 KB / gzip 13.94 KB)

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-16、handoff 直前)
- mjc-main-20260504-17 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-17 mjc-main` を実行して、あなたが canonical mjc-main を引き継ぐ。これにより旧 mjc-main は自動で kill される。
5. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本セッションで定着)
- **「Loop 1 で意図的妥協を明示分離 → Loop 3 で消化」パターン**: Loop 1 で「最小スコープでは流用、polish は次ループ」と明示して 1 commit を絞り、Loop 3 で Tidy After として消化。Kent Beck 流 Tidy First/After の周期との親和性が実証された。
- **「文字列リテラル定数化 (Tidy First) で frontend と rust の同期点を test 化」パターンの 2 件目 application** (Loop 2): 前 mjc-main-15 Loop 2 (AUDIO_DROP_EVENT_NAME) に続く 2 件目 = 知見が運用パターンとして定着。
- **「per-source 移行 + 集約削除」パターン** (Loop 1): UI 表示の物理的近接化 = "Move Method" のリファクタの UI 版 (state は親に残しつつ表示責務だけ子に移譲)。

### 累積パターン (前 mjc-main-7/8/9/10/11/12/13/14/15 SUMMARY 参照、変更なし)

### 重要な技術的注意点 (本セッション分のみ抜粋)
- **audio-source-drop-badge / silence-badge のサイズ系 5 prop は同一** (App.css line 824-844): badge サイズ変更時は両 class を同時に変更する必要あり (視覚的一貫性とのトレードオフ)
- **audio_event.rs に AUDIO_SOURCE_MICROPHONE / AUDIO_SOURCE_SYSTEM_AUDIO 追加** (Loop 2): transcription.rs (line 1052/1056/1320 etc) の同名文字列とは別責務 (event payload 出力 vs CLI/設定入力 parse) で意味的境界に注意。同じ文字列値だが安易に同じ定数で集約すると後で困る。
- **TranscriptView.tsx line 2370-2381 の集約表示は完全削除済み** (Loop 1): 同様の集約ブロックを再追加する誤改修への注意。drop count 表示は per-source 化された (MicrophoneSection / SystemAudioSection の header 内)。
- **MicrophoneSection / SystemAudioSection の drop badge prop は必須 (optional でない)** (Loop 1): TranscriptView 側 prop pass 漏れを tsc 型エラーで弾く検知装置として機能。
- **既存累積技術注意点** (前 mjc-main-7〜15 SUMMARY 参照、本セッションで触れていない領域は前 SUMMARY 必読)

### AGENT_LOG.md 構造の問題 (要整理、優先度低)
- 前 mjc-main-15 セッションで worker 2 件が AGENT_LOG.md の **先頭** に worker log を誤追記している問題が継続。本セッションの 3 worker は全て末尾追記で正しく記録された。
- 機能影響なし、可読性問題のみ。余裕があれば整理を検討 (低優先)

## 次ループ候補 (優先順位順、本セッションで未着手)

### F (最有力, 継承). 会議終了検知の遅延監視 (規模 S〜M, 価値 8)
- handle_detection 内で watched bundle 起動 → 非アクティブ経過時間 → N 分以上で会議終了通知。
- **累積 polish が一段落したので機能追加方向へ転換するタイミング** (本セッション 3 ループは UX polish 中心だったため、次は機能側へ重心移動推奨)
- 実装スコープ確認のため src-tauri/src/detection.rs 系を最初に読む

### D (継承). transcription.rs error path 残り (規模 S〜M)
- 1896 行最大規模。`load_model` path 変換失敗、`feed`/`finalize` resampler state missing 周辺。複数ループ構成。

### S (継承). settings.rs Tidy First リファクタ (規模 M, 2 ループ構成)

### K (継承). clippy::pedantic の選択的有効化 (規模 L, 非優先)

### G4 (新). transcription.rs の "microphone" / "system_audio" 文字列定数化 (規模 S, 価値小)
- audio_event.rs の AUDIO_SOURCE_MICROPHONE / AUDIO_SOURCE_SYSTEM_AUDIO は event payload 出力責務
- transcription.rs (line 1052/1056/1320 etc) は parse 処理 (CLI/設定入力) 責務で別物
- 新規 transcription_source.rs モジュールを切って同等の定数を分離するか、または「同じ文字列値を 2 箇所で定数定義する」設計選択 (DRY 違反だが責務境界優先)
- 設計選択を伴うため worker prompt で明示判断が必要

### B (新). transcript_bridge.rs / session_store.rs 周辺の test 補強残り (規模 S, 価値中, 確実性高い)
- 累積パターン (Option 3 状態 / unused field / ? Some(0) / OR 5 状態 / saturating 飽和) の application 続編

## 検証制約 (再掲)
- cmake あり → cargo test 457 件全 pass している (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <ファイル群>` (Conventional Commits 日本語)
  - **注意**: コミットメッセージに `||` (バックティック内 OR) を含めると fish shell で parse error になる。`OR` 表記等で回避すること。
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-17 mjc-main`

## コミット周期目標
- 1 ループ 10-15 分前後を目標。worker 1 件 ≒ 1 コミット。
- 本セッション実績: Loop 1 = ~12 分 / Loop 2 = ~16 分 (cargo test 含む) / Loop 3 = ~10 分、平均 ~13 分/loop = 目標 15 分以内達成、累積 worker 完走 35/35 = 全完走率 100%。
- worker prompt は **5 セクション構造** (背景/Why/担当範囲/実装上の注意/検証手順 + AGENT_LOG.md 記載項目) で書くと sonnet が 1 ターンで完走しやすい。
- frontend 系の場合、既存 pattern との「**完全対称コピー**」を強調すると、worker が設計判断に時間を使わず手筋を 1 つに固定できる。
- **prompt の数値矛盾を worker が自律検出・修正できる書き方** (累積知見): 具体的な数値例 + ラベル + 計算式の 3 点セット。worker 側で chrono 表現上限などの実コード制約を自律検出させる余地を残す。
- **「Loop 1 で意図的妥協を明示分離 → Loop N で Tidy After 消化」パターン** (本セッション 16 で確立): worker prompt で「最小スコープでは X、polish は次ループ送り」と明示することで 1 commit を絞り、後続ループで Tidy After として回収。Kent Beck 流リズムとの親和性高い。

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針。
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲。
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる。
- 本セッションでは 3 ループ + SESSION SUMMARY を終え、context 推定 ~35-50% で予防的ハンドオフ判断 (前 9 セッション (mjc-main-7〜15) と同じ 3 ループパターン継承で予測可能性優先)。

## ユーザー直伝指示 (未消化)
- なし。watchdog 継続指示は本セッション中ゼロ (canonical 移譲により旧 mjc-main を継いで開始したため、watchdog は新 main に対しても idle 検知のみ)。

## 累積成果 (本セッション)
- **テスト 456 → 457 passed** (+1 件、Loop 2 のみで rust test 追加)
- **コミット 3 件 + 本 SUMMARY 1 件 (commit ffcf1af)**
- **clippy 警告ゼロ維持** (default + -D warnings)
- **npm run build 通過維持** (bundle JS 486.47 KB / gzip 137.51 KB / CSS 86.34 KB / gzip 13.94 KB)
- **product-concept 優先度 9 録音状態の透明性 chain の per-source UX 移行完結** (Loop 1 = drop count badge を該当音声 source の状態 badge と並べて認知負荷低減)
- **drop badge の意味と視覚の一致** (Loop 3 = silence (グレー neutral) と drop (赤系 warning) の視覚分離)
- **audio source 識別子の rust 側集約 + frontend 同期検知装置確立** (Loop 2 = 前 mjc-main-15 Loop 2 と完全対称な 2 件目 application で知見定着)
- **累積 worker 完走 35/35** (100%)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -350 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-17 mjc-main` を実行 (冪等)。
4. 候補 F (会議終了検知遅延監視、価値 8、本セッションが UX polish 中心だったため次は機能側へ重心移動推奨、最有力) または G4 (transcription.rs source 定数化、Loop 2 の延長、規模 S) または B (test 補強、確実性高い) のいずれかを最小スライスとして worker に発注して 1 ループ目を始める。**F が最有力** (機能追加方向への転換、価値 8、ただし detection.rs を最初に読んでスコープ確認必要)。
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
