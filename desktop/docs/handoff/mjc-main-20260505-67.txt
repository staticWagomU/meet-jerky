あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-67) です。
旧 mjc-main (= mjc-main-20260505-66) から **3 ループ完走 + 通常 cadence handoff** のハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (Section 2.3 観測追記 19 件目 mjc-main-20260505-66 Loop 132 で更新観測、Phase 1-4 段階導入方針)**
- AGENT_LOG.md は ~33,358+ 行 / ~4MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-66 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2 〜 65**: Phase 1-6 + 各種抽出 + 大型 file 責務分離 12-41 file 目 + 検知拡張 plan + Phase 1 + Q5/Q6 Resolved + AGENT_LOG.md archive 戦略 plan + scope 28 軸目開拓 + 既存 file 拡張軸 = 新 paradigm + R6 段階的分割 1-3 件目 + utils 内 cross-import paradigm 1-2 件目
- **mjc-main-20260505-66 (本セッション)**: **3 ループ完走 = Loop 131 (frontend 軸 = R6 段階的分割 4 件目 = transcriptionEngineHelpers.ts 新規 = scope 29 軸目開拓 = 大型 frontend file 責務分離 42 file 目 = メイン批判判断 連続 65 セッション目 = handoff 主張「engine 関連 utils 既存ゼロ」を grep で批判的検証 → liveCaptionStatus.ts + modelSelectorHelpers.ts 既存発見 + 責務階層精査で異階層判定 = predicate/categorization tier 独立性確保) + Loop 132 (K 軸復帰 = Section 2.3 観測追記 19 件目 = +49 行 / 平均 ~24.5 行/loop = SS 1 件 + 説明軽量化パターン 3 連続観測 = 階層化 paradigm 強化証拠 + frontend 軸軽量化 paradigm 4 度目観測) + Loop 133 (frontend 軸 = R6 段階的分割 5 件目 = aiTransmissionHelpers.ts 新規 = scope 30 軸目開拓 = 大型 frontend file 責務分離 43 file 目 = AI Transmission 3 件 抽出 = display tier vs status object tier vs predicate tier の 3 階層精査で異階層判定 + utils 内 cross-import paradigm 3 件目確立 = aiTransmissionHelpers.ts → liveCaptionStatus の isExternalTransmissionLabel)** = メイン批判判断 連続 65 セッション目達成 + worker 自律 2-commit pattern 連続 74 ループ目達成 + harness 衛生連続 61 セッション目 + ファイル参照型 handoff prompt 連続 62 セッション目 + scope 多様性 30 軸到達 + alternation pattern 25 連続成功 (frontend(129) → K(130) → frontend(131) → K(132) → frontend(133) → K(134 推奨))

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価** (本セッション Loop 131 で「engine 関連 utils 既存ゼロ」主張を批判的検証 → liveCaptionStatus.ts + modelSelectorHelpers.ts 発見した precedent あり)
- **本セッション Loop 133 = R6 (TranscriptView.tsx 段階的分割) 5 件目採用 = 28 件純粋関数中 11 件抽出済 (audio level 2 + permission 2 + Track Aria 2 + engine 2 + AI Transmission 3) = 残 17 件 (5 グループ: Transcription Source 4 / Meeting Start 1 / Audio Source 4 / Engine Status 3 / External API Key 3)** = 後継も R6 段階的分割継続推奨だが alternation pattern 維持のため Loop 134 = K 軸 → Loop 135 = frontend 軸 が最適路
- **責務階層精査優先 (Loop 119/121/123/125/127/129/131/133 教訓)**: paradigm 反復よりも責務階層精査を優先 = 統合候補が同責務階層なら統合 OK、異階層なら新 file 作成軸採用 + 後継も同様の paradigm 選択は責務階層精査を優先 + **責務階層 = display tier / status object tier / predicate tier / model display tier / display tier (AI 送信状態) の 5 軸が確認済**
- **utils 内 cross-import paradigm**: Loop 133 で 3 件目確立 (1 件目 = transcriptDisplayHelpers.ts → audioTrackLabels、2 件目 = trackStatusAriaLabels.ts → audioTrackLabels、3 件目 = aiTransmissionHelpers.ts → liveCaptionStatus) = 新 file が他の utils file から定数/型/関数 import するパターン = 後継も活用検討可能
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **大型 frontend file 責務分離 = 43 file 目達成 (Loop 133)**
- **scope 30 軸目到達** (`aiTransmissionHelpers.ts`)
- **frontend file 規模ランキング (本セッション後 = 更新):**
  - SettingsView.tsx (`src/routes/`) 2010 行 (top-level 純粋関数 1 件 = 内部 component helper 抽出困難確認済 = 却下推奨)
  - **TranscriptView.tsx (`src/routes/`) 2361 行 (Loop 133 後)** = 残純粋関数 17 件 (5 グループ) = R6 段階的分割継続候補
  - SessionList.tsx (`src/routes/`) 553 行 (Loop 116 抽出後)
  - LiveCaptionWindow.tsx (`src/components/`) 18KB (Loop 108 抽出済)
  - MeetingDetectedBanner.tsx (`src/components/`) 474 行 (Loop 114 抽出後)
  - TranscriptDisplay.tsx (`src/components/`) 17KB (Loop 110 抽出済)
  - ModelSelector.tsx (`src/components/`) 16KB (Loop 112 抽出済)
- **rust file 規模ランキング (本セッション後 = 不変):**
  - app_detection.rs 2458 行 (Loop 123 抽出後) = 純粋関数候補ほぼなし = state 依存 wrapper + tests 集中
  - session_manager.rs 1245 行 / audio.rs 949 行 / transcript_bridge.rs 826 行 / settings.rs 801 行 / openai_realtime.rs 794 行 / cloud_whisper.rs 778 行 / session.rs 773 行 / session_store.rs 725 行 (各既抽出済 or 抽出余地小)

# 引き継ぎサマリ (2026-05-06 ~JST 時点)

## 現在の git 状態
- branch: main, ahead 492 (origin/main から先行、本セッション +7 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-66) の commit:
  1. `1b2a65e` refactor(frontend): TranscriptView の engine 機能特性 helpers を utils/transcriptionEngineHelpers.ts に抽出 (Loop 131)
  2. `14e9207` chore(agent-log): Loop 131 entry 追記
  3. `fd2f9c0` docs(architecture): Section 2.3 観測追記 19 件目 (Loop 132)
  4. `d155321` chore(agent-log): Loop 132 entry 追記
  5. `05cd5dd` refactor(frontend): TranscriptView の AI Transmission 系 helpers を utils/aiTransmissionHelpers.ts に抽出 (Loop 133)
  6. `7bb49f0` chore(agent-log): Loop 133 entry 追記
  7. `397431f` chore(agent-log): mjc-main-20260505-66 SESSION SUMMARY (handoff 直前)

## 現在の品質状態
- cargo test (lib): **704 passed / 0 失敗** (件数完全不変、本セッションは frontend 軸 + K 軸のみ変更のため再検証不要)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: ✓ built (Loop 133 = ~1926 modules transformed)
- bash -n scripts/claude-agent-*.sh: OK
- agent-verify.sh: 全項目 OK (Loop 133)

## 大型 file 責務分離実績 (累計 43 件)

### Rust 軸 (32 件累積、本セッション +0)
(前 handoff と同じ、Loop 131/132/133 は frontend + K 軸のため rust 不変)

### Frontend 軸 (Loop 107-133 = 11 件、本セッション +2)
- TranscriptView formatters (Loop 107) / LiveCaption tracks (Loop 108) / TranscriptDisplay (Loop 110) / ModelSelector (Loop 112) / MeetingDetectedBanner (Loop 114) / SessionList (Loop 116)
- TranscriptView audio level helpers → audioLevelHelpers.ts (Loop 125) = R6 段階的分割 1 件目
- TranscriptView permission status helpers → permissionStatusHelpers.ts (Loop 127) = R6 段階的分割 2 件目
- TranscriptView track ARIA helpers → trackStatusAriaLabels.ts (Loop 129) = R6 段階的分割 3 件目 = utils 内 cross-import paradigm 2 件目
- **TranscriptView engine 機能特性 helpers → transcriptionEngineHelpers.ts (Loop 131) = R6 段階的分割 4 件目 = scope 29 軸目開拓 = predicate/categorization tier 独立性確保**
- **TranscriptView AI Transmission helpers → aiTransmissionHelpers.ts (Loop 133) = R6 段階的分割 5 件目 = scope 30 軸目開拓 = display tier (AI 送信状態) = utils 内 cross-import paradigm 3 件目**

### 残候補 (Loop 134+ 候補)

- **K = agent-log-archive-plan.md Section 2.3 観測追記 20 件目** = Loop 134 推奨 = alternation 26 連続維持 (frontend(133) → K(134))
- **R6 続行 = TranscriptView.tsx 残 17 純粋関数 (5 グループ)** = Loop 135 推奨 = scope 31 軸目候補 = 規模 SS-S = 要 grep + 責務階層精査 (Loop 119-133 教訓継承)
  - グループ別候補: Transcription Source (4) / Meeting Start (1) / **Audio Source (4) = ~5-7 分完走見込み = 推奨 1 件目候補 = 新 file `audioSourceHelpers.ts`** / Engine Status (3) / External API Key (3) / Helper共通 (1)
  - 推奨 1 件目候補 = **Audio Source 系 4 件 = 規模 SS-S = ~5-7 分完走 = 新 file 候補** (要 grep で関数名と挿入位置確認)
  - 推奨 2 件目候補 = **Engine Status 系 3 件 = 規模 SS = ~3-5 分完走 = 新 file 候補**
- rust 軸続行候補は枯渇傾向 (R6 採用後の Loop 137+ で具体スライス grep 検討)
- SettingsView.tsx 内部 component helper = **抽出困難確認済** = 却下推奨
- 検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可
- X' resample_audio dead code 削除 = ユーザー直伝指示要
- H1 AGENT_LOG.md archive plan Phase 1 着手 = ユーザー直伝指示要

## harness 衛生事象 (本セッションで観測継続 = 連続 61 セッション目)
- canonical 移譲後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 61 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern (refactor/docs + chore エントリ)** は本セッション Loop 131/132/133 で worker 自律実行 = 連続 74 ループ目達成 = **paradigm pivot (frontend + K + frontend)** 全て完璧成功
- **API 互換維持戦略 (Loop 131/133 補強)**: frontend 軸 = 純粋関数 export 化 + import 1 行追加で完結 + 新 file が他の utils file (Loop 133 = liveCaptionStatus.ts の isExternalTransmissionLabel) から内部 import = **utils 内 cross-import paradigm 3 件目** (1 件目 = transcriptDisplayHelpers.ts、2 件目 = trackStatusAriaLabels.ts、3 件目 = aiTransmissionHelpers.ts) = paradigm 安定発展

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-66、handoff 直前)
- mjc-main-20260505-67 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -10` で現状確認 (上 7 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-67` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-67 mjc-main` を実行 (前 62 セッション precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 61 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 62 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-66) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 131/132/133 + 3 ループ完走 + handoff 候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 30 ループで構造分離 35 件 (Loop 131/133 含む) + 機能拡張/docs 3 件 + harness 衛生 18 件 (Loop 132 含む) = 35:3:18 多軸分散 + scope 多様性 30 軸到達 + frontend 軸 11 件 + alternation pattern 25 連続成功 = Loop 134 は K 軸 (Section 2.3 観測追記 20 件目) = alternation 26 連続維持の最適路**.

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 134 開始時点)

直近 30 ループ: Loop 104-133 (本セッション後 = 30 ループ)
- 軸 pattern: 構造分離 (35 件 = Loop 107/108/110/112/114/116/117/119/121/123/125/127/129/131/133 frontend + rust 含む) + 機能拡張/docs (3 件) + harness 衛生 (18 件 = Loop 113/115/118/120/122/124/126/128/130/132 含む) = **35:3:18 多軸分散**
- scope 内訳: 30 scope 分散 (rust 21 + frontend 9)
- paradigm pivot 連続: alternation pattern 25 連続成功 (frontend(129) → K(130) → frontend(131) → K(132) → frontend(133))
- **Loop 134 では更なる paradigm pivot 推奨 (frontend 軸 (Loop 133) からの pivot = K 軸 = alternation 26 連続維持)**

### 候補 K (Loop 134 候補 A = 最有力推奨 = K 軸続行 + alternation 維持 = 26 連続)

**agent-log-archive-plan.md Section 2.3 観測追記 20 件目**:
- precedent: Loop 79/81/86/95/98/102/106/109/111/113/115/118/120/122/124/126/128/130/132 = 19 件
- 観測時点: Loop 133 完了時点 = 観測すべき AGENT_LOG.md 行数を `wc -l AGENT_LOG.md` で確認 (~33,358 行想定)
- 規模 SS = ~3-5 分完走見込み
- alternation pattern: frontend(133) → K(134) = **26 連続維持の最適路**
- **最有力推奨**

### 候補 R6f (Loop 134 候補 B = R6 続行 = Audio Source 系)

**TranscriptView.tsx Audio Source 系 4 純粋関数抽出**:
- 対象 = 残 17 純粋関数のうち Audio Source 系 4 件 (要 grep で正確な関数名 + 行番号確認)
- 規模 = SS-S = ~5-7 分完走見込み
- paradigm = frontend 軸 12 件目 = 純粋関数機能分離軸 frontend 版 9 件目
- precedent = mjc-main-20260505-66 Loop 131/133 (R6 段階的分割 4-5 件目)
- **新 file 名候補**: `audioSourceHelpers.ts`
- 副推奨 (alternation reset リスク = K(132) → frontend(133) → frontend(134) = 連続 frontend = pivot 失敗)
- **Loop 135 推奨**

### 候補 R6g (Loop 134 候補 C = R6 続行 = Engine Status 系 = 規模 SS)

**TranscriptView.tsx Engine Status 系 3 純粋関数抽出**:
- 対象 = 残 17 純粋関数のうち Engine Status 系 3 件 (要 grep で正確な関数名 + 行番号確認)
- 規模 = SS = ~3-5 分完走見込み
- 副推奨 = Loop 135-137 候補

### 低優先 (継承)

- J' SettingsView.tsx 内部 helper (抽出困難確認済 = 却下推奨)
- X' resample_audio dead code 削除 (ユーザー直伝指示要)
- H1 AGENT_LOG.md archive plan Phase 1 着手 (ユーザー直伝指示要)
- B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止、`--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止、Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-67 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認**
  - 観測時間目安: 規模 SSS = ~2-3 分、規模 SS = ~3-5 分、規模 SS-S = ~4-7 分、規模 S = ~5-15 分
  - **本セッション Loop 131 例**: 規模 SSS = ~2.5 分完走 (worker prompt 140 行 = 純粋関数 2 件抽出 + 新 file ~20 行)
  - **本セッション Loop 132 例**: 規模 SS = ~3 分完走 (worker prompt 130 行 = K 軸 docs entry)
  - **本セッション Loop 133 例**: 規模 SS-S = ~3-4 分完走 (worker prompt 145 行 = 純粋関数 3 件抽出 + 新 file ~40 行 + cross-import paradigm 3 件目)
  - **worker 自律 2-commit pattern**: Loop 60-133 の **74 ループ連続** で観測 = 完全に安定 pattern 化
  - **worker 自律学習進化**: refactor/docs commit hash placeholder の実 hash 置換 + chore commit hash 意図的 placeholder 化 + cfg(macos) module 分離 + frontend 軸 paradigm + 既存 file 拡張軸 + API 互換維持戦略 (`pub use` / `pub(crate) use` / inline module 全体抽出 + visibility 修正) + frontend 軸の純粋関数 export 化 + import 1 行追加で完結 + **utils 内 cross-import paradigm (新 file → 別 utils file の constants/types/関数 を import) Loop 133 で 3 件目確立** も成功

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない**
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 62 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-133 経験から)**:
- worker prompt に「2-commit pattern」を **明示的に** 記載 = 74 連続ループで完全安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨 (Loop 133 で audioLevelHelpers.ts + permissionStatusHelpers.ts + trackStatusAriaLabels.ts + transcriptionEngineHelpers.ts の 4 file cat 推奨で AI Transmission 抽出成功 = utils 内 cross-import paradigm 3 件目確立)
- **worker prompt の長さに注意**: ~83-230 行で短縮効果あり (Loop 67-133 で実証 ~1.5-13 分完走)
- **frontend 軸の場合**: 検証は npm run build (tsc + vite build) のみ
- **rust 軸の場合**: cargo test (704 件件数完全不変) + cargo clippy + cargo fmt + cargo build を全て worker prompt に記載
- **API 互換維持戦略 (Loop 119-133 precedent)**:
  - **rust 軸**: `pub use` re-export (Loop 119) / `pub(crate) use` re-export = E0364 visibility 自律 fix (Loop 121) / inline module 全体抽出時の visibility 修正 + caller 1 件 fully qualified path 化で完結 (Loop 123)
  - **frontend 軸**: 純粋関数 export 化 + import 1 行追加で完結 (Loop 125/127/131/133 標準 paradigm) / **新 file が他の utils file から定数/型/関数 import = utils 内 cross-import paradigm (Loop 129 = 2 件目, Loop 133 = 3 件目)**
- **責務階層精査 (Loop 119-133 教訓)**: paradigm 反復よりも責務階層精査を優先。低レベル parser と高レベル分類器を強引に同 file 統合すると責務肥大化リスク。本 Loop 131/133 = display tier vs status object tier vs predicate tier vs model display tier vs display tier (AI 送信状態) の 5 階層精査で異階層判定 + 新 file 作成軸採用 = Loop 119-129 教訓の継承発展

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 131 = 規模 SSS ~2.5 分 / Loop 132 = 規模 SS ~3 分 / Loop 133 = 規模 SS-S ~3-4 分 = **3 ループ全て目標 15 分以内達成**)
- worker 1 件 = 1-2 commit (本セッション 3 ループ完走 = 6 commit + 1 SESSION SUMMARY commit = 7 commit)
- 累積 worker 完走 232/232 (本セッション +3 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **3 ループ完走 + 通常 cadence handoff** = handoff 文書「2-3 ループ完走推奨」上限達成 = 前任 mjc-main-20260505-65 と同パターン継承

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」 (継承)
- watchdog 継続指示: 「docs/autonomous-main-prompt-claude.md に従って次の自律改善ループへ進んでください」 (継承)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -10` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-67` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-67 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 61 セッション目「再出現せず」観測)
5. **戦略転換の核心を継承**: 単一軸 4 連続集中をしない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。
6. **最初の Loop 候補**: variety 規則「frontend(133) → 次は K 軸 = alternation 26 連続維持」を踏まえ、Loop 134 で **(a) 候補 K = K 軸続行 = 最有力推奨 (規模 SS) または (b) 候補 R6f = Audio Source 系 4 件抽出 = 副推奨 (規模 SS-S) または (c) 候補 R6g = Engine Status 系 3 件抽出 = Loop 135-137 候補 (規模 SS) または (d) 候補 X' / H1 = ユーザー直伝指示要** のいずれか採用検討。**最有力推奨 = 候補 K (Section 2.3 観測追記 20 件目)**。worker prompt は「Loop 60-133 と同じ 2-commit pattern」を明示 + ~120-160 行 (規模 SS、Loop 132 precedent)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (本セッション Loop 131 で実証 = handoff 主張を批判的検証 → liveCaptionStatus.ts 発見 + 責務階層精査済 R6 採用 + Loop 133 = utils 内 cross-import paradigm 3 件目確立 → メイン批判判断 連続 65 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 3 ループ + 通常 cadence handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**、**worker prompt は ~80-180 行を目安に simpler 化**
10. **worker 完走確認は `git status --short` または `git log --oneline -3` を主観測手段とする**
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK**
12. **commit hash 更新漏れパターン (Loop 60-133 で 74 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern」を明示記載 = 74 連続ループで安定動作。**API 互換維持戦略 (Loop 133 frontend 軸版 = 純粋関数 export 化 + import 1 行追加 + utils 内 cross-import paradigm 3 件目)** を後継も活用検討可能

# Loop 134 への期待 (推奨ループ設計)

直近 30 ループで構造分離軸 35 件 (Loop 107/108/110/112/114/116/117/119/121/123/125/127/129/131/133 frontend + rust 含む) + 機能拡張/docs 軸 3 件 + harness 衛生軸 18 件 = frontend(133) → **Loop 134 では更なる paradigm 多様化推奨 (frontend 軸 (Loop 133) からの pivot = K 軸 = alternation 26 連続維持)**。

メインは grep で 1 候補に絞る (本ハンドオフでは候補 K = Section 2.3 観測追記 20 件目 = 規模 SS = paradigm pivot + alternation 26 連続維持 を最有力推奨、R6f = Audio Source 系 4 件 = Loop 135 推奨、R6g = Engine Status 系 3 件 = Loop 135-137 候補) → worker prompt 作成 (~120-160 行) → 起動 = 通常の自律改善ループへ復帰。

**最有力安全候補は候補 K = Section 2.3 観測追記 20 件目 (K 軸)**:
- 規模 SS 想定 (~3-5 分完走) = worker prompt は ~120-160 行
- paradigm = K 軸 = harness 衛生軸 19 件目
- precedent = mjc-main-20260505-66 Loop 132 (Section 2.3 観測追記 19 件目)
- 観測時点: Loop 133 完了時点 = AGENT_LOG.md 行数 = `wc -l AGENT_LOG.md` で確認 (~33,358 行想定)
- alternation pattern: frontend(133) → K(134) = 26 連続維持

**K 軸採用前の観測手順 (Loop 134 で適用)**:
1. `wc -l AGENT_LOG.md` で現行行数を取得
2. 前回観測値 (Loop 131 完了時点 = 33,309 行) との差分計算
3. 期間 (Loop 132 → Loop 133 = 2 ループ間 + SESSION SUMMARY 1 件 mjc-main-20260505-66) と比較して平均行数/loop 計算
4. precedent 観測 entry (Loop 132 = SS 1 件 ~24.5 行/loop) との比較で paradigm 安定性検証
5. **メイン批判判断**: 観測値が precedent と整合するか、想定外の異常値が出ていないか精査
- 推奨抽出順序 (Loop 135 以降): Audio Source 4 (規模 SS-S = ~5-7 分) → Engine Status 3 (規模 SS) → External API Key 3 (規模 SS-S) → Transcription Source 4 (規模 S = 4 件まとめ) → Meeting Start 1 (規模 SSS) → Helper共通 1 (規模 SSS)
