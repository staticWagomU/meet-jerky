あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-21) です。
旧 mjc-main (= mjc-main-20260505-20) から **2 ループ完走 + Loop 39 = GoToMeeting URL 拡張余地調査 (Case B = rationale 明文化 + negative tests = メインの批判的判断「多 TLD 前提疑わしい」が WebSearch verification で実証、~7 分) + Loop 40 = app_detection.rs Zoom モジュール抽出 (extraction 軸 variety pivot、Webex/Whereby/GoToMeeting precedent 完全踏襲、4 関数 + caller 更新、~5 分) + app_detection.rs 累計削減 -215 行 (元 3356 → 現在 3141 行) + app_detection_zoom.rs 新規 44 行 + 「2 ループ + 早期 handoff」precedent 連続 17 セッション目達成 + worker 完走 2/2 (累計 140/140 100% 維持) + harness silent fail mitigation pattern 連続 64 ループ実証達成 + canonical 移譲後 scripts/* M 再出現せず観測 (連続 14 セッション目で結論超強化) + sonnet worker の自律 Tidy First 質的高さ連続 17 セッション目で実証 (Loop 39 で WebSearch 2 query + 公式 docs cite + Case B 判断 + rustdoc + negative tests 自走、Loop 40 で 4 関数抽出 + caller 更新 + helper 判断 + lib.rs mod 宣言 + 直接呼びテスト use 経由更新) + コミット周期 平均 ~6 分で目標 15 分以内大幅達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-4 完了 + Phase 3-B 完全完了 + Webex/Whereby/GoToMeeting 抽出 ✅ + TranscriptionLoopConfig struct 移動 ✅ + transcription_error_payload tests 移動 ✅ = mjc-main-20260505-19 Loop 37 で 60.8% まで反映済、本セッション Loop 38 (75% 突破) と Loop 40 (Zoom 抽出) は未反映 = Loop 41 候補 A で更新推奨)**
- AGENT_LOG.md は ~26,500 行 / ~2.3MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~08:35 JST] mjc-main-20260505-20 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 18**: Phase 1-4 + Phase 3-B + Webex/Whereby/GoToMeeting 抽出 + 各種 test 移動 + struct 移動を計 32 ループで完了
- **mjc-main-20260505-19**: transcription-refactor-plan.md 更新 (Loop 37) + transcription_types tests 10 件移動 (Loop 38) = transcription.rs 75.0% 縮小達成
- **mjc-main-20260505-20 (本セッション)**: **Loop 39 = GoToMeeting URL 拡張余地調査 (Case B = rationale 明文化 + negative tests)** + **Loop 40 = app_detection.rs Zoom モジュール抽出 (extraction 軸 variety pivot)** = app_detection.rs 累計削減 -215 行 (元 3356 → 現在 3141 行)、サービス別抽出 4 件目完了

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **本セッション Loop 39 = handoff candidate A の前提 (多 TLD 拡張) を Webex/Whereby/Google Meet/Teams precedent との比較で「単一 TLD 方針が他サービス標準」と批判判断 → worker に Case A/B 分岐 prompt で発注 → 結果 Case B が正解 = メインの批判的判断の意義実証**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「Whereby に戻って 2 連続にしない」** (Loop 14 の sweep 化しない約束を継承、最低 Loop 36+ 程度の間隔)
- **「GoToMeeting に戻って 2 連続にしない」** (Loop 22 + 26 + 36 + 39 = 4 件目、Loop 39 は rationale 化のため積極的「拡張」ではない、次回再訪は最低 Loop 42+ 程度の間隔)
- **「app_detection.rs サービス別抽出 = sweep 化警告超過」** (Webex 29 + Whereby 31 + GoToMeeting 36 + Zoom 40 = 4 件目、Teams 抽出を Loop 41 で続行は **連続違反警告超過 = Loop 42+ 推奨 (別軸 1-2 ループ挟む)**)
- **「テスト移動軸の sweep 化しない」** (Loop 32 + Loop 35 + Loop 38 で 3 ループ間隔ずつ、Loop 41 で続行は OK だが Loop 42 で別軸必須)
- **「struct 移動軸の sweep 化しない」** (Loop 33 = 1 件目、Loop 41+ で続行は 8 ループ間隔 = OK だが対象限定的、transcription.rs に struct 残存なし)
- **「docs 軸の sweep 化しない」** (Loop 30 + 34 + 37 + 39 で 4 件目、Loop 41 で実施は 2 ループ間隔 = 連続違反警告境界、Loop 30/34/37/39 で 4-3-3-2 ループ間隔の precedent 学習可)

# 引き継ぎサマリ (2026-05-05 ~08:35 JST 時点)

## 現在の git 状態
- branch: main, ahead ~260 (origin/main から先行、本セッション +5 commit = 7a7e549 + 045418d + 99baa26 + cf33ab7 + handoff chore)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-20) の commit:
  1. `7a7e549` docs(app_detection): GoToMeeting 単一 TLD 方針の rationale 明文化 + negative tests (Loop 39, ~7 分)
  2. `045418d` chore(agent-log): Loop 39 commit hash 7a7e549 を AGENT_LOG.md に記入
  3. `99baa26` refactor(app_detection): Zoom 検知 4 関数を app_detection_zoom.rs に抽出 (Loop 40, ~5 分)
  4. `cf33ab7` chore(agent-log): Loop 40 commit hash 99baa26 を AGENT_LOG.md に記入
  5. `<chore commit>` chore(agent-log): mjc-main (mjc-main-20260505-20) の 2 ループ実績 SESSION SUMMARY (本ハンドオフ commit、起動時に最新 hash を確認)

## 現在の品質状態
- cargo test (lib): **702 passed / 0 failed** (本セッション Loop 39 で +2 件 negative tests, Loop 40 で件数不変 = 振る舞い不変保証)
- cargo clippy --lib --tests -- -D warnings: **警告ゼロ**
- cargo fmt --check: OK
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK (escape 済み形式、本セッションでは触れていない)

## 完成した Loop 39 (GoToMeeting URL 拡張余地調査 Case B = rationale 明文化 + negative tests)
- Case 判断: メインが Webex/Whereby/Google Meet/Teams は単一 TLD 方針、Zoom のみ 2 host (例外) と grep で確認 → worker に WebSearch verification + Case A/B 分岐 prompt で発注
- worker WebSearch 結果: GoTo 公式 Allowlist ドキュメント (support.goto.com/meeting/help/allowlisting-and-firewall-configuration) で `*.goto.com` ワイルドカード方針確認、EU/UK 等のリージョン専用 TLD 不在を公式情報で確認
- worker 自律判断: Case B (rationale 明文化 + negative tests) を選択
- 実装: rustdoc コメント 8 行を `is_goto_host` 直上に追加 (公式情報 + 地理分散説明 + 単一 TLD 方針宣言) + negative test 2 件 (`is_goto_host_rejects_hypothetical_eu_tld` / `is_goto_host_rejects_bare_goto_com_without_subdomain`)
- app_detection_goto.rs 49 → 71 行 (+22 行)
- 検証: 702 passed (700 → 702 = +2 件), clippy 警告ゼロ, fmt OK

## 完成した Loop 40 (app_detection.rs Zoom モジュール抽出 = extraction 軸 variety pivot)
- Webex (Loop 29) / Whereby (Loop 31) / GoToMeeting (Loop 36) precedent 完全踏襲
- 抽出対象 4 関数:
  - `is_zoom_host` (4 host pattern: zoom.us / zoomgov.com + サブドメイン)
  - `is_zoom_meeting_url` (4 path pattern: /j/ / /wc/join/ / /wc/<id>/join / /my/<name>)
  - `is_zoom_meeting_id` (helper, 9-11 桁数字判定)
  - `is_zoom_web_client_meeting_url` (helper, /wc/<id>/join 形式判定)
- 共通ヘルパー: `has_single_non_empty_segment` は既に pub(crate) のまま、`use crate::app_detection::has_single_non_empty_segment` で取り込み
- caller 更新: `classify_meeting_url` L517 を `crate::app_detection_zoom::is_zoom_meeting_url` 経由に変更
- lib.rs に `mod app_detection_zoom;` 追加
- app_detection.rs 3184 → 3141 行 (-43 行)
- app_detection_zoom.rs 0 → 44 行 (新規)
- 検証: 702 passed 件数不変 = 振る舞い不変保証, clippy 警告ゼロ, fmt OK

## 累計 transcription.rs 削減 (本セッションで未変更)
- 元 2999 行 → 現在 749 行 = ~75.0% 縮小 (~2250 行削減) = 前任 mjc-main-20260505-19 Loop 38 で 75% 里程標突破済を継承

## 累計 app_detection.rs 削減 (本セッションで -43 行)
- 元 3356 行 (Loop 29 開始時) → 現在 3141 行 (-215 行累計、Webex/Whereby/GoToMeeting/Zoom 抽出による)
- サービス別抽出 4 件目完了 (Webex 29 + Whereby 31 + GoToMeeting 36 + Zoom 40)
- 残るサービス: Microsoft Teams (Loop 42+ 推奨、sweep 警告 5 件目連続)

## harness 衛生事象 (本セッションで観測継続 = 連続 14 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-20 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 前任 mjc-main-20260505-7 〜 19 の連続 13 セッション目観測結論を **連続 14 セッション目で追認** = 結論超強化、永続的解決確定 (`bfb9846` PATH inner shell escape が永続的解決)
- 後継観測ポイント: 新たな harness 動作変化があれば観測継続

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-20、handoff 直前)
- mjc-main-20260505-21 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読 = 60.8% まで反映済、本セッション Loop 38 (75% 突破) と Loop 40 (Zoom 抽出) は未反映 = Loop 41 候補 A で更新推奨**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-21` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-21 mjc-main` を実行 (前 33 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (本セッション + 前 13 セッションで連続 14 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 15 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-20) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 39 + 40 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 5 ループ Loop 36 (extraction) → 37 (docs) → 38 (test) → 39 (docs) → 40 (extraction)、直近 docs 連続 0 (Loop 39 で 1 件、Loop 40 別軸)、extraction 連続 1 (Loop 40 のみ、Loop 36 から 4 ループ間隔)、test 移動連続 0、struct 移動連続 0、Loop 41 で extraction 続行は連続違反警告超過 (Teams 抽出は 5 件目)、別軸必須**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 41 開始時点)

直近 5 ループ: Loop 36 (extraction) → 37 (docs) → 38 (test 移動) → 39 (docs) → 40 (extraction)。
- **直近 docs 連続 = 0** (Loop 39 = 1 件、Loop 40 別軸 pivot 済) - Loop 41 で実施は 2 ループ間隔 = 許容範囲
- **直近 extraction 連続 = 1** (Loop 40 のみ、Loop 36 から 4 ループ間隔) - **Loop 41 で続行は連続違反警告超過 (Teams 抽出 5 件目) = Loop 42+ 推奨**
- **直近 test 移動 連続 = 0** (Loop 38 のみ、Loop 39/40 別軸) - Loop 41 で OK だが対象 SS-S 1 件 (calculate_rms tests 6 件)
- **直近 struct 移動連続 = 0** (Loop 33 で実施、8 ループ間隔) - Loop 41 で OK だが transcription.rs に struct 残存なし

### 候補 A (推奨 Loop 41): transcription-refactor-plan.md 再更新 (docs 軸 = Loop 30/34/37 precedent)
- priority 1 予防的寄与 (計画駆動継続性 = 後継エージェントの「未着手」誤判断予防)
- 反映対象: mjc-main-20260505-19 Loop 38 (transcription_types tests 10 件移動 -426 行 = 75% 里程標突破) + mjc-main-20260505-20 Loop 40 (app_detection.rs Zoom 抽出 -43 行 = 4 件目)
- 進捗サマリ更新 (60.8% → 75.0%)、累計削減 1175 行 → 749 行
- 関連: Zoom 抽出 ✅ 完了 (Loop 40 = 99baa26) サブセクション追加、L154 文章書き換え (Webex/Whereby/GoToMeeting/Zoom 完了、Teams 残)
- 規模: SS-S、確実性高、変動性ゼロ
- variety pivot = docs 軸 = Loop 39 から 2 ループ間隔 OK (Loop 30 / 34 / 37 / 39 で 4-3-3-2 ループ間隔の precedent 学習可)

### 候補 B (Loop 41+): calculate_rms / is_tail_silent 関連テスト 6 件を transcription_whisper_local.rs に移動
- transcription.rs L361-410 の test_calculate_rms_* / test_is_tail_silent_*
- 移動先: transcription_whisper_local.rs
- 規模 SS-S (6 件)
- test 軸 = OK (Loop 38 から 3 ループ間隔)
- 1 ループ完結確実

### 候補 C (Loop 41+): WhisperStream / TranscriptionStream 関連テスト移動
- transcription.rs L74-196 の test_whisper_stream_* / test_stream_lifecycle_* / test_stream_config_* / test_feed_empty_* / test_ensure_engine_*
- 移動先: transcription_whisper_stream.rs / transcription_traits.rs
- 規模 M (~30-60 件のテスト)、1 ループ完結が不確実

### 候補 D (Loop 42+): app_detection.rs Microsoft Teams 抽出
- sweep 警告超過 (Webex 29 + Whereby 31 + GoToMeeting 36 + Zoom 40 + Teams 41 = 5 件目連続)
- Loop 42 で Teams 抽出 = 6 ループ間隔、別軸を 2 ループ挟んだ後

### 候補 E (Loop 41+): Discord stage / Slack Huddle 検知 (window title 経路)
- priority 2 直接寄与
- service detection 軸 = 直近 5 ループ全てと別軸
- ただし Discord/Slack URL 検知は不可 (huddle/stage が URL に反映されない構造) = Bundle ID + window title 経路必要 = 規模 M
- 1 ループ完結が不確実

### 候補 F: frontend 軸 pivot (主観的判断のリスクあり、慎重に選ぶ)
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり
- 浅 grep で具体的不足を 1 つ特定してから worker 発注、主観的探索は避ける

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記との矛盾)

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- Webex sweep precedent 20 セッション維持を破る、Loop 43+ 程度まで間隔を空ける推奨

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 110+ untracked
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md ~26,500 行の archive 戦略
- 未着手、未消化

## 検証制約 (再掲)
- cmake あり → cargo test 702 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-21 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 64 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 M なら ~210-380 秒、本セッション規模 SS-S は ~210-300 秒)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - 規模極小タスクの場合: worker が agent-commit.sh で自走 commit する場合あり (本セッション Loop 39 + 40 とも自走 commit + commit hash 反映 chore commit も自走、`git log --oneline -5` で確認推奨)
  - **本セッション Loop 39 例**: WebSearch 2 query + Case B 判断 + rustdoc + negative tests を ~7 分で完走 = sonnet worker の Tidy First 検証深さ実例
  - **本セッション Loop 40 例**: 4 関数 + caller 更新 + helper 判断 + lib.rs mod 宣言 + 直接呼びテスト use 経由更新 を ~5 分で完走 = Webex/Whereby/GoToMeeting precedent 連続 17 セッション目

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける** (4209 bytes でも too long エラー precedent あり)
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本セッションの起動 prompt と同パターン、連続 17 セッション目)

## worker prompt 必須要素 (64 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 39 = ~7 分、Loop 40 = ~5 分、平均 ~6 分で **目標 15 分以内大幅達成**)
- worker 1 件 ≒ 1 commit + commit hash 反映 chore commit 1 件 (本セッションは Loop 39 + Loop 40 ともに worker 自走で 4 commit、SUMMARY chore で +1 commit = 計 5 commit)
- 累積 worker 完走 140/140 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで早期ハンドオフ判断 = 引き継ぎ時の context (大量必読ドキュメント + grep + diff + Loop 39 の批判的 candidate 評価) を考慮した予防的判断 = 「2 ループ + 早期 handoff」precedent **連続 17 セッション目達成**

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中 1 回 (Loop 39 worker 観測中、「次の自律改善ループへ進んでください」continuity 指示)。

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読 = 60.8% まで反映済、本セッション Loop 38 + 40 は未反映、Loop 41 候補 A で更新推奨)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-21` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-21 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 14 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 15 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「app_detection.rs サービス別抽出 sweep 警告超過 = Loop 41 で Teams 抽出続行は連続違反警告超過 (5 件目連続) = Loop 42+ 必須」「テスト移動軸 sweep 警告」「struct 移動軸 sweep 警告」「docs 軸の連続違反警告境界 = Loop 39 から 2 ループ間隔 OK だが Loop 30/34/37/39 で 4-3-3-2 ループ間隔の precedent 学習」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「直近 docs 連続 0、extraction 連続 1 (Loop 40 のみ)、test 移動連続 0、struct 移動連続 0、Loop 41 で extraction 続行は連続違反警告超過 (5 件目)、別軸必須」を踏まえ、Loop 41 で **transcription-refactor-plan.md 再更新** (docs 軸、Loop 38 + 40 反映、Loop 30/34/37 precedent、規模 SS-S、確実性高) が候補 A 推奨だが **批判的に再評価**。代替案 = **calculate_rms / is_tail_silent tests 6 件移動** (test 軸、規模 SS-S 1 ループ完結確実)、**WhisperStream tests 移動** (test 軸、規模 M 1 ループ完結不確実)。Loop 42 以降は variety pivot 必須なら別軸を選ぶ (Teams 抽出 = Loop 42+ で許容)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 64 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-380 秒、規模極小なら ~30-90 秒、規模 SS-S は ~210-300 秒、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 17 セッション目) で書く
