あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-8) です。
旧 mjc-main (= mjc-main-20260505-7) から **2 ループ完走 + Phase 2-A 完全完了 (WhisperStream 抽出で 6 件責務分離達成) + Whereby blacklist marketing pages 4 件追加 (priority 2 直接寄与, variety pivot 規則準拠) + 累計 transcription.rs ~599 行削減 (元 2999 → 現在 2400 行 = ~20% 縮小) + 「2 ループ + 早期 handoff」precedent 連続 4 セッション目達成 + 「ファイル参照型 handoff prompt」precedent 継承 (連続 4 セッション目) + worker 完走 2/2 (累計 114/114 100% 維持) + harness silent fail mitigation pattern 連続 38 ループ実証達成 + harness 衛生事象の観測終了 (canonical 移譲後 scripts/* M 再出現せず = 前任 bfb9846 が永続的解決と確認) + コミット周期 Loop 13 ~8 分 / Loop 14 ~3 分 平均 ~5.5 分で目標 15 分以内達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1 完了済 + Phase 2-A 完全完了済 (= 本セッションで達成) + Phase 2-B ModelManager 完了済 / Phase 3 (TranscriptionManager / commands) + Phase 4 (Worker loop) は未着手)**
- AGENT_LOG.md は ~24,750 行 / ~1.97MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~03:50 JST] mjc-main-20260505-7 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」も同質のローカル最適化に変質しうる。**「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」** ルール確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent 創出 (B1 Teams window title を前任者 rustdoc 注記との矛盾で却下)、「Mutex poison panic 候補発見」の自己誤認訂正、**「grep 結果は Read で精読する」運用ルール強化**
- **mjc-main-20260505-4**: 「stdin redirect 起動」precedent 創出、「2 ループ + 早期 handoff」precedent 強化、Whereby URL 検知で Webex sweep からの 4 セッション連続 variety pivot 維持、Phase 2-A 着手 = audio_utils.rs への純粋関数集約パターン確立
- **mjc-main-20260505-5**: 「stdin redirect 化を script に焼き付け」 = 3 件 precedent からの harness 本体改善、「ファイル参照型 handoff prompt」精度継承、Phase 2-B ModelManager 抽出完了
- **mjc-main-20260505-6**: Phase 2-A WhisperLocal 抽出完了 + Phase 2-A Audio resampling 統合完了、「2 ループ + 早期 handoff」precedent 連続 3 セッション目達成、harness 衛生 commit (`chore(harness)` で scripts/* M = canonical 移譲時の自動 PATH escape 修正を別 commit 化、出処は本セッション特定不能 = 後継への調査委譲)
- **mjc-main-20260505-7 (本セッション)**: **Phase 2-A 完全完了** (Loop 13 = WhisperStream 抽出 ~189 行 + 定数 6 つ pub(crate) 昇格 + 構造体 fields pub(crate) + is_tail_silent を tests mod 内 import に整理) + **Whereby blacklist marketing pages 4 件追加** (Loop 14 = priority 2 直接寄与, service detection 軸 variety pivot, sweep 化しない約束)、**「2 ループ + 早期 handoff」precedent 連続 4 セッション目達成**、**harness 衛生事象の観測終了** (canonical 移譲後 scripts/* M 再出現せず = 前任 mjc-main-20260505-6 の bfb9846 chore(harness) commit が永続的解決と確認 = 「観測してから判断」運用の効果実例)、**「鵜呑みにせず批判的判断」継承実例** (前任が「Whereby blacklist は Webex sweep 同質リスク」と懸念したが批判的再評価で単発補強と sweep 化は本質的に異なると判定して採用)

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「Whereby に戻って 2 連続にしない」** (Loop 14 の sweep 化しない約束を継承、最低 Loop 18-20 程度の間隔)

# 引き継ぎサマリ (2026-05-05 ~03:50 JST 時点)

## 現在の git 状態
- branch: main, ahead 211 (origin/main から先行、本セッション +3 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-7) の commit:
  1. `76eeb92` refactor(transcription): WhisperStream を transcription_whisper_stream.rs に抽出 (Tidy First, Phase 2-A 完全完了, Loop 13, ~189 行抽出 + 互換層 + lib.rs mod 宣言, 定数 6 つ pub(crate) 昇格 + struct fields pub(crate) + is_tail_silent を tests mod 内に移動 + whisper_rs::WhisperContext の transcription.rs 側 use 削除, 674 passed 件数不変, ~8 分)
  2. `3a8c68e` feat(app_detection): Whereby marketing pages (download / app / for-teams / developers) を WHEREBY_NON_ROOM_PATHS blacklist に追加 (Loop 14, AGENTS.md 優先順位 2 = 会議検知の網羅性, mjc-main-20260505-4 Loop 7 の Whereby 検知実装の残リスク消化, 6 ループ間隔の単発補強 = sweep 化しない明示的約束, TDD Red→Green, テスト 4 件追加で 678 passed = 674 + 4, ~3 分)
  3. `4434266` chore(agent-log): mjc-main (mjc-main-20260505-7) の 2 ループ実績 SESSION SUMMARY

## 現在の品質状態
- cargo test (lib): **678 passed / 0 failed** (Loop 14 で +4 件 = 674 → 678)
- cargo clippy --lib -- -D warnings: **警告ゼロ** (本セッション 2 ループで維持)
- cargo fmt --check: OK
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK (escape 済み形式、本セッションでは触れていない)

## 完成した Phase 2-A WhisperStream (Loop 13)
- `src-tauri/src/transcription_whisper_stream.rs` (新規, 200 行): `pub struct WhisperStream` (10 fields すべて `pub(crate)` で test helper 互換) + `impl WhisperStream` (`pub(crate) fn new` + private flush_full_chunks + private run_inference) + `impl TranscriptionStream for WhisperStream`
- `src-tauri/src/transcription.rs`: WhisperStream セクション ~189 行削除 + `pub use crate::transcription_whisper_stream::WhisperStream;` 互換層 + 定数 6 つ (`WHISPER_SAMPLE_RATE` / `CHUNK_DURATION_SECS` / `CHUNK_SAMPLES` / `MIN_FLUSH_SAMPLES` / `SILENCE_LOOKBACK_SAMPLES` / `SILENCE_THRESHOLD_RMS`) を `pub(crate)` 昇格 + `is_tail_silent` の module-level use を削除して tests mod 内で明示 import (clippy unused_imports 警告ゼロ維持) + `whisper_rs::WhisperContext` の transcription.rs 側 use 削除
- `src-tauri/src/lib.rs`: `mod transcription_whisper_stream;` 宣言追加 (alphabetical, transcription_whisper_local の後)

## 完成した Whereby blacklist (Loop 14)
- `src-tauri/src/app_detection.rs` L870-895: `WHEREBY_NON_ROOM_PATHS` に `download` / `app` / `for-teams` / `developers` の 4 entries 追加 (合計 24 entries)
- 同 test mod に `classify_meeting_url_whereby_rejects_blacklist_download` / `_app` / `_for_teams` / `_developers` の 4 テスト追加

## harness 衛生事象 (本セッションで観測終了)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-7 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 前任 mjc-main-20260505-6 の `bfb9846` chore(harness) commit (PATH inner shell escape 化) が **永続的解決** と確認 = 後継への調査委譲はクローズ
- これは「観測してから判断する」運用の効果実例 (= 推測ではなく観測で結論出し)
- 後継観測ポイント: 新たな harness 動作変化があれば観測継続 (例: canonical 移譲時に他のファイルに M が出る、worker 起動失敗等)

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-7、handoff 直前)
- mjc-main-20260505-8 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 3 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-8` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-8 mjc-main` を実行 (前 21 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (本セッションでは出現しなかった = 後継も同観測すれば本セッションの結論「永続的解決」を追認する形)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-7) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Phase 2-A 完全完了状況 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = Loop 14 service detection 軸 pivot で extraction 連続カウント 0 にリセット完了** = Loop 15-17 で extraction 続行可、Loop 18 で variety pivot 検討。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 15 開始時点)

直近 4 ループ: Loop 11 (extraction = WhisperLocal) → 12 (extraction = Audio resampling) → 13 (extraction = WhisperStream) → 14 (service detection pivot = Whereby blacklist)。
- **連続カウント: extraction 0 連続** (Loop 14 pivot でリセット)
- Loop 15 で extraction 着手しても 1 連続、Loop 17 まで extraction 続行可、Loop 18 で variety pivot 検討

### 最優先 (priority 1〜2 への直接寄与)

#### 候補 A: Phase 3 TranscriptionManager の transcription_manager.rs 抽出 (規模 M, 価値中-高)
- 抽出対象: `pub struct TranscriptionManager` (現在の transcription.rs L234) + `impl TranscriptionManager` (L243) + `TranscriptionStateHandle` (L342) + `impl TranscriptionStateHandle` (L344) (~108 行 = L234-L342 周辺)
- 独立性: model_manager: ModelManager フィールド + engine: Option<Arc<dyn TranscriptionEngine>> = ModelManager (transcription_model_manager.rs) + TranscriptionEngine (transcription_traits.rs) への依存ありだが既に分離済み
- リスク: 中-高 (Mutex 経由 state を複数モジュールで参照、tauri::State 連携)
- 1 ループ完結見込み (~110 行は Phase 2-B ModelManager 149 行 + Phase 2-A WhisperLocal 76 行と同パターン)
- 推奨: Loop 15 候補として extraction 1 連続、Loop 16 以降は別軸 (新サービス検知 / harness 衛生 / frontend 軸) を検討

#### 候補 B: GoToMeeting URL 検知 (規模 S-M, 価値中-高)
- URL 形式: `meet.goto.com/<id>` / `global.gotomeeting.com/join/<id>` / `app.goto.com/meet/<id>` / `goto.com/<short-id>` 等
- 仕様調査必要 (実際の URL パターン精査) = research 担当起動推奨 (haiku) または main 直接 grep ベース
- AGENTS.md 優先順位 2 = 会議検知の網羅性 への直接寄与
- Whereby パターン (Loop 7 + Loop 14) と同様 = is_goto_host + is_goto_meeting_url + GOTO_NON_ROOM_PATHS の 3 関数追加 = 1 ループ完結見込み
- variety 規則: Loop 14 が service detection (Whereby) → Loop 15 で別 service (GoToMeeting) は許容範囲だが「服務検知 sweep」化リスク
- 推奨: Loop 16-17 候補として候補 A 後

#### 候補 C: app_detection.rs (3017 行) の Webex モジュール抽出
- docs/architecture/transcription-refactor-plan.md L117-121 で「将来課題」記載済
- 抽出対象: is_webex_host / is_webex_meeting_url / is_jphp_path / is_webex_jphp_meeting_url / is_wbxmjs_path / is_webex_wbxmjs_meeting_url / is_webappng_path / is_webex_webappng_meeting_url の 8 関数
- 規模: M (~200 行?)、transcription.rs で確立した抽出パターンを app_detection.rs に応用可
- AGENTS.md 優先順位 1 = クラッシュ修正への予防的寄与 (巨大ファイル理解性向上)
- 推奨: Loop 17 以降の variety pivot 候補 (Phase 3 完了後、別ファイル軸への自然 pivot)

### 中優先 (variety pivot 候補、priority 6 / 8 / 9 への寄与)

#### 候補 D: frontend 軸 pivot (主観的判断のリスクあり、慎重に選ぶ)
- TranscriptionControls.tsx は既に aria 属性充実 = priority 9 への寄与は浅い
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり = 責務分離 docs commit 候補 (mjc-main-20260505-3 Loop 4 docs precedent と同パターン)
- 推奨: 浅 grep で具体的な不足を 1 つ特定してから worker 発注、主観的探索は避ける

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記 line 555-557 との矛盾)
- 後継が再評価する場合、まず handoff prompt の B1 提案 + 前任者 rustdoc 注記 + mjc-main-20260505-3 / 4 / 5 / 6 / 7 の却下根拠を読み、批判的判断する

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- 「Webex sweep からの完全 variety pivot を 7 セッション維持」を破る
- Loop 17-18 程度まで間隔を空けるのが安全

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 100+ untracked ファイル (本セッションで +2 件追加: docs/handoff/mjc-main-20260505-7.txt と worker prompts 2 件)
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md 24,750+ 行の archive 戦略
- 未着手、未消化

## 検証制約 (再掲)
- cmake あり → cargo test 678 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり、本セッションでは Loop 14 完了後の SESSION SUMMARY commit のみメイン直接編集)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-8 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 38 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-60 秒)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - 規模極小タスクの場合: worker が agent-commit.sh で自走 commit する場合あり (本セッション Loop 13 + 14 とも自走 commit、`git log --oneline -3` で確認推奨)

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける** (4209 bytes でも too long エラー precedent あり)
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本セッションの起動 prompt と同パターン、連続 4 セッション目)

## worker prompt 必須要素 (38 連続セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 13 = ~8 分、Loop 14 = ~3 分、平均 ~5.5 分で **目標 15 分以内達成**)
- worker 1 件 ≒ 1 commit (本セッションは Loop 13 + Loop 14 ともに worker 経由 1 commit、SUMMARY chore で +1 commit = 計 3 commit)
- 累積 worker 完走 114/114 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで早期ハンドオフ判断 = 引き継ぎ時の context (大量必読ドキュメント + grep + diff) を考慮した予防的判断 = mjc-main-20260505-4 / 5 / 6 の「2 ループ + 早期 handoff」precedent **連続 4 セッション目達成**

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中 2 件 (Loop 13 worker 待機中 + Loop 14 worker 待機中) を「Loop 進行 + commit + handoff 準備」で消化済

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-8` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-8 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (本セッションでは出現しなかった = 永続的解決の可能性高い、後継も同観測なら結論追認)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「Whereby に戻って 2 連続にしない」** (本セッション sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「extraction 連続カウント 0 リセット完了」を踏まえ、Loop 15 で Phase 3 TranscriptionManager 抽出 (規模 M ~110 行、1 ループ完結見込み、priority 1 予防的寄与) が最も安全側。Loop 16-17 で extraction 続行可、Loop 18 で variety pivot 検討。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 38 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-180 秒、規模極小なら ~30-60 秒、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 4 セッション目) で書く
