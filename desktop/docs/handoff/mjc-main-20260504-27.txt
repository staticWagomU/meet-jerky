あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-27) です。
旧 mjc-main (= 旧 mjc-main-20260504-26) から 3 ループ完了の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 21000+ 行 / 1.8MB+ あるので `tail -350 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~18:05 JST] mjc-main-20260504-26 状況メモ (ループ 1-3 詳細) があります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~18:06 JST 時点)

## 現在の git 状態
- branch: main, ahead 134 (origin/main から先行、本セッション +4 commits)
- working tree: 全ての変更 commit 済、untracked は前と同じ docs/handoff/* と docs/worker-prompts/ + 本セッション分追加
- 旧 mjc-main-20260504-26 が canonical 名 mjc-main を持ち、本セッションで 3 ループ + 1 SESSION SUMMARY を完了:
  1. `a0a9c5f` test(app_detection): is_valid_dns_label RFC 1035 残境界 3 軸 (Loop 1, +3 件 → 527 passed) — `is_valid_dns_label` の 6 軸全網羅達成
  2. `df34f05` test(session_manager): SessionManager::start title passthrough 3 軸 (Loop 2, +3 件 → 530 passed) — SessionManager 主要 4 経路で責務分離テスト完成
  3. `999fdbc` test(settings): permission_status_to_string fail-safe 3 軸 (Loop 3, +3 件 → 533 passed) — 「fail-safe semantic の executable specification 化」パターン新規確立
  4. `ad45698` chore(agent-log): SESSION SUMMARY 追記

## 現在の品質状態
- cargo test (lib): **533 passed / 0 failed** (前 524 → +9 件)
- cargo clippy default lint: **警告ゼロ**
- cargo clippy -D warnings: **警告ゼロ** (Loop 全 3 で維持)
- npm run build (tsc + vite build): 本セッションで frontend 触れていないため変更なし

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-26、handoff 直前)
- mjc-main-20260504-27 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `claude-agent-handoff-main.sh` で起動された場合、tmux 上では `mjc-main-20260504-27` という名前で起動するはずです。`tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260504-27` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-27 mjc-main` を実行 (前 mjc-main-25/26 でも初回確認が必要だった precedent あり、冪等)。
5. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本セッションで application 確認 + 新規 1 件確立)

#### 1. 「対称的補強の 3 軸構造」パターン (本セッション全 3 ループで application、6 連続セッション目)
- Loop 1: 1B 最短 accept / 末尾ハイフン reject / 先頭ハイフン reject (is_valid_dns_label RFC 1035 残境界)
- Loop 2: 空文字 / NUL byte / 10_000 chars (SessionManager::start title passthrough)
- Loop 3: 負値 / 隣接 unknown 値 3 / i32::MAX (permission_status_to_string fail-safe)
- = mjc-main-22/23/24/25/26 で全 15 ループに application = 1 ループ = 3 軸 = 1 関数群の境界仕様の executable specification 化が標準形
- 後続継続適用必要

#### 2. 「同一関数の完全網羅戦略」パターン (mjc-main-26 で確立、本セッション全 3 ループで application)
- 前回未保護軸を残らず埋め切る application
- Loop 1: mjc-main-25 Loop 3 (a00d76d, 3 軸) + 本 Loop 1 (3 軸) で `is_valid_dns_label` の 6 軸全網羅達成
- Loop 2/3 で他関数 (SessionManager::start, permission_status_to_string) に拡張
- 後続継続適用候補: SessionManager::start_with_output (本セッション残リスク)、from_legacy_str / default_output_directory / update_settings (settings 残関数)

#### 3. 「責務分離の executable specification 化」パターン (Loop 2 で application)
- 上位層が下位層の責務に干渉しない契約を test 化
- 既存 session.rs:249 (Session struct level) と相補的に SessionManager level で重ね保護
- application 候補: tauri command (commands.rs) ↔ core impl (各種 service) の境界も同様の責務分離 test 化が可能

#### 4. **新規パターン (本セッション Loop 3 で確立)**: 「fail-safe semantic の executable specification 化」
- match の `_` (catch-all) ブランチが「fail-safe = 安全側に倒す」設計の場合、catch-all 経路に到達する具体的な値を test で固定する application
- 過去パターンと直交する 4 つ目の独立パターン
- 今後 unknown 値処理を含む全関数 (e.g. error code → user message mapper、HTTP status → error category mapper、TranscriptionEngineType の legacy 値 → fallback 等) で application 可能
- application 候補: settings.rs::deserialize_engine_type_with_fallback (l.6 から)、cloud_whisper.rs / openai_realtime.rs / elevenlabs_realtime.rs の error category mapping

#### 5. 「worker prompt で末尾追記を明示するパターン」(継続 application、6 連続セッション目)
- 全 3 ループで「ファイル末尾追記必須 + 先頭は絶対に触らない + tail -10 で末尾確認」明示 → 事故ゼロ
- mjc-main-21 → 22 → 23 → 24 → 25 → 26 で **6 連続セッション先頭追記事故ゼロ** = 後続継続適用必要

### 累積パターン (前 mjc-main-7〜25 SUMMARY 参照、変更なし)

### 重要な技術的注意点 (本セッション分のみ抜粋)

#### 1. log file 直接 cat による worker 完走確認手法 (Loop 2 で発見)
- `agent-tail-output.sh` は worker 完走 → tmux pane kill 直後に呼ぶと「pane not found」を返すが、`logs/agent/<session>.txt` (tee 出力) は完走報告を含む
- 今後は `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/logs/agent/<session>.txt | tail -50` を直接実行する方が確実
- mjc-main-26 Loop 2 で agent-tail-output.sh が空表示で 5 分待機する事故が発生 = 直接 cat で即時確認が後継への運用知見

#### 2. 優先順位 #9 (権限・透明性) 軸への application 拡大 (Loop 3 で達成)
- 過去 6 セッション (mjc-main-21〜25) は優先順位 #2 (会議検知信頼性) 中心
- 本セッション Loop 3 で初めて #9 軸 (`permission_status_to_string` fail-safe) へ application 拡大
- application 候補継続: `check_microphone_permission` / `check_screen_recording_permission` / commands.rs の権限ガード経路

#### 3. trailing whitespace warning の発生 (Loop 3 worker)
- worker (sonnet) が AGENT_LOG.md 末尾追記時に trailing whitespace を含めた
- git --check 警告のみで commit 阻害なし、ただし次セッション以降の worker prompt で「行末の空白文字 (trailing whitespace) 禁止」を明示推奨
- worker prompt 必須要素 7 つ目として追加 (詳細は本ハンドオフ末尾)

#### 4. cadence 短縮要因の同定
- Loop 1 ~7 分 / Loop 2 ~10 分 / Loop 3 ~9 分、平均 ~9 分/loop
- mjc-main-22/23 と同等の高速 cadence、mjc-main-25 (~12 分/loop) より速い
- 短縮要因 = (a) sleep 待機を 90s から 240s への調整で worker 完走後の即座確認、(b) prompt 規模指定の functional 化、(c) メイン側でのカバレッジ重複事前確認 (Loop 1 で `/my/` 経路重複発見、別軸へ即切替)

#### 5. 別ファイル切替による「test 密度の偏り是正」継続
- 本セッション 3 ループで 3 ファイル横断 (app_detection.rs → session_manager.rs → settings.rs)
- 関数別の保護密度バランス改善 = 今後は browser_detection.rs / cloud_whisper.rs / openai_realtime.rs / elevenlabs_realtime.rs / commands.rs の保護密度を上げる時期

#### 既存累積技術注意点 (前 mjc-main-7〜25 SUMMARY 参照、本セッションで触れていない領域は前 SUMMARY 必読)

### AGENT_LOG.md 構造の問題 (継承、優先度低)
- 本セッション中の全 worker は AGENT_LOG.md 末尾追記成功 (worker prompt 改善継続)
- AGENT_LOG.md 全体は **21000+ 行** (本セッションで +200 行程度) で構造的に整理が必要 = 別 Loop 候補 (低優先)

## 次ループ候補 (優先順位順、本セッションで未着手)

### B 候補継続 (推奨、規模 S, 即効性あり、cadence ~9 分維持見込み)
- `SessionManager::start_with_output` の output_dir 引数境界 (空 path / 存在しない path / traversal `../` 等)
- 「責務分離の executable specification 化」パターン継続 application
- 1 ループで 3 軸網羅可能、本セッション Loop 2 残リスク 3 として明記済

### S 候補継続 (settings.rs 残関数、規模 M, 統合候補あり)
- `from_legacy_str` (l.41) の境界 (大文字混在、空文字、不明値) → fail-safe pattern 拡張可能
- `default_output_directory` (l.139) の境界 (環境変数欠如、Home 検出失敗)
- `update_settings` (l.171) の境界 (mutex 競合、partial update)
- `deserialize_engine_type_with_fallback` (l.6) の境界 (各種 engine string variants → fail-safe legacy 値の 3 軸)
- `MEETING_INACTIVE_THRESHOLD = 600s` を const から settings 経由で変更可能にする統合 (mjc-main-20 SUMMARY 既明示、未消化)

### M 候補継続 (app_detection.rs 残境界、規模 S, 限定的)
- `parse_url_host_and_path` の hostname 全体 253 バイト上限 (FQDN), IDN/Punycode handling
- `validate_port` の境界 (0 / 65535 / 65536 / 非数字 / 空)
- `extract_query` の境界 (fragment 含む / 複数 ? / empty query)

### F-Loop6 (継承、規模 M, 価値中)
- タイマースレッド shutdown 対応 (mjc-main-21 由来、未着手)
- Arc<AtomicBool> flag + start_detection 終了時 notify
- 既存 detection_callback / browser_url_callback の shutdown と整合性検討必要

### Realtime/Whisper 系 (規模 S-M、複数ファイル候補)
- cloud_whisper.rs, openai_realtime.rs, elevenlabs_realtime.rs の error path 残境界
- 本セッションで未着手の音声経路も「サービス cross product による会議検知信頼性の集中強化」パターンに準じた application 可能 = 「実装系 × 共通エラー仕様」マトリクス充填型

### D 候補 (transcription.rs error path 残、別ファイル切替)
- 本セッション開始前確認で `validate_stream_count_for_engine` 完全網羅済と判明 (前 mjc-main-22 で 5 件追加 + apple_speech_allows_zero_streams で完全網羅) = 残候補は run_transcription_loop / run_transcription_worker_with_panic_guard (Tauri AppHandle 必要 = mock 必要、規模 M-L)

### K (継承). clippy::pedantic の選択的有効化 (規模 L, 非優先)

## 検証制約 (再掲)
- cmake あり → cargo test 533 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: **コミットメッセージはファイル経由が安全** (fish shell parse error 回避):
  - `/tmp/mjc-commit-msg-loopN.txt` に **Read → Write の 2 ステップ** でメッセージを書く (初回も既存ファイル想定で先に Read 必須、touch でファイル作成後に Read)
  - `git add <files> && git commit -F /tmp/mjc-commit-msg-loopN.txt`
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-27 mjc-main` (現セッション名が `mjc-main` でない場合のみ実行)
- worker 完走確認: **agent-tail-output.sh より直接 cat の方が確実** (mjc-main-26 Loop 2 教訓)
  - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/logs/agent/<session>.txt | tail -50`

## worker prompt 必須要素 (本セッションで実証済、継続適用 + 7 つ目追加)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」** = 末尾追記の場所を視覚的に学習させる
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」** を明記
3. **「先頭は絶対に触らない」** を明記
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照** を明記
7. **新規追加 (mjc-main-26 で発見)**: **「行末の空白文字 (trailing whitespace) 禁止」** を明記 = AGENT_LOG.md 追記時の git --check warning 防止

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション実績 ~9 分/loop で目標大幅クリア)
- worker 1 件 ≒ 1 コミット
- 本セッション実績: Loop 1 ~7 分, Loop 2 ~10 分, Loop 3 ~9 分、平均 ~9 分/loop
- 累積 worker 完走 65/65 = 全完走率 100%

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針。
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲。
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる。
- 本セッションでは 3 ループ + SESSION SUMMARY を終え、context 推定 ~70-80% で予防的ハンドオフ判断 (前 19 セッション (mjc-main-7〜25) と同じ 3 ループパターン継承で予測可能性優先)。

## ユーザー直伝指示 (未消化)
- なし。watchdog 継続指示 2 回受領も自然に消化済 (3 ループを通常テンポで進めたため)。

## 累積成果 (本セッション)
- **テスト 524 → 533 passed** (+9 件、mjc-main-22/23/24/25 と完全対称な「+9 件/セッション」を 5 セッション連続達成)
- **コミット 3 件 + 本 SUMMARY 1 件**
- **clippy 警告ゼロ維持** (default + -D warnings)
- **3 ファイル横断補強** (app_detection.rs / session_manager.rs / settings.rs)
- **「同一関数の完全網羅戦略」パターン全 3 ループで application 確認** (本セッションで application 標準形化)
- **「fail-safe semantic の executable specification 化」パターン新規確立** (Loop 3, 過去パターンと直交する 4 つ目の独立パターン)
- **優先順位 #9 (権限・透明性) 軸への application 拡大** (Loop 3, 過去 6 セッション中心の #2 軸から横展開)
- **worker prompt 末尾追記明示の継続適用** (6 連続セッション先頭追記事故ゼロ)
- **累積 worker 完走 65/65** (100%)
- **コミット周期 ~9 分/loop** (mjc-main-22/23 と同等の高速、目標 15 分以内大幅クリア)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -350 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260504-27` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-27 mjc-main` を実行 (冪等)。
4. 候補 **B 候補 (SessionManager::start_with_output 境界、規模 S, 推奨)** を最有力として進める。
   - メインで `grep -n 'start_with_output\|output_dir\|path_for_session' src-tauri/src/session_manager.rs src-tauri/src/session_store.rs` で既存 test カバレッジ確認
   - output_dir の境界 test (空 path / 存在しない path / traversal `../`) を選定
   - 「責務分離の executable specification 化」パターンを継続 application + SessionManager 主要 5 経路 (start / start_with_output / append / finalize / discard) 完成へ寄与
   - 別ファイル切替 (S / Realtime/Whisper 系 / F-Loop6) も次の選択肢
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
6. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
7. **worker 完走確認は cat <log_file> | tail -50 を直接使う** (agent-tail-output.sh より確実)
