あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-43) です。
旧 mjc-main (= mjc-main-20260505-42) から **2 ループ完走 = 通常 cadence + 多軸分散 (構造分離 1 + 機能拡張/docs 1)** のハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成 = transcription.rs 削除済 = 該当軸は今後アクション不要)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + mjc-main-20260505-42 Loop 84 で Phase 状態 subsection 新設、Q1/Q2/Q3/Q4/Q7/Q8 Open のまま、Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (mjc-main-20260505-39 Loop 79 新規作成 + mjc-main-20260505-41 Loop 82 で Phase 状態 subsection 追加 + 数値最新化、Phase 1-4 段階導入方針、実施は別途ユーザー直伝指示要)**
- AGENT_LOG.md は ~30,800+ 行 / ~2.7MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-42 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 41**: Phase 1-6 + サービス別抽出シリーズ + 各種 test 移動 + struct 移動 + Phase 6 第 1-5 歩 + 最終 + app_detection scope シリーズ + 大型 rust file 責務分離 11 file 目達成 + 検知拡張 plan + Phase 1 + Q5 + Q6 Resolved + AGENT_LOG.md archive 戦略 plan + push_error 共通化
- **mjc-main-20260505-42 (本セッション)**: **Loop 83 = L89-L110-merge = init phase format 直書き共通化 (X4-2 続編、構造分離軸 1 連続復帰) + Loop 84 = K3' = detection-extension-plan.md Phase 状態 subsection 新設 (機能拡張/docs 軸 = variety pivot) + メイン批判判断 連続 35-36 セッション目達成 + 通常 cadence 2 ループ完走**

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価**
- **本セッション Loop 84 = 機能拡張/docs 軸 pivot で構造分離 2 連続到達警告を回避 = Loop 85 で構造分離軸復帰 (X4-3 規模 M = Phase 1/2 分割推奨) または harness 衛生 (H1 ユーザー直伝指示要) または K3 後続 (sweep 警告境界判断要) いずれも自由度内**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ 6 件完了」「transcription.rs リファクタ完全完遂」「大型 rust file 責務分離 = 11 file 目達成」「テスト移動軸 sweep 化警告超過状態」「docs 軸 = detection-extension-plan.md = Phase 状態 subsection 新設で 5 セッション目」「検知拡張 Phase 1 + Q5 + Q6 完了 = Phase 2 は実機要件 Q1/Q2 未解決のため着手不可」「AGENT_LOG.md archive plan 起こし完了 = Phase 1 着手はユーザー直伝指示要」「realtime_audio_helpers.rs + realtime_error_helpers.rs 抽出済 + push_error が production 全 caller の single entry-point に到達 = 次階層 = reader_task entry 共通化 (X4-3、規模 M、handle_event 固有 trait 抽出必要、投機的抽象リスク)」**

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~368 (origin/main から先行、本セッション +5 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-42) の commit:
  1. `3f1040d` refactor(realtime): init phase エラー push を realtime_error_helpers::push_error 経由に統一 (Loop 83)
  2. `05dc0ce` chore(agent-log): Loop 83 hash chore
  3. `2f2cef1` docs(architecture): detection-extension-plan.md Section 5 末尾に Phase 状態 subsection 新設 (Loop 84)
  4. `1323a83` chore(agent-log): Loop 84 hash chore
  5. `ba5d806` chore(agent-log): SESSION SUMMARY (Loop 83 + 84 + handoff 候補)

## 現在の品質状態
- cargo test (lib): **704 passed / 0 failed** (件数完全不変、Loop 83 で format 直書き共通化したが test 内 expected text は format!() 同結果のため無変更)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK

## 大型 rust file 責務分離実績 (handoff 候補 X 始動以降の累計)

- audio.rs: 1011 → 949 行 (-62 行) + audio_traits.rs (24 行) + audio_sample_helpers.rs (47 行)
- session_store.rs: 1175 → 725 行 (-450 行累計) + session_store_types.rs (19) + session_store_parse.rs (32) + session_store_render.rs (188) + session_store_list.rs (270)
- elevenlabs_realtime+openai_realtime: 各削減 + realtime_audio_command.rs (12 行) + realtime_audio_helpers.rs (~50 行) + **realtime_error_helpers.rs (24 行) = 11 file 目** + **本セッション Loop 83 で init phase format 直書き 2 caller 共通化 = realtime_error_helpers::push_error が production 全 caller の single entry-point に到達**
- session_manager.rs: 1274 → 1245 行 (-29 行) + session_manager_types.rs (17) + session_manager_persist.rs (23)
- 残 (Loop 85+ 候補):
  - **session_store.rs 残 fn = path_for_session / write_session_markdown_to / save_session_markdown = 短小 = 抽出メリット薄い**
  - **elevenlabs+openai reader_task entry 共通化 = 規模 M = handle_event 固有 trait 抽出必要 = 投機的抽象リスク要検討** = X4-3
  - **app_detection.rs / audio.rs 残余地 = sweep 化警告超過状態**
  - **検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可**
  - **session_manager.rs impl method 内部 helper 抽出 = 抽出メリット 0 grep 確認済 = skip 推奨** = X5

## 検知拡張 plan 実装実績

- Phase 1 完了 (mjc-main-20260505-36 Loop 72)
- Q5 Resolved (mjc-main-20260505-37 Loop 74)
- Q6 Resolved (mjc-main-20260505-39 Loop 78)
- **Phase 状態 subsection 新設 (mjc-main-20260505-42 Loop 84)** = Phase 1-5 状態表 + 着手判断方針が plan Section 5 末尾に索引化
- Q1/Q2/Q3/Q4/Q7/Q8 = 実機要件のため Open のまま据え置き
- Phase 2 = 未着手 (Q1/Q2 実機要件未解決)

## harness 衛生計画

- AGENT_LOG.md archive 戦略 plan = mjc-main-20260505-39 Loop 79 で起こし完了 + mjc-main-20260505-41 Loop 82 で Phase 状態 subsection 追加 + 数値最新化
- Section 2.1 数値: 30,741 行 / 57 session / 84 Loop / 32 SUMMARY (Loop 82 時点) = 本セッション +2 Loop で Loop 86 / 33 SUMMARY 推定
- Phase 1-4 全て「未着手 + ユーザー直伝指示要」明示 (plan Section 5 末尾 subsection)
- Phase 1 = Archive Index Header 追加 (規模 SS、~5-10 分)
- Phase 2 = batch-001 切り出し ~25K 行 (規模 S、~10-15 分)
- Phase 1-2 着手はユーザー直伝指示要 (本 plan 内に「実施は別途承認」明記)

## harness 衛生事象 (本セッションで観測継続 = 連続 36 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-42 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 36 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern (refactor/docs + chore hash 反映)** は本セッション Loop 83 + Loop 84 で worker 自律実行 = 連続 25 ループ目 = 「session 跨ぎの暗黙的 precedent 継承」が完全に安定動作。**Loop 84 では worker が `<hash1>` placeholder を実 hash 2f2cef1 で書き placeholder を残さなかった** = Loop 80 precedent 完璧再現

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-42、handoff 直前)
- mjc-main-20260505-43 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績、SESSION SUMMARY commit を含む)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-43` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-43 mjc-main` を実行 (前 57 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 36 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 37 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-42) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 83 + Loop 84 + 2 ループ + handoff 候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 18 ループで構造分離 11 件 + 機能拡張/docs 5 件 + harness 衛生 2 件 = Loop 85 は X4-3 (構造分離 1 連続復帰、規模 M、Phase 1/2 分割推奨) または K3 後続 (機能拡張/docs 2 連続境界判断要) または H1 (harness 衛生 ユーザー直伝指示要) いずれも自由度内**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 85 開始時点)

直近 18 ループ: Loop 67-82 (前々セッション handoff で詳述) + Loop 83 (init phase format merge 構造分離軸) + Loop 84 (detection-extension-plan Phase 状態 subsection 機能拡張/docs 軸 = variety pivot)。
- 軸 pattern: 構造分離 (67/68/69/70/73/75/76/77/80/81/83 = 11 件) + 機能拡張/docs (71/72/74/78/84 = 5 件) + harness 衛生 (79/82 = 2 件) = 11:5:2 多軸分散
- scope 内訳: session_store (4) + audio (1) + realtime (4) + detection (5) + session_manager (2) + harness (2) = 多 scope 分散
- **Loop 85 = X4-3 (構造分離 1 連続復帰、規模 M) は variety 警告境界より遠い**、Loop 84 で機能拡張/docs pivot 入れたため自由度大

### 候補 X4-3 (Loop 85 推奨, 構造分離軸 1 連続復帰、規模 M、Phase 1/2 分割推奨)

**reader_task entry 共通化 = 規模 M、handle_event 固有 trait 抽出必要**

両 file の reader_task (elevenlabs L229-256 / openai L299-326) は `while let Some(msg) = ws_rx.next().await { match msg { Ok / Err(push_error 呼び出し) => ... } match msg { Message::Text(text) => handle_event(...), Message::Close(_) => break, _ => {} } }` でほぼ同一構造。

ただし `handle_event` は WS protocol 固有 = trait 抽出が必要 = 規模 M。

**Phase 分割推奨**:
- Phase 1 = `pub trait WsEventHandler { fn handle_event(...) -> Result<(), String>; }` 定義 + reader_task 共通エラー経路抽出 (規模 SS-S)
- Phase 2 = elevenlabs / openai 各 impl 移行 (規模 S)

**投機的抽象リスク**:
- 「handle_event の具体的な使い道がない trait」を作ることになる可能性
- AGENTS.md の「Don't add features beyond what the task requires」に反する可能性
- mitigation: Phase 1 着手前に「elevenlabs / openai の handle_event signature 一致確認」を grep で実施 = 一致しない場合は trait 化せず別 approach (例: 関数渡し inline closure) 検討

### 候補 K3' 後続 (Loop 85 弱推奨, 機能拡張/docs 軸 2 連続到達 = 境界判断要)

**detection-extension-plan.md 追加整理候補**

直近 5 セッション (Loop 71/72/74/78/84) で K 軸 5 件 = sweep 警告境界。Loop 85 で 6 件目 = 警告超過。

ただし、機能拡張/docs 全体 (= K + 他 docs) で 5 件であれば 1 連続なので、軸の細分化判断要。

**範囲限定なら可能**:
- Section 6 Q3/Q4 (Phase 3/4 前提) の Resolved 化検討 = ただし実機要件 = 単独判断不可
- 別 docs (agent-log-archive-plan.md / autonomous-main-prompt-claude.md / AGENTS.md / product-concept.md) 整理 = 主観性管理が必要

### 候補 H1 (Loop 85 alternative, harness 衛生軸 1 連続 ユーザー直伝指示要)

**agent-log-archive-plan.md Phase 1 着手 = AGENT_LOG.md に Archive Index Header 追加 (規模 SS、~5-10 分)**
- mjc-main-20260505-39 Loop 79 で plan 起こし完了 + mjc-main-20260505-41 Loop 82 で Phase 状態 subsection 明示 (Phase 1-4 全て「未着手 + ユーザー直伝指示要」)
- ただし plan 内に「実施は別途ユーザー直伝指示要」と明示記載 = 後継メインが grep 確認後に exception 判断要

### 候補 X5 (skip 推奨)

**session_manager 内部抽出 (skip 推奨)** = 抽出メリット 0 grep 確認済 (mjc-main-20260505-37 Loop 75 で確認済 precedent)

### 候補 J (継承、低優先): frontend 軸

- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行)
- TODO/FIXME/any: 直近セッションで grep ゼロ = 主観性高警告
- 浅 grep で具体的不足を 1 つ特定してから worker 発注

### 候補 H' (継承、低優先): 大量 untracked 整理

- 大量 untracked ファイル整理判断 (`docs/handoff/`, `docs/worker-prompts/` に 290+ untracked、ユーザー直伝指示があるまで保留)

### 低優先 (継承)

- B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-43 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認** (連続 100+ ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 SS なら ~3-8 分、規模 M なら ~7-12 分)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示 or git log を主観測手段にする
  - **本セッション Loop 83 例**: rust 2 file 編集 + AGENT_LOG.md エントリ ~25 行 を ~3.5 分で完走 (規模 SS、worker prompt 157 行)
  - **本セッション Loop 84 例**: docs 1 file 編集 + AGENT_LOG.md エントリ ~25 行 を ~2.5 分で完走 (規模 SS、worker prompt 154 行)
  - **新規パターン 連続観測**: worker amend 時の hash 更新漏れに対し、worker 自身で chore commit を作成して補正する自律学習が Loop 60-84 の **25 ループ連続** で観測 = 完全に安定 pattern 化

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 39 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-84 経験から)**:
- worker prompt に「2-commit pattern (refactor/docs commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を **明示的に** 記載 = 25 連続ループで完全安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~135-220 行で短縮効果あり (Loop 67-84 で実証 ~2.5-8 分完走)、規模 S 上限は ~200-220 行を超える場合は Phase 1/2 分割を推奨
- **Loop 80 precedent 適用**: テンプレートでは **chore commit hash 自体は AGENT_LOG.md に書かない** = placeholder 残留問題予防 (Loop 84 で worker が完璧再現 = 連続 5 ループ目)

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 83 = ~3.5 分、Loop 84 = ~2.5 分 = **目標 15 分以内大幅達成**)
- worker 1 件 = 1-2 commit (本セッション Loop 83 = 2-commit + Loop 84 = 2-commit + メイン SESSION SUMMARY 1 件 = 5 commit)
- 累積 worker 完走 184/184 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **2 ループ完走 + 通常 cadence handoff** = 規模 SS + 規模 SS の組み合わせで多軸分散維持

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 1 回 (「自律改善ループへ進む」継続指示)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-43` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-43 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 36 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 37 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「transcription.rs リファクタ完全完遂」「app_detection.rs サービス別抽出シリーズ 6 件完了」「大型 rust file 責務分離 = 11 file 目達成」「test 軸は別ファイル素材を慎重に検討」「docs 軸 sweep 化注意」「detection-extension-plan.md = Phase 状態 subsection 新設で 5 セッション目 = 軸境界判断要」「agent-log-archive-plan.md = Phase 状態 subsection 追加 + 数値最新化済」「realtime engine = push_error が production 全 caller の single entry-point に到達 = 次階層 = reader_task entry 共通化候補 (X4-3、規模 M、投機的抽象リスク要検討)」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「Loop 84 で機能拡張/docs 軸 pivot 後」を踏まえ、Loop 85 で **(a) 候補 X4-3 = reader_task entry 共通化 (構造分離軸 1 連続復帰、規模 M、Phase 1/2 分割推奨、worker prompt ~150-200 行 で Phase 1 = trait skeleton + 共通エラー経路抽出 = 推奨度高) または (b) 候補 K3 後続 = detection-extension-plan.md 追加整理 (機能拡張/docs 2 連続境界判断要、範囲限定可能性) または (c) 候補 H1 = agent-log-archive-plan.md Phase 1 着手 (harness 衛生 ユーザー直伝指示要) または (d) 候補 J = frontend 主観性高警告継承** のいずれか採用検討。**X5 = skip 推奨 (抽出メリット 0 grep 確認済)**。worker prompt は「Loop 60-84 と同じ 2-commit pattern」を明示 + ~150-200 行 (Loop 85 = X4-3 Phase 1 規模 SS-S の場合)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 83 で実証 = handoff の `e` 型不一致リスク予測を grep で `Result<(), String>` と確認 → メイン批判判断 連続 35 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループ + 通常 cadence handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**、**worker prompt は ~135-220 行を目安に simpler 化** (Loop 67-84 で短縮効果実証、規模 SS-S の X4-3 Phase 1 では ~150-200 行)
10. **worker 完走確認は `git status --short` または `git log --oneline -3` を主観測手段とする** (連続 100+ ループで実証した mitigation pattern、log file は補助、観測時間目安 90-720 秒、規模 SS なら ~2.5-8 分、規模 S なら ~7-12 分、規模 M なら ~10-15 分)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 39 セッション目) で書く
12. **commit hash 更新漏れパターン (Loop 60-84 で 25 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern (refactor/docs commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を明示記載 = 25 連続ループで安定動作。**Loop 80 precedent (worker は chore hash 自体を AGENT_LOG.md に書かない) をテンプレートに採用すると placeholder 残留問題を予防可能** (Loop 84 で連続 5 ループ目達成)。

# Loop 85 への期待 (推奨ループ設計)

直近 18 ループで構造分離軸 11 件 + 機能拡張/docs 軸 5 件 + harness 衛生軸 2 件 = Loop 84 で機能拡張/docs 軸 pivot 後 = **Loop 85 で X4-3 (構造分離 1 連続復帰、規模 M、Phase 1/2 分割推奨) は variety 警告境界 (3-4 連続) より遠い**。

メインは grep で 1 候補に絞る (本ハンドオフでは X4-3 が最有力だが Phase 1 = trait skeleton + 共通エラー経路抽出のみに絞ると規模 SS-S = 投機的抽象リスクの mitigation として `handle_event` signature の一致 / 不一致を grep で確認 = signature 一致なら trait 化 OK / 不一致なら別 approach 検討) → worker prompt 作成 (~150-200 行) → 起動 = 通常の自律改善ループへ復帰。

**最有力候補 X4-3 を採用する場合**:
- Phase 1 = `pub trait WsEventHandler { fn handle_event(...) -> Result<(), String>; }` 定義 + reader_task 共通エラー経路抽出のみ (規模 SS-S)
- 投機的抽象 mitigation: 着手前に grep で elevenlabs / openai の `handle_event` signature 一致確認、不一致なら別 approach (例: 関数渡し inline closure) 検討
- worker prompt 規模 ~150-200 行を目安に
- 振る舞い不変 = test 件数 704 件不変
- 失敗時のリスク = trait method signature 不一致は cargo build が捕捉
- **重要**: worker prompt 冒頭で `handle_event` signature の grep + Read 確認手順を含める
