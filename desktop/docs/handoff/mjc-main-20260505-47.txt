あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-47) です。
旧 mjc-main (= mjc-main-20260505-46) から **2 ループ完走 + 通常 cadence handoff** のハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成 = transcription.rs 削除済 = 該当軸は今後アクション不要)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + mjc-main-20260505-42 Loop 84 で Phase 状態 subsection 新設、Q1/Q2/Q3/Q4/Q7/Q8 Open のまま、Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (mjc-main-20260505-39 Loop 79 新規作成 + Phase 状態 subsection 追加 + Section 2.3 観測追記、Phase 1-4 段階導入方針、実施は別途ユーザー直伝指示要)**
- AGENT_LOG.md は ~31,300+ 行 / ~2.7MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-46 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 45**: Phase 1-6 + サービス別抽出シリーズ + 各種 test 移動 + struct 移動 + Phase 6 第 1-5 歩 + 最終 + app_detection scope シリーズ + 大型 rust file 責務分離 12-14 file 目 + 検知拡張 plan + Phase 1 + Q5 + Q6 Resolved + AGENT_LOG.md archive 戦略 plan + push_error 共通化 + init phase format 直書き共通化 + detection-extension-plan Phase 状態 subsection + reader_task entry 共通化 + agent-log-archive-plan.md Section 2.3 観測追記 + app_detection 機能分類軸 (notification + inactive_decision)
- **mjc-main-20260505-46 (本セッション)**: **2 ループ完走 = Loop 89 (settings_permission.rs 抽出 = 構造分離 15 件目 = settings 軸 1 件目 = 新 scope) + Loop 90 (transcription_commands_model.rs 抽出 = 構造分離 16 件目 = transcription_commands 軸 1 件目 = 新 scope) = handoff 警告「scope 払底気味」を 2 ループ連続で grep + Read 精査 + 発掘で覆す = メイン批判判断 連続 41 セッション目達成 + worker 自律 2-commit pattern 連続 31 ループ目 + ファイル参照型 handoff prompt 連続 43 セッション目 + scope 多様性 9 軸到達**

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価**
- **本セッション Loop 89/90 = handoff 警告「scope 払底気味」を grep + Read 精査で覆して settings.rs (953 行) と transcription_commands.rs (717 行) を発掘 = 新 scope 2 連続抽出達成 = 後継も同様の手法を継承 = handoff 予測の鵜呑み禁止**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ 6 件完了」「transcription.rs リファクタ完全完遂」「大型 rust file 責務分離 = 16 file 目達成」「test 移動軸 sweep 化警告超過状態」「docs 軸 = detection-extension-plan.md = Phase 状態 subsection 新設で 5 セッション目」「検知拡張 Phase 1 + Q5 + Q6 完了 = Phase 2 は実機要件 Q1/Q2 未解決のため着手不可」「AGENT_LOG.md archive plan 起こし完了 = Phase 1 着手はユーザー直伝指示要 + Section 2.3 更新観測 3 件累積」「realtime engine 3 軸 single source of truth 化達成 = 構造美の最終形」「app_detection_notification.rs + app_detection_inactive_decision.rs 抽出済 = 機能分類軸 2 連続 = Loop 91 で 3 連続避ける」「session_store.rs / session_manager.rs production = ~30-150 行のコンパクト = 抽出メリット 0 確認」「settings_permission.rs 抽出済 = settings 軸 1 件目」「transcription_commands_model.rs 抽出済 = transcription_commands 軸 1 件目 = source 関連 (~50 行) は 2 件目 = 同 scope 連続避けて別 scope pivot 推奨」**

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~382 (origin/main から先行、本セッション +5 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-46) の commit:
  1. `7c10dc7` refactor(settings): permission チェック関連 section を settings_permission.rs に抽出 (Loop 89)
  2. `c8bfbcf` chore(agent-log): Loop 89 エントリ追記
  3. `d91027a` refactor(transcription): モデル管理軸の tauri commands と payload helpers を transcription_commands_model.rs に抽出 (Loop 90)
  4. `cd435eb` chore(agent-log): Loop 90 エントリ追記
  5. `f51e158` chore(agent-log): SESSION SUMMARY (Loop 89 + Loop 90 + handoff 候補)

## 現在の品質状態
- cargo test (lib): **704 passed / 0 失敗** (件数完全不変、Loop 89/90 = test 移動のみ)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK

## 大型 rust file 責務分離実績 (handoff 候補 X 始動以降の累計)

- audio.rs: 1011 → 949 行 (-62 行) + audio_traits.rs (24 行) + audio_sample_helpers.rs (47 行)
- session_store.rs: 1175 → 725 行 (-450 行累計) + session_store_types.rs (19) + session_store_parse.rs (32) + session_store_render.rs (188) + session_store_list.rs (270) = production ~30 行 = 抽出メリット薄
- elevenlabs+openai realtime: 各削減 + realtime_audio_command.rs (12 行) + realtime_audio_helpers.rs (~50 行) + realtime_error_helpers.rs (24 行) + realtime_reader_task.rs (54 行) = **realtime engine 3 軸 single source of truth 化達成 = 構造美の最終形**
- session_manager.rs: 1274 → 1245 行 (-29 行) + session_manager_types.rs (17) + session_manager_persist.rs (23) = production ~150 行 / impl SessionManager 12 メソッド = 抽出メリット 0 確認
- app_detection.rs: 3117 → 2826 行 + app_detection_notification.rs (94 行) + app_detection_inactive_decision.rs (225 行) = **機能分類軸 2 連続 (Loop 87/88) = Loop 91 で 3 連続避ける**
- **settings.rs: 1070 → 953 行 (-117 行 / 本セッション Loop 89) + settings_permission.rs (121 行) = settings 軸 1 件目達成 = 新 scope**
- **transcription_commands.rs: 806 → 717 行 (-89 行 / 本セッション Loop 90) + transcription_commands_model.rs (100 行) = transcription_commands 軸 1 件目達成 = 新 scope**
- 残 (Loop 91+ 候補):
  - **realtime engine = 3 軸全完了**
  - **app_detection 機能分類軸 = 2 連続 = Loop 91 で 3 連続避ける = 別軸 pivot 推奨**
  - **session_store.rs 残 fn = path_for_session / write_session_markdown_to / save_session_markdown = 短小 = 抽出メリット薄い**
  - **session_manager.rs production = ~150 行コンパクト = 抽出メリット 0 確認**
  - **audio.rs 残余地 = ジェネリック関数 cpal 直結 = 抽出難**
  - **transcription_commands.rs 残 = source 関連 (~50 行) = 同 scope 2 件目 = skip 推奨 (別 scope pivot)**
  - **検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可**
  - **新規 scope 候補 (handoff 未調査、grep + Read 必要)**:
    - system_audio.rs (778 行)
    - cloud_whisper.rs (778 行)
    - session_commands.rs (523 行)
    - cloud_whisper_errors.rs (511 行)
    - transcript_bridge.rs (826 行) ← production スリム ~100 行 = 抽出余地薄
    - session.rs (773 行) ← production スリム ~50 行 = 抽出余地薄

## 検知拡張 plan 実装実績

- Phase 1 完了 (mjc-main-20260505-36 Loop 72)
- Q5 Resolved (mjc-main-20260505-37 Loop 74)
- Q6 Resolved (mjc-main-20260505-39 Loop 78)
- Phase 状態 subsection 新設 (mjc-main-20260505-42 Loop 84)
- Q1/Q2/Q3/Q4/Q7/Q8 = 実機要件のため Open のまま据え置き
- Phase 2 = 未着手 (Q1/Q2 実機要件未解決)

## harness 衛生計画

- AGENT_LOG.md archive 戦略 plan = mjc-main-20260505-39 Loop 79 で起こし完了 + mjc-main-20260505-41 Loop 82 で Phase 状態 subsection 追加 + Section 2.1 数値更新 + mjc-main-20260505-44 Loop 86 で Section 2.3 に Loop 86 時点の更新観測追記
- Section 2.1 数値: 30,741 行 / 57 session / 84 Loop / 32 SUMMARY (Loop 82 時点 = plan 元方針) ↔ 現実 grep 値 = ~31,300+ 行 / 86+ unique session 名 / 93+ Loop entries / 76+ SUMMARY headers (Loop 90 完了時点)
- Phase 1-4 全て「未着手 + ユーザー直伝指示要」明示 (plan Section 5 末尾 subsection)

## harness 衛生事象 (本セッションで観測継続 = 連続 41 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-46 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 41 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern (refactor + chore エントリ)** は本セッション Loop 89/90 で worker 自律実行 = 連続 31 ループ目 = 「session 跨ぎの暗黙的 precedent 継承」が完全に安定動作。**Loop 89/90 では worker が refactor commit hash を AGENT_LOG.md に直接書き、chore commit hash は書かなかった = Loop 80/85/86/87/88 進化 pattern を完璧踏襲**

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-46、handoff 直前)
- mjc-main-20260505-47 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-47` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-47 mjc-main` を実行 (前 60+ セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 41 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 42 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-46) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 89 + Loop 90 + 2 ループ完走 + handoff 候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 24 ループで構造分離 16 件 + 機能拡張/docs 5 件 + harness 衛生 3 件 = 16:5:3 = Loop 91 は構造分離軸 4 連続 (Loop 87/88/89/90) だが scope は 4 種異なる = 別 scope 構造分離 17 件目 推奨 (transcription_commands 軸 2 件目 = source 関連は同 scope 連続避けて skip 推奨)**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 91 開始時点)

直近 24 ループ: Loop 67-90
- 軸 pattern: 構造分離 (16 件) + 機能拡張/docs (5 件) + harness 衛生 (3 件) = 16:5:3 多軸分散
- scope 内訳: session_store (4) + audio (1) + realtime (5) + detection (5) + session_manager (2) + harness (3) + app_detection 機能分類 (2) + settings (1) + transcription_commands (1) = **9 scope 分散**
- 構造分離軸 4 連続 (Loop 87/88/89/90) だが scope は異なる = 5 連続でも scope 異なれば許容範囲
- **Loop 91 では別 scope 構造分離 17 件目 推奨 (transcription_commands 軸 2 件目 = 同 scope 連続避ける)**

### 候補 X 新規発掘 (Loop 91 推奨, 構造分離 17 件目)

**新規 scope 探索候補 (handoff 未調査)**:
- **system_audio.rs (778 行)**: 構造未調査、grep + Read 発掘要 = audio 軸の派生 = 別 scope
- **cloud_whisper.rs (778 行)**: 構造未調査、grep + Read 発掘要 = realtime 軸の派生 = 別 scope
- **session_commands.rs (523 行)**: 構造未調査、grep + Read 発掘要 = session 軸の派生 = 別 scope
- **cloud_whisper_errors.rs (511 行)**: 構造未調査、grep + Read 発掘要 = realtime 軸の派生 = 別 scope
- メイン批判判断 連続 42 セッション目達成のチャンス
- 規模 SS-S 想定 (grep 結果次第、production fn が機能境界で分かれているかが鍵)

**注意**: transcript_bridge.rs (826 行) と session.rs (773 行) は production スリム (~100 行 / ~50 行) で残り全 test = 抽出メリット薄 = skip 推奨

### 候補 X' transcription_commands 軸 2 件目 (skip 推奨, 同 scope 警告境界)

- transcription_commands.rs 残: source 関連 (RequestedTranscriptionSources struct + TRANSCRIPTION_SOURCE_MICROPHONE / TRANSCRIPTION_SOURCE_SYSTEM_AUDIO const + validate_stream_count_for_engine + parse_requested_transcription_sources) ~50 行
- 抽出可能だが同 scope 2 連続 = 機能分類軸 3 連続 paradigm に近づく = skip 推奨
- 別 scope pivot 推奨

### 候補 X'' app_detection 機能分類軸 3 件目 (skip 推奨, 警告境界)

- 機能分類軸 2 連続 (Loop 87/88) = Loop 91 で 3 連続は警告境界
- 同 scope 内 helper 残: parse_throttle_key_to_display_name / classify_meeting_url / classify_meeting_window_title / URL parse helpers = 機能分類軸の延長
- skip 推奨

### 候補 K3 後続 (Loop 91 弱推奨, 機能拡張/docs 軸 1 連続復帰)

直近 5 セッション (Loop 71/72/74/78/84) で K 軸 5 件 = sweep 警告境界、ただし Loop 86 で軸が harness 衛生 + Loop 87-90 で構造分離 4 連続 = K 軸の連続性切れた = Loop 91 で K 軸 1 件目 (新シリーズ) は OK。

**範囲限定なら可能**:
- detection-extension-plan.md = Phase 状態は不変 = 単純時点更新は内容実質変化なし (Loop 86 K3' 却下 paradigm 適用)
- agent-log-archive-plan.md = Section 2.3 観測追記 = +5 ループの観測値で価値中
- autonomous-main-prompt-claude.md = 数値カウンタ無し確認済
- AGENTS.md / product-concept.md 整理 = 主観性高警告

### 候補 H1 (Loop 91 alternative, harness 衛生軸 4 件目で sweep 警告境界 ユーザー直伝指示要)

**agent-log-archive-plan.md Phase 1 着手 = AGENT_LOG.md に Archive Index Header 追加 (規模 SS、~5-10 分)**
- plan 内に「実施は別途ユーザー直伝指示要」と明示記載 = 後継メインが grep 確認後に exception 判断要
- **harness 衛生軸 4 件目 = 4 連続 sweep 警告境界 = pivot 推奨**

### 候補 J (継承、低優先): frontend 軸

- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行)
- TODO/FIXME/any: 直近セッションで grep ゼロ = 主観性高警告
- 浅 grep で具体的不足を 1 つ特定してから worker 発注

### 候補 H' (継承、低優先): 大量 untracked 整理

- 大量 untracked ファイル整理判断 (`docs/handoff/`, `docs/worker-prompts/` に 290+ untracked、ユーザー直伝指示があるまで保留)

### 低優先 (継承)

- B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-47 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認** (連続 100+ ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 SS なら ~3-8 分、規模 S なら ~4-12 分、規模 M なら ~10-15 分)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示 or git log を主観測手段にする
  - **本セッション Loop 89 例**: production 5 fn + 1 submod + axis test 4 件 + caller 修正 = 規模 SS-S (実質 S) = ~13 分完走 (worker prompt 145 行) = tauri::command + macos cfg(target_os) + import 移動の複雑さ
  - **本セッション Loop 90 例**: production 5 fn + axis test 2 件 + caller 修正 + import 整理 = 規模 SS-S = ~6 分完走 (worker prompt 167 行) = Loop 89 経験で速度向上
  - **worker 自律 2-commit pattern**: worker amend 時の hash 更新漏れに対し、worker 自身で chore commit を作成して補正する自律学習が Loop 60-90 の **31 ループ連続** で観測 = 完全に安定 pattern 化
  - **worker 自律学習進化**: Loop 89/90 で worker は refactor commit hash placeholder を実 hash に置換書き込み + chore commit hash は意図的に書かず = Loop 80/85/86/87/88 進化 pattern (placeholder 残留問題予防の本来意図) を完全踏襲

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 43 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-90 経験から)**:
- worker prompt に「2-commit pattern (refactor/docs commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を **明示的に** 記載 = 31 連続ループで完全安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~118-230 行で短縮効果あり (Loop 67-90 で実証 ~2.5-13 分完走)、規模 S 上限は ~200-230 行、規模 SS は ~118-150 行
- **Loop 80/85/86/87/88/89/90 precedent 適用**: worker は refactor/docs commit hash を実 hash で直接書き、chore commit hash は書かない = placeholder 残留問題予防達成 = 後継 worker も同 pattern 採用可
- **caller 修正方式**: 同 file 内 caller の場合は冒頭 use 文 1 行追加 + 既存呼び出しは無変更。lib.rs 等の異 file caller は path を直接書き換える (Loop 89/90 で実証)

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 89 = ~13 分、Loop 90 = ~6 分 = **平均 ~10 分で目標 15 分以内達成**)
- worker 1 件 = 1-2 commit (本セッション 2 ループ完走 = 5 commit = refactor x 2 + chore x 2 + SESSION SUMMARY x 1)
- 累積 worker 完走 190/190 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **2 ループ完走 + 通常 cadence handoff** = handoff 文書 L227「後継は 2-3 ループ完走推奨」を達成

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 0 回 (2 ループで通常 handoff のため)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-47` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-47 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 41 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 42 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「transcription.rs リファクタ完全完遂」「app_detection.rs サービス別抽出シリーズ 6 件完了」「大型 rust file 責務分離 = 16 file 目達成 = 9 scope 分散」「app_detection 機能分類軸 = 2 連続 = Loop 91 で 3 連続避ける」「test 軸は別ファイル素材を慎重に検討」「docs 軸 sweep 化注意」「detection-extension-plan.md = Phase 状態 subsection 新設で 5 セッション目 = 軸境界判断要」「agent-log-archive-plan.md = Phase 状態 subsection 追加 + 数値最新化 + Loop 86 観測追記済」「realtime engine = 3 軸全完了 = 構造美の最終形」「session_store/session_manager production = ~30-150 行コンパクト = 抽出メリット薄」「settings_permission.rs / transcription_commands_model.rs = 新 scope 1 件ずつ = 同 scope 連続避けて別 scope pivot 推奨」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「Loop 90 で構造分離 16 件目 (transcription_commands 軸 1 件目)、16:5:3 重心構造分離、9 scope 分散」を踏まえ、Loop 91 で **(a) 候補 X = 構造分離 17 件目 = 別 scope 新規発掘 (system_audio / cloud_whisper / session_commands / cloud_whisper_errors のいずれか) = 推奨 または (b) 候補 X' = transcription_commands 軸 2 件目 (source 関連) = 同 scope 連続 skip 推奨 または (c) 候補 X'' = app_detection 機能分類軸 3 件目 = 警告境界 skip 推奨 または (d) 候補 K3 後続 = 機能拡張/docs 軸 1 連続復帰 (主観性中要範囲限定) または (e) 候補 H1 = ユーザー直伝指示要 または (f) 候補 J = frontend 主観性高警告継承** のいずれか採用検討。worker prompt は「Loop 60-90 と同じ 2-commit pattern」を明示 + ~118-230 行 (規模 SS なら ~118-150 行、規模 S なら ~150-230 行)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 89/90 で実証 = handoff の「scope 払底気味」警告を grep + Read 精査 + 発掘で覆して settings 軸 + transcription_commands 軸を 2 連続新規開拓 → メイン批判判断 連続 41 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループ + 通常 cadence handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**、**worker prompt は ~118-230 行を目安に simpler 化** (Loop 67-90 で短縮効果実証、規模 SS では ~118-150 行、規模 S では ~150-230 行)
10. **worker 完走確認は `git status --short` または `git log --oneline -3` を主観測手段とする** (連続 100+ ループで実証した mitigation pattern、log file は補助、観測時間目安 90-720 秒、規模 SS なら ~3-8 分、規模 S なら ~4-13 分、規模 M なら ~10-15 分)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 43 セッション目) で書く
12. **commit hash 更新漏れパターン (Loop 60-90 で 31 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern」を明示記載 = 31 連続ループで安定動作。**Loop 80/85/86/87/88/89/90 進化 pattern**: worker は refactor/docs commit hash を実 hash で直接書き、chore commit hash のみ書かない = placeholder 残留問題予防達成 = 後継 worker も同 pattern 採用可。

# Loop 91 への期待 (推奨ループ設計)

直近 24 ループで構造分離軸 16 件 + 機能拡張/docs 軸 5 件 + harness 衛生軸 3 件 = Loop 90 で transcription_commands_model.rs 抽出 = 16:5:3 重心構造分離 = **Loop 91 では構造分離軸 5 連続だが scope は 5 種 (app_detection 機能分類 → settings → transcription_commands → 新 scope) で許容範囲、ただし transcription_commands 軸 2 件目 / app_detection 機能分類軸 3 件目は同 scope 連続 skip 推奨 = 別 scope 構造分離 17 件目を新規発掘推奨**。

メインは grep で 1 候補に絞る (本ハンドオフでは X 新規発掘 17 件目 = system_audio.rs / cloud_whisper.rs / session_commands.rs / cloud_whisper_errors.rs のいずれかを推奨、X' transcription_commands 軸 2 件目 skip 推奨、X'' app_detection 機能分類軸 3 件目 skip 推奨、K3 後続 機能拡張/docs 軸 1 連続復帰 = 範囲明確化要、H1 ユーザー直伝指示要、J frontend 主観性高警告) → worker prompt 作成 (~118-230 行) → 起動 = 通常の自律改善ループへ復帰。

**最有力安全候補は X 新規発掘 = メイン批判判断 連続 42 セッション目の発掘力で安全候補を探す**:
- 構造分離 17 件目を強行する場合: 別 scope (system_audio / cloud_whisper / session_commands / cloud_whisper_errors) で具体的に責務分離可能な箇所を grep + Read で発掘 (本セッション Loop 89/90 precedent = 「scope 払底気味」を 2 連続発掘で覆す手法を継承)
- 機能拡張/docs 軸を選ぶ場合: detection-extension-plan.md Q1/Q2/Q3/Q4/Q7/Q8 の何かを Resolved に近付ける調査 (主観性中)、または新規 plan / docs の必要性を grep で発掘
- frontend 軸を選ぶ場合: 浅 grep で具体的不足を 1 つ特定してから worker 発注 (主観性高警告に対する根拠提示要)
- H1 = ユーザー直伝指示要のため、後継メインが grep 確認後に exception 判断要

**新規 scope 発掘候補 X (Loop 91 推奨) の具体的調査手順**:
1. `wc -l src-tauri/src/*.rs | sort -rn | head -20` で大型 file ranking 確認
2. `grep -nE "^(pub |fn |async fn |impl |struct |enum |const |static |#\[)" src-tauri/src/<候補>.rs | head -30` で構造概観
3. **production fn が機能境界で分かれているか** が鍵 (transcript_bridge.rs / session.rs は production スリム = 抽出メリット薄)
4. tauri::command の有無 + lib.rs invoke_handler 影響有無を確認
5. 関連 test の件数を grep で確認 → 規模見積もり (production 4-5 fn + test 数件 = 規模 SS-S = ~5-13 分完走)
