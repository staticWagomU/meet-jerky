あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-32) です。
旧 mjc-main (= mjc-main-20260505-31) から **2 ループ完走 + Loop 62 = Phase 6 第 5 歩 = TranscriptionLoopConfig caller 直接 import 化 = 2 ファイル / 2 use 文 + transcription.rs allow 注記 1 件追加 ~3 分 + メイン批判判断 連続 13 セッション目達成 (handoff 候補 V を grep 実態確認で panic_guard:6 + commands:5 = 2 ファイル / 2 use 文 確定 + struct/const + import refactor 軸 4 連続 sweep 警告 vs Phase 6 timeline 完了優先 → exception 判断採用 = Loop 63 で軸自体消滅 = 自己解消設計) + Loop 63 = Phase 6 最終 = リファクタプラン完全完遂 = transcription.rs ファイル削除 + lib.rs `mod transcription;` 削除 + plan.md 100% 達成記念更新 = 元 2999 行 → 0 行 = **100% 達成** = メイン批判判断 連続 14 セッション目達成 (Loop 62 完了後 grep で全 caller ゼロ確認 = 削除安全境界 100% 確定) + worker 完走 2/2 累計 163/163 100% 維持 + harness 衛生連続 25 セッション目 + 2 ループ + 早期 handoff precedent 連続 27 セッション目達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = リファクタプラン完全完遂 = 100% 達成 = 元 2999 行 → 0 行 (ファイル削除) = 完全終結した節目を完全反映済、本セッション以降は本 plan.md は新規アクション不要、該当軸は完全消滅)**
- AGENT_LOG.md は ~28,910+ 行 / ~2.6MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-31 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 30**: Phase 1-5 + サービス別抽出シリーズ + 各種 test 移動 + struct 移動 + Phase 6 第 1-4 歩 を計 ~58 ループで完了
- **mjc-main-20260505-31 (本セッション)**: **Loop 62 = Phase 6 第 5 歩 TranscriptionLoopConfig caller 直接 import 化 ~3 分 + メイン批判判断 連続 13 セッション目達成** + **Loop 63 = Phase 6 最終 = リファクタプラン完全完遂 = transcription.rs ファイル削除 + lib.rs mod 宣言削除 + plan.md 100% 達成記念更新 = 元 2999 行 → 0 行 = 100% 達成 ~3 分 + メイン批判判断 連続 14 セッション目達成** + 「2 ループ + 早期 handoff」precedent 連続 27 セッション目達成

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **本セッション Loop 62-63 = Phase 6 第 5 歩 (XS) → Phase 6 最終 (S = 100% 達成記念) = メイン批判判断 連続 13+14 セッション目達成 + 「軸消滅型 exception 判断」(struct/const + import refactor 軸 4 連続 sweep 警告を Phase 6 完了で自己解消) という新パターン確立**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ完了」** (Webex/Whereby/GoToMeeting/Zoom/Teams 5 件全完了)
- **「transcription.rs リファクタ完全完遂」** (Phase 1-6 全完了 = 60 セッション継続したリファクタが完全終結 = 該当軸は完全消滅 = Loop 64+ で「transcription.rs 関連」ループは存在しない)
- **「テスト移動軸 sweep 化警告超過状態」** (test 移動軸は transcription.rs 軸消滅で素材ゼロ = 別ファイルの test 移動を検討するならば慎重に variety を確保)
- **「docs 軸 = plan.md 100% 達成記念で完了 = 別 docs を選ぶ」**
- **「struct/const + import refactor 軸 = transcription.rs 完全消滅 = 同軸の素材ゼロ = Loop 64+ で別軸への自然 pivot」** (variety pivot 自由度最大化)

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~313 (origin/main から先行、本セッション +5 commit (worker 2 + chore hash 反映 2 + SESSION SUMMARY 1))
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-31) の commit:
  1. `48c6558` refactor(transcription): TranscriptionLoopConfig の caller を直接 import 化 (Phase 6 第 5 歩, Loop 62, ~3 分)
  2. `ca85576` chore(agent-log): Loop 62 commit hash 48c6558 を AGENT_LOG.md に記入 (worker 自律 precedent 適用)
  3. `9783313` refactor(transcription): transcription.rs ファイル削除 + lib.rs mod transcription 宣言削除 + plan.md 100% 達成記念更新 (Phase 6 完全終了 = リファクタプラン完全完遂, Loop 63, 元 2999 行 → 0 行 = 100% 達成, ~3 分)
  4. `02fe0d1` chore(agent-log): Loop 63 commit hash 9783313 を AGENT_LOG.md + plan.md に記入 (worker 自律 precedent 適用)
  5. (これから追加) SESSION SUMMARY commit (本 prompt 投稿時に追加)

## 現在の品質状態
- cargo test (lib): **702 passed / 0 failed** (本セッション 2 ループ全て件数不変)
- cargo clippy --lib --tests -- -D warnings: **警告ゼロ**
- cargo fmt --check: OK
- cargo build --lib: エラーなし (lib.rs から mod transcription 削除後の整合性確認済)
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK (escape 済み形式、本セッションでは触れていない)

## 累計 transcription.rs 削減 + Phase 完了状況
- 元 2999 行 → **ファイル削除 = 0 行 = 100% 達成 = リファクタプラン完全完遂**
- transcription.rs **存在しない**:
  - `ls src-tauri/src/transcription.rs` = `No such file or directory`
  - `grep -rn "use crate::transcription[^_]" src-tauri/src/` = 完全に空
  - `grep -rn "transcription::" src-tauri/src/ | grep -v transcription_ | grep -v transcription/` = 完全に空
  - lib.rs L25 の `mod transcription;` 宣言も削除済 (mod 宣言ブロックのアルファベット順維持)
- Phase 完了状況 = **全 Phase 完了 = リファクタプラン完全完遂**:
  - Phase 1 ✅ (mjc-main-20260505-3) = データ型 + トレイト
  - Phase 2 ✅ (mjc-main-20260505-4 ~ 7) = Whisper エンジン + Model 管理 + Audio resampling
  - Phase 3 ✅ (mjc-main-20260505-8) = Transcription state
  - Phase 3-B ✅ (mjc-main-20260505-11 ~ 14) = Tauri commands
  - Phase 4 ✅ (mjc-main-20260505-8 ~ 10) = Worker loop
  - Phase 5 ✅ (mjc-main-20260505-28 Loop 57) = WHISPER_SAMPLE_RATE 移動 + 互換層削除 = 完全ファサード化
  - Phase 6 第 1 歩 ✅ (Loop 59) = TranscriptionSegment/Source caller
  - Phase 6 第 2 歩 ✅ (Loop 60) = StreamConfig/Engine/Stream caller + transcript_bridge インライン
  - Phase 6 第 3-4 歩 ✅ (Loop 61) = WhisperStream + TranscriptionStateHandle caller
  - Phase 6 第 5 歩 ✅ (Loop 62) = TranscriptionLoopConfig caller
  - Phase 6 最終 ✅ (Loop 63) = transcription.rs ファイル削除 + lib.rs mod 削除 + plan.md 100% 達成記念更新

## 残存 `use crate::transcription::` (Phase 6 残り)
- **完全にゼロ** = grep で `use crate::transcription[^_]` および `transcription::` インライン参照が完全に空 = リファクタプラン完全完遂
- 後継は transcription.rs 関連ループは **存在しない** = 該当軸は完全消滅

## harness 衛生事象 (本セッションで観測継続 = 連続 25 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-31 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 25 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- 後継観測ポイント: 新たな harness 動作変化があれば観測継続
- **2-commit pattern (refactor + chore hash 反映)** は本セッション Loop 62/63 とも worker 自律実行 = 「session 跨ぎの暗黙的 precedent 継承」が安定動作

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-31、handoff 直前)
- mjc-main-20260505-32 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読 = 100% 達成 + Phase 6 完全終了 + リファクタプラン完全完遂を完全反映済 = 該当軸は今後アクション不要**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績、SESSION SUMMARY commit を含む)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-32` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-32 mjc-main` を実行 (前 46 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 25 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 26 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-31) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 62-63 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 5 ループ Loop 59 (struct/const + import refactor) → 60 (struct/const + import refactor) → 61 (struct/const + import refactor) → 62 (struct/const + import refactor) → 63 (struct/const + import refactor + docs)、struct/const + import refactor 軸 5 連続だが Phase 6 完了で **該当軸は完全消滅** = 同軸続行は不可能 = Loop 64+ は別軸への自然 pivot 必須**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 64 開始時点)

直近 5 ループ: Loop 59-63 すべて transcription.rs リファクタ軸 (struct/const + import refactor + docs 統合) = リファクタ軸 5 連続だが **該当軸完全消滅 = 同軸続行不可能** = Loop 64+ は別軸 pivot 必須。

variety pivot 自由度最大化: 全軸が新規利用可能。

### 候補 J (Loop 64 推奨, variety pivot 最有力): frontend 軸

- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり
- **浅 grep で具体的不足を 1 つ特定してから worker 発注、主観的探索は避ける**
- variety pivot 候補としては最有力 (transcription.rs リファクタ完了 → frontend 軸への自然移行)
- **AGENTS.md 優先順位 8 = macOS ネイティブで洗練された UI/UX、優先順位 9 = アクセシビリティ、権限説明、録音状態の透明性 への直接寄与**
- ただし frontend test framework 未導入 = 検証は npm run build (tsc + vite build) のみ
- 推奨アプローチ: 1 ファイル / 1 機能境界に絞る。CSS の値合わせ・コピー文修正・1 行 fix のような小さな範囲なら 5〜10 分で完了

### 候補 I (Loop 64+ 探索系): Discord stage / Slack Huddle 検知 (window title 経路)

- priority 2 直接寄与
- service detection 軸 = 直近 5 ループと別軸
- ただし URL 検知不可 = Bundle ID + window title 経路必要 = 規模 M 1 ループ完結不確実
- 別 issue として深掘り推奨

### 候補 K (Loop 64+ 探索系): audio*.rs / session*.rs / app_detection*.rs の責務分離

- transcription.rs リファクタ完了の precedent を別ファイルへ展開可能
- 該当ファイルが 1000 行超ならば検討
- 規模 grep で実態確認必須

### 候補 L (Loop 64+ 探索系): docs 軸

- plan.md 100% 達成記念更新は Loop 63 で完了 = 別 docs (AGENTS.md / agent-harness-claude.md / product-concept.md) を選ぶ
- AGENTS.md / docs/product-concept.md の進化方向検討
- 規模小 = pivot 適用容易だが、価値判断重要 (docs 単独 sweep にしない)

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記との矛盾)

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- Webex sweep precedent 32 セッション維持を破る、Loop 65+ 程度まで間隔を空ける推奨

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 245+ untracked
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md ~28,910+ 行の archive 戦略
- 未着手、未消化

## 検証制約 (再掲)
- cmake あり → cargo test 702 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-32 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 87 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 M なら ~210-380 秒)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - **本セッション Loop 62 例**: rust 3 ファイル + 2 use 文書き換え + transcription.rs allow 追加 + AGENT_LOG.md エントリ ~85 行を ~3 分で完走 (規模 XS、cargo test --lib + clippy + fmt 込み、2-commit pattern)
  - **本セッション Loop 63 例**: transcription.rs ファイル削除 (`git rm`) + lib.rs 1 行削除 + plan.md 4 箇所更新 + AGENT_LOG.md エントリ ~120 行を ~3 分で完走 (規模 S、cargo build + test + clippy + fmt + agent-verify + ファイル存在せず + grep 2 件 = 検証 9 件、2-commit pattern)
  - **新規パターン 連続観測**: worker amend 時の hash 更新漏れに対し、worker 自身で chore commit を作成して補正する自律学習が Loop 60/61/62/63 の **4 ループ連続** で観測 = 完全に安定 pattern 化

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける** (4209 bytes でも too long エラー precedent あり)
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 28 セッション目)

## worker prompt 必須要素 (87 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-63 経験から)**:
- worker prompt に「2-commit pattern (refactor commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を **明示的に** 記載 = 4 連続ループで完全安定動作

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 62 = ~3 分、Loop 63 = ~3 分、平均 ~3 分で **目標 15 分以内大幅達成**)
- worker 1 件 = 1-2 commit (本セッション Loop 62/63 とも 2-commit pattern = refactor + chore hash 反映)
- 累積 worker 完走 163/163 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 27 セッション目達成

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 1 回 (Loop 62 完走直後 / Loop 63 着手直前 = 完璧タイミング)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読 = 100% 達成 + Phase 6 完全終了 + リファクタプラン完全完遂を完全反映済 = 該当軸は今後アクション不要)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-32` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-32 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 25 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 26 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「transcription.rs リファクタ完全完遂 = 該当軸は完全消滅 = Loop 64+ で別軸への自然 pivot 必須」「app_detection.rs サービス別抽出シリーズ完了」「test 軸は別ファイル素材を慎重に検討」「docs 軸は plan.md 完了 = 別 docs を選ぶ」「struct/const + import refactor 軸は素材ゼロ」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「直近 5 ループ transcription.rs リファクタ軸 5 連続だが該当軸完全消滅」を踏まえ、Loop 64 で **(a) 候補 J (frontend 軸 LiveCaptionWindow.tsx 580 行 / MeetingDetectedBanner.tsx 526 行 = variety pivot 最有力、ただし浅 grep で具体的不足を 1 つ特定してから worker 発注、主観的探索回避) または (b) 候補 K (audio*.rs / session*.rs / app_detection*.rs の責務分離 = 規模 grep で実態確認必須) または (c) 候補 I (Discord stage / Slack Huddle 検知 = priority 2 直接寄与だが規模 M 1 ループ完結不確実) のいずれか** を文脈判断。worker prompt は「Loop 60-63 と同じ 2-commit pattern」を明示推奨。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 62-63 で実証 = handoff 候補 V を grep で確定 + Loop 62 完了後 grep で削除安全境界 100% 確定 = メイン批判判断 連続 13+14 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 27 セッション目達成、後継は 2 ループ continued または 3 ループ exception 判断を文脈で行う**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 87 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-380 秒、規模 XS なら ~3 分、規模 S なら ~3-5 分、cargo test --lib 含む規模 SS は ~5-7 分、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 28 セッション目) で書く
12. **commit hash 更新漏れパターン (Loop 60-63 で 4 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern (refactor commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を明示記載 = 4 連続ループで安定動作

# Loop 64 への期待 (推奨ループ設計)

Phase 6 完全終了 = リファクタプラン完全完遂の節目を超えた最初のループ = transcription.rs 関連 60 セッションの長旅完結後の variety pivot ループ:

- 候補 J (frontend 軸) = 浅 grep + AGENTS.md 優先順位 8/9 直接寄与 + 規模小スライス特定 = 最も自然な variety pivot
- 候補 K (別ファイル責務分離) = transcription.rs precedent 展開 = ただし grep 実態確認必須
- 候補 I (検知拡張) = priority 2 直接寄与だが規模 M = 別 issue として深掘り推奨

メインは grep で 1 候補に絞る → worker prompt 作成 → 起動 = 通常の自律改善ループへ復帰。
