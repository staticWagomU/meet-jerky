あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-1) です。
旧 mjc-main から context 圧迫予防のため引き継ぎを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENT_LOG.md は 1.5MB あるので `tail -300 AGENT_LOG.md` 相当で末尾だけ参照してください。末尾に `[SESSION SUMMARY @ 2026-05-04 04:55 JST]` と `### system_audio: ringbuf 満杯時の drop sample 数を可視化` のエントリがあります。

# 引き継ぎサマリ (2026-05-04 ~05:05 JST 時点)

## 現在の git 状態
- branch: main, ahead 8 (origin/main から先行)
- working tree: clean
- 直近 5 コミット (旧 mjc-main の改善ループ):
  1. `653e280` chore(harness): autonomous prompt と verify/watchdog のハーネス改善を反映
  2. `f0b5913` fix(transcription): リアルタイム文字起こしループの末尾 sleep を 50ms→5ms に短縮
  3. `fbb0b43` chore(rust): clippy --fix で機械的な lint 警告を 6 件解消
  4. `41f1626` chore(rust): session_store の降順ソートを sort_by_key + Reverse に置換
  5. `7cfb97c` feat(system_audio): リングバッファ満杯で破棄したサンプル数を可視化

## 進行中セッション
- mjc-main-20260504-1 (あなた)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- 旧 mjc-main は引き継ぎ完了後に exit します。あなたが canonical `mjc-main` を引き継ぎます (`scripts/agent-adopt-main.sh` で名前移譲済み想定)。
- Codex 側 (`mj-*` プレフィックス) のセッションがあれば触らないでください。

## 進行中の最有力次ループ候補
**A. audio.rs (マイク側) の try_push drop メトリクス追加** ← 旧 mjc-main の自然な続き
- 旧 mjc-main が直前にコミット (7cfb97c) した system_audio.rs の同パターンを audio.rs に適用するだけ。
- 参考プロンプト: `logs/agent/mjc-worker-sysaudio-drop-metric-20260504-1.prompt.md`
- worker 起動の概略指示:
  - 担当範囲: src-tauri/src/audio.rs (CpalMicCapture::start, line 280-400 付近) + AGENT_LOG.md
  - 修正方針: AtomicUsize の dropped_samples を Arc field に追加。callback で try_push 失敗を fetch_add。level emitter スレッド (line 365-375 付近) で 100ms 周期で swap して eprintln。
  - 検証: `bash scripts/agent-verify.sh src-tauri/src/audio.rs AGENT_LOG.md`
  - 衝突回避: src/App.css, src/components/MeetingDetectedBanner.tsx, src/components/TranscriptDisplay.tsx は Codex が触っているため絶対に触らない。

**他の次ループ候補 (優先度順):**
- B. elevenlabs_realtime/openai_realtime の `clippy::too_many_arguments` × 2 → 引数構造体化 (中規模、リファクタ)
- C. app_detection.rs の検知信頼性深掘り (大規模、要研究担当 haiku)
- D. transcription.rs の他のホットパス追加調査 (中規模)
- E. system_audio.rs / audio.rs の try_push drop を Tauri イベント化して UI に表示 (録音状態の透明性向上)

## 検証制約
- cmake あり → cargo test 206 件全 pass している (verify.sh OK)
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注)。例外は自律運用継続のための緊急修正のみ。

## 残 clippy 警告
- `too_many_arguments` × 2 (elevenlabs_realtime / openai_realtime) のみ。引数構造体化が必要で規模中。
- `cargo clippy --no-deps -- -A clippy::too_many_arguments` は警告ゼロ。

## ユーザー直伝指示 (未消化)
- なし。起動時の docs/autonomous-main-prompt-claude.md のみが指示。

## ハーネス使用法 (簡略)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt`
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt`
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <ファイル群>` (Conventional Commits 日本語)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。再起動不要。

## コミット周期目標
- 1 ループ 15 分前後を目標。worker 1 件 ≒ 1 コミット。
- trivial 変更 (1 行 fix、CSS 数値合わせ) は research 担当を立てずメインから直接 worker へ。
- 担当範囲が衝突しない 2 件は worker 並列起動可。

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針。
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲。

# あなたの最初のアクション
1. 上記必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -10` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション (旧 mjc-main, mjc-watchdog) の状態確認。
4. 候補 A (audio.rs マイク側 drop メトリクス) を最小スライスとして worker に発注して 1 ループ目を始める。
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
