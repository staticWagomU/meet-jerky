あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-35) です。
旧 mjc-main (= mjc-main-20260505-34) から **2 ループ完走 + Loop 68 = audio.rs private pure helpers 4 件 (for_each_mono_sample / normalize_sample_to_f32 / sanitize_sample / calculate_rms_from_sum) を audio_sample_helpers.rs (新規 47 行) に抽出 = ~40 行抽出 + lib.rs mod 追加 + audio.rs use 文追加 ~6-7 分 + メイン批判判断 連続 20 セッション目達成 (handoff X' (elevenlabs/openai struct 抽出) を grep で external caller 1 件のみ発見 → audio_utils.rs precedent 直接展開可能な audio.rs pure helper 抽出を新候補発見) + Loop 69 = session_store.rs pure fn 2 件 + 定数 1 件 (unescape_inline_markdown_text + parse_session_started_at_secs + MAX_JS_DATE_UNIX_SECS) を session_store_parse.rs (新規 32 行) に抽出 = ~30 行抽出 + lib.rs mod 追加 + session_store.rs use 文追加 ~3 分 + メイン批判判断 連続 21 セッション目達成 (variety 規則 6 連続 sweep 警告境界を踏まえ session_store scope 続行を exception 判断 = 規模 XS + precedent 直接展開でリスク最小) + worker 完走 2/2 累計 169/169 100% + harness 衛生連続 28 セッション目 + 2 ループ + 早期 handoff precedent 連続 30 セッション目達成 + worker 自律 2-commit pattern 連続 10 ループ目達成 + 大型 rust file 責務分離 audio scope -62 行 / session_store scope -42 行 / 残 3 件 (session_manager / elevenlabs_realtime / openai_realtime)** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成 = transcription.rs 削除済 = 該当軸は今後アクション不要)**
- AGENT_LOG.md は ~29,400+ 行 / ~2.7MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-34 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 33**: Phase 1-6 + サービス別抽出シリーズ + 各種 test 移動 + struct 移動 + Phase 6 第 1-5 歩 + 最終 + app_detection scope シリーズ + 大型 rust file 責務分離 始動 (audio + session_store)
- **mjc-main-20260505-34 (本セッション)**: **Loop 68 = audio.rs pure helper 抽出 + メイン批判判断 連続 20 セッション目 + Loop 69 = session_store.rs pure fn 抽出 + メイン批判判断 連続 21 セッション目 + 大型 rust file 責務分離 6 連続到達 + 「2 ループ + 早期 handoff」precedent 連続 30 セッション目達成**

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **本セッション Loop 68-69 = 大型 rust file 責務分離 6 連続到達 = メイン批判判断 連続 20-21 セッション目達成**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ 6 件完了」「transcription.rs リファクタ完全完遂」「大型 rust file 責務分離 = 6 連続到達 = 強い variety pivot 推奨」「テスト移動軸 sweep 化警告超過状態」「docs 軸 = plan.md 100% 達成記念で完了 = 別 docs を選ぶ」**

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~329 (origin/main から先行、本セッション +5 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-34) の commit:
  1. `86ffc7a` refactor(audio): private pure helpers を audio_sample_helpers.rs に抽出 (Loop 68, ~6-7 分)
  2. `4f1d53c` chore(agent-log): Loop 68 commit hash を AGENT_LOG.md に記入 (worker 自律 precedent 適用 9 連続)
  3. `7aeb72f` refactor(session_store): pure fn 2 件 + 定数 1 件を session_store_parse.rs に抽出 (Loop 69, ~3 分)
  4. `1571fd4` chore(agent-log): Loop 69 commit hash を AGENT_LOG.md に記入 (worker 自律 precedent 適用 10 連続)
  5. `479e636` chore(agent-log): SESSION SUMMARY commit (Loop 68-69 + handoff 判断)

## 現在の品質状態
- cargo test (lib): **702 passed / 0 failed** (本セッション 2 ループ全て件数不変)
- cargo clippy --lib --tests -- -D warnings: **警告ゼロ**
- cargo fmt --check: OK
- cargo build --lib: エラーなし
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK

## 大型 rust file 責務分離実績 (handoff 候補 X 始動以降の累計)

- audio.rs: 1011 行 (mjc-main-20260505-32 終了時) → 949 行 (-62 行) + audio_traits.rs (24 行) + audio_sample_helpers.rs (47 行)
- session_store.rs: 1175 行 (mjc-main-20260505-32 終了時) → 1133 行 (-42 行) + session_store_types.rs (19 行) + session_store_parse.rs (32 行)
- 残 3 件 (Loop 70+ 候補):
  - session_manager.rs (1274 行)
  - elevenlabs_realtime.rs (1146 行)
  - openai_realtime.rs (1102 行)
- transcription.rs リファクタ: **完全完遂** (mjc-main-20260505-31 Loop 63 = transcription.rs ファイル削除)
- app_detection.rs サービス別抽出: **6 件完了** (Webex/Whereby/GoToMeeting/Zoom/Teams/Google Meet)

## harness 衛生事象 (本セッションで観測継続 = 連続 28 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-34 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 28 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern (refactor + chore hash 反映)** は本セッション Loop 68/69 とも worker 自律実行 = 連続 10 ループ目 = 「session 跨ぎの暗黙的 precedent 継承」が完全に安定動作

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-34、handoff 直前)
- mjc-main-20260505-35 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全完遂済 = 該当軸は今後アクション不要**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 5 コミットが本セッションの実績、SESSION SUMMARY commit を含む)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-35` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-35 mjc-main` を実行 (前 49 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 28 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 29 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-34) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 68-69 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 6 ループ Loop 64-69 = 大型 rust file 責務分離 6 連続到達 = 強い variety pivot 検討必須**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 70 開始時点)

直近 6 ループ: Loop 64 (app_detection URL helper) → 65 (app_detection Google Meet) → 66 (audio trait) → 67 (session_store struct) → 68 (audio pure helper) → 69 (session_store pure fn)。
- 大型 rust file 責務分離 = 6 連続到達 = **強い variety pivot 検討必須**
- pattern 内訳: pure fn (4 回) + trait (1 回) + struct (1 回) = pattern 切替で variety 維持しているが scope 全体で 6 連続
- scope 内訳: app_detection (2) + audio (2) + session_store (2) = 多 scope 分散
- **Loop 70 で同 pattern 続行は 7 連続到達 = 別軸 variety pivot 強く推奨**
- ただし規模 XS で precedent 直接展開可能なら exception 判断可

### 候補 X'' (Loop 70 推奨, variety pivot = 別軸シフト)

#### (a) 検知拡張 = Discord stage / Slack Huddle
- priority 2 直接寄与 (会議サービス検知)
- 規模 M = 1 ループ完結不確実 = Phase 設計推奨
- grep 確認: `discord|slack` 言及完全ゼロ = 完全未実装
- Bundle ID + window title 経路必要

#### (b) frontend 軸
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行)
- 浅 grep で具体的不足を 1 つ特定してから worker 発注 = 主観的探索を避ける
- TODO/FIXME/any: 直近セッションで grep ゼロ確認

#### (c) docs 軸
- AGENTS.md / agent-harness-claude.md / product-concept.md (sweep 化禁止、価値判断重要)
- plan.md 100% 達成記念は Loop 63 で完了済 = 別 docs 選ぶ

### 候補 X''' (Loop 70 exception 判断, 大型 rust file 責務分離続行 = sweep 警告超過注意)

- **session_manager.rs (1274 行)**: 規模最大 file = grep 実態確認後に struct/enum/pure fn 抽出。前任 Loop 67 の handoff 候補で「SessionManagerError 外部 caller ゼロ」と判明していたため、別アイテムを grep で探す
- **elevenlabs_realtime.rs (1146 行) / openai_realtime.rs (1102 行)**: 両 file の `enum AudioCommand` (private 同名) は完全に同名・同構造の重複 (Samples + Finalize) = 共通化候補だが規模 M (shared module 設計判断必要)
- **session_store.rs scope 続行 (3 連続) または audio.rs scope 続行 (3 連続)**: exception 判断必要、grep で実態確認後に判断

### 候補 J (継承、低優先): frontend 軸

- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行)
- 浅 grep で具体的不足を 1 つ特定してから worker 発注 = 主観的探索を避ける

### 候補 I (継承、低優先): Discord stage / Slack Huddle 検知

- priority 2 直接寄与
- 規模 M = 1 ループ完結不確実 = 別 issue として深掘り推奨

### 候補 L (継承、低優先): docs 軸

- plan.md 100% 達成記念は Loop 63 で完了済 = 別 docs 選ぶ

### 低優先 (継承)

#### B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
#### A1. Webex 日本語 window title (継承)

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断 (`docs/handoff/`, `docs/worker-prompts/` に 250+ untracked、ユーザー直伝指示があるまで保留)
#### I. AGENT_LOG.md ~29,400+ 行の archive 戦略 (未着手)

## 検証制約 (再掲)
- cmake あり → cargo test 702 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-35 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 93 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 SS なら ~3-8 分、規模 M なら ~210-380 秒)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - **本セッション Loop 68 例**: rust 3 ファイル (新規 1 + 修正 2) + AGENT_LOG.md エントリ ~50 行 を ~6-7 分で完走 (規模 XS、worker prompt ~150 行 simpler 化)
  - **本セッション Loop 69 例**: rust 3 ファイル (新規 1 + 修正 2) + AGENT_LOG.md エントリ ~50 行 を ~3 分で完走 (規模 XS、worker prompt ~150 行 simpler 化、precedent 直接展開で短縮)
  - **新規パターン 連続観測**: worker amend 時の hash 更新漏れに対し、worker 自身で chore commit を作成して補正する自律学習が Loop 60-69 の **10 ループ連続** で観測 = 完全に安定 pattern 化

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 31 セッション目)

## worker prompt 必須要素 (93 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

**追加推奨 (Loop 60-69 経験から)**:
- worker prompt に「2-commit pattern (refactor commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を **明示的に** 記載 = 10 連続ループで完全安定動作
- 大型 file 抽出時は **precedent file (例: audio_utils.rs / audio_sample_helpers.rs / session_store_parse.rs) の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~150 行 simpler 化が短縮効果あり (Loop 67-69 で実証 ~3-6 分完走)

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 68 = ~6-7 分、Loop 69 = ~3 分、平均 ~5 分で **目標 15 分以内大幅達成**)
- worker 1 件 = 1-2 commit (本セッション Loop 68/69 とも 2-commit pattern = refactor + chore hash 反映)
- 累積 worker 完走 169/169 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 30 セッション目達成

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 2 回 (Loop 68 worker 起動後 + Loop 69 worker 起動後 = improver による継続指示、適切タイミング)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全完遂済 = 該当軸は今後アクション不要)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-35` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-35 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 28 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 29 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「transcription.rs リファクタ完全完遂 = 該当軸完全消滅」「app_detection.rs サービス別抽出シリーズ 6 件完了」「大型 rust file 責務分離 = 6 連続到達 = 強い variety pivot 推奨」「test 軸は別ファイル素材を慎重に検討」「docs 軸は plan.md 完了 = 別 docs を選ぶ」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「大型 rust file 責務分離 6 連続 (Loop 64-69)」を踏まえ、Loop 70 で **(a) 候補 X'' = 強い variety pivot = 検知拡張 (Discord/Slack 規模 M = Phase 設計推奨) または frontend 軸 (浅 grep で具体不足 1 つ特定推奨) または docs 軸 (別 docs 選択) を文脈判断、または (b) 候補 X''' = exception 判断で大型 rust file 責務分離続行 = session_manager.rs (1274 行) Phase 1 = grep 実態確認後に struct/enum/pure fn 抽出、または elevenlabs/openai AudioCommand 共通化 (規模 M shared module 設計)** のいずれか。worker prompt は「Loop 60-69 と同じ 2-commit pattern」を明示 + simpler 化 (Loop 67-69 precedent ~150 行) で短縮効果。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 68-69 で実証 = handoff X' を grep で「caller 1 件のみ」発見 → 新候補発見 = メイン批判判断 連続 20-21 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 30 セッション目達成、後継は 2 ループ continued または 3 ループ exception 判断を文脈で行う**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**、**worker prompt は ~150 行を目安に simpler 化** (Loop 67-69 で短縮効果実証)
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 93 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-380 秒、規模 XS なら ~3-7 分、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 31 セッション目) で書く
12. **commit hash 更新漏れパターン (Loop 60-69 で 10 連続観測 = 完全安定 pattern 化)**: worker prompt に「2-commit pattern (refactor commit → hash 取得 → AGENT_LOG.md / plan.md 更新 → chore commit)」を明示記載 = 10 連続ループで安定動作

# Loop 70 への期待 (推奨ループ設計)

大型 rust file 責務分離 6 連続 (Loop 64-69) の節目を踏まえた **強い variety pivot 検討ループ**:

- 候補 X'' (a) 検知拡張 = priority 2 直接寄与だが規模 M = Phase 設計推奨
- 候補 X'' (b) frontend = 主観性高警告継承、浅 grep で具体不足 1 つ特定推奨
- 候補 X'' (c) docs = sweep 化禁止、価値判断重要
- 候補 X''' = exception 判断 (session_manager Phase 1 / elevenlabs+openai AudioCommand 共通化)

メインは grep で 1 候補に絞る → worker prompt 作成 (~150 行 simpler 目安) → 起動 = 通常の自律改善ループへ復帰。
