あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-30) です。
旧 mjc-main (= mjc-main-20260505-29) から **2 ループ完走 + Loop 58 = plan.md 再更新 (Loop 57 を 98.8% → 99.0% 反映 + Phase 5 完全終了 + 完全ファサード化記載 ~2.25 分 = Loop 56/57 を上回る効率性で 1 commit 完結) + Loop 59 = Phase 6 第 1 歩 TranscriptionSegment/Source 4 ファイル / 7 箇所 use 文 直接 import 化 ~6 分 + メイン批判判断 連続 10 セッション目達成 (handoff の候補 R 予測「6 ファイル」を grep で「4 ファイル / 7 箇所 use 文」に訂正) + worker 追加批判判断で transcript_bridge.rs L461/L472/L477 インライン参照 3 箇所 (計 10 件) を発見 + 担当範囲外として賢い分割判断 = メイン超える精度 + worker 二重批判判断 pub use 互換層 clippy 警告なし確認で残置と判断 + worker 完走 2/2 (累計 159/159 100% 維持) + harness silent fail mitigation pattern 連続 83 ループ実証達成 + canonical 移譲後 scripts/* M 再出現せず観測 (連続 23 セッション目で結論超強化) + sonnet worker の Tidy First 質的高さ連続 26 セッション目で実証 + 「2 ループ + 早期 handoff」precedent 連続 25 セッション目達成 + コミット周期 平均 ~4 分/loop で目標 15 分以内大幅達成 + 1 セッション削減量 0 行 (Phase 6 caller refactor は transcription.rs サイズ不変、互換層解体への移行段階)** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-5 完全終了 + Phase 5 完全終了 = transcription.rs 完全ファサード化 = mjc-main-20260505-29 Loop 58 で 99.0% 反映完了、本セッション Loop 59 (Phase 6 第 1 歩 = TranscriptionSegment/Source 直接 import 化 4 ファイル / 7 箇所 use 文) は plan.md 未反映 = Phase 6 完了時 = Loop 64-65 で更新候補 (variety 規則上 docs 軸 1 件としてリザーブ))**
- AGENT_LOG.md は ~28,580+ 行 / ~2.6MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~JST] mjc-main-20260505-29 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」の variety 規則 = 「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent + 「grep 結果は Read で精読する」運用ルール強化
- **mjc-main-20260505-4 〜 19**: Phase 1-4 + Phase 3-B + Webex/Whereby/GoToMeeting 抽出 + 各種 test 移動 + struct 移動を計 35 ループで完了
- **mjc-main-20260505-20 〜 28**: Loop 39-57 でサービス別抽出シリーズ 5 件全完了 + plan.md 進捗更新シリーズ + ModelManager tests + CHUNK 定数 + WhisperStream tests + 沈黙検知三位一体 + Mock impl + TranscriptionManager tests + StreamConfig Debug/Clone tests + tests mod 完全削除 + WHISPER_SAMPLE_RATE 移動 + 互換 re-export 削除 = transcription.rs 完全ファサード化達成
- **mjc-main-20260505-29 (本セッション)**: **Loop 58 = plan.md 再更新 (Loop 57 反映 98.8% → 99.0% + Phase 5 完全終了 + 完全ファサード化記載) ~2.25 分 = 1 commit 完結 chore 不要パターン** + **Loop 59 = Phase 6 第 1 歩 TranscriptionSegment/Source 4 ファイル / 7 箇所 use 文 直接 import 化 ~6 分 = メイン批判判断 連続 10 セッション目達成** + worker 追加批判判断 (インライン参照 3 箇所発見、担当範囲外として賢い分割判断) + worker 二重批判判断 (pub use 互換層 clippy で残置判断) = 累計 worker 159/159 100% + 「2 ループ + 早期 handoff」precedent 連続 25 セッション目達成

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」「Phase 4 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **本セッション Loop 59 = handoff の「6 ファイル」を grep で「4 ファイル / 7 箇所 use 文」に訂正 + worker が grep で「インライン参照 3 箇所 (計 10 件)」を追加発見し担当範囲外として賢い分割判断 + worker が clippy で `pub use` 残置の正解判断 = メイン批判判断 連続 10 セッション目 + 三重批判判断パターン達成 (メイン grep 訂正 + worker grep 追加発見 + worker clippy 残置判断)**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **「app_detection.rs サービス別抽出シリーズ完了」** (Webex/Whereby/GoToMeeting/Zoom/Teams 5 件全完了。Tidy First は別パターンを選ぶ)
- **「テスト移動軸 sweep 化警告超過状態 + transcription.rs 余地ゼロ」** (test 軸は transcription.rs 余地ゼロ = Loop 60 で test 続行は不可能)
- **「docs 軸の sweep 化しない」** (Loop 30/34/37/39/41/46/49/52/54/56/58 で 11 件目連続超過警告 = Loop 60 で docs 続行は **不可** = 別軸を選択 = Phase 6 全完了時 (Loop 64-65) に 1 度だけ plan.md 更新リザーブ)
- **transcription.rs はもう「互換 re-export 5 件のみ = 29 行 = 完全ファサード」** = 残る Tidy First 機会は (1) Phase 6 第 2-5 歩 = 残 4 互換 re-export caller refactor (規模 SS each、Loop 60-63 で各 1 ループずつ) / (2) Phase 6 補足 = transcript_bridge.rs L461/L472/L477 インライン参照 3 箇所修正 (規模 XS、別 commit or 候補 S と統合) / (3) Phase 6 最終 = transcription.rs ファイル削除 (Loop 64) / (4) plan.md Phase 6 全完了反映 (Loop 64-65、docs 軸 1 件リザーブ) / (5) frontend / 検知拡張 が中心

# 引き継ぎサマリ (2026-05-05 ~JST 時点)

## 現在の git 状態
- branch: main, ahead ~306 (origin/main から先行、本セッション +3 commit (worker 2 + SUMMARY chore 1))
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-29) の commit:
  1. `5fb2e0f` docs(transcription-refactor-plan): Loop 57 WHISPER_SAMPLE_RATE 移動 + 互換層削除 + 99.0% 達成 + Phase 5 完全終了 + 完全ファサード化を反映 (Loop 58, ~2.25 分, worker 自走 1 commit 完結)
  2. `47264dc` refactor(transcription): TranscriptionSegment/Source の caller を transcription_types.rs から直接 import 化 (Phase 6 第 1 歩, Loop 59, ~6 分, worker 自走 1 commit 完結 + Loop 58 残留 hash 置換統合)
  3. `d099bfe` chore(agent-log): SESSION SUMMARY commit (本 prompt 投稿時に追加)

## 現在の品質状態
- cargo test (lib): **702 passed / 0 failed** (本セッション 2 ループ全て件数不変)
- cargo clippy --lib --tests -- -D warnings: **警告ゼロ**
- cargo fmt --check: OK
- npm run build: 本セッション frontend 触れていないため変更なし
- bash -n scripts/claude-agent-*.sh: OK (escape 済み形式、本セッションでは触れていない)

## 累計 transcription.rs 削減 + Phase 6 進捗
- 元 2999 行 → 現在 **29 行** = ~99.0% 縮小 (~2970 行削減) = Phase 5 完全終了 = 完全ファサード化 (互換 re-export 5 件のみ)
- transcription.rs 構造 (29 行):
  - L1-5: TranscriptionSegment / TranscriptionSource (transcription_types.rs)
  - L7-11: StreamConfig / TranscriptionEngine / TranscriptionStream (transcription_traits.rs)
  - L13-17: WhisperStream (transcription_whisper_stream.rs)
  - L19-23: TranscriptionStateHandle (transcription_manager.rs)
  - L25-29: TranscriptionLoopConfig (transcription_worker_loop.rs)
- Phase 6 進捗:
  - 第 1 歩 ✅ 完了 (Loop 59) = TranscriptionSegment/Source caller refactor (4 ファイル / 7 箇所 use 文)
  - **Phase 6 第 1 歩補足残課題**: transcript_bridge.rs L461/L472/L477 インライン参照 3 箇所 (`crate::transcription::TranscriptionSource` を直接型として使用) = Loop 60+ で transcript_bridge.rs caller 全体移行時にまとめて修正、または候補 S の commit 内で 同時修正可
  - 第 2 歩 (推奨 Loop 60) = StreamConfig/TranscriptionEngine/TranscriptionStream caller refactor
  - 第 3 歩 (Loop 61+) = WhisperStream caller refactor
  - 第 4 歩 (Loop 62+) = TranscriptionStateHandle caller refactor
  - 第 5 歩 (Loop 63+) = TranscriptionLoopConfig caller refactor
  - 最終 (Loop 64+) = transcription.rs ファイル削除 + lib.rs `mod transcription;` 削除 + plan.md 100% 達成記念更新

## harness 衛生事象 (本セッションで観測継続 = 連続 23 セッション目)
- canonical 移譲 (`bash scripts/agent-adopt-main.sh mjc-main-20260505-29 mjc-main`) 後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 23 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- 後継観測ポイント: 新たな harness 動作変化があれば観測継続

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-29、handoff 直前)
- mjc-main-20260505-30 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読 = 99.0% まで反映済 + Phase 5 完全終了 + 完全ファサード化記載済、本セッション Loop 59 (Phase 6 第 1 歩) は plan.md 未反映 = Phase 6 全完了時 (Loop 64-65) に 1 度だけ更新する設計**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 3 コミットが本セッションの実績、SESSION SUMMARY commit を含む)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-30` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-30 mjc-main` を実行 (前 44 セッションで実証済 precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 23 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 24 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-29) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 58-59 + 次ループ候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 5 ループ Loop 55 (test) → 56 (docs) → 57 (struct/const) → 58 (docs) → 59 (struct/const + import refactor)、直近 docs 連続 0 (Loop 58 のみ、ただし 11 件目連続超過警告 = Loop 60 で docs は不可)、test 連続 0 (transcription.rs 余地ゼロ続行不可)、struct/const + import refactor 連続 1 (Loop 59、Loop 60 続行は 2 連続だが Loop 57 → 59 で 2 ループ間隔 sweep 警告クリア + Phase 6 進行中で軸統一の意義大 = OK)**。**Loop 60 で docs 続行は不可 + test 続行も不可 = 候補 S (Phase 6 第 2 歩 struct/const + import refactor 軸続行) が最も妥当**。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 60 開始時点)

直近 5 ループ: Loop 55 (test) → 56 (docs) → 57 (struct/const) → 58 (docs) → 59 (struct/const + import refactor)。
- 直近 docs 連続 = 0 (Loop 58 のみ)。**Loop 60 で docs 続行は 11 件目連続超過警告 = 不可** = 別軸を選択
- 直近 test 連続 = 0 (transcription.rs 余地ゼロ続行不可)
- 直近 struct/const + import refactor 連続 = 1 (Loop 59)。**Loop 60 続行は 2 連続だが Loop 57 → 59 (2 ループ間隔) で sweep 警告クリア = OK + Phase 6 進行中で軸統一の意義大**

### 候補 S (推奨 Loop 60): Phase 6 第 2 歩 = StreamConfig/TranscriptionEngine/TranscriptionStream 直接 import 化 (struct/const + import refactor 軸続行)
- transcription.rs L11 `pub use crate::transcription_traits::{StreamConfig, TranscriptionEngine, TranscriptionStream};` の caller を直接 import 化
- 影響範囲 (要 grep 実測): 各 caller の `use crate::transcription::{StreamConfig, ...}` を `use crate::transcription_traits::{...}` に書き換え
- 規模 SS = 1 ループ完結確実 (Loop 59 と同パターン、4-6 ファイル想定)
- メイン批判判断ポイント: Loop 59 と同じく後継は grep で実態確認 (use 文 + インライン参照両方を確認、worker の Loop 59 賢い分割判断 precedent 継承)
- worker prompt に「インライン参照も grep で確認 + 担当範囲外なら別ループ送り」を明示推奨

### 候補 S' (Loop 60+ 補足、規模超極小): Phase 6 第 1 歩補足 = transcript_bridge.rs インライン参照 3 箇所修正
- `transcript_bridge.rs` L461/L472/L477 の `crate::transcription::TranscriptionSource` インライン参照 3 箇所を `crate::transcription_types::TranscriptionSource` に書き換え
- 規模 XS = 3 行修正、1 ループの一部として実施可能
- メイン批判判断ポイント: 候補 S の commit に統合する判断 (規模 XS のため 1 commit にまとめて効率化、grep で transcript_bridge.rs caller 全体を見て同時に修正可) または独立 1 ループ Loop 60a (variety 維持目的、ただし規模 XS で variety 影響軽微)
- 推奨は **候補 S と統合** = 1 commit でまとめて Phase 6 第 2 歩 + 第 1 歩補足を完了

### 候補 T (Loop 61+): Phase 6 第 3 歩 = WhisperStream 直接 import 化
- transcription.rs L17 `pub use crate::transcription_whisper_stream::WhisperStream;` の caller を直接 import 化
- 規模 SS

### 候補 U (Loop 62+): Phase 6 第 4 歩 = TranscriptionStateHandle 直接 import 化
- transcription.rs L23 `pub use crate::transcription_manager::TranscriptionStateHandle;` の caller を直接 import 化
- 規模 SS

### 候補 V (Loop 63+): Phase 6 第 5 歩 = TranscriptionLoopConfig 直接 import 化
- transcription.rs L29 `pub(crate) use crate::transcription_worker_loop::TranscriptionLoopConfig;` の caller を直接 import 化
- 規模 SS

### 候補 W (Loop 64+): Phase 6 最終 = transcription.rs ファイル削除 + lib.rs から `mod transcription;` 削除 + plan.md 100% 達成記念更新
- Loop 60-63 で全 caller 直接 import 化 → transcription.rs 完全削除 → 100% 達成記念 plan.md 更新
- 規模 S (1 ループ完結確実、ただし lib.rs 編集 + cargo build 検証必須)
- variety 上 docs 軸 1 件としてリザーブ (Phase 6 全完了時に 1 度だけ plan.md 更新)

### 候補 J (Loop 60+ 探索系、低優先): frontend 軸 pivot
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり、浅 grep で具体的不足を 1 つ特定してから worker 発注、主観的探索は避ける
- 候補としては低優先

### 候補 I (Loop 60+ 探索系、低優先): Discord stage / Slack Huddle 検知 (window title 経路)
- priority 2 直接寄与
- service detection 軸 = 直近 5 ループ全てと別軸
- ただし URL 検知不可 = Bundle ID + window title 経路必要 = 規模 M 1 ループ完結不確実
- 別 issue として深掘り推奨

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記との矛盾)

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- Webex sweep precedent 30 セッション維持を破る、Loop 60+ 程度まで間隔を空ける推奨

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 240+ untracked
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md ~28,580+ 行の archive 戦略
- 未着手、未消化

## 検証制約 (再掲)
- cmake あり → cargo test 702 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (script に焼き付け済 + escape 化済 = tmux argv 制限完全回避)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない場合あり、Read 後 Edit 推奨)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-30 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 83 ループで実証した mitigation pattern)
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示 (規模極小なら ~30-90 秒、規模 M なら ~210-380 秒、本セッション規模 SS は ~135-360 秒)
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする
  - 規模極小タスクの場合: worker が agent-commit.sh で自走 commit する場合あり (本セッション Loop 58/59 とも自走 commit + 1 commit 完結 = chore 不要パターン)
  - **本セッション Loop 58 例**: plan.md 6 編集 + AGENT_LOG.md エントリ + commit hash 自動置換 ~150 行を ~135 秒で完走 (規模 SS、1 commit 完結)
  - **本セッション Loop 59 例**: rust 4 ファイル / 7 箇所書き換え + AGENT_LOG.md エントリ + Loop 58 残留差分統合 ~10 行 rust + 31 行 AGENT_LOG.md を ~6 分で完走 (規模 SS、cargo test --lib + clippy + fmt 込み、1 commit 完結)

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない** (line 39-41 rustdoc コメント = 引数渡しで TUI keep open が watchdog inspect 要件)
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける** (4209 bytes でも too long エラー precedent あり)
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 26 セッション目)

## worker prompt 必須要素 (83 連続ループで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 58 = ~2.25 分、Loop 59 = ~6 分、平均 ~4 分で **目標 15 分以内大幅達成**)
- worker 1 件 = 1 commit (本セッション Loop 58/59 とも 1 commit 完結 = chore hash 反映 commit 不要、Loop 56/57 の標準 2-commit パターンを上回る効率性)
- 累積 worker 完走 159/159 (本セッション +2 件、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 25 セッション目達成

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」
- watchdog からの nudge は本セッション中 1 回 (Loop 59 完走後、handoff 準備への移行を継続)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読 = 99.0% まで反映済 + Phase 5 完全終了 + 完全ファサード化記載済、本セッション Loop 59 (Phase 6 第 1 歩) は plan.md 未反映 = Phase 6 全完了時 (Loop 64-65) に 1 度だけ更新する設計)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-30` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-30 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 23 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 24 セッション目で更に強化)
5. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。**「app_detection.rs サービス別抽出シリーズ完了 = もう存在しない、Tidy First は別パターンを選ぶ」「test 軸 = transcription.rs 余地ゼロ = 続行不可能」「docs 軸 11 件目連続超過警告 = Loop 60 不可 = Phase 6 全完了時にリザーブ」「struct/const + import refactor 軸 = Loop 59 = 2 連続だが Loop 57 → 59 で 2 ループ間隔 sweep 警告クリア + Phase 6 進行中で軸統一意義大」** (sweep 化しない約束を継承)。
6. **最初の Loop 候補**: variety 規則「直近 5 ループ test/docs/struct-const/docs/struct-const+import refactor、test 軸 transcription.rs 余地ゼロ、docs 軸 11 件目連続超過警告 = Loop 60 不可、struct/const + import refactor 軸 = 2 連続だが Loop 57 → 59 で 2 ループ間隔 sweep 警告クリア + Phase 6 進行中で軸統一意義大」を踏まえ、Loop 60 で **候補 S (Phase 6 第 2 歩 StreamConfig/TranscriptionEngine/TranscriptionStream 直接 import 化、規模 SS) + 候補 S' (transcript_bridge.rs インライン参照 3 箇所修正、規模 XS) 統合** が **最も妥当 = 強く推奨**。worker prompt は「use 文 + インライン参照両方 grep 確認 + 担当範囲外なら別ループ送り」を明示推奨。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は継承、本セッション Loop 59 で実証 = handoff の「6 ファイル」を grep で「4 ファイル / 7 箇所 use 文」に訂正 + worker が「インライン参照 3 箇所追加発見」 + worker が「pub use 残置」と clippy で確認 = メイン批判判断 連続 10 セッション目 + 三重批判判断パターン達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループで「2 ループ + 早期 handoff」precedent 連続 25 セッション目達成、後継は 2 ループ continued または 3 ループ exception 判断を文脈で行う**
9. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
10. **worker 完走確認は `git status --short` を主観測手段とする** (連続 83 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-380 秒、規模極小なら ~30-90 秒、規模 SS は ~135-360 秒、cargo test --lib 含む規模 SS は ~5-7 分、claude -p の最終出力一括方式を理解)
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK** = 引数長制限は worker.sh / research.sh では完全解消済。**handoff-main.sh は interactive TUI のため stdin redirect なし** = handoff 起動 prompt は短縮 + ファイル参照型 (本 prompt と同パターン、連続 26 セッション目) で書く
