あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-77) です。
旧 mjc-main (= mjc-main-20260505-76) から **2 ループ完走 (Loop 153 + Loop 154)** のハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1-6 完全終了 = 100% 達成)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/detection-extension-plan.md (Phase 1 完了 + Q5 + Q6 Resolved + Phase 2 = 実機要件 Q1/Q2 未解決のため着手不可)**
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/agent-log-archive-plan.md (Section 2.3 観測追記 30 件目 mjc-main-20260505-76 Loop 154 で更新観測 = 節目突破 + 第 4 階層離脱 + SUMMARY 軽量化期間入り、Phase 1-4 段階導入方針)**
- AGENT_LOG.md は ~33,891+ 行 / ~4MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-76 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2 〜 75**: Phase 1-6 + 各種抽出 + 大型 file 責務分離 12-50 file 目 (Loop 151 累計) + 検知拡張 plan + Phase 1 + Q5/Q6 Resolved + AGENT_LOG.md archive 戦略 plan + scope 36 軸目開拓 + 既存 file 拡張軸 = 新 paradigm 1-3 件目 + R6 段階的分割 1-10 件目 + R6 完結マイルストーン + utils 内 cross-import paradigm 1-7 件目
- **mjc-main-20260505-76 (本セッション)**: **2 ループ完走 = Loop 153 (frontend 軸 = 重複削除 + 既存 file 拡張軸 paradigm 4 件目達成 = utils 間集約軸 = re-export 戦略 = transcriptDisplayHelpers.ts の getSpeakerLabel 重複定義を liveCaptionTrackHelpers.ts から re-export + DRY 違反解消 + 戻り値型 string 統一 + TranscriptDisplay.tsx + LiveCaptionWindow.tsx 共に無変更で API 互換維持 + 1 file -7/+3 純減 4 行) + Loop 154 (K 軸 = Section 2.3 観測追記 30 件目 = 節目突破 = +41 行 / 平均 ~20.5 行/loop = Loop 151 観測値からの -44% 大幅減少 = 第 4 階層 (中量化) からの離脱 + SUMMARY 軽量化期間入り)** = メイン批判判断 連続 75 セッション目達成 + worker 自律 2-commit pattern 連続 10 ループ目達成 = 節目 10 件突破 (Loop 145-154) + harness 衛生連続 71 セッション目 + ファイル参照型 handoff prompt 連続 72 セッション目 + scope 多様性 36 軸維持 + alternation pattern 43 連続成功 (K(154) → ?(155 推奨 = R8'' = rust))

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補 / 状況記述を鵜呑みにせず批判的再評価** (mjc-main-20260505-74 Loop 150 R7''' INVALID_STATUS_PAYLOAD_ERROR 候補は固有メッセージで真の重複ではないと grep + Read 精読で批判判断 + 本セッション Loop 153 で getSpeakerLabel が src/ 配下で 2 箇所同実装で定義されていることを独自 grep + Read 精読で発見 → paradigm 4 件目達成)
- **本セッション Loop 154 = 観測 30 件目節目突破達成 = SUMMARY 軽量化期間入り = AGENT_LOG.md 増分階層 4 段階 (SS 0 件 ~11-12 / SS 1 件軽量化 ~20-26 / SS 1 件中量化 ~30-37 / SS 1 件大量化 ~42) のうち軽量化階層下端寄り変動 (~20.5 行/loop) を新観測 = paradigm 完全成熟期維持 + 軽量化階層の範囲下端拡大** = 後継も同階層化 paradigm を継続観測可能 (今後 ~20-22 行/loop が再観測されると軽量化階層の範囲が更に拡大)
- **責務階層精査優先 (Loop 119/121/123/125/127/129/131/133/135/137/139/141/143/145/147/149/151/153 教訓継承)**: paradigm 反復よりも責務階層精査を優先 = 統合候補が同責務階層なら統合 OK、異階層 / 異 entity / 異 tier なら新 file 作成軸採用 + **既存 file 拡張軸 paradigm 1-4 件目 (Loop 145/147/149/153)** + **新 file 作成軸 (Loop 151 = speaker normalization tier)** も選択肢 = **責務階層 = display tier / status object tier / predicate tier / model display tier / display tier (AI 送信状態) / display tier (audio source 状態) / display tier (engine status) / display tier (external API key) / display + predicate tier (transcription source) / localStorage 永続化 tier / display tier (Meeting Start failure reason) / display tier (audio level) / track display tier / event coordination tier / speaker normalization tier / speaker display tier (Loop 153 集約完成) の 16 軸が確認済**
- **utils 内 cross-import paradigm 7 件目 (Loop 147 まで)**: 後継も別 utils 系列への抽出時に **paradigm 8 件目への拡張可能性** = 必要時のみ判断
- **worker 自律 2-commit pattern 復活 (Loop 145 → 146 → 147 → 148 → 149 → 150 → 151 → 152 → 153 → 154 = 10 連続 = 節目 10 件突破)**: Loop 144 fault リセットからの完全復活継続中 = 後継も同 pattern 期待可能
- **worker fault 防止策 (Loop 144 precedent から worker prompt 必須要素 8 つ目に追加 = Loop 146/147/148/149/150/151/152/153/154 で 9 連続実証成功)**: worker prompt の Step 4 (AGENT_LOG.md append) で「append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む。」を **明示記載** = 二重書き防止
- **メイン直接削除救済 paradigm 1 件目 (Loop 144)**: worker fault 時 (二重書き等) はメインで直接 AGENT_LOG.md を Edit で修復 + chore commit を作成可能 = AGENT_LOG.md SESSION SUMMARY と chore hash 修正の precedent 拡張
- **worker 自律学習進化 paradigm 3 件目 (Loop 143/145/149 で確立)**: worker prompt 未指示事項を自律予測 + 対応 = TranscriptionEngineType 不要 import 削除 / MicrophoneSection callsite 自律置換 / multi-line existing import への統合判断 = 行数 ±0 最適化 = 後継も worker prompt の grep 確認手順を明示すれば自律学習進化 paradigm 期待可能
- **grep 結果は必ず Read で精読してから「発見」を主張する** (mjc-main-20260505-74 Loop 150 で実証 + Loop 151 でも実証 + 本セッション Loop 153 で再実証 = handoff prompt 主張の R7''' 誤同定を継承し独自 grep + Read 精読で getSpeakerLabel 真の重複発見)
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける**
- **大型 file 責務分離 = 50 file 目達成 (Loop 151 累計 = rust 32 件 + frontend 18 件 = 50 件)** **本セッション Loop 153 は重複削除 + 既存 file 拡張軸で新 file 作成なし** = 累計 50 件維持
- **scope 36 軸目維持 (Loop 151 = speaker_normalize 新 file)** **本セッション Loop 153 は既存 speaker display tier 内集約 = scope 軸開拓ならず**
- **frontend file 規模ランキング (本セッション後 = 不変):**
  - SettingsView.tsx (`src/routes/`) 2010 行 (top-level 純粋関数 1 件 = 内部 component helper 抽出困難確認済 = 却下推奨)
  - **TranscriptView.tsx (`src/routes/`) 2055 行** = R6 段階的分割完了 = 残純粋関数 0 件 + MEETING_START_REQUEST_EVENT 重複削除済
  - SessionList.tsx (`src/routes/`) 553 行 (top-level 純粋関数 0 件 + 内部 useCallback は Tauri invoke 系 + useMemo filteredSessions は既存 utility wrapper = 抽出候補ならず)
  - **LiveCaptionWindow.tsx (`src/components/`) 505 行** = TRACKS + TrackMeta + AudioSource 統合済
  - **MeetingDetectedBanner.tsx (`src/components/`) 474 行** = MEETING_START_REQUEST_EVENT 重複削除済 + INVALID_STATUS_PAYLOAD_ERROR は固有メッセージで重複ではない (Loop 150 確認済)
  - TranscriptDisplay.tsx (`src/components/`) 489 行 (Loop 110 抽出済 + top-level 純粋関数 0 件 + 内部 useCallback は DOM/clipboard 副作用系 + useMemo は既存 utility wrapper = 抽出候補ならず + Loop 153 で getSpeakerLabel 重複削除に関与 = 無変更維持)
  - ModelSelector.tsx (`src/components/`) 485 行 (Loop 112 抽出済 + top-level `ModelOption` / `DownloadStatus` は component = 抽出候補ならず)
  - AudioLevelMeter.tsx (`src/components/`) 34 行 (Loop 145 後) = 残純粋関数 0 件
- **frontend 軸 + rust top-level 純粋関数候補は枯渇域 (Loop 153 確認済)**: top-level / 内部 helper 双方で枯渇 = 後続候補は **(a) R8'' = inline module 抽出軸 rust 版 (Loop 121/123 precedent と同形 = 最有力推奨 = scope 37 軸目開拓候補) / (b) R7'''' = 重複候補 grep 探索 paradigm 5 件目 (今回 src/ のみカバー = src-tauri 含む rust 重複も探索可能 = 副推奨) / (c) K' = K 軸再度 = alternation reset リスクで Loop 156 推奨 / (d) X' / H1 = ユーザー直伝指示要**

# 引き継ぎサマリ (2026-05-06 ~JST 時点)

## 現在の git 状態
- branch: main, ahead 542 (origin/main から先行、本セッション +5 commit = refactor + chore + docs + chore + SUMMARY)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-76) の commit:
  1. `316fbda` refactor(frontend): getSpeakerLabel 重複定義を transcriptDisplayHelpers から削除 + liveCaptionTrackHelpers から re-export (Loop 153)
  2. `c180a1c` chore(agent-log): mjc-main-20260505-76 Loop 153 entry
  3. `27c47d6` docs(architecture): Section 2.3 観測 30 件目 (Loop 154)
  4. `1683837` chore(agent-log): mjc-main-20260505-76 Loop 154 entry
  5. `652c416` chore(agent-log): mjc-main-20260505-76 SESSION SUMMARY

## 現在の品質状態
- cargo test (lib): **704 passed / 0 失敗** (本セッションは frontend + docs 軸 = rust 無変更で再検証不要)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ (前セッション継承)
- cargo fmt --check: OK (前セッション継承)
- cargo build --lib: エラーなし (前セッション継承)
- npm run build: ✓ built (Loop 153 で再検証済 = 883ms)
- bash -n scripts/claude-agent-*.sh: OK
- agent-verify.sh: 全項目 OK (Loop 153 + 154)

## 大型 file 責務分離実績 (累計 50 件、本セッション +0 = Loop 153 は既存 file 内重複削除)

### Rust 軸 (32 件累積、本セッション +0)
- 過去 32 件 (Loop 67-151 累積)

### Frontend 軸 (18 件累積、本セッション +0)
- Loop 107-149 累積、本セッション内 frontend 軸変更は重複削除のみで新 file なし

## 既存 file 拡張軸 paradigm (4 件累積、本セッション +1)

- Loop 145 = 同 file 内 sanitize 重複削除 (内部 + import 両方統合) = paradigm 1 件目
- Loop 147 = 別 file から既存 file に統合 (TRACKS + AudioSource) = paradigm 2 件目
- Loop 149 = 3 file 重複定義を utils file に集約 (MEETING_START_REQUEST_EVENT) = paradigm 3 件目
- **Loop 153 = 2 utils 間 重複削除 + re-export 戦略** (getSpeakerLabel) = **paradigm 4 件目** (本セッション新規確立 = utils 間集約軸)

## 残候補 (Loop 155+ 候補)

- **K = agent-log-archive-plan.md Section 2.3 観測追記 31 件目** = Loop 156 推奨 = alternation 44 連続維持 (K(154) → ?(155) → K(156))
  - precedent: Loop 79/81/86/95/98/102/106/109/111/113/115/118/120/122/124/126/128/130/132/134/136/138/140/142/144/146/148/150/152/154 = 30 件
  - 観測時点: Loop 155 完了時点
- **R8'' = inline module 抽出軸 rust 版 (Loop 121/123 precedent と同形) = 最有力推奨**:
  - 候補 file: audio.rs / session_manager.rs / openai_realtime.rs / cloud_whisper.rs / elevenlabs_realtime.rs / session.rs / session_store.rs
  - 探索手順: `grep -nE "^(pub )?mod " <file>` で inline module 探索 + Read で副作用なし確認
  - 規模 SS-S = ~5-15 分完走見込み (rust 軸は cargo test + clippy + fmt + build で時間かかる)
  - alternation pattern: K(154) → rust(155) = pivot 成功 (alternation 44 連続維持)
  - paradigm = rust 軸 33 件目 + scope 37 軸目開拓候補
- **R7'''' = 重複候補 grep 探索 paradigm 5 件目を狙う**:
  - 今回 src/ のみカバー = src-tauri 含む rust 重複も探索可能
  - 規模 SS = ~5-7 分完走見込み (探索 + 精読 + 判定 + worker 起動)
  - alternation pattern: K(154) → frontend(155) = pivot 成功 (alternation 44 連続維持)
  - paradigm = 既存 file 拡張軸 paradigm 5 件目候補
- **R7''' = 内部 helper grep = TranscriptDisplay / SessionList = 枯渇確認済 (Loop 153 = 前セッション内部 grep 結果)**:
  - SettingsView (2010 行) の内部 helper は **未探索** = 残候補だが component helper の可能性高 (handoff prompt の Loop 150 で却下推奨と確認済)
- 検知拡張 Phase 2 = Q1/Q2 実機要件未解決のため着手不可
- X' resample_audio dead code 削除 = ユーザー直伝指示要
- H1 AGENT_LOG.md archive plan Phase 1 着手 = ユーザー直伝指示要

## harness 衛生事象 (本セッションで観測継続 = 連続 71 セッション目)
- canonical 移譲後、`git status --short` で **scripts/* に M 表示が再出現せず** = 連続 71 セッション目観測結論 = `bfb9846` PATH inner shell escape が永続的解決
- **2-commit pattern**: 本セッション Loop 153 + Loop 154 で worker 自律実行 = **連続 10 ループ目達成 = 節目 10 件突破** (Loop 144 fault → Loop 145-154 = 10 連続安定)
- **API 互換維持戦略 (Loop 153 補強)**: frontend 軸 = re-export 戦略 = 1 file 内変更で TranscriptDisplay.tsx + LiveCaptionWindow.tsx 共に無変更維持 = 影響範囲最小化 paradigm 確立
- **本セッション Loop 153 完走時間 ~3 分** (規模 SS = precedent ~5-7 分よりやや短め = 1 file 変更のため)
- **本セッション Loop 154 完走時間 ~3 分** (規模 SS = precedent ~3-5 分通り)

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-76、handoff 直前)
- mjc-main-20260505-77 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ)。
2. `git status --short --branch` と `git log --oneline -10` で現状確認 (上 5 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-77` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-77 mjc-main` を実行 (前 72 セッション precedent あり、冪等)。
5. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 71 セッション目「再出現せず」観測 = 後継も同観測なら結論「永続的解決」が連続 72 セッション目で更に強化)
6. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-76) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Loop 153 + Loop 154 + 2 ループ完走 候補を確認**
7. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = 直近 30 ループで構造分離 44 件 + 機能拡張/docs 3 件 + harness 衛生 30 件 (Loop 154 含む) = 44:3:30 多軸分散 + scope 多様性 36 軸 + frontend 軸 18 件 + rust 軸 32 件 + alternation pattern 43 連続成功 = Loop 155 は frontend or rust 軸 = alternation 44 連続維持の最適路**.

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 155 開始時点)

直近 30 ループ: Loop 125-154 (本セッション後 = 30 ループ)
- 軸 pattern: 構造分離 (44 件) + 機能拡張/docs (3 件) + harness 衛生 (30 件 = Loop 124/126/128/130/132/134/136/138/140/142/144/146/148/150/152/154 含む) = **44:3:30 多軸分散**
- scope 内訳: 36 scope 分散 (rust 22 + frontend 14)
- paradigm pivot 連続: alternation pattern 43 連続成功 (rust(151) → K(152) → frontend(153) → K(154))
- **Loop 155 では更なる paradigm pivot 推奨 (K 軸 (Loop 154) からの pivot = frontend or rust 軸 = alternation 44 連続維持)**

### 候補 R8'' (Loop 155 候補 A = 最有力推奨 = inline module 抽出軸 rust 版)

**state 中心 rust file の inline module 抽出 (Loop 121/123 precedent と同形)**:
- 候補 file: audio.rs / session_manager.rs / openai_realtime.rs / cloud_whisper.rs / elevenlabs_realtime.rs / session.rs / session_store.rs
- 探索手順: `cd src-tauri && grep -nE "^(pub )?mod " src/<file>` で inline module 探索 + Read で副作用なし確認
- 規模 SS-S = ~5-15 分完走見込み (rust 軸は cargo test + clippy + fmt + build で時間かかる)
- alternation pattern: K(154) → rust(155) = pivot 成功 (alternation 44 連続維持)
- paradigm = rust 軸 33 件目 + scope 37 軸目開拓候補

### 候補 R7'''' (Loop 155 候補 B = 重複候補 grep 探索 paradigm 5 件目)

**handoff 主張に頼らず独自精読で新たな重複候補を探索 (今回 src-tauri 含めて拡張)**:
- 既知候補は誤同定 / 集約済 (Loop 150/151/153 確認済): INVALID_STATUS_PAYLOAD_ERROR (固有メッセージ) / CONTROL_CHARACTER_PATTERN (異 entity) / getSpeakerLabel (Loop 153 で集約済)
- 探索方法: rust util 関数名 / 型名 / 定数名で `cd src-tauri && grep -rn "^pub fn" src/` で複数箇所定義 + 各定義の値を Read で精読 (変数名同じでも値が違うパターンに注意)
- 規模 SS = ~5-7 分完走見込み (探索 + 精読 + 判定 + worker 起動)
- alternation pattern: K(154) → frontend or rust(155) = pivot 成功 (alternation 44 連続維持)
- paradigm = 既存 file 拡張軸 paradigm 5 件目候補

### 候補 K' (Loop 155 候補 C = K 軸再度 = alternation reset リスク)

**agent-log-archive-plan.md Section 2.3 観測追記 31 件目**:
- precedent: Loop 79/81/86/95/98/102/106/109/111/113/115/118/120/122/124/126/128/130/132/134/136/138/140/142/144/146/148/150/152/154 = 30 件
- 観測時点: Loop 154 完了時点
- 規模 SS = ~3-5 分完走見込み
- alternation pattern: K(154) → K(155) = **連続 K = pivot 失敗** (alternation reset = 43 → 1)
- 副推奨 = Loop 156 候補 (frontend or rust(155) → K(156) で alternation 44 連続維持の最適路)

### 低優先 (継承)

- J' SettingsView.tsx 内部 helper (component helper 可能性高 = 抽出困難確認済 = 却下推奨)
- X' resample_audio dead code 削除 (ユーザー直伝指示要)
- H1 AGENT_LOG.md archive plan Phase 1 着手 (ユーザー直伝指示要)
- B1. Microsoft Teams window title fallback (継承 = mjc-main-20260505-3 で批判的に却下済)
- A1. Webex 日本語 window title (継承)

## 検証制約 (再掲)
- cmake あり → cargo test 704 件全 pass (verify.sh OK) = **cargo は `cd src-tauri` してから実行する** (desktop 直下に Cargo.toml なし)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止、`--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止、Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY と chore hash 修正と handoff prompt のみメイン直接編集の precedent あり + Loop 144 で worker fault 時の AGENT_LOG.md 直接修復 paradigm 拡張)

## ハーネス使用法 (script に焼き付け済)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku、stdin redirect 化 + PATH escape 化済)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet、stdin redirect 化 + PATH escape 化済)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend/docs を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-77 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認 or `git log --oneline -3` で 2-commit 反映確認**
  - 観測時間目安: 規模 SSS = ~2-3 分、規模 SS = ~3-5 分、規模 SS-S = ~5-10 分、規模 S = ~5-15 分
  - **本セッション Loop 153 例**: 規模 SS = ~3 分完走 (worker prompt ~131 行 = 1 file 変更で軽量)
  - **本セッション Loop 154 例**: 規模 SS = ~3 分完走 (worker prompt ~107 行 = K 軸の定型タスクで最軽量)
  - **worker 自律 2-commit pattern**: Loop 145-154 で **10 連続スタート = 節目 10 件突破** (Loop 144 fault リセット完全復活継続)

## handoff 起動の制約 (重要)
- `claude-agent-handoff-main.sh` は **interactive TUI 起動のため stdin redirect 化されていない**
- そのため handoff prompt は依然として **tmux 引数長制限の影響を受ける**
- 対策 = 「ファイル参照型 handoff prompt」**precedent を継承**: 起動 prompt は短縮 + フル handoff 内容は別ファイル (`docs/handoff/mjc-main-YYYYMMDD-N.txt`) に書き、起動 prompt 内で「Read で完全に読め」と指示する (本 prompt と同パターン、連続 72 セッション目)

## worker prompt 必須要素 (100+ 連続ループで実証済、継続適用、8 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**
8. **「AGENT_LOG.md への append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む」 (Loop 144 fault precedent から新規追加 = Loop 146/147/148/149/150/151/152/153/154 で 9 連続成功実証)**

**追加推奨 (Loop 60-154 経験から)**:
- worker prompt に「2-commit pattern」を **明示的に** 記載 = Loop 145-154 で 10 連続スタート安定動作
- 大型 file 抽出時は **precedent file の cat 全体読み込み** を明示推奨
- **worker prompt の長さに注意**: ~107-190 行で短縮効果あり (Loop 67-154 で実証 ~60-480 秒完走、本セッション Loop 154 = 107 行で最軽量)
- **frontend 軸の場合**: 検証は npm run build (tsc + vite build) のみ
- **rust 軸の場合**: cargo test (704 件件数完全不変) + cargo clippy + cargo fmt + cargo build を全て worker prompt に記載
- **API 互換維持戦略 (Loop 119-153 precedent)**:
  - **rust 軸**: `pub use` re-export (Loop 119) / `pub(crate) use` re-export = E0364 visibility 自律 fix (Loop 121) / inline module 全体抽出時の visibility 修正 + caller 1 件 fully qualified path 化で完結 (Loop 123) / `use` 経由で fn 抽出 + tests 移動 + 件数完全不変保証 (Loop 151 = scope 36 軸目開拓 = speaker normalization tier paradigm)
  - **frontend 軸**: 純粋関数 export 化 + import 1 行追加で完結 (Loop 125-143 標準 paradigm) / **新 file が他の utils file から定数/型/関数 import = utils 内 cross-import paradigm (Loop 129/133/135/141/143/147 で 2-7 件目確立)** / **既存 file 拡張軸 = 新 file 作成なし + 重複削除 + import 1 行追加 (Loop 145/147/149 = 1-3 件目)** + **utils 間集約軸 = 1 file 内重複削除 + import + re-export 追加で他 callsite 無変更 (Loop 153 = paradigm 4 件目達成 = 影響範囲最小化 paradigm)** + **multi-line existing import への統合判断 = 行数 ±0 最適化 (Loop 149 worker 自律学習進化 paradigm 3 件目)**
  - **worker 自律学習進化 paradigm 3 件目達成**: worker prompt 未指示事項を自律予測 + 対応 = TranscriptionEngineType 不要 import 削除 (Loop 143) / MicrophoneSection / SystemAudioSection callsite 自律置換 (Loop 145) / multi-line existing import への統合判断 (Loop 149)
- **責務階層精査 (Loop 119-153 教訓)**: paradigm 反復よりも責務階層精査を優先。**既存 file 拡張軸 paradigm (Loop 145/147/149/153 = 4 件目達成)**: 同 entity の重複候補発見時は新 file ではなく既存 file 拡張軸採用 = DRY 集約。**新 file 作成軸 (Loop 151 = speaker normalization tier)**: 異 entity / 独立責務階層は新 file 作成軸採用 = scope 開拓。**utils 間集約軸 (Loop 153 = re-export 戦略)**: 2 utils 間の同 entity 重複は re-export 経由で他 callsite 無変更で集約

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 153 = 規模 SS ~3 分 + Loop 154 = 規模 SS ~3 分 = **目標 15 分以内達成 + 平均 ~3 分/loop = 高速化記録達成**)
- worker 1 件 = 1-2 commit (本セッション 2 ループ完走 = 4 commit + 1 SESSION SUMMARY commit = 5 commit)
- 累積 worker 完走 252/253 (本セッション +2 件完走、99.6%)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは **2 ループ完走 + handoff** = handoff 文書「2-3 ループ完走推奨」下限達成 + context 衛生優先

## ユーザー直伝指示 (本セッション)
- 起動時 prompt: 「待機モード禁止、final answer で停止せず改善ループを継続」 (継承)
- watchdog 継続指示: 「docs/autonomous-main-prompt-claude.md に従って次の自律改善ループへ進んでください」 (継承)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む
2. `git status --short --branch` / `git log --oneline -10` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-77` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-77 mjc-main` を実行 (冪等)。
4. **canonical 移譲後 `git status --short` で scripts/* に M 表示が再出現するか観測** (連続 71 セッション目「再出現せず」観測)
5. **戦略転換の核心を継承**: 単一軸 4 連続集中をしない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。
6. **最初の Loop 候補**: variety 規則「K(154) → 次は frontend or rust 軸 = alternation 44 連続維持」を踏まえ、Loop 155 で **(a) 候補 R8'' = inline module 抽出軸 rust 版 = 規模 SS-S = 最有力推奨 (rust 軸 33 件目 + scope 37 軸目開拓候補) または (b) 候補 R7'''' = 重複候補 grep 探索 paradigm 5 件目 = 規模 SS = 副推奨 または (c) 候補 K' = K 軸再度 = alternation reset リスクで Loop 156 推奨 または (d) X' / H1 = ユーザー直伝指示要** のいずれか採用検討。**最有力推奨 = 候補 R8'' (rust 軸 33 件目 + scope 37 軸目開拓)**。worker prompt は「Loop 60-154 と同じ 2-commit pattern + Loop 144 fault precedent から append 1 回のみ明示」を含めて ~107-190 行 (規模 SS-S、Loop 153/154 precedent)。
7. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (本セッション Loop 153 で実証 = handoff prompt 主張の R7''' 誤同定を継承し独自 grep + Read 精読で getSpeakerLabel 真の重複発見 → メイン批判判断 連続 75 セッション目達成)
8. 待機モード禁止。final answer で停止せず、改善ループを継続する。**本セッションは 2 ループ + handoff = 後継は 2-3 ループ完走推奨**
9. **worker prompt には worker prompt 必須要素 8 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning + 二重書き fault を防止**、**worker prompt は ~107-190 行を目安に simpler 化**
10. **worker 完走確認は `git status --short` または `git log --oneline -3` を主観測手段とする**
11. **worker 起動は script に焼き付け済 stdin redirect + PATH escape 方式 (`scripts/claude-agent-start-worker.sh`) で OK**
12. **commit hash 更新漏れパターン (Loop 145-154 で 10 連続観測 = 完全復活 pattern + 節目突破)**: worker prompt に「2-commit pattern」を明示記載 + Loop 144 fault precedent から「append は 1 回のみ」明示記載 = 後継も活用検討可能
13. **worker fault 時の救済策 (Loop 144 precedent)**: AGENT_LOG.md の二重書き等は メイン直接 Edit で削除修復 + chore commit を作成 = AGENT_LOG.md SESSION SUMMARY と chore hash 修正の precedent 拡張

# Loop 155 への期待 (推奨ループ設計)

直近 30 ループで構造分離軸 44 件 (Loop 125-153 frontend + rust 含む) + 機能拡張/docs 軸 3 件 + harness 衛生軸 30 件 = K(154) → **Loop 155 では更なる paradigm 多様化推奨 (K 軸 (Loop 154) からの pivot = frontend or rust 軸 = alternation 44 連続維持)**。

メインは grep で 1 候補に絞る (本ハンドオフでは候補 R8'' = inline module 抽出軸 rust 版 = 規模 SS-S を最有力推奨、R7'''' = 重複候補 grep 探索 paradigm 5 件目 = 規模 SS = 副推奨、K' = alternation reset = Loop 156 推奨) → worker prompt 作成 (~107-190 行) → 起動 = 通常の自律改善ループへ復帰。

**最有力推奨は候補 R8'' = inline module 抽出軸 rust 版 (rust 軸 33 件目 + scope 37 軸目開拓候補)**:
- 規模 SS-S 想定 (~5-15 分完走) = worker prompt は ~140-160 行
- paradigm = rust 軸 33 件目 + scope 37 軸目開拓候補 (発見できれば) または却下判定 (発見できなければ R7'''' / K' へ pivot)
- precedent = mjc-main-20260505-XX Loop 121/123 (rust inline module 抽出) 等
- 探索方法: `cd src-tauri && grep -nE "^(pub )?mod " src/<file>` で inline module 探索 + Read で副作用なし確認
- alternation pattern: K(154) → rust(155) = 44 連続維持

**R8'' 採用前の事前確認手順 (Loop 155 で適用)**:
1. `cd src-tauri && grep -lnE "^(pub )?mod [a-z_]+ \{" src/` で inline module 含む rust file 列挙
2. 候補 file ごとに `grep -nE "^(pub )?mod [a-z_]+ \{" <file>` で inline module 名 + 行番号取得
3. **発見した候補は必ず Read で精読** (副作用なし確認 + visibility 構造 + 抽出時のリスク評価)
4. inline module の規模 (行数) + 依存数 + tests 数で抽出可否判定
5. 真の抽出可能候補と判定できれば worker prompt 作成、誤同定なら R7'''' / K' へ pivot

- 推奨抽出順序 (Loop 156 以降): K = Section 2.3 観測追記 31 件目 (規模 SS = ~3-5 分 = alternation 45 連続維持) または R7'''' = 重複候補探索 paradigm 5 件目 = 規模 SS = 副推奨
