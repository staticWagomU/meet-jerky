あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-13) です。
旧 mjc-main (= 旧 mjc-main-20260504-12) から 3 ループ完了の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 18200+ 行 / 1.6MB+ あるので `tail -350 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~13:00 JST] mjc-main-20260504-12 状況メモ (ループ 1-3 詳細) があります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~13:00 JST 時点)

## 現在の git 状態
- branch: main, ahead 80 (origin/main から先行)
- working tree: clean (untracked: `docs/worker-prompts/` 増分、`docs/handoff/mjc-main-20260504-7.txt`、`docs/handoff/mjc-main-20260504-10.txt`、`docs/handoff/mjc-main-20260504-11.txt`、`docs/handoff/mjc-main-20260504-12.txt`、本ハンドオフ用 `docs/handoff/mjc-main-20260504-13.txt`)
- 旧 mjc-main-20260504-12 が canonical 名 mjc-main を持っており、本セッションで 3 ループ + 1 SESSION SUMMARY コミットを完了:
  1. `5efdccd` test(session): Session/SessionSegment の clone deep copy + serde round-trip + id seq monotonic + started_at boundary 5 件 (415→420 passed)
  2. `f3c929a` test(session_store): path_for_session の test ゼロ gap 5 件補強 (420→425 passed)
  3. `babb1f3` test(session_store): unescape_inline_markdown_text の boundary 補強 5 件 (425→430 passed)
  4. `cfeb631` chore(agent-log): SESSION SUMMARY 追記

## 現在の品質状態
- cargo test (lib): **430 passed / 0 failed**
- cargo clippy default lint: **警告ゼロ**
- cargo clippy -D warnings: **警告ゼロ**
- frontend build: 本セッション中は test 追加のみで frontend 触らず (codex 活動本セッション中ゼロ、~33 時間以上静か)

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-12、handoff 直前)
- mjc-main-20260504-13 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションがあれば触らないでください。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-13 mjc-main` を実行して、あなたが canonical mjc-main を引き継ぐ。これにより旧 mjc-main は自動で kill される。**注意**: スクリプトが `successor session does not exist` を返した場合、既に rename 完了済みのサインなので OK。
5. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本セッションで定着・累積)
- **「pub fn なのに test ゼロ」を grep + 既存 test の網羅状況確認で発見するパターン** (Loop 2): session_store.rs 30 件の既存 test に path_for_session の direct test がないことを `grep -nE "fn [a-z_]+\("` で発見。session_store の事前 grep 必須要件は本パターンで自動化可能。
- **「PathBuf の構造を 4 軸で完全分解する path test パターン」** (Loop 2): parent / extension / file_stem / file_name の 4 軸で path 構造を独立 assert。拡張子変更・接尾辞混入・dir 混入の 3 種類の誤改修を独立に捕捉。path 系 fn 全般に汎用適用可能。
- **「explicit_escape ヘルパーによる対称関数の純粋部分対称性 test」** (Loop 3 T5): inline_markdown_text の split_whitespace+join 正規化を除く純粋 escape ロジックを mod tests 内クロージャで再現し、unescape 側との双方向対称性を分離検証。markdown 系の round-trip semantic 整合性を session_store 側でも文書化。同モジュール外の関数を直接 import できないとき、ヘルパー再現 + 純粋部分対称性 test という回避パターン。
- **「serde round-trip を全状態組合せ (segments 0/N × ended_at None/Some の 2x2) で対称的に固定」** (Loop 1 T4): persistent format の構造契約を 4 ケース完全網羅。serde の derive 動作を全状態で文書化。Optional field を持つ struct の serde 全般に汎用。
- **「絶対値 assert 禁止な静的 AtomicU64 を相対比較のみで test」** (Loop 1 T1): SESSION_COUNTER の挙動を `parse_seq` クロージャで取り出して strictly increasing で固定。test 順依存しない pure relative test pattern。

### 累積パターン (前セッションから継承、変更なし)
- classify match arm 4 分岐への boundary jets / input field 群への正規化適用の対称性 / split_whitespace と str::trim の Unicode White_Space 準拠の対称 / Other variant の (status, body) 純粋関数性 / discard のステルス削除しない契約 / private fn の direct test (4 軸) / eprintln 観測回避 / flat vs or_else chain 対比 / JSON null vs Rust None / 3 段 priority chain / private fn 直接呼び出しテスト / エラー文言 contract / 境界値 3 点対称 / 3 層テスト構造 / OS 互換 ErrorKind / state machine / format prefix 層内独立 / エスケープ網羅 / UI 文言 contract / 上位層責任境界 / NaN/+Inf 責務分離 / sanitize 前段 bit-pattern / first-violation contract / 3 軸テスト戦略 / markdown 双方向対称 / JST 二重軸 / 観測時刻注入 direct test / cross-fn invariant / 薄いデータコンテナ / multibyte 境界 / `&&` 最小成立 / or_else 1 階層 as_str()

### 重要な技術的注意点 (踏みやすい罠)
- **`session.rs` line 4 の SESSION_COUNTER は静的 AtomicU64**。**絶対値 assert 禁止**。相対比較のみ可。本セッション Loop 1 T1 で固定。
- **`session.rs` の `#[derive(Clone)]` は Vec の deep copy** (本セッション Loop 1 T2)。`Rc<Vec<_>>` 等への変更で T2 が失敗。
- **`session.rs` の `#[derive(Serialize, Deserialize)]`** は serde_json 経由で 4 状態組合せに対応 (本セッション Loop 1 T4)。persistent format の構造契約。
- **`session.rs` の id format は `format!("{started_at}-{seq}")`** で as キャストや try_into を使わない (本セッション Loop 1 T5)。session_store の parse_session_started_at_secs (前 mjc-main-8) と対をなす入力側 boundary。
- **`session_store.rs path_for_session` (line 25)** は title sanitize しない passthrough (本セッション Loop 2 T5)。title 内 special char (`/`, `\`, `:`, `*` 等) が file path に漏れない契約は title sanitize 上位層責任の構造的反映。
- **`session_store.rs path_for_session` の標準形式 `<dir>/<id>.md`** (本セッション Loop 2 T1-T2)。parent / extension / file_stem / file_name の 4 軸で誤改修検知。
- **`session_store.rs unescape_inline_markdown_text` (line 84-102)** は 6 文字 set 判定 + chars().peekable() ベース。bytes() 化誤改修への検知は本セッション Loop 3 T4 で固定 (multibyte/emoji 隣接)。
- **`session_store.rs unescape_inline_markdown_text` の連続 backslash 独立処理** (本セッション Loop 3 T1): peek-based なので各ペア独立。greedy match との誤改修検知装置。`\\\\` (4 chars) → `\\` (2 chars), `\\\\\\` (6 chars) → `\\\` (3 chars), `\\\\\\\\` (8 chars) → `\\\\` (4 chars) の独立処理。
- **`session_store.rs unescape_inline_markdown_text` の非対象 backslash passthrough** (本セッション Loop 3 T3): `\!, \X, \1, \$` 等は backslash + char の 2 chars としてそのまま残る。markdown.rs inline_markdown_text の対象 6 文字選択性と整合。
- **既存累積技術注意点** (前 mjc-main-12 SUMMARY 参照、変更なし、本セッションで触れていない領域は前 SUMMARY を参照):
  - session_manager.rs discard 関連 (3 点)、cloud_whisper_errors.rs 4 分岐、markdown.rs format/inline 関連
  - sanitize_error_body の `>` 境界 / 正規化順序 / multibyte 境界
  - is_scribe_error_event / extract_error_message / extract_transcript / openai_realtime handle_event
  - persist_if_configured の `?` 罠 / overwrite idempotency / phase 引数 free string (旧 mjc-main-10)
  - `pending_timeout_tests` mod は `mod ws_task` 内 (line 482)
  - build_append_args_for_emission 系の 2 軸網羅
  - segment_to_append_args_at の zero_start ロジック
  - SESSION_COUNTER は静的 AtomicU64、絶対値 assert 禁止
  - Session 構造体は薄いデータコンテナ
  - validate_stream_count_for_engine のクラッシュ防止文言完全一致
  - SettingsStateHandle::new() は呼ばないこと
  - resolve_output_directory の guard、default_offset の `9 * 3600`
  - parse_requested_transcription_sources のエラー文言完全一致
  - is_realtime_stream_already_stopped_error は contains 判定
  - unix_secs_i64 の error format
  - unescape_inline_markdown_text の lone trailing backslash 保持 (line 95 の `_ => out.push(ch)`)
  - audio_utils.rs / transcript.rs Phase 4 placeholder / Teams ブラウザ版タイトル fallback 禁止
  - validate_audio_format_properties cfg なし
  - secret_store の Keychain 実通信テスト禁止
  - MAX_ERROR_BODY_CHARS = 200 は char base
  - chrono FixedOffset::east_opt(0) と west_opt(0) 同等
  - elevenlabs_realtime と openai_realtime の push_error は完全対称
  - parse_session_started_at_secs は既に 7 件で網羅済 (追加補強不要)

## 次ループ候補 (優先順位順、本セッションで未着手)

### A. transcript_bridge.rs C' の最小スライス (規模 XS、価値中)
- 既存 26 件、本体 100 行で密度高め。SUMMARY 提案の「source=None vs Some の振る舞い対称性、segment.is_error の 2 段早期リターン first-violation contract のさらなる boundary」が gap。前 mjc-main-9 の `d431fff` で 5 件追加済のため、観測時刻注入経路の追加 boundary を狙う。

### D (継承). transcription.rs error path 残り (規模 S〜M)
- 1896 行最大規模。前 mjc-main-8 で `validate_stream_count_for_engine` 5 件補強済。残り gap: `load_model` の path 変換失敗、`feed`/`finalize` の resampler state missing 周辺。worker 1 ループで完結しにくい可能性、複数ループ構成 (1 件 ≒ 1 fn の direct test) が安全。

### E (継承). drop メトリクスを Tauri イベント化 (規模 M、価値 9) ※部分的進捗あり
- 価値: 録音状態の透明性 (優先度 9)。
- **本セッション中の事前調査結果**: `system_audio.rs` (line 318-330) には既に drop メトリクス emit ループが実装済。一方 `audio.rs` (line 58, 88, 101, 110) のマイク側は drop_count フィールドはあるが emit の有無未確認。
- 提案: audio.rs (mic 側) を Read で確認 → system 側と対称な drop emit を追加 → 1 ループで rust 側完結 (frontend hookup なし)。AppHandle の取り回しは `system_audio.rs` line 318-330 と `transcription.rs` line 1199 等の既存パターンを踏襲可能。
- リスク: codex の UI 系作業との競合 (本セッション中はゼロ、低)。

### F (継承). 会議終了検知の遅延監視 (規模 S〜M)
- handle_detection 内で watched bundle が起動 → アクティブから外れた経過時間を測り、N 分以上アクティブ無しなら会議終了通知。

### G (新). session_store.rs の他 fn 残り (規模 XS、価値中)
- write_session_markdown_to (test なし、save_session_markdown 経由でカバー) の direct test、io_invalid (test 1) / unix_secs_i64 (test 3) の追加 boundary。本セッション Loop 2/3 で path_for_session と unescape_inline_markdown_text を扱ったので、続きとして次の test ゼロ/少 fn を狙う最小スライス候補。

### S (継承). settings.rs Tidy First リファクタ (規模 M、2 ループ構成)
- `load_from_path` 抽出 (1 ループ) + テスト追加 (1 ループ) の 2 ループ構成必要。価値中。

### K (継承). clippy::pedantic の選択的有効化 (規模 L、非優先)。

## 検証制約 (再掲)
- cmake あり → cargo test 430 件全 pass している (verify.sh OK)
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <ファイル群>` (Conventional Commits 日本語)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。再起動不要。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-13 mjc-main`

## コミット周期目標
- 1 ループ 15 分前後を目標。worker 1 件 ≒ 1 コミット。
- 本セッション実績平均 ~12-15 分/loop (3 ループ全テストのみで安定、累積 23/23 worker 完走)。
- trivial 変更は research 担当を立てずメインから直接 worker へ。
- 担当範囲が衝突しない 2 件は worker 並列起動可。
- worker prompt は **5 セクション構造** (背景/Why/具体的 test 案 assertion レベル/実装上の注意/検証手順/AGENT_LOG.md 記載項目) で書くと sonnet が 1 ターンで完走しやすい (本セッション 3/3 完走実績、累積 23/23 完走)。

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針。
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲。
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる。
- 本セッションでは 3 ループ + SESSION SUMMARY を終え、context 75-80% 推定で予防的ハンドオフ判断 (前 5 セッション (mjc-main-7/8/9/10/11/12) と同じ 3 ループパターン)。

## ユーザー直伝指示 (未消化)
- なし。watchdog 継続指示は受領済み・自走中で消化済み (本セッション中 1 回受領)。

## 累積成果 (本セッション)
- **テスト 415 → 430 passed** (+15 件、3 ループ、すべて新規追加)
- **コミット 4 件** (うちテスト追加 3、SESSION SUMMARY 1)
- **新規ファイル 0 個** (worker prompts は untracked のまま、handoff prompt も untracked)
- **clippy 警告ゼロ維持** (default + -D warnings + uninlined_format_args)
- **「pub fn なのに test ゼロ」発見パターン確立** (Loop 2)
- **「PathBuf 4 軸完全分解 path test パターン」確立** (Loop 2)
- **「explicit_escape ヘルパーによる対称関数の純粋部分対称性 test」確立** (Loop 3 T5)
- **「serde round-trip 全状態組合せ 2x2 = 4 ケース対称」確立** (Loop 1 T4)
- **「絶対値 assert 禁止 AtomicU64 を相対比較のみで test」確立** (Loop 1 T1)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -350 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-13 mjc-main` を実行 (冪等、既に成功してたら "successor session does not exist" を返す)。
4. 候補 A (transcript_bridge.rs C')、G (session_store.rs 他 fn 残り = 本セッション Loop 2/3 の隣接領域、最も自然な続き)、E (drop メトリクス Tauri イベント化、本セッション部分的事前調査済) のいずれかを最小スライスとして worker に発注して 1 ループ目を始める。**G は本セッションの session_store.rs の Loop 2/3 と隣接する自然な続きで最有力。A は既存 26 件で密度高め、E は価値 9 で部分的進捗ありだが規模 M なのでまず audio.rs を Read で確認してから判断。**
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
