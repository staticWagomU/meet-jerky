あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-4) です。
旧 mjc-main (= mjc-main-20260505-3) から **3 ループ完走 + transcription.rs 責務分離プラン docs 創出 + Phase 1 完了 (データ型 + トレイト定義の分離達成、累計 ~90 行 transcription.rs から削減) + 5 連続 Webex sweep からの完全 variety pivot 達成 + AGENTS.md 優先順位 1 = クラッシュ修正への予防的寄与達成 + 「handoff prompt の B1 (Teams window title) を前任者 rustdoc 注記との矛盾で批判的却下」「Mutex poison panic 候補発見の自己誤認も訂正」の 2 件の批判的判断 precedent 創出 + worker 完走 107/107 100% 維持 + harness silent fail mitigation pattern 連続 31 ループ実証達成 + コミット周期 ~10 分/loop で目標 15 分以内達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (本セッション Loop 4 で創出、Phase 1 完了済 / Phase 2-4 未着手)**
- AGENT_LOG.md は 24,300+ 行 / 1.9MB+ あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~02:25 JST] mjc-main-20260505-3 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## mjc-main-20260505-1 が確立した戦略 (継承中)
26 連続「Debug 軸補強」のローカル最適化からの完全脱却。

## mjc-main-20260505-2 が確立した戦略 (継承中)
「Webex sweep」も同質のローカル最適化に変質しうる。**「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」**を新ルールとして確立。

## mjc-main-20260505-3 (本セッション) が新たに確立した判断
1. **「handoff prompt の主要候補を批判的に再評価する」precedent 創出**: B1 (Teams window title) を前任者 rustdoc 注記 (line 555-557 「外部チュートリアルや解説ページと区別できないため fallback 対象外」) との矛盾で批判的却下。AGENTS.md「鵜呑みにせず批判的・中立的に判断」原則の具体的実践。
2. **「Mutex poison panic 候補発見」の自己誤認訂正**: session_manager.rs:400/417/758 を panic 候補と誤検知 → Read で精読 → test code (`is_active_recovers_from_poisoned_mutex_without_panic` 等) と判明 → handoff prompt の panic 監査結論「3 件 provably-safe」の正確性を再確認。**「grep 結果は Read で精読する」の運用ルール強化**。
3. **「research のみで commit 困難」評価への批判的却下**: main 直接 grep + Write の precedent (handoff prompt 作成、SESSION SUMMARY 追記) を docs ファイル作成にも応用 = worker 起動を省略するハーネス省略策の新 precedent。
4. **「plan → 実行への段階的推進」パターン創出**: Loop 4 (docs commit) → Loop 5/6 (実コード抽出) で plan を実行に移す自然な流れ = variety 規則発動回避の好例。

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- **「Webex sweep」「Zoom sweep」「Teams sweep」等の単一サービス補強パターンに 4 ループ以上集中しない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **grep 結果は必ず Read で精読してから「発見」を主張する**

# 引き継ぎサマリ (2026-05-05 ~02:25 JST 時点)

## 現在の git 状態
- branch: main, ahead 196 (origin/main から先行、本セッション +3 commit (Loop 4-6))
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 旧 mjc-main-20260505-3 が canonical 名 mjc-main を持ち、本セッションで 3 ループを完了:
  1. `b5dcff9` docs(architecture): transcription.rs (2999 行) の責務分離プランを記録 (Loop 4, ~12 分)
  2. `a7d9fc8` refactor(transcription): データ型 4 つを transcription_types.rs に抽出 (Tidy First, Phase 1, Loop 5, ~10 分)
  3. `cafbef6` refactor(transcription): トレイト定義 3 つを transcription_traits.rs に抽出 (Tidy First, Phase 1 仕上げ, Loop 6, ~10 分)

## 現在の品質状態
- cargo test (lib): **667 passed / 0 failed** (前 667、本セッションでは Tidy First のため件数不変が指標)
- cargo clippy --lib -- -D warnings: **警告ゼロ** (3 ループ全てで維持)
- cargo fmt --check: OK
- npm run build: 本セッションで frontend 触れていないため変更なし

## 完成した transcription.rs Phase 1 (Loop 5-6)
**データ型 + トレイト定義の分離完了**:
- `src-tauri/src/transcription_types.rs` (新規 42 行): TranscriptionSource enum / TranscriptionSegment struct / TranscriptionErrorPayload struct / ModelInfo struct
- `src-tauri/src/transcription_traits.rs` (新規 49 行): StreamConfig struct / TranscriptionEngine trait / TranscriptionStream trait
- `src-tauri/src/transcription.rs`: 累計 ~90 行削減 (~2999 → ~2910 行)、互換層 `pub use` で既存 import 元 (cloud_whisper.rs / transcript_bridge.rs / elevenlabs_realtime.rs) を変更不要に維持
- `src-tauri/src/lib.rs`: `mod transcription_traits;` + `mod transcription_types;` 追加

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-3、handoff 直前)
- mjc-main-20260505-4 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 3 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-4` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-4 mjc-main` を実行 (前 17 セッションで実証済 precedent あり、冪等)。
5. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-3) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Phase 1 完了 + 次ループ候補を確認**
6. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**Loop 5/6 と同パターン extraction の 3 連続目になる Phase 2 (Whisper エンジン抽出) は variety 規則発動条件のため、慎重に判断**。
7. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 次ループ候補 (優先順位順、variety pivot 推奨)

### 注意: variety pivot を考慮

Loop 5/6 が transcription.rs 抽出の 2 連続。Loop 7 で同パターンを続けると 3 連続 = 「3 ループ続いたら 4 ループ目に variety pivot」規則の発動条件を満たす。**Loop 7 は Phase 2 ではなく別軸を選び、Phase 2 は Loop 8 以降に回す道もある**。

### 最優先 (variety pivot を兼ねた priority 1〜4 への直接寄与)

#### B1. Microsoft Teams window title fallback (規模 M, 価値中、AGENTS.md 優先順位 2)
- **本セッション (mjc-main-20260505-3) で批判的に却下した候補**: 前任者 rustdoc 注記 (line 555-557) との矛盾で safe pattern が見つからない限り危険
- 後継が再評価する場合、**まず handoff prompt の B1 提案 + 前任者 rustdoc 注記 + 本セッションの却下根拠 (handoff prompt 作成) を読み、批判的判断する**
- 安全側スライス特定が困難な場合、再度却下 = 「priority 2 への寄与は窓タイトル fallback では困難」と結論可能

#### A1. Webex Japanese window title (`Webex ミーティング`) — variety 維持のため Loop 7 でも推奨せず
- 規模: 極小 (3-4 行)
- 価値: 低-中 (Webex 検知の英 / 日 symmetry)
- **本 mjc-main-20260505-3 ハンドオフ後に再考可能、ただし「Webex sweep からの完全 variety pivot」を 3 セッション連続維持中のため、Loop 8-9 程度まで間隔を空けるのが安全**

#### A2. Whereby サポート (規模 S-M, 価値中)
- URL: `whereby.com/<room-name>` または `<subdomain>.whereby.com/<room-name>`
- リスク: about / pricing / blog 等の通常ページとの区別が難しい = 仕様調査必須
- **Loop 7 で variety pivot 候補として再考価値あり**: Webex 関連と区別できる新サービス対応 = priority 2 への直接寄与 + Webex sweep からの完全 pivot

#### Z. 録音状態の透明性 UI 改善 (規模 S, 価値中、AGENTS.md 優先順位 9)
- 本セッションで触れず、handoff prompt も触れず
- AGENTS.md 優先順位 9 (アクセシビリティ、権限説明、録音状態の透明性) への直接寄与
- 1 ループで実装可能なスライス: 例: 録音中インジケータの色 / アニメーション / aria-label 追加
- **Frontend 側 = Rust から完全 pivot**: 本セッション 3 ループの transcription.rs Tidy First から最も離れた軸
- ただし、UI 改善は具体的スライス選定が主観的 = まず frontend grep で現状把握必要

### 中優先 (Phase 2 着手、ただし 3 連続 extraction になる懸念)

#### Phase 2-A. WhisperLocal + WhisperStream + 純粋関数の transcription_whisper.rs 抽出 (規模 M)
- 抽出対象: WhisperLocal struct + impl (~70 行)、WhisperStream struct + impl (~150 行)、calculate_rms / is_tail_silent (~30 行)
- 計 ~270 行 = Loop 5/6 の 2 倍規模
- **1 ループ超過リスク**: cargo test が長時間化、抽出後の use 文整理が複雑
- **Loop 7 でこれを選ぶと extraction 3 連続 = variety 規則発動条件**
- 推奨: Phase 2-A は Loop 8 以降、Loop 7 は variety pivot で別軸を選ぶ

#### Phase 2-B. ModelManager の transcription_model_manager.rs 抽出 (規模 M)
- 抽出対象: ModelManager struct + impl (~150 行)
- 同様に 3 連続 extraction の懸念あり
- 推奨: Loop 8 以降

### 低優先 (priority 1 = クラッシュ修正の追加調査)

#### G2. transcription.rs の Mutex 利用箇所の poison-safe 監査
- 本セッション grep で session_manager.rs を確認したが、transcription.rs 自体の Mutex (TranscriptionStateHandle) は未点検
- ただし、handoff prompt の panic 監査結論「3 件 provably-safe」がやや包括的でなかった可能性 (本セッション再点検は test code 誤検知で実質ゼロ収穫)
- **再点検は無駄になる確率が高い** = 不推奨

### 低優先 (harness 衛生)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 100+ untracked ファイル (本セッションで +5 件追加 = handoff/mjc-main-20260505-3.txt + worker-prompts/mjc-worker-transcription-types-extract... + transcription-traits-extract... + transcription-window-title-... + 他)
- **ユーザー直伝指示があるまで保留**

#### I. AGENT_LOG.md 24,300+ 行の archive 戦略
- **未着手、未消化**

## 検証制約 (再掲)
- cmake あり → cargo test 667 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり、本セッションで docs/architecture/* もメイン直接編集 precedent 創出)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-4 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 31 ループで実証した mitigation pattern)
  - log file (`logs/agent/<session>.txt`) と tmux pane は補助観測手段
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示

## worker prompt 必須要素 (31 連続セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション平均 ~10 分/loop で **目標 15 分以内達成**)
- worker 1 件 ≒ 1 コミット
- 累積 worker 完走 107/107 (本セッション +2 件 = Loop 5/6 worker、Loop 4 は main 直接 = worker 起動なし、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中 3 件 (Loop 4 開始直後 + Loop 5 worker 起動後 + Loop 6 worker 起動後) を「Loop 進行 + commit」で消化済

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-4` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-4 mjc-main` を実行 (冪等)。
4. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。
5. **最初の Loop 候補**: Loop 5/6 transcription.rs Tidy First の 2 連続を踏まえ、**Loop 7 で extraction 3 連続を避ける variety pivot 推奨**。Whereby (A2) / 録音状態 UI (Z) / Teams window title (B1, ただし批判的再評価必須) が候補。Phase 2 (Whisper エンジン抽出) は Loop 8 以降に回すのが variety 維持上安全。
6. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、本セッションでも grep を Read で精読する重要性を再確認)
7. 待機モード禁止。final answer で停止せず、改善ループを継続する。
8. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
9. **worker 完走確認は `git status --short` を主観測手段とする** (連続 31 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-180 秒)
