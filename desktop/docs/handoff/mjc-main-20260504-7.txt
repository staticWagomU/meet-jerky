あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-7) です。
旧 mjc-main (= 旧 mjc-main-20260504-6) から 5 ループ完了の良い区切りでハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 16500+ 行 / 1.5MB+ あるので `tail -300 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~08:55 JST] mjc-main-20260504-6 状況メモ (ループ 1-5 詳細) があります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~08:55 JST 時点)

## 現在の git 状態
- branch: main, ahead 52 (origin/main から先行)
- working tree: clean (untracked: `docs/worker-prompts/`、`docs/handoff/mjc-main-20260504-7.txt` は本ハンドオフ用)
- 旧 mjc-main-20260504-6 が canonical 名 mjc-main を持っており、本セッションで 5 ループ + 1 SESSION SUMMARY コミットを完了:
  1. `d002aac` test(openai_realtime): ws_task::push_error の visibility を pub(crate) に広げ prefix/suffix 文言完全一致 + zero timestamps + is_error=Some(true) 不変条件 + source/speaker passthrough + 空/multibyte message format 保持 (315→320 passed)
  2. `b29818a` test(cloud_whisper): build_whisper_api_url の空 base_url / "/" のみ / 多重 trailing slash 全削除 と build_whisper_authorization_header の空 api_key / 前後 whitespace 込み / multibyte api_key の no-trim/no-validation 現挙動 を 5 件で URL/api_key バリデーション上位層責任境界として固定 (320→325 passed)
  3. `50497d7` test(audio): calculate_rms_from_sum の NaN sum (is_nan() 防御コード保護) / +Inf sum (NaN との非対称な responsibility) / sqrt 中間値精度 (eps 1e-6) / count=1 単一サンプル境界 / clamp 上限 1.0 ジャスト の 5 件で UI レベルメーター駆動の核心 fn を保護 (325→330 passed)
  4. `775d396` test(elevenlabs_realtime): ws_task::push_error 直接テスト 5 件を inner mod pending_timeout_tests に追加し openai_realtime と完全対称な error path 直接テスト構造を確立 (330→335 passed)
  5. `4e8ed85` test(system_audio): read_f32_ne の bit-pattern 直接読み取り 4 件 (sanitize 前段の正確性保護) と validate_audio_format_properties の short-circuit 順序契約 1 件 を追加 (335→340 passed)
  6. `5037b1a` chore(agent-log): SESSION SUMMARY 追記

## 現在の品質状態
- cargo test: **340 passed / 0 failed**
- cargo clippy default lint: **警告ゼロ**
- cargo clippy -W uninlined_format_args: **警告ゼロ**
- frontend build: 通過 (codex 活動本セッション中ゼロ、~24 時間以上静か)

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-6、handoff 直前)
- mjc-main-20260504-7 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションがあれば触らないでください。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-7 mjc-main` を実行して、あなたが canonical mjc-main を引き継ぐ。これにより旧 mjc-main は自動で kill される。
5. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本セッションで定着・累積)
- **両 Realtime engine の push_error 完全対称構造** (本セッション ループ 1+4): openai と elevenlabs の `[XX Realtime エラー: {message}]` prefix 文言契約 + start_ms=0/end_ms=0/is_error=Some(true) 不変条件 + source/speaker passthrough を 5 件×2 = 10 件で対称的に保護。openai 側は visibility を `pub(crate)` 化 (同 mod 内 `pub(crate) fn float_to_pcm16` の流儀)、elevenlabs 側は inner mod (`pending_timeout_tests`) に `super::push_error` で呼ぶ visibility 維持アプローチ。
- **「上位層責任境界の契約強制」パターン** (本セッション ループ 2): build_whisper_api_url / build_whisper_authorization_header が validation/trim を一切しない現挙動を「`""`/`"/"`/前後 whitespace/multibyte」で固定。「ここでは何もしない、上位層がチェックする」契約をテストで明示。
- **NaN/+Inf の非対称責務分離パターン** (本セッション ループ 3): is_nan() 早期 return → 0.0 vs clamp → 1.0 の意図的な非対称を NaN/+Inf の対称ペアテストで「異常値種別ごとの最小/最大値返却」設計を契約として固定。
- **sanitize 前段の bit-pattern 直接テストパターン** (本セッション ループ 5): defensive pipeline (read → sanitize → process) で、sanitize 後では観測不能な NaN/Inf bit-pattern の正確性を read 段階で直接 test。`f32::NAN.to_ne_bytes()` ↔ `read_f32_ne` の round-trip を IEEE 754 仕様の保証として固定。同種 defensive pipeline で再利用可能な汎用パターン。
- **first-violation contract** (本セッション ループ 5): 複数 violation 同時入力で関門順序を文言完全一致で固定。順序入れ替えリファクタの検知装置。

### 累積パターン (前セッションから継承)
- private fn 直接呼び出しテスト: 関数分離リファクタへの耐性 + 防御コード存在意義の裏付け
- エラー文言の contract enforcement: 実装と test に同じ `&'static str`
- 境界値テストの「対称ペア」: 同じ境界を 2 層の責任で観測
- 3 層テスト構造: 共通実装 + wrapper 経由で同じ仕様を 3 視点保護
- OS 互換 ErrorKind 契約強制: `unwrap_err().kind() == ErrorKind::NotFound`
- state machine 混在遷移テスト: guard logic 統合/分離リファクタへの耐性
- format prefix の層内独立確認
- エスケープ漏れの網羅補完
- UI 文言一致の契約強制: 2 つの API の error 文言完全一致

### 重要な技術的注意点 (踏みやすい罠)
- **visibility 拡大の流儀** (本セッション ループ 1 確立): 同 mod 内既存パターンに揃える (openai_realtime ws_task では `pub(crate) fn float_to_pcm16` の流儀)。
- **read_f32_ne は private のまま** (本セッション ループ 5 確立): `use super::*;` 経由で同 mod 内テストから呼べる。
- **trim_end_matches('/') の連続 char 削除挙動** (本セッション ループ 2): `strip_suffix("/")` への誤リファクタを T3 で検知。
- **`audio_utils.rs` は補強余地ほぼゼロ**: 既存 6 件で全分岐カバー。
- **`transcript.rs` は Phase 4 用 placeholder** (TODO 1 行のみ)、対象外。
- **`persist_if_configured` の `?` リファクタリング罠** (継承): エラーを呑み込む設計を `?` に書き換えると in-memory consistency が壊れる。
- **`classify_meeting_window_title` の whitespace-only rest 仕様**: 「`Meet -  ` (rest = whitespace のみ) は Some を返す」現挙動は docstring 未記載。
- **Teams ブラウザ版タイトル fallback は引き続き禁止** (既存方針)。
- **`validate_audio_format_properties` は cfg なし**: 同ファイルの `validate_audio_format_description` のみ macOS gate。
- **wrapper fn (sanitize_sample / sanitize_pcm_sample) は削除しない**: 前セッションで意図的に残した。
- **session.rs の SESSION_COUNTER はグローバル**: 並列実行下で絶対値 assert 禁止、差異 assert のみ。
- **secret_store の Keychain 実通信テスト禁止**: バリデーション層 (空判定 → `"API キーが空です"`) まで。
- **markdown.rs `inline_markdown_text` のエスケープ対象は 6 文字 + whitespace 正規化**。
- **cloud_whisper_errors.rs `MAX_ERROR_BODY_CHARS = 200` は char base** (前セッション)、byte base 誤リファクタは UTF-8 panic 致命バグ。
- **chrono の `FixedOffset::east_opt(0)` と `west_opt(0)` は同等**、leap second 非対応。
- **elevenlabs_realtime と openai_realtime の `push_error` は完全対称** (本セッション 確認・テスト化済): text format prefix だけ違う (`"[OpenAI Realtime エラー: ..."` vs `"[ElevenLabs Realtime エラー: ..."`)。

## 次ループ候補 (優先順位順)

### Q. transcription.rs の追加 error path (規模 S〜M)
- 既存 37 件 (file size 1896 行、最大規模)。
- 候補 fn: `load_model` の path 変換失敗、`feed`/`finalize` の resampler state missing 周辺、AppleSpeech / OpenAIRealtime / ElevenLabsRealtime のエラーパス。
- 実環境依存性高い fn を避ければ 5-7 件補完可能。要 worker 内で確認。
- 規模やや大きく、worker prompt で「実環境依存性を確認しつつ最大 5 件補完」のように余裕持たせる。

### R. session_commands.rs (規模 S, 13 件既存、399 行)
- tauri command 層、UI と直接関わる。command 引数バリデーション層の補強候補。
- 価値: command パラメータの edge case (空 string、巨大数、Option None など) の現挙動固定。
- 要事前確認 (実装で何を直接 test できるか)。

### S. settings.rs error path (規模 M、Tidy First リファクタ要)
- `load()` / `save()` は `Self::config_path()` を直接呼ぶため path 注入できる API が無い。
- `load_from_path` 抽出 (1 ループ) + テスト追加 (1 ループ) の 2 ループ構成必要。価値中。
- 1 ループ 15 分目標を超える可能性が高いため後回し。

### T. session_manager.rs / session_store.rs の追加 (規模 XS〜S)
- 既存 16/25 件で前セッションで補強済み。残り gap は cross-fn invariant や error path の 2-3 件程度の可能性。価値低中。

### F (継承候補): drop メトリクスを Tauri イベント化 (規模 M)
- 価値: 録音状態の透明性 (優先度 9)。
- 提案: rust 側に payload struct + emit のみ追加 (frontend hookup なし) で 1 ループ。AppHandle 渡し方は研究担当に先に確認。
- リスク: codex の UI 系作業と部分的に競合の可能性 (frontend hookup 段階で)。

### G' (継承候補): 会議終了検知の遅延監視 (規模 S〜M)
- handle_detection 内で watched bundle が起動 → アクティブから外れた経過時間を測り、N 分以上アクティブ無しなら会議終了通知。

### K (継承候補): clippy::pedantic の選択的有効化 (規模 L、非優先)。

## 検証制約 (再掲)
- cmake あり → cargo test 340 件全 pass している (verify.sh OK)
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <ファイル群>` (Conventional Commits 日本語)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。再起動不要。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-7 mjc-main`

## コミット周期目標
- 1 ループ 15 分前後を目標。worker 1 件 ≒ 1 コミット。
- 本セッション実績平均 ~15 分/ループ (5 ループ全テストのみで安定)。
- trivial 変更は research 担当を立てずメインから直接 worker へ。
- 担当範囲が衝突しない 2 件は worker 並列起動可。
- worker prompt は **5 セクション構造** (背景/Why、具体的 test 案 assertion レベル、実装上の注意、検証手順、AGENT_LOG.md 記載項目) で書くと sonnet が 1 ターンで完走しやすい (本セッション 5/5 完走実績)。

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針。
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲。
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる。
- 本セッションでは 5 ループ + SESSION SUMMARY を終え、context 余裕中程度で予防的ハンドオフ判断 (前セッション 5 ループハンドオフパターン踏襲)。

## ユーザー直伝指示 (未消化)
- なし。watchdog 継続指示は受領済み・自走中で消化済み。

## 累積成果 (本セッション)
- **テスト 315 → 340 passed** (+25 件、5 ループ、すべて新規追加)
- **コミット 6 件** (うちテスト追加 5、SESSION SUMMARY 1)
- **新規ファイル 0 個** (worker prompts は untracked のまま、handoff prompt も untracked)
- **clippy 警告ゼロ維持** (default + uninlined_format_args)
- **両 Realtime engine の error path 完全対称化** (push_error 直接テスト 10 件、prefix 文言の違いのみ残し他は構造的同一)
- **defensive pipeline の sanitize 前段保護パターン確立** (read_f32_ne 4 件で同種 pipeline で再利用可能な汎用テストパターンを確立)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -300 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲**: `bash scripts/agent-adopt-main.sh mjc-main-20260504-7 mjc-main` を実行。
4. 候補 Q (transcription.rs の error path 補強) または R (session_commands.rs の command 引数バリデーション層補強) のいずれかを最小スライスとして worker に発注して 1 ループ目を始める。**確認を兼ねて、まず Q または R のテスト件数と対象 fn の構造を grep で確認してから worker prompt を書くこと。** Q は 1896 行と最大規模なので 5 件選定にやや時間かかる、R は 399 行で gap 把握しやすい。
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
