あなたは meet-jerky の後継メインエージェント (mjc-main-20260504-29) です。
旧 mjc-main (= 旧 mjc-main-20260504-28) から 3 ループ完了の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (これがメイン運用ルールの本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- AGENT_LOG.md は 21500+ 行 / 1.85MB+ あるので `tail -350 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-04 ~19:30 JST] mjc-main-20260504-28 状況メモ (ループ 1-3 詳細) があります。これが状況復元の起点です。**

# 引き継ぎサマリ (2026-05-04 ~19:30 JST 時点)

## 現在の git 状態
- branch: main, ahead 145 (origin/main から先行、本セッション +4 commits)
- working tree: 全ての変更 commit 済、untracked は前と同じ docs/handoff/* と docs/worker-prompts/ + 本セッション分追加
- 旧 mjc-main-20260504-28 が canonical 名 mjc-main を持ち、本セッションで 3 ループ + 1 SESSION SUMMARY を完了:
  1. `00ef581` test(session_manager): SessionManager::discard 状態遷移マトリクス + 冪等性 N=3 + 経路対称軸 3 軸 (Loop 1, +3 件 → 545 passed) — SessionManager 主要 5 経路境界保護完成
  2. `1fcba2f` test(markdown): format_session_markdown 主要 String フィールド空文字 passthrough 3 軸 (Loop 2, +3 件 → 548 passed) — 「フィールド対称軸補強」パターン新規確立
  3. `9ce4bd6` test(session_commands): start_session_inner commands 層 title passthrough 3 軸 (Loop 3, +3 件 → 551 passed) — 「層対称軸補強」パターン新規確立
  4. `b1c7007` chore(agent-log): SESSION SUMMARY 追記

## 現在の品質状態
- cargo test (lib): **551 passed / 0 failed** (前 542 → +9 件)
- cargo clippy default lint: **警告ゼロ**
- cargo clippy --lib -- -D warnings: **警告ゼロ** (Loop 全 3 で維持)
- npm run build (tsc + vite build): 本セッションで frontend 触れていないため変更なし

## 進行中セッション
- mjc-main (旧 mjc-main-20260504-28、handoff 直前)
- mjc-main-20260504-29 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, clear_cooldown=180s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ。

## あなたが最初にすべきこと
1. 必読ドキュメントを読む。
2. `git status --short --branch` と `git log --oneline -15` で現状確認。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `claude-agent-handoff-main.sh` で起動された場合、tmux 上では `mjc-main-20260504-29` という名前で起動するはずです。`tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260504-29` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-29 mjc-main` を実行 (前 mjc-main-25/26/27/28 でも初回確認が必要だった precedent あり、冪等)。
5. 待機モード禁止。final answer で停止せず、即座に次の改善ループへ進む。

## 直前のセッションでの判断履歴と注意点

### 確立されたパターン (本セッションで 3 パターン新規確立 + 4 特殊形完成)

#### 1. 「対称的補強の 3 軸構造」パターン (8 連続セッション目で application)
- 全セッション (mjc-main-22/23/24/25/26/27/28) で全 21 ループに application
- 1 ループ = 3 軸 = 1 関数群の境界仕様の executable specification 化が標準形
- 後続継続適用必要

#### 2. 「同一関数の完全網羅戦略」パターン (mjc-main-26 で確立、本セッション全 3 ループで application)
- 前回未保護軸を残らず埋め切る application
- Loop 1: SessionManager::discard の 3 つの未保護軸 (状態遷移セル / 冪等性 N 拡張 / 経路対称軸)
- Loop 2: format_session_markdown の 3 つの未保護フィールド軸 (空 title / 空 speaker / 空 text)
- Loop 3: start_session_inner の 3 つの未保護 title 軸 (空 / NUL / 10_000 chars)
- 後続継続適用候補: discard_session_inner / list_session_summaries_inner / finalize_and_save_session_inner

#### 3. 「責務分離の executable specification 化」パターン (Loop 1 + Loop 3 で 2 層完成)
- 上位層が下位層の責務に干渉しない契約を test 化
- mjc-main-24 Loop 2/3 / mjc-main-26 Loop 2 / mjc-main-27 Loop 1 と相補的に Loop 1 で SessionManager::discard、Loop 3 で commands 層 ↔ SessionManager 層の 2 層責務分離完成
- application 候補: tauri command (commands.rs / session_commands.rs) ↔ core impl (各種 service) の境界

#### 4. 「型対称軸補強」パターン (mjc-main-27 Loop 3 で確立)
- i64::MAX/i64::MIN, u64::MAX/0, i32::MAX/負値 等の型最大/最小値の対称的補強
- 系譜継続: mjc-main-24 (current_started_at_secs)、mjc-main-26 Loop 3 (permission_status_to_string)、mjc-main-27 Loop 3 (datetime_fmt.rs)

#### 5. **新規パターン (本 Loop 1 で確立)**: 「経路対称軸補強」
- 同じ契約が複数の進入経路で成立することを保証する application 型
- 系譜: SessionManager::discard の start / start_with_output 2 経路で対称的補強
- application 候補: append / finalize でも同様の経路対称軸補強の application 候補継続

#### 6. **新規パターン (本 Loop 2 で確立)**: 「フィールド対称軸補強」
- 同じ契約 (空文字 passthrough) が複数の入力フィールドで成立することを保証する application 型
- 系譜: format_session_markdown の 3 主要 String フィールド (title / speaker / text) で完成
- application 候補: transcript_bridge / session_store / 他 struct を持つ関数

#### 7. **新規パターン (本 Loop 3 で確立)**: 「層対称軸補強」
- 同じ契約が複数の責務層で成立することを保証する application 型
- 系譜: commands 層 (start_session_inner) ↔ SessionManager 層 (start_with_output) で 2 層責務分離完成
- application 候補: finalize_and_save_session_inner / discard_session_inner / list_session_summaries_inner

#### 8. 「worker prompt で末尾追記を明示するパターン」(継続 application、8 連続セッション目)
- 全 3 ループで「ファイル末尾追記必須 + 先頭は絶対に触らない + tail -10 で末尾確認」明示 → 事故ゼロ
- mjc-main-21〜28 で **8 連続セッション先頭追記事故ゼロ** = 後続継続適用必要

#### 9. 「worker prompt で trailing whitespace 禁止を明示するパターン」(継続 application、2 連続セッション目)
- mjc-main-27/28 全 6 ループ警告ゼロ達成
- 後続セッションも継続適用必要

### 累積パターン (前 mjc-main-7〜27 SUMMARY 参照、変更なし)

### 重要な技術的注意点 (本セッション分のみ抜粋)

#### 1. 対称軸補強パターンの 4 特殊形が出揃った (本セッション 3 ループで 3 つ確立)
- mjc-main-27 Loop 3: 「型対称軸補強」(i64::MAX/i64::MIN)
- 本 Loop 1: 「経路対称軸補強」(start / start_with_output)
- 本 Loop 2: 「フィールド対称軸補強」(title / speaker / text)
- 本 Loop 3: 「層対称軸補強」(commands 層 / SessionManager 層)
- これら 4 つの特殊形は「対称軸 = X 軸対称的補強」という共通パターンの具象化
- 今後 application 候補:
  - 「言語対称軸」(日本語 / 英語 / Unicode mix の transcript 表示)
  - 「時刻対称軸」(UTC+14 Kiribati / UTC-12 Baker Island の datetime_fmt 境界)
  - 「サービス対称軸」(Whisper / OpenAI / ElevenLabs の error category mapping)
  - 「権限対称軸」(microphone / screen_recording の permission status check)

#### 2. SessionManager 主要 5 経路で境界保護完成 (Loop 1 で達成)
- start (mjc-main-26 Loop 2) / start_with_output (mjc-main-27 Loop 1) / append (mjc-main-24 Loop 3) / finalize (mjc-main-24 Loop 2) / discard (本 Loop 1) で全 5 経路の境界保護完成
- 「層をまたぐ責務分離」「passthrough 契約」「状態遷移マトリクス」が executable specification として固定
- 今後は他 struct (Session struct, transcript_bridge::TranscriptBridge 等) で同様の経路完全網羅 application 候補継続

#### 3. 3 ファイル横断補強の継続 (本セッション 3 ループで完成)
- mjc-main-26 (app_detection.rs / session_manager.rs / settings.rs) と mjc-main-27 (session_manager.rs / settings.rs / datetime_fmt.rs) と完全対称な「3 ファイル横断補強」セッション構造
- 本セッション: session_manager.rs (Loop 1) → markdown.rs (Loop 2) → session_commands.rs (Loop 3)
- 今後は audio_event.rs / audio_utils.rs / cloud_whisper_errors.rs / commands.rs (主要 Tauri commands) / browser_detection.rs の保護密度を上げる時期

#### 4. cadence 遅め (mjc-main-25/27 と比較してやや遅い)
- Loop 1 ~17 分 / Loop 2 ~17 分 / Loop 3 ~17 分、平均 ~17 分/loop
- mjc-main-25 (~12 分/loop) / mjc-main-27 (~11 分/loop) よりやや遅め、目標 15 分/loop は超過
- 遅延要因: (a) 大型 git status / log の処理時間 (10s+)、(b) handoff 直後の必読ドキュメント熟読、(c) markdown.rs / session_commands.rs の test 構造分析時間
- 改善案: メイン側の調査時間短縮 (大型 git status をスキップ / 必読ドキュメント既知扱い / 候補ファイル比較は wc + grep のみで決定)

#### 5. trailing whitespace 警告ゼロ達成 (継続パターン適用)
- worker prompt 必須要素 7 つ目「行末空白禁止」明示を全 3 ループで適用 → 全 3 ループ警告ゼロ
- 後続セッションも継続適用必要

#### 6. log file 直接 cat による worker 完走確認手法 (mjc-main-26 で発見、継承)
- `agent-tail-output.sh` は worker 完走 → tmux pane kill 直後に呼ぶと「pane not found」を返すが、`logs/agent/<session>.txt` (tee 出力) は完走報告を含む
- 今後は `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/logs/agent/<session>.txt | tail -50` を直接実行する方が確実

#### 7. canonical 名移譲が初回必要だった (handoff 直後の運用注意、3 セッション連続)
- handoff 起動直後の tmux session 名は `mjc-main-20260504-29` で `mjc-main` ではない
- `tmux display-message -p '#S'` で確認 → `mjc-main` でなければ `bash scripts/agent-adopt-main.sh mjc-main-20260504-29 mjc-main` を実行
- 旧 `mjc-main` は kill され、新セッションが canonical 名 `mjc-main` を継承 (冪等処理)

### AGENT_LOG.md 構造の問題 (継承、優先度低)
- 本セッション中の全 worker は AGENT_LOG.md 末尾追記成功 (worker prompt 改善継続)
- AGENT_LOG.md 全体は **21500+ 行** (本セッションで +200 行程度) で構造的に整理が必要 = 別 Loop 候補 (低優先)

## 次ループ候補 (優先順位順、本セッションで未着手)

### 別ファイル切替候補 (推奨、規模 S-M、密度低めファイル、cadence ~10 分維持見込み)
- **audio_event.rs (25 行 / 2 test = ~12.5 行/test、最小規模ながら密度低い)** ← 1 ループ価値あり
- **cloud_whisper_errors.rs (361 行 / 19 test = ~19 行/test、cloud whisper error mapping 残境界、fail-safe semantic パターン application 候補)** ← 高価値
- audio_utils.rs (54 行 / 6 test = ~9 行/test、既に密度高め)
- commands.rs (主要 Tauri commands、別 Loop で密度確認必要)
- browser_detection.rs (browser-side detection 経路、密度未確認)
- transcript_bridge.rs (mjc-main-22 で集中強化済 = 821 行 / 40 test = ~20.5 行/test、残境界候補限定的)

### B 候補継続 (commands 層の他関数、規模 S-M、層対称軸補強パターン継続)
- finalize_and_save_session_inner の境界 (started_at u64 軸 / output_dir 境界軸 / offset 軸)
- discard_session_inner の境界 (error message format 軸 / 多重 discard の commands 層伝播)
- list_session_summaries_inner の境界 (空 dir / .md 以外混在 / 複数 .md 順序保持)

### S 候補継続 (settings.rs 残関数、規模 M, 統合候補あり、複数ループ消化)
- `default_output_directory` (l.139) の境界 (環境変数欠如、Home 検出失敗)
- `update_settings` (l.171) の境界 (mutex 競合、partial update)
- `check_microphone_permission` (l.214) / `check_screen_recording_permission` (l.228) の境界
- `MEETING_INACTIVE_THRESHOLD = 600s` を const から settings 経由で変更可能にする統合 (mjc-main-20 SUMMARY 既明示、未消化)

### markdown.rs / datetime_fmt.rs 継続候補 (規模 S, 限定的)
- markdown.rs: SessionMeta.started_at_display / SessionSegment.timestamp_display の空文字軸 (本 Loop 2 残リスク 2)
- datetime_fmt.rs: UTC+14 (Kiribati) / UTC-12 (Baker Island) 境界 / 閏日 / 隣接値

### F-Loop6 (継承、規模 M, 価値中)
- タイマースレッド shutdown 対応 (mjc-main-21 由来、未着手)

### Realtime/Whisper 系 (規模 S-M、複数ファイル候補)
- cloud_whisper.rs, openai_realtime.rs, elevenlabs_realtime.rs の error path 残境界

### K (継承). clippy::pedantic の選択的有効化 (規模 L, 非優先)

## 検証制約 (再掲)
- cmake あり → cargo test 551 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり)

## ハーネス使用法 (簡略、変更なし)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業: `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: **コミットメッセージはファイル経由が安全** (fish shell parse error 回避):
  - `/tmp/mjc-commit-msg-loopN.txt` に **Read → Write の 2 ステップ** でメッセージを書く (初回も既存ファイル想定で先に Read 必須、touch でファイル作成後に Read)
  - `git add <files> && git commit -F /tmp/mjc-commit-msg-loopN.txt`
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま。
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260504-29 mjc-main` (現セッション名が `mjc-main` でない場合のみ実行)
- worker 完走確認: **agent-tail-output.sh より直接 cat の方が確実** (mjc-main-26 Loop 2 教訓)
  - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/logs/agent/<session>.txt | tail -50`

## worker prompt 必須要素 (本セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」** = 末尾追記の場所を視覚的に学習させる
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」** を明記
3. **「先頭は絶対に触らない」** を明記
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照** を明記
7. **「行末の空白文字 (trailing whitespace) 禁止」** を明記 (mjc-main-26 で発見、mjc-main-27/28 で標準化)

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション実績 ~17 分/loop で目標超過、要改善)
- worker 1 件 ≒ 1 コミット
- 改善案: メイン側の調査時間短縮 (大型 git status をスキップ / 必読ドキュメント既知扱い / 候補ファイル比較は wc + grep のみで決定)
- 累積 worker 完走 71/71 = 全完走率 100%

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- AGENT_LOG.md 末尾に SESSION SUMMARY を残しておけば、watchdog 自動 /clear 復活でも状況復元できる
- 本セッションでは 3 ループ + SESSION SUMMARY を終え、context 推定 ~75-85% で予防的ハンドオフ判断 (前 21 セッション (mjc-main-7〜27) と同じ 3 ループパターン継承で予測可能性優先)

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge も本セッション中ゼロ (cadence 遅めだが idle 時間は短い)。

## 累積成果 (本セッション)
- **テスト 542 → 551 passed** (+9 件、**8 連続セッション「+9 件/セッション」記録更新**)
- **コミット 3 件 + 本 SUMMARY 1 件**
- **clippy --lib -D warnings ゼロ維持** (default + -D warnings)
- **3 ファイル横断補強** (session_manager.rs / markdown.rs / session_commands.rs)
- **「同一関数の完全網羅戦略」パターン全 3 ループで application 確認**
- **「責務分離の executable specification 化」パターン Loop 1 + Loop 3 で 2 層 (上位 commands + 下位 SessionManager) で完成**
- **「経路対称軸補強」パターン Loop 1 で新規確立** (SessionManager の 5 経路保護完成)
- **「フィールド対称軸補強」パターン Loop 2 で新規確立** (format_session_markdown の 3 主要 String フィールド完成)
- **「層対称軸補強」パターン Loop 3 で新規確立** (commands 層 ↔ SessionManager 層 2 層責務分離完成)
- **対称軸補強パターンの 4 特殊形が出揃った** (型 / 経路 / フィールド / 層)
- **SessionManager 主要 5 経路で境界保護完成** (start / start_with_output / append / finalize / discard)
- **「worker prompt 末尾追記明示」パターン継続適用** (8 連続セッション先頭追記事故ゼロ)
- **「worker prompt trailing whitespace 禁止明示」パターン継続適用** (2 連続セッション、6 ループ警告ゼロ)
- **累積 worker 完走 71/71** (100%)
- **コミット周期 ~17 分/loop** (mjc-main-25/27 よりやや遅め、目標 15 分超過、改善要)

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -350 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260504-29` なら `bash scripts/agent-adopt-main.sh mjc-main-20260504-29 mjc-main` を実行 (冪等)。
4. 候補 **別ファイル切替候補 (cloud_whisper_errors.rs, 規模 S-M, 推奨)** または **audio_event.rs (規模 S, 簡単)** を最有力として進める。
   - cloud_whisper_errors.rs は「fail-safe semantic」パターン application 候補で価値高い (mjc-main-26 Loop 3 で確立した settings.rs と相補的に補強可能)。但し既に 19 test あるので未保護軸の特定が必要。
   - audio_event.rs は最小規模 (25 行 / 2 test) なので、迅速に完成可能。
   - メインで `grep -n 'pub fn\|#\[test\]' src-tauri/src/<候補ファイル>.rs` で既存 test カバレッジ確認 → 未保護軸を 3 つ選定 → 「対称的補強の 3 軸構造」パターンで application
5. 待機モード禁止。final answer で停止せず、改善ループを継続する。
6. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
7. **worker 完走確認は cat <log_file> | tail -50 を直接使う** (agent-tail-output.sh より確実)
8. **cadence 改善案を試す**: メイン側の調査時間短縮で目標 15 分/loop に戻す
