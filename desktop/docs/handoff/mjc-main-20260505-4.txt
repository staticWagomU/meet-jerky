あなたは meet-jerky の後継メインエージェント (mjc-main-20260505-5) です。
旧 mjc-main (= mjc-main-20260505-4) から **2 ループ完走 + Whereby URL 検知追加 (priority 2 直接寄与) + Phase 2-A 最小スライス完了 (priority 1 予防的寄与) + tmux 引数長制限の stdin redirect 回避策 新 precedent 創出 + transcription.rs 累計 ~112 行削減 (Phase 1 の 90 行 + Phase 2-A 最小の 22 行) + 5 連続 Webex sweep からの完全 variety pivot を 4 セッション連続維持 + 「2 ループ + 早期 handoff」precedent 強化 + worker 完走 2/2 100% (累計 109/109 100% 維持) + harness silent fail mitigation pattern 連続 33 ループ実証達成 + コミット周期 Loop 7 ~11 分 / Loop 8 ~6 分で目標 15 分以内達成** の予防的ハンドオフを受けました。

# 最初に必ず読み従うべき運用ルール
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md (運用ルール本体)
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/product-concept.md
- /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
- **/Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/architecture/transcription-refactor-plan.md (Phase 1 完了済 + Phase 2-A 最小スライス完了済 / Phase 2-A 残り + Phase 2-B + Phase 3-4 未着手)**
- AGENT_LOG.md は 24,400+ 行 / ~1.95MB あるので `tail -400 AGENT_LOG.md` 相当で末尾だけ参照。**末尾には [SESSION SUMMARY @ 2026-05-05 ~02:40 JST] mjc-main-20260505-4 状況メモがあります。これが状況復元の起点です。**

# 戦略転換の核心 (継承中、最重要)

ユーザーの最重要方針 = 「鵜呑みにせず、批判的・中立的に判断する」「目的は、この Mac アプリを最高にすること」。

## 過去セッションが確立した戦略 (継承中)

- **mjc-main-20260505-1**: 26 連続「Debug 軸補強」のローカル最適化からの完全脱却
- **mjc-main-20260505-2**: 「Webex sweep」も同質のローカル最適化に変質しうる。**「同じパターン 3 ループ続いたら 4 ループ目に variety pivot を検討」** ルール確立
- **mjc-main-20260505-3**: 「handoff prompt の主要候補を批判的に再評価する」precedent 創出 (B1 Teams window title を前任者 rustdoc 注記との矛盾で却下)、「Mutex poison panic 候補発見」の自己誤認訂正、**「grep 結果は Read で精読する」運用ルール強化**
- **mjc-main-20260505-4 (本セッション)**: **「stdin redirect 起動」precedent 創出** = tmux 引数長制限を完全回避する harness 改善、**「2 ループ + 早期 handoff」precedent 強化**、Whereby URL 検知で Webex sweep からの 4 セッション連続 variety pivot 維持、**Phase 2-A 着手 = audio_utils.rs への純粋関数集約パターン確立**

## 後継が継承すべき方針 (最重要)
- 「Debug 軸補強」「Copy 軸補強」「serde 軸補強」「PartialEq 軸補強」等の **派生型 CI 保護パターンに戻らない**
- 「Webex sweep」「Zoom sweep」「Teams sweep」「Whereby sweep」「Phase 2 sweep」等の **単一軸 4 連続集中をしない**
- 各ループで「これは AGENTS.md 優先順位 1〜9 のどれに直接寄与するか?」を明示できるタスクを選ぶ
- 寄与が薄い場合は **その候補をスキップして次を探す**
- **同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**
- **handoff prompt の主要候補を鵜呑みにせず批判的再評価。前任者の rustdoc 注記等の意図的判断を尊重**
- **grep 結果は必ず Read で精読してから「発見」を主張する**
- **「録音状態 UI 主観改善」のような具体スライス特定困難な探索を避ける** (本セッションで判定済)

# 引き継ぎサマリ (2026-05-05 ~02:40 JST 時点)

## 現在の git 状態
- branch: main, ahead 200 (origin/main から先行、本セッション +3 commit)
- working tree: clean (docs/handoff/* と docs/worker-prompts/* untracked のみ、これは継承)
- 本セッション (mjc-main-20260505-4) の commit:
  1. `d0ba7df` feat(app_detection): Whereby (`whereby.com` / `<sub>.whereby.com`) ミーティング URL 検知を classify_meeting_url に追加 (Loop 7, ~11 分)
  2. `fbac06f` chore(agent-log): mjc-main-20260505-4 Loop 7 (Whereby URL 検知 worker 実績) を AGENT_LOG.md に追記
  3. `2e9b923` refactor(transcription): 純粋関数 calculate_rms / is_tail_silent を audio_utils.rs に集約 (Tidy First, Phase 2-A 最小スライス, Loop 8, ~6 分)

## 現在の品質状態
- cargo test (lib): **674 passed / 0 failed** (Loop 7 で 667 → 674 +7 件 = Whereby テスト 7 件追加、Loop 8 で件数不変 = Tidy First)
- cargo clippy --lib -- -D warnings: **警告ゼロ** (2 ループ全てで維持)
- cargo fmt --check: OK
- npm run build: 本セッション frontend 触れていないため変更なし

## 完成した Phase 2-A 最小スライス (Loop 8)
**純粋関数 2 つを audio_utils.rs に集約**:
- `src-tauri/src/audio_utils.rs` (+23 行): `pub(crate) fn calculate_rms` + `pub(crate) fn is_tail_silent` を `sanitize_audio_sample` 直後に追加
- `src-tauri/src/transcription.rs`: 旧関数 −22 行 + use +2 行 (非テスト用 `is_tail_silent` トップレベル + テスト用 `calculate_rms` を `mod tests` 内に分離 = clippy unused_imports 回避)
- 累計 transcription.rs 削減: ~112 行 (Phase 1 の 90 行 + Phase 2-A 最小の 22 行)

## 進行中セッション
- mjc-main (旧 mjc-main-20260505-4、handoff 直前)
- mjc-main-20260505-5 (あなた、新メイン)
- mjc-watchdog (interval=180s, nudge_cooldown=300s, overflow 検知 + /clear 自動復活付き)
- Codex 側 (`mj-*` プレフィックス) のセッションは本セッション中ゼロ

## あなたが最初にすべきこと
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、**docs/architecture/transcription-refactor-plan.md は完全読**)。
2. `git status --short --branch` と `git log --oneline -15` で現状確認 (上 3 コミットが本セッションの実績)。
3. `tmux list-sessions | grep mjc` で他セッション状態確認。
4. **canonical 名移譲の運用注意**: `tmux display-message -p '#S'` で現セッション名を確認し、`mjc-main` なら canonical 移譲済 = `agent-adopt-main.sh` 不要。`mjc-main-20260505-5` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-5 mjc-main` を実行 (前 18 セッションで実証済 precedent あり、冪等)。
5. **AGENT_LOG.md 末尾の SESSION SUMMARY (mjc-main-20260505-4) を `tail -400 AGENT_LOG.md` で読み、戦略転換 + Phase 2-A 最小完了 + 次ループ候補を確認**
6. **最初の Loop 候補は下記「次ループ候補」から選ぶ**。**variety 規則の状態 = extraction 1 連続中 (Loop 8 のみ)** を反映した判断を行う。

## 次ループ候補 (優先順位順、variety 規則を考慮)

### variety 規則の状態 (Loop 9 開始時点)

直近 4 ループ: Loop 5 (extraction) → 6 (extraction) → 7 (Whereby pivot) → 8 (extraction)。
- 連続カウント: extraction が **1 連続中** (Loop 8 のみ)
- Loop 9 で extraction 続けると **2 連続**、Loop 10 で 3 連続到達 = Loop 11 で variety pivot 検討
- **安全選択**: Loop 9 で Phase 2-B (ModelManager) を選び、Loop 10 で variety pivot (frontend or 新サービス) = 規則を素直に発動

### 最優先 (priority 1〜4 への直接寄与)

#### Phase 2-B: ModelManager の transcription_model_manager.rs 抽出 (規模 M, 価値中-高)
- 抽出対象: `pub struct ModelManager` + `impl ModelManager` (line 327-555 周辺、~150 行)
- 独立性: model 管理 (download / load / list) = WhisperLocal / WhisperStream とは独立性高い
- リスク: 中 (Tauri state 管理、ModelInfo は既に transcription_types.rs に分離済 = import 整理が必要)
- 推奨理由: Loop 9 候補として最も安全側 (Phase 1 + Phase 2-A 最小と同パターン)
- 工数: 1 ループ完結可能

#### Phase 2-A 続き: WhisperLocal struct + impl の transcription_whisper.rs 抽出 (規模 M, 価値中-高)
- 抽出対象: `pub struct WhisperLocal` + `impl WhisperLocal` + `impl TranscriptionEngine for WhisperLocal` (line 108-200 周辺、~80-100 行)
- リスク: 中 (TranscriptionEngine trait は transcription_traits.rs に分離済 = import 整理必要、whisper_rs 依存)
- 推奨理由: Phase 2-A の継続、純粋関数抽出後の自然な次スライス
- 工数: 1 ループ完結可能

#### Phase 2-A 続き: WhisperStream struct + impl の transcription_whisper_stream.rs 抽出 (規模 M-L, 価値中-高)
- 抽出対象: `pub struct WhisperStream` + `impl WhisperStream` + `impl TranscriptionStream for WhisperStream` (line 200-298 周辺、~150 行)
- リスク: 中-高 (resampler 状態 + buffer 管理、calculate_rms / is_tail_silent への参照あり)
- **1 ループ超過リスクあり** = Loop 10 以降に回す方が安全

#### Phase 2-A 続き: Audio resampling (sinc_params / resample_audio) の audio_utils.rs 統合 (規模 M, 価値中)
- 抽出対象: `fn sinc_params` + `fn resample_audio` (line 932-1029 周辺、~100 行)
- リスク: 中 (rubato 依存 + WhisperStream からの参照)
- 推奨: Phase 2-A 最小と同じ統合先パターン (audio_utils.rs)、Loop 9 候補として価値中

### 中優先 (variety pivot 候補、priority 2 への直接寄与)

#### 新サービス追加 (Loop 10-11 推奨、Whereby と異なる軸)
- **GoToMeeting** (`*.goto.com` または `meet.gotomeeting.com`): 仕様調査必要、URL 形式は `meeting/<meeting-id>`
- **Slack Huddle**: huddle URL は `slack.com/...` だが、ブラウザ URL ではなく desktop アプリ経路 = bundle id 検知が主、URL 検知は限定的
- **Discord** (`discord.com/channels/<server>/<channel>`): server URL 形式の判定難易度中
- いずれも仕様調査必須 = research 担当起動推奨

### 中優先 (variety pivot 候補、priority 6 / 8 / 9 への寄与)

#### frontend 軸 pivot (主観的判断のリスクあり、慎重に選ぶ)
- TranscriptionControls.tsx は既に aria 属性充実 = priority 9 への寄与は浅い
- LiveCaptionWindow.tsx (580 行) / MeetingDetectedBanner.tsx (526 行) は規模あり = 責務分離 docs commit 候補 (Loop 4 docs precedent と同パターン)
- 推奨: 浅 grep で具体的な不足を 1 つ特定してから worker 発注、主観的探索は避ける

### 低優先 (継承、再考の価値限定的)

#### B1. Microsoft Teams window title fallback
- mjc-main-20260505-3 で批判的に却下済 (前任者 rustdoc 注記 line 555-557 との矛盾)
- 後継が再評価する場合、まず handoff prompt の B1 提案 + 前任者 rustdoc 注記 + mjc-main-20260505-3 / 4 の却下根拠を読み、批判的判断する

#### A1. Webex 日本語 window title (`Webex ミーティング`)
- 規模: 極小 (3-4 行)
- 「Webex sweep からの完全 variety pivot を 4 セッション維持」を破る
- Loop 12-13 程度まで間隔を空けるのが安全

### 低優先 (harness 衛生、未着手)

#### H. 大量 untracked ファイル整理判断
- `docs/handoff/`, `docs/worker-prompts/` に 100+ untracked ファイル (本セッションで +3 件追加)
- ユーザー直伝指示があるまで保留

#### I. AGENT_LOG.md 24,400+ 行の archive 戦略
- 未着手、未消化

#### J. scripts/claude-agent-start-worker.sh の stdin redirect 化
- **本セッションで発見した tmux 引数長制限の harness 改善**
- 現状: `tmux new-session -d -s NAME "claude -p \"$(cat $FILE)\""` で prompt が引数展開 → command line 長制限ヒット (4209 bytes でも too long エラー)
- 推奨: `tmux new-session -d -s NAME "cat $FILE | claude -p"` で stdin pipe 経由 → 完全回避
- 規模: スクリプト 1 行修正、価値高 (将来の worker 発注で同じ罠を回避)

## 検証制約 (再掲)
- cmake あり → cargo test 674 件全 pass (verify.sh OK)
- frontend test framework 未導入 → npm run build (tsc + vite build) を主検証として運用
- 課金禁止 (elevenlabs/openai 系の実 API 叩きは厳禁、unit test 範囲のみ)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- Keychain 実通信禁止 (macOS 権限ダイアログ防止)
- Apple SpeechAnalyzer 実通信禁止 (macOS 権限ダイアログ防止)
- メインは原則アプリコード/ハーネスを直接編集しない (worker に発注、AGENT_LOG.md SESSION SUMMARY のみメイン直接編集の precedent あり、本セッションでも docs/architecture/* / handoff/* はメイン直接編集 precedent 継承)

## ハーネス使用法 (簡略、stdin redirect 方式を追記)
- 調査: `scripts/claude-agent-start-research.sh mjc-research-<topic> /path/to/prompt.txt` (haiku)
- 作業 (従来): `scripts/claude-agent-start-worker.sh mjc-worker-<topic> /path/to/prompt.txt` (sonnet)
- **作業 (新方式、tmux 引数長制限回避)**: 直接 `tmux new-session -d -s mjc-w-<short> "cat /tmp/prompt.txt | claude --model sonnet --dangerously-skip-permissions -p 2>&1 | tee /tmp/log.txt"` (本セッション 2 連続成功 precedent)
- 検証: `bash scripts/agent-verify.sh <変更ファイル群>` (rust/frontend を自動 skip 判定)
- コミット: `bash scripts/agent-commit.sh "メッセージ" <files>` または直接 `git add ... && git commit -m "..."`
- AGENT_LOG.md 末尾 SUMMARY 追記: ファイルへ Write → `cat <file> >> AGENT_LOG.md` で append (Edit ツールでは末尾 `---` が unique にならない)
- watchdog: `mjc-watchdog` セッションが既に走っているのでそのまま
- canonical 名移譲: `bash scripts/agent-adopt-main.sh mjc-main-20260505-5 mjc-main`
- worker 完走確認: **`git status --short` で対象ファイル + AGENT_LOG.md の `M` 表示確認** (連続 33 ループで実証した mitigation pattern)
  - log file (`/tmp/log.txt` または `logs/agent/<session>.txt`) と tmux pane は補助観測手段
  - 観測時間目安: worker 起動 → 90-180 秒で対象ファイル + AGENT_LOG.md 両方に M 表示
  - **claude -p print mode は最終出力一括方式**: stdout は処理完了まで空のまま、AGENT_LOG.md M 表示を主観測手段にする

## worker prompt 必須要素 (33 連続セッションで実証済、継続適用、7 つ全て明示)

worker prompt には以下を **必ず** 含める:
1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」**
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」**
3. **「先頭は絶対に触らない」**
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示
6. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照**
7. **「行末の空白文字 (trailing whitespace) 禁止」**

## コミット周期目標
- 1 ループ 9-15 分前後を目標 (本セッション Loop 7 = ~11 分、Loop 8 = ~6 分、平均 ~8.5 分で **目標 15 分以内達成**)
- worker 1 件 ≒ 1 コミット (Loop 7 は feature + chore agent-log の 2 commit、Loop 8 は feature + AGENT_LOG.md 同梱の 1 commit、過去 Loop 5/6 と同パターン)
- 累積 worker 完走 109/109 (本セッション +2 件 = Loop 7/8 worker、100% 維持)

## context 管理
- 70% 超で次ハンドオフ判断、85% 超で必ずアクション、watchdog の overflow 自動 /clear に最終的に任せる方針
- ハンドオフ時は `docs/handoff/mjc-main-YYYYMMDD-N.txt` に prompt を書いて `scripts/claude-agent-handoff-main.sh` で起動、`scripts/agent-adopt-main.sh` で canonical 名移譲
- 本セッションは 2 ループで早期ハンドオフ判断 = 引き継ぎ時の context (大量必読ドキュメント + grep + diff) を考慮した予防的判断 = mjc-main-20260504-39 の「1 ループ + 早期 handoff」precedent 強化版

## ユーザー直伝指示 (未消化)
- なし。watchdog からの nudge は本セッション中 2 件 (Loop 7 worker 起動後 + Loop 8 worker 起動後) を「Loop 進行 + commit」で消化済

# あなたの最初のアクション (再掲)
1. 必読ドキュメントを読む (AGENT_LOG.md は末尾 400 行のみ、docs/architecture/transcription-refactor-plan.md は完全読)。
2. `git status --short --branch` / `git log --oneline -15` / `tmux list-sessions | grep mjc` / `tail -400 AGENT_LOG.md` で現状確認。
3. **canonical 名移譲確認**: `tmux display-message -p '#S'` で現セッション名確認。`mjc-main` なら不要、`mjc-main-20260505-5` なら `bash scripts/agent-adopt-main.sh mjc-main-20260505-5 mjc-main` を実行 (冪等)。
4. **戦略転換の核心を継承**: 26 連続「Debug 軸補強」 + 5 連続「Webex sweep」**両方** に戻らない。各ループで AGENTS.md 優先順位 1〜9 のどれに直接寄与するかを明示できるタスクを選ぶ。**同じパターン 3 ループ続いたら 4 ループ目に入る前に variety pivot を検討**。
5. **最初の Loop 候補**: variety 規則「extraction 1 連続中 (Loop 8 のみ)」を踏まえ、**Loop 9 で Phase 2-B (ModelManager) または Phase 2-A 続き (WhisperLocal) を選ぶのが安全側**。Phase 2-A WhisperStream は規模 M-L で 1 ループ超過リスク → Loop 10 以降推奨。
6. **実コードの構造 / 関数名 / field 名は必ず grep/sed で確認** (handoff サマリの予測は誤認の precedent あり、grep を Read で精読する重要性は本セッションでも再確認)
7. 待機モード禁止。final answer で停止せず、改善ループを継続する。
8. **worker prompt には worker prompt 必須要素 7 つ (上記) を含めて AGENT_LOG.md 先頭追記事故 + trailing whitespace warning を防止**
9. **worker 完走確認は `git status --short` を主観測手段とする** (連続 33 ループで実証した mitigation pattern、log file は補助、観測時間目安 90-180 秒、claude -p の最終出力一括方式を理解)
10. **worker 起動は本セッションで確立した stdin redirect 方式 (`cat /tmp/prompt.txt | claude -p`) で行う**: 従来の `claude -p "$(cat $FILE)"` 方式は tmux 引数長制限で失敗することがあるため (4209 bytes prompt でも too long エラー precedent あり)。/tmp/ 配下の短いファイル名でプロンプトを保存 → `tmux new-session -d -s NAME "cat /tmp/prompt.txt | claude --model sonnet --dangerously-skip-permissions -p 2>&1 | tee /tmp/log.txt"`
