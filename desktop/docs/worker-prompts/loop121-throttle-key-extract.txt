あなたは meet-jerky プロジェクトの worker (mjc-worker-loop121-throttle-key-extract) です。
Loop 121 = rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸 = `src-tauri/src/app_detection.rs` の **parse_throttle_key_to_display_name** 1 関数を新 file `src-tauri/src/app_detection_throttle_key.rs` に抽出する作業を担当します。Loop 119 (meeting_classifier 抽出) と同 paradigm。

# 戦略 (重要、Loop 119 教訓継承)

- **WATCHED_BUNDLE_IDS const は app_detection.rs 残置** (handle_detection / macos module / 多数 caller があるため移動リスク回避)
- **WATCHED_BUNDLE_IDS の visibility を `pub(crate)` に変更**: 新 file から `use crate::app_detection::WATCHED_BUNDLE_IDS;` で参照可能にする
- **MatchStrategy enum (L44) も `pub(crate)` 化**: WATCHED_BUNDLE_IDS の type が field として見える必要があるため (rustc visibility ルール)
- 新 file で `use crate::app_detection::WATCHED_BUNDLE_IDS;` で参照
- app_detection.rs に `pub use crate::app_detection_throttle_key::parse_throttle_key_to_display_name;` を追加 = **caller L271 と test 9 件 (super::* import) 全て無変更で動く API 互換維持** (Loop 119 precedent と同 pattern)
- alternation pattern: K(120) → rust(121) = 13 連続維持
- paradigm: 純粋関数機能分離軸 rust 版 + 新 file 作成軸 (Loop 119 と同 paradigm)
- scope 24 軸目開拓 (新 file `app_detection_throttle_key.rs`)
- **責務階層精査済 (メイン批判判断)**: throttle_key parser は URL parser (url_helpers) や meeting service 分類 (meeting_classifier) とは責務階層が異なる文字列パターンマッチング処理 = 統合不適切 = 新 file 作成が妥当 (Loop 119 教訓継承)

# 最初に必ず実施

1. `tail -350 AGENT_LOG.md` で末尾だけ読む (~33,051 行 / ~4MB あるので Read 全体禁止)。AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は **必ずファイル末尾** に行う。**先頭は絶対に触らない**。
2. `tail -10 AGENT_LOG.md` で末尾の `---` 区切りを確認
3. `wc -l src-tauri/src/app_detection.rs` で現在行数確認 (期待値: 2617 行)
4. `sed -n '396,436p' src-tauri/src/app_detection.rs` で parse_throttle_key_to_display_name の全体 (doc + body) を確認 (~40 行)
5. `sed -n '40,70p' src-tauri/src/app_detection.rs` で MatchStrategy enum + WATCHED_BUNDLE_IDS const 定義確認
6. `cat src-tauri/src/app_detection_meeting_classifier.rs` で precedent 新 file 構造を参考
7. `head -15 src-tauri/src/lib.rs` で mod 登録順 (アルファベット) を確認

# 作業範囲 (担当範囲外の編集禁止)

## 1. 新 file 作成: `src-tauri/src/app_detection_throttle_key.rs`

内容 (~50 行 = doc comment + use 文 + 関数 1 件):

```rust
//! `last_seen_secs` HashMap の throttle_key を会議アプリ表示名に解釈する純粋関数。
//!
//! 3 つの key 形式 (アプリ経路 = bundle_id 単独 / browser 経路 = `"browser:..."` /
//! window-title 経路 = `"window-title:..."`) を文字列パターンマッチングで分解し、
//! 表示名 (Zoom / Google Meet 等) に変換する。
//! `app_detection_url_helpers` の URL parser や `app_detection_meeting_classifier`
//! の service 分類とは責務階層が異なる (key string 分解 vs URL/title 構造解析)。

use crate::app_detection::WATCHED_BUNDLE_IDS;

/// throttle_key を会議アプリ表示名に変換する純粋関数。
///
/// `last_seen_secs` HashMap に格納される 3 つの key 形式を解釈する:
/// - **アプリ経路 (handle_detection)**: bundle_id 単独 (例: `"us.zoom.xos"`) → `WATCHED_BUNDLE_IDS` lookup で `Some("Zoom")`
/// - **browser 経路**: `"browser:<bundle_id>:<service>:<host>"` → 3 つ目のセグメント (service) を `Some` で返す
/// - **window-title 経路**: `"window-title:<bundle_id>:<service>"` → 3 つ目のセグメント (service) を `Some` で返す
/// - 上記いずれにも該当しない不正形式・未知 bundle_id は `None` を返す
///
/// `check_all_inactive_bundles` の iteration で `last_seen_secs` の key 全件巡回時に
/// display name を解決するために呼ばれる。
pub(crate) fn parse_throttle_key_to_display_name(key: &str) -> Option<String> {
    if let Some(rest) = key.strip_prefix("browser:") {
        // "browser:<bundle_id>:<service>:<host>" → splitn(3, ':') → [bundle_id, service, host]
        let parts: Vec<&str> = rest.splitn(3, ':').collect();
        if parts.len() < 2 {
            return None;
        }
        let service = parts[1];
        if service.is_empty() {
            return None;
        }
        return Some(service.to_string());
    }
    if let Some(rest) = key.strip_prefix("window-title:") {
        // "window-title:<bundle_id>:<service>" → splitn(2, ':') → [bundle_id, service]
        let parts: Vec<&str> = rest.splitn(2, ':').collect();
        if parts.len() < 2 {
            return None;
        }
        let service = parts[1];
        if service.is_empty() {
            return None;
        }
        return Some(service.to_string());
    }
    // アプリ経路: bundle_id 単独 → WATCHED_BUNDLE_IDS lookup
    WATCHED_BUNDLE_IDS
        .iter()
        .find(|(bundle_id, _, _)| *bundle_id == key)
        .map(|(_, app_name, _)| app_name.to_string())
}
```

**重要**:
- 元の doc comment を全て保持 (省略禁止)
- visibility は `pub(crate)` (元は `fn` = file-private、外部 module から呼ばれないが Loop 119 同様 `pub` 化で API 互換性維持)
- ただし re-export 維持のため、元 file の `fn` は **削除** + `pub use` re-export で同名公開

## 2. `src-tauri/src/app_detection.rs` の修正

### 2a. MatchStrategy enum の visibility 変更 (L44)

```rust
// 修正前: enum MatchStrategy {
// 修正後: pub(crate) enum MatchStrategy {
```

### 2b. WATCHED_BUNDLE_IDS const の visibility 変更 (L51)

```rust
// 修正前: const WATCHED_BUNDLE_IDS: &[(&str, &str, MatchStrategy)] = &[
// 修正後: pub(crate) const WATCHED_BUNDLE_IDS: &[(&str, &str, MatchStrategy)] = &[
```

### 2c. parse_throttle_key_to_display_name 関数削除 + re-export 追加

L406-L436 の `parse_throttle_key_to_display_name` 全体 (doc 含む = ~40 行 = L396-L436) を削除。

削除位置に **re-export** を追加:

```rust
// parse_throttle_key_to_display_name は app_detection_throttle_key に移動。
// caller + test 互換性維持のため re-export。
pub use crate::app_detection_throttle_key::parse_throttle_key_to_display_name;
```

### 2d. caller (L271) と test 9 件 (L1273-L1353) は **変更不要**

`pub use` re-export により、`super::*` import + 直接呼び出しの test も全て無変更で動く (Loop 119 precedent と同じ)。

## 3. `src-tauri/src/lib.rs` に mod 登録追加

現在 `mod app_detection_notification;` の後に `mod app_detection_throttle_key;` を追加 (アルファベット順)。

実際の挿入位置確認: `grep -n "mod app_detection_" src-tauri/src/lib.rs` で確認し、`app_detection_notification` の **直後** に挿入する。

```
mod app_detection_notification;
mod app_detection_throttle_key;  // ← 新規追加
mod app_detection_url_helpers;
```

# 検証

```bash
cd src-tauri && cargo fmt --check && cargo clippy --lib --tests -- -D warnings && cargo build --lib && cargo test --lib 2>&1 | tail -5 && cd ..
bash scripts/agent-verify.sh src-tauri/src/app_detection.rs src-tauri/src/app_detection_throttle_key.rs src-tauri/src/lib.rs
```

期待結果:
- cargo fmt --check: OK
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo build --lib: エラーなし
- cargo test --lib: **704 passed / 0 failed (件数完全不変)**
- agent-verify.sh: 全項目 OK

# AGENT_LOG.md 末尾追記

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に以下のエントリを追記:

```
[mjc-main-20260505-62 Loop 121 / 2026-05-06]
worker: mjc-worker-loop121-throttle-key-extract (作業)
範囲: src-tauri/src/app_detection.rs (-40 行 ~/+ 3 行 re-export + 2 行 visibility 変更 = 純減 ~35 行 想定) + src-tauri/src/app_detection_throttle_key.rs (新規 ~50 行) + src-tauri/src/lib.rs (mod 登録 1 行追加)
内容: app_detection.rs の parse_throttle_key_to_display_name (L396-L436 = doc + body 約 40 行) を新 file app_detection_throttle_key.rs に抽出。WATCHED_BUNDLE_IDS const は app_detection.rs 残置 (handle_detection/macos module からも参照のため移動リスク回避) + visibility を `pub(crate)` 化。MatchStrategy enum も `pub(crate)` 化 (WATCHED_BUNDLE_IDS の field type rustc visibility ルール対応)。app_detection.rs に pub use re-export 追加 = caller L271 + test 9 件 (super::* import) 全て無変更で API 互換維持。新 file 内 use 文 = WATCHED_BUNDLE_IDS のみ。lib.rs アルファベット順 (app_detection_notification の直後 + app_detection_url_helpers の直前) に mod 登録追加。**rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸 = scope 24 軸目開拓 (app_detection_throttle_key = 新 file)**。**alternation pattern 13 連続成功** (K(118) → rust(119) → K(120) → rust(121))。**paradigm pivot (K → rust)**。Loop 119 = 同 paradigm 反復 + 責務階層精査済 (throttle_key parser は URL parser/meeting classifier と責務階層異なる = 新 file 作成軸が妥当)。
振る舞い: cargo test (lib) 704 件件数完全不変、cargo clippy + cargo fmt + cargo build 全 OK。frontend 無変更のため npm run build 不要 (前 Loop 116 で確認済)
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(rust): app_detection の parse_throttle_key_to_display_name を新 file app_detection_throttle_key.rs に抽出 (re-export で API 互換維持)
AGENTS.md priority: 1 (大型 rust file 責務分離継続 + scope 24 軸目開拓)

---
```

(`<REFACTOR_HASH>` は refactor commit の実 hash で置換。chore commit hash は意図的に書かない = Loop 60-120 で 60 連続成功した 2-commit pattern)

# 2-commit pattern (worker 自律 連続 60 ループ成功 = 完全安定 pattern 化)

1. **refactor commit (1 件目)**: 全 rust 変更をまとめて add + commit
   ```
   git add src-tauri/src/app_detection.rs src-tauri/src/app_detection_throttle_key.rs src-tauri/src/lib.rs
   git commit -m "refactor(rust): app_detection の parse_throttle_key_to_display_name を新 file app_detection_throttle_key.rs に抽出 (mjc-main-20260505-62 Loop 121, rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸, scope 24 軸目開拓, 大型 rust file 責務分離 37 file 目, WATCHED_BUNDLE_IDS + MatchStrategy は app_detection.rs 残置 + pub(crate) 化 + pub use re-export で API 互換維持, AGENTS.md priority 1 寄与)"
   ```
2. **refactor commit hash 取得**: `git log -1 --format=%H | head -c 7`
3. **AGENT_LOG.md 末尾追記**: 上記エントリの `<REFACTOR_HASH>` を実 hash に置換し、`tail -10 AGENT_LOG.md` で末尾確認後、Write ツールで一時 file に書いた後 `cat <tmp_file> >> AGENT_LOG.md` で末尾に append (Edit ツールでは末尾 `---` が unique にならない場合あり = Write 後 cat append 推奨)
4. **chore commit (2 件目)**: `git add AGENT_LOG.md && git commit -m "chore(agent-log): mjc-main-20260505-62 Loop 121 = app_detection の parse_throttle_key_to_display_name を新 file app_detection_throttle_key.rs に抽出 (構造分離 37 file 目, rust 軸, scope 24 軸目開拓, 純粋関数機能分離軸 rust 版 + 新 file 作成軸, alternation 13 連続成功, WATCHED_BUNDLE_IDS + MatchStrategy 残置 + pub(crate) + pub use re-export で API 互換維持)"` (chore commit 自身の hash は AGENT_LOG.md に書かない = 60 連続 precedent)

# 規模超過防止

- 編集対象 file: 3 つ (app_detection.rs + app_detection_throttle_key.rs (新規) + lib.rs) + AGENT_LOG.md
- app_detection.rs 削減: ~40 行削減 + ~3 行追加 (pub use + comment) + 2 行 visibility 変更 = 純減 ~35 行
- 新 file: ~50 行 (use 1 行 + doc 6 行 + 関数 1 件 doc + body)
- lib.rs: 1 行追加
- test 件数の上限: 704 件 (件数完全不変)
- cargo test 失敗 0 件

# 注意点

- **行末の空白文字 (trailing whitespace) 禁止**
- 大型ファイルは Read 全体禁止 = `tail`/`head`/`grep`/`sed -n 'X,Yp'` で対象範囲のみ参照
- WATCHED_BUNDLE_IDS const + MatchStrategy enum は **app_detection.rs に残置** (visibility のみ `pub(crate)` 化、移動禁止)
- test 9 件 (L1273-L1353) は **app_detection.rs 残置** (super::* import 経由で動く)
- macos module の `use super::{handle_browser_url_detection, handle_detection, WATCHED_BUNDLE_IDS};` (L452) は **変更不要** (super:: は同一 file の `pub(crate)` でも参照可能)

# 完走後のメイン報告

完走時に以下を main agent に報告:
- refactor commit hash + chore commit hash
- `wc -l src-tauri/src/app_detection.rs src-tauri/src/app_detection_throttle_key.rs` (期待: app_detection.rs ~2582 行 / throttle_key ~50 行)
- cargo test 結果 (704 passed)
- agent-verify.sh 結果

以上。
