あなたは作業担当エージェント (Claude Code, print mode) です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。
担当範囲は厳密に以下のみ:
- src-tauri/src/settings.rs の mod tests 末尾への test 関数 3 件追加
- AGENT_LOG.md への末尾追記 1 件 (worker 完走報告)

**他のファイルは一切編集しないでください。** settings.rs の関数本体 (l.1-260) や既存 test (l.266-577 既存) も触らないでください。

## 必読 (順序固定)
1. AGENTS.md
2. docs/product-concept.md
3. docs/agent-harness-claude.md
4. **AGENT_LOG.md は 20800+ 行 / 1.7MB+ で巨大なため絶対に Read 全体を実行しないでください。** `tail -350 AGENT_LOG.md` 相当 (= Read で offset 指定する) で末尾だけ参照してください。AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**。新規追記は **必ずファイル末尾** に行います。**先頭は絶対に触らないでください。**

## 背景 (ループ概要)
- セッション: mjc-main-20260504-26 Loop 3
- 候補種別: S (settings.rs / 「fail-safe semantic の executable specification 化」)
- 直前 commit: df34f05 (mjc-main-26 Loop 2) で SessionManager::start title passthrough 3 軸を CI 固定済 (527 → 530 passed)
- `permission_status_to_string` (l.202-210) は match の `_ => "denied"` で「不明値 → denied 側に倒す」fail-safe 設計 = 権限ダイアログ周りで「誤って許可を与える誤改修」を防ぐ重要契約
- 既存 test (l.295-304) は 3 known 値 (UNDETERMINED/DENIED/GRANTED) + 1 unknown 値 (99) を test 済だが、**負値 / 隣接 unknown / 極端値の 3 軸が未保護**
- 1 ループで上記 3 軸を埋めれば fail-safe 契約が完全網羅される。
- **優先順位 #9 (アクセシビリティ・権限・透明性) への直接寄与** = 録音状態の透明性に関わる安全性契約

## 対象関数 (実装は変更しない、test 追加のみ)
- l.198-200 定数:
  ```rust
  const PERMISSION_UNDETERMINED: i32 = 0;
  const PERMISSION_DENIED: i32 = 1;
  const PERMISSION_GRANTED: i32 = 2;
  ```
- l.202-210 `permission_status_to_string`:
  ```rust
  fn permission_status_to_string(status: i32) -> String {
      match status {
          PERMISSION_UNDETERMINED => "undetermined",
          PERMISSION_GRANTED => "granted",
          PERMISSION_DENIED => "denied",
          _ => "denied",
      }
      .to_string()
  }
  ```
- 既存 test (l.295-304):
  ```rust
  #[test]
  fn test_permission_status_to_string_maps_known_values() {
      assert_eq!(permission_status_to_string(PERMISSION_UNDETERMINED), "undetermined");
      assert_eq!(permission_status_to_string(PERMISSION_DENIED), "denied");
      assert_eq!(permission_status_to_string(PERMISSION_GRANTED), "granted");
      assert_eq!(permission_status_to_string(99), "denied");
  }
  ```

## 追加する test の場所
- src-tauri/src/settings.rs の `mod tests` 末尾
- 直前の test = `test_load_recovers_valid_fields_from_partially_invalid_json` (mod tests 内最後の test) の **直後** (= 該当 test の閉じカッコ `}` の次の空行) に追記
- mod tests の閉じ `}` (l.578 ファイル末尾) の **手前** に挿入する
- 位置確認: 事前に `tail -40 src-tauri/src/settings.rs` で末尾を確認すること

## 追加 test 3 件 (内容固定、自由解釈禁止)

### T1: 負値の fail-safe → "denied"
```rust
#[test]
fn permission_status_to_string_returns_denied_for_negative_values_as_fail_safe() {
    // 既存 l.295 (test_permission_status_to_string_maps_known_values) は known 3 値 + unknown 99 のみ。
    // 負値は match の `_ => "denied"` で受けられる。誤って許可を付与しないための fail-safe 契約 (#9 透明性)。
    // 「`_ => "granted"` に変える誤改修」「負値専用ブランチを追加して別文字列を返す誤改修」を遮断する装置。
    assert_eq!(
        permission_status_to_string(-1),
        "denied",
        "負値 -1 は fail-safe で 'denied' に倒す現契約。permission ダイアログで誤って granted を返さない安全性を CI 固定。"
    );
    assert_eq!(
        permission_status_to_string(i32::MIN),
        "denied",
        "i32::MIN (極端な負値) も fail-safe で 'denied' に倒す現契約。overflow ガードなしの match で `_` で受ける挙動を CI 固定。"
    );
}
```

### T2: 隣接 unknown 値 (3) の fail-safe → "denied"
```rust
#[test]
fn permission_status_to_string_returns_denied_for_value_adjacent_to_granted_as_fail_safe() {
    // PERMISSION_GRANTED = 2 の +1 隣接値 (3) が unknown であることを CI 固定。
    // OS 側の API が将来「PERMISSION_PROVISIONAL = 3」等の新値を返した場合に
    // 「明示的に新値の意味を考えてから対応する」ことを強制する装置 (silent な誤分類を防ぐ)。
    // 「3 を granted 扱いする誤改修」を遮断する。
    assert_eq!(
        permission_status_to_string(3),
        "denied",
        "PERMISSION_GRANTED (=2) 直後の 3 は unknown として 'denied' に倒す現契約。OS 側の新値追加に silent に追従しないための fail-safe を CI 固定。"
    );
}
```

### T3: i32::MAX の fail-safe → "denied"
```rust
#[test]
fn permission_status_to_string_returns_denied_for_i32_max_as_fail_safe() {
    // i32::MAX (2147483647) も match `_` で受ける fail-safe 動作を CI 固定。
    // 過去 mjc-main-24 (current_started_at_secs u64::MAX 保護) と同種の「型上限境界での truncation/overflow ガード」軸。
    // 「i32::MAX を特別扱いする (panic 等) 誤改修」を遮断する装置。
    assert_eq!(
        permission_status_to_string(i32::MAX),
        "denied",
        "i32::MAX (型上限) も fail-safe で 'denied' に倒す現契約。型境界で panic/overflow しない安全性を CI 固定。"
    );
}
```

## 規模超過防止
- **追加 test は厳密に 3 件**。**4 件目を追加しないでください。**
- **settings.rs の関数本体 (l.1-260) と既存 test (l.266-577) は一切変更禁止**。
- 既存 import / use 宣言 / mod tests の `use super::*` 等の構造も変更禁止。
- 大型ファイル (settings.rs: 578 行) は **Read 全体禁止 = `tail -50` や `Read(offset=..., limit=...)` で対象範囲のみ参照**。
- 既存 test との重複: T1/T2/T3 は不明値 99 (l.303) しかカバーしていないため追加で OK。
- AGENT_LOG.md は **絶対に先頭から書かない**。`tail -10 AGENT_LOG.md` で末尾の `---` を確認し、その **直後の行から追記** する (区切り線 `---` の手前ではなく **直後**)。

## 検証手順 (worker 内で実施)
1. `cd src-tauri && cargo fmt --check` (差分なし確認)
2. `cd src-tauri && cargo clippy -- -D warnings` (警告ゼロ確認)
3. `cd src-tauri && cargo test --lib --test-threads=1 -- 2>&1 | tail -10` (530 → 533 passed 確認)

## AGENT_LOG.md 末尾追記の最低要素
- 開始日時 (JST) / 担当セッション (`mjc-worker-permission-status-fail-safe-tests-20260504-26-3`) / 役割 (worker)
- 作業範囲 (settings.rs mod tests 末尾 + AGENT_LOG.md 末尾追記)
- 指示内容 (上記 T1/T2/T3 の 3 件追加)
- 結果 (cargo fmt OK / clippy 警告ゼロ / cargo test 530 → 533 passed)
- 変更ファイル (src-tauri/src/settings.rs, AGENT_LOG.md)
- 検証結果 (各検証コマンドの末尾 1-3 行 quote)
- 依存関係追加なし
- 失敗理由 (発生した場合のみ、なければ「なし」)
- 次アクション (「メインへ報告、commit 待ち」)

## 1 ターン完結
最後に変更内容、変更ファイル、検証結果、残リスクを 1 ターンで簡潔に報告してください。コミットは禁止 = メインが行います。
