あなたは meet-jerky の rust worker です. mjc-main Loop 201 のタスクを 2-commit pattern で完走してください.

# 最初に必ず実施

1. `tail -350 /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENT_LOG.md` で末尾だけ読む. **AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う**. 先頭は絶対に触らない.
2. `tail -10 /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENT_LOG.md` で末尾形状を再確認 (末尾の `---` を確認).

# タスク

`src-tauri/src/system_audio_format.rs` の error message (完全一致 3 callsite = 本番 1 + test 2) を **新 const 1 件を追加** + **3 callsite を const 参照に統一** する.

## 背景 (Loop 200 = secret_store.rs ERROR_KEYCHAIN_NOT_SUPPORTED 集約 paradigm の継承)

- 本 file には const 未確立 = **新規 const 追加軸 paradigm 反復 12 件目** = paradigm 系統樹拡張 11 件目反復 5 連続 (Loop 194 cloud_whisper.rs / Loop 198 transcription_error_payload.rs / Loop 200 secret_store.rs と同系統樹).
- 本番品質寄与: 本番 1 (L22 = validate_audio_format_properties 第 1 関門) + test 2 (L128 = error message assertion + L162 = short-circuit 順序契約 test) = 計 3 callsite を統一.
- AGENTS.md priority 1 (UI/log 文言契約 cohesion + DRY 違反解消) 寄与.
- alternation: rust(200) → rust(201) = 2 連続だが本番品質寄与で正当化 (handoff prompt 第二推奨候補).
- 関数戻り型 `Result<(), &'static str>` のため、const も `&'static str` で OK = `.to_string()` 不要 (Loop 200 secret_store.rs `Result<(), String>` と異なる callsite 形態).

## 具体手順

### Step 1: file 全体を Read

`src-tauri/src/system_audio_format.rs` (165 行 = 中規模、Read 全体 OK) を確認.

### Step 2: const 1 件を追加

L10 の `use screencapturekit::CMFormatDescription;` 直後 (= L11 の空行を維持しつつ) に以下 1 行を追加:

```rust
const ERROR_NON_F32_PCM_FORMAT: &str = "非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)";
```

**配置設計の根拠**:
- 本 const は **pure function `validate_audio_format_properties` (cfg なし)** と test 内で使用 = cfg ガード不要 = 全プラットフォームで定義
- visibility は `const` のみ (file 内のみで使用、`pub(crate)` 不要)
- 命名規則: SCREAMING_SNAKE_CASE で他 file の error const と並列

### Step 3: 3 callsite を const 参照に統一

完全一致 callsite 3 件を置換 (3 件すべて同 file 内、戻り型 `Result<(), &'static str>` のため `.to_string()` 不要):

- **L22** (本番 `validate_audio_format_properties` 第 1 関門 is_float check):
  ```rust
  // Before
  return Err("非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)");
  // After
  return Err(ERROR_NON_F32_PCM_FORMAT);
  ```

- **L128** (test `validate_audio_format_properties_error_messages_match_documented_strings_for_known_paths`):
  ```rust
  // Before
  assert_eq!(
      non_float.unwrap_err(),
      "非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)"
  );
  // After
  assert_eq!(non_float.unwrap_err(), ERROR_NON_F32_PCM_FORMAT);
  ```
  (test mod は `use super::*;` (L59) 経由で const 参照可能)

- **L162** (test `validate_audio_format_properties_returns_first_violation_when_multiple_invalid` short-circuit 順序契約):
  ```rust
  // Before
  assert_eq!(
      result,
      Err("非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)")
  );
  // After
  assert_eq!(result, Err(ERROR_NON_F32_PCM_FORMAT));
  ```

**触らないもの**:
- L149 のコメント `// 第 1 関門 (is_float check) が最初に発火し "非 f32 PCM..." を返す現挙動を固定。` = コメント内の部分文字列 = 触らない
- 他の error message (`"BigEndian フォーマット (NativeEndian が必要)"`, `"bits_per_channel が 32 ではない"`, `"channel 数が設定値と不一致"` 等 5 種類 = 各 2 callsite) = 別 semantic = 本ループでは触らない (future candidate として備考に記載)

### Step 4: 検証

`cd src-tauri` してから:
- `cargo test --lib` (704 件 passed / 0 failed = **件数完全不変**)
- `cargo clippy --lib --tests -- -D warnings` (警告ゼロ)
- `cargo fmt --check`
- `cargo build --lib` (エラーなし)

### Step 5: agent-verify.sh

`cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop && bash scripts/agent-verify.sh src-tauri/src/system_audio_format.rs`

### Step 6: 1 commit 目 (refactor)

`git add src-tauri/src/system_audio_format.rs && bash scripts/agent-commit.sh "refactor(rust): ERROR_NON_F32_PCM_FORMAT を system_audio_format.rs に集約 + 3 callsite を const 参照に統一 (mjc-main Loop 201, rust 軸 = const 集約 paradigm 反復 12 件目, paradigm 系統樹拡張 11 件目反復 5 連続 = Loop 194/198/200 precedent と同系統樹, '非 f32 PCM フォーマット (kAudioFormatFlagIsFloat 未設定)' 3 callsite (本番 1 = L22 第 1 関門 + test 2 = L128 文言契約 + L162 short-circuit 順序契約) を統一 + DRY 違反解消, 関数戻り型 &'static str で .to_string() 不要 = callsite 簡潔化, alternation rust(200) → rust(201) = 2 連続だが本番品質寄与で正当化, AGENTS.md priority 1 寄与)" src-tauri/src/system_audio_format.rs`

### Step 7: 2 commit 目 (AGENT_LOG.md append)

ファイルへ Write して `cat <file> >> AGENT_LOG.md` で append.

**重要**:
- entry テンプレート冒頭に `---` を **含めない** (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める)
- AGENT_LOG.md への append は **1 回のみ**. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む.
- **行末の空白文字 (trailing whitespace) 禁止**

entry テンプレート (冒頭 `---` なし、末尾 `---` 1 個):

```
[mjc-main Loop 201 / 2026-05-06]
worker: mjc-worker-loop201-non-f32-pcm-const (作業)
範囲: src-tauri/src/system_audio_format.rs (const ERROR_NON_F32_PCM_FORMAT 追加 + L22 (本番) + L128 (test 文言契約) + L162 (test short-circuit 順序契約) = 3 callsite を const 参照に統一)
変更: const ERROR_NON_F32_PCM_FORMAT を system_audio_format.rs に追加 (cfg ガードなし = pure function 内使用 = 全プラットフォームで定義) + L22 validate_audio_format_properties 第 1 関門 + L128 文言契約 test + L162 short-circuit 順序契約 test = 3 callsite を const 参照に統一 + 関数戻り型 `Result<(), &'static str>` のため `.to_string()` 不要 = callsite 簡潔化 + DRY 違反解消 + ネット行数 ~+1 行 (const 1 行追加 + callsite 3 件は同行書き換え + multiline assert を 1 行に圧縮可能箇所あり)
意義: rust 軸 = const 集約 paradigm 反復 12 件目達成 = paradigm 系統樹拡張 11 件目反復 5 連続 (Loop 194 cloud_whisper.rs / Loop 196 transcription_commands_helpers.rs / Loop 198 transcription_error_payload.rs / Loop 200 secret_store.rs / Loop 201 system_audio_format.rs) = 新規 const 追加軸 paradigm 反復 5 連続 + 本番 1 件 + test 2 件 = 計 3 callsite を統一 + UI/log 文言契約 cohesion 向上 + 文言改修時の整合性保証 + DRY 違反解消 + AGENTS.md priority 1 寄与 (品質/可用性 = error message 一貫性) + alternation rust(200) → rust(201) = 2 連続だが本番品質寄与で正当化 (handoff prompt 第二推奨候補) + paradigm 完全成熟期 cross-language 拡張更なる持続達成 = const 集約軸 frontend 10 + rust 5 = 計 15 連続
検証: cargo test (704 件件数完全不変) + cargo clippy --lib --tests -- -D warnings (警告ゼロ) + cargo fmt --check OK + cargo build --lib OK / agent-verify.sh は rust skip 判定 OK / grep `"非 f32 PCM フォーマット"` = 2 件 (const 定義行 L11 + L149 コメント) のみ確認
備考: paradigm 反復 12 件目達成 = rust 軸 const 集約軸の paradigm 完全成熟期 cross-language 拡張更なる持続 = paradigm 系統樹拡張 11 件目反復 5 連続 = const 集約軸 frontend 10 + rust 5 = 計 15 連続 = paradigm 系統樹拡張第 11 件目の連続反復観測継続. 同 file 内の他 error message 5 種類 (BigEndian/bits_per_channel が 32/bits_per_channel 取得不可/channel 数不一致/channel 数取得不可 = 各 2 callsite) は future candidate として残存 (規模 SS-S = 集約価値中). 後継候補 = R34 (K 軸 = Section 2.3 観測 54 件目) / R30'''' (system_audio_format.rs 残 5 種類 error message 集約 = 規模 SS-S) / R37 (大型 file 責務分離 52 件目候補) のいずれか採用検討.
---
```

(末尾は `---` 1 個 + 改行で締める. 冒頭は `---` を含めない = 前 entry の閉じ `---` を再利用)

`bash scripts/agent-commit.sh "chore(agent-log): mjc-main Loop 201 = ERROR_NON_F32_PCM_FORMAT を system_audio_format.rs に集約 (rust 軸, const 集約 paradigm 反復 12 件目, paradigm 系統樹拡張 11 件目反復 5 連続, 新規 const 追加軸 = Loop 194/196/198/200 precedent と同系統樹, 本番 1 + test 2 = 計 3 callsite 統一, alternation rust(200) → rust(201) = 2 連続だが本番品質寄与で正当化, AGENTS.md priority 1 寄与) のエントリ追記" AGENT_LOG.md`

### Step 8: 完走報告

`git status --short` (working tree clean を確認) と `git log --oneline -3` (2 commit 反映を確認) を出力.

# 規模超過防止 (重要)

- **担当範囲外の編集禁止**: src-tauri/src/system_audio_format.rs と AGENT_LOG.md のみ編集. 他の rust file は触らない.
- **test 件数の上限**: cargo test 後の件数は 704 件 (passed) 完全不変. 件数が変動したら原因を分析して報告.
- **大型ファイル Read 全体禁止**: AGENT_LOG.md は **tail で末尾のみ読む**. 全体 Read 禁止 (~3.2MB+).
- **system_audio_format.rs (165 行) は中規模** = Read 全体 OK.
- **同 file 内の他 error message 5 種類は触らない**: scope creep 防止のため、本ループでは "非 f32 PCM" 3 callsite のみに限定. 他は future candidate.

# 制約 (再掲)

- `cargo` は `cd src-tauri` してから実行 (desktop 直下に Cargo.toml なし)
- `--no-verify` 禁止
- `--dangerously-skip-permissions` は harness 内のみ
- 1 ループ目標 9-15 分以内 (規模 SSS = ~3-5 分目安)
- 完了したら mjc-main に戻る (= worker process exit すれば自動)

完走後、メインが check するため `git status --short` clean + `git log --oneline -3` で 2 commit 確認できる状態にしてください.
