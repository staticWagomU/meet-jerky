あなたは meet-jerky の sonnet worker です。Phase 2-A 最後の責務分離 = WhisperStream を transcription_whisper_stream.rs に抽出してください (Tidy First, behavior-preserving)。

# 必読 (順守必須)
1. **AGENT_LOG.md は ~24,500 行の巨大ファイルなので Read 全体禁止**。`tail -350 AGENT_LOG.md` で末尾だけ読め。
2. AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**。新規追記は **必ずファイル末尾** に行う。**先頭は絶対に触らない**。
3. 末尾追記の具体手順: (a) `tail -10 AGENT_LOG.md` で末尾の `---` セパレータを確認 → (b) 末尾の `---` 直後に新規エントリを追記 → (c) 追記後 `tail -50 AGENT_LOG.md` で構造の整合を確認。
4. 行末の空白文字 (trailing whitespace) 禁止。`git diff --check` で確認すること。
5. 担当範囲外の編集禁止: 下記「変更ファイル一覧」以外は編集しない。
6. cargo test --lib の合計件数は **674 passed のまま不変** が指標 (Tidy First = 振る舞い不変)。件数増減があれば即座に作業中断して報告。
7. docs/architecture/transcription-refactor-plan.md は完全読 OK (~130 行、参考用)。transcription.rs / src-tauri/src/lib.rs は **必要範囲のみ** Read (transcription.rs は L1-235 + L600-630 + L820-900 程度、L1110-2599 は触らない)。

# Phase 2-A WhisperStream 抽出の方針

## 目的
- transcription.rs (現在 2589 行) から WhisperStream の実装を transcription_whisper_stream.rs に分離
- Phase 2-A の最後の責務 (WhisperLocal / Audio resampling は完了済) = Phase 2-A 完全完了
- AGENTS.md 優先順位 1 (クラッシュ修正) への予防的寄与継続

## 抽出対象 (transcription.rs L34-222 周辺、約 189 行)
- L33-35: コメントセクションヘッダー (`// WhisperStream — Whisper 用ストリーミング実装`)
- L37-44: rustdoc コメント
- L45-59: `pub struct WhisperStream` (10 fields、ctx は `#[cfg(not(test))]` / `#[cfg(test)]` の二重定義)
- L61-147: `impl WhisperStream` (`pub(crate) fn new` + `fn flush_full_chunks` + `fn run_inference`)
- L149-222: `impl TranscriptionStream for WhisperStream` (feed + drain_segments + finalize)

## 依存関係 (要解決)

### 1. 定数 6 つ (transcription.rs に残す = WHISPER_SAMPLE_RATE 残し判断と同じ哲学)
- L603 `WHISPER_SAMPLE_RATE` / L610 `CHUNK_DURATION_SECS` / L613 `CHUNK_SAMPLES` / L616 `MIN_FLUSH_SAMPLES` / L619 `SILENCE_LOOKBACK_SAMPLES` / L623 `SILENCE_THRESHOLD_RMS`
- これらの定数を **pub(crate) に昇格** (現在 private const)
- 新モジュール側で `use crate::transcription::{WHISPER_SAMPLE_RATE, CHUNK_DURATION_SECS, CHUNK_SAMPLES, MIN_FLUSH_SAMPLES, SILENCE_LOOKBACK_SAMPLES, SILENCE_THRESHOLD_RMS};` で import
- 移動しない理由: transcription.rs 内 L1408+ のテスト群 (`SILENCE_THRESHOLD_RMS` / `SILENCE_LOOKBACK_SAMPLES` / `MIN_FLUSH_SAMPLES` 参照) との互換性維持、Loop 12 の WHISPER_SAMPLE_RATE 残し判断との哲学的一貫性

### 2. audio_utils 由来 (新モジュール側で再 import)
- `is_tail_silent` / `sinc_params` / `RESAMPLE_CHUNK_SIZE` → `use crate::audio_utils::{is_tail_silent, sinc_params, RESAMPLE_CHUNK_SIZE};`

### 3. WhisperLocal::transcribe_chunk (pub(crate) 既存)
- `use crate::transcription_whisper_local::WhisperLocal;` で直接 import (現在は transcription_traits 経由でなく WhisperLocal 直接呼び出し)
- L123, L129 の `WhisperLocal::transcribe_chunk(...)` は維持

### 4. trait / 型
- `use crate::transcription_traits::{StreamConfig, TranscriptionStream};`
- `use crate::transcription_types::{TranscriptionSegment, TranscriptionSource};`

### 5. 外部 crate
- `use rubato::{Resampler, SincFixedIn};`
- `use std::sync::Arc;`
- `use whisper_rs::WhisperContext;` (cfg(not(test)) と cfg(test) の両方で必要)

### 6. test helper との関係 (L841-854)
- transcription.rs の test mod に `stream_with_missing_resampler(resample_input_buffer: Vec<f32>) -> WhisperStream` があり、struct field を直接構築する。
- 解決策: **WhisperStream の全 10 field を `pub(crate)` に昇格** (現在 private)。これで transcription.rs の test helper が変更不要で構築可能。

## 互換層
- transcription.rs に L31 の WhisperLocal 互換層と同パターンで以下を追加:
  ```rust
  pub use crate::transcription_whisper_stream::WhisperStream;
  ```
- 既存 import (`crate::transcription::WhisperStream` for transcription_whisper_local.rs:5) は再エクスポートで変更不要

## lib.rs
- `mod transcription_whisper_stream;` を alphabetical order で追加 (transcription_whisper_local の後、transcription_traits 等よりは先)。実コードを `grep -n "^mod transcription" src-tauri/src/lib.rs` で確認してから挿入位置決定。

# 変更ファイル一覧 (これ以外は編集禁止)
1. `src-tauri/src/transcription_whisper_stream.rs` (新規作成、~190 行)
2. `src-tauri/src/transcription.rs` (約 189 行削除 + `pub use` 互換層 1 行追加 + 定数 6 つに `pub(crate)` 修飾子追加 + struct fields に pub(crate) 修飾子追加 → ただし struct ごと移動なので transcription.rs 側の struct field 修飾子変更は不要、新ファイル側で pub(crate) で書く)
3. `src-tauri/src/lib.rs` (`mod transcription_whisper_stream;` 1 行追加)
4. `AGENT_LOG.md` (末尾追記、別 commit ではなく feature commit に含める)

# 検証コマンド (必ず全て実行)
```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
bash scripts/agent-verify.sh src-tauri/src/transcription_whisper_stream.rs src-tauri/src/transcription.rs src-tauri/src/lib.rs
# 内訳: cargo test --lib (674 passed 件数不変)、cargo clippy --lib -- -D warnings (警告ゼロ)、cargo fmt --check
```

検証成功後:
```bash
git diff --check  # trailing whitespace 検出
```

# AGENT_LOG.md 末尾追記の雛形

```
## 2026-05-05 JST mjc-main-20260505-7 Loop 13 (Phase 2-A WhisperStream 抽出 → transcription_whisper_stream.rs)
- 担当セッション: mjc-main (canonical) = mjc-main-20260505-7 (メイン, opus)
- 役割: Phase 2-A 最後の責務分離 = WhisperStream を新モジュールに分離 = Phase 2-A 完全完了
- 作業範囲: src-tauri/src/transcription_whisper_stream.rs (新規) + src-tauri/src/transcription.rs (削除 + 互換層 + 定数 pub(crate) 昇格) + src-tauri/src/lib.rs (mod 宣言)
- 指示内容: WhisperStream struct + impl + impl TranscriptionStream for WhisperStream の ~189 行抽出。定数 6 つは pub(crate) 昇格で transcription.rs 残し (テスト互換性 + WHISPER_SAMPLE_RATE 残し判断との哲学的一貫性)。struct fields は pub(crate) で test helper との互換性維持。
- 実行結果: cargo test --lib 674 passed (件数不変)、cargo clippy --lib -D warnings 警告ゼロ、cargo fmt --check OK、transcription.rs 累計削減 ~600 行 (元 2999 → 約 2400)
- 戦略判断: extraction 3 連続到達 (Loop 11 + Loop 12 + Loop 13) = Loop 14 で必須 variety pivot 約束、Phase 2-A 完全完了で Phase 3 (TranscriptionManager) に進める段階
- 次アクション: Loop 14 で variety pivot 必須発動 (新サービス検知 / harness 衛生 / frontend 軸)
---
```

(実行結果は worker が実測値で書き換える)

# 規模超過防止
- 1 ループ完結が目標 (= 15 分以内)
- 範囲が膨らんだら手前で止めて報告 (Tidy First = 1 件 1 抽出原則)
- 件数増減 (674 件以外) を観測したら即座に中断して原因報告
