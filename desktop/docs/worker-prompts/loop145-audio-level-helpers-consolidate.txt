あなたは meet-jerky の worker です。Loop 145 = frontend 軸 17 件目 = **重複削除 + 既存 file 拡張軸 (paradigm 新規確立)** を担当します。

# 必読 (順番)

1. **AGENT_LOG.md の末尾 350 行を `tail -350 AGENT_LOG.md` で読む** (先頭は絶対に触らない、ファイル全体 Read 禁止 = ~33,588 行ある)。
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md (priority 1 = コード品質、DRY 原則)。

# 重要な制約 (絶対)

- **AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う。**
- **先頭 (L1 〜) は絶対に触らない。**
- **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に新エントリを追記する。
- **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照** (本 Loop の対象 file は小さい = 全 Read OK)。
- **行末の空白文字 (trailing whitespace) 禁止。**
- 担当範囲外の編集禁止。本 Loop 145 は **src/components/AudioLevelMeter.tsx** + **src/utils/audioLevelHelpers.ts** + **AGENT_LOG.md** のみ。
- **2 commits を確実に作成する**: refactor + chore の 2-commit pattern。

# 本 Loop の目的

`AudioLevelMeter.tsx` の 2 個の純粋関数を `audioLevelHelpers.ts` に統合する。1 つは既存と完全重複なので **削除**、もう 1 つは **移動** (export 化)。

## 発見済の問題 (メインで精査済)

1. **DRY 違反**: `AudioLevelMeter.tsx` L23-L28 の `sanitizeAudioLevelForDisplay(level)` は、`audioLevelHelpers.ts` L1-L6 の `sanitizeAudioLevel(level)` と **完全に同じ logic + 同じ機能** = 重複。
2. **集約候補**: `AudioLevelMeter.tsx` L6-L21 の `getLevelColor(level)` (file-private) は色 gradient (green → yellow → red) を計算する純粋関数 = audio level display tier helper として `audioLevelHelpers.ts` に集約可能。
3. **責務階層**: 両関数とも entity = audio level, tier = display = `audioLevelHelpers.ts` と同階層 → **既存 file 拡張軸** (Loop 119-143 の paradigm 進化 = 新 file 作成だけが正解ではない)。

## 期待効果

- AudioLevelMeter.tsx 規模縮小 (純粋関数 2 件 = ~22 行削除)
- audioLevelHelpers.ts への色決定機能追加 (= 既存 file 拡張軸 1 件目 = paradigm 新規)
- DRY 違反解消 = code quality 向上 (AGENTS.md priority 1 寄与)
- alternation pattern 35 連続維持 (K(144) → frontend(145))

# 具体手順 (厳守、2-commit pattern)

## Step 1: `audioLevelHelpers.ts` を更新

現在の内容 (15 行):

```ts
export function sanitizeAudioLevel(level: number): number {
  if (!Number.isFinite(level)) {
    return 0;
  }
  return Math.max(0, Math.min(1, level));
}

export function getPopoverLevelBars(level: number): [number, number, number] {
  const normalized = sanitizeAudioLevel(level);
  return [
    Math.max(0.45, normalized * 0.9),
    Math.max(0.32, normalized * 0.65),
    Math.max(0.5, normalized * 0.78),
  ];
}
```

末尾に `getLevelColor` を追加 (Edit ツールで `getPopoverLevelBars` 関数の終端 `}` 直後に append):

```ts

export function getLevelColor(level: number): string {
  if (level < 0.5) {
    const ratio = level / 0.5;
    const r = Math.round(76 + (234 - 76) * ratio);
    const g = Math.round(175 + (179 - 175) * ratio);
    const b = Math.round(80 + (8 - 80) * ratio);
    return `rgb(${r}, ${g}, ${b})`;
  }
  const ratio = (level - 0.5) / 0.5;
  const r = Math.round(234 + (220 - 234) * ratio);
  const g = Math.round(179 + (38 - 179) * ratio);
  const b = Math.round(8 + (38 - 8) * ratio);
  return `rgb(${r}, ${g}, ${b})`;
}
```

注意:
- 元のコメント (`// green → yellow`、`// yellow → red`) は **削除して構わない** (関数名と引数名で意図は明確)
- ただし trailing whitespace 禁止
- 既存 `sanitizeAudioLevel` + `getPopoverLevelBars` は **触らない**

## Step 2: `AudioLevelMeter.tsx` を更新

現在の内容 (56 行):

```tsx
interface AudioLevelMeterProps {
  level: number;
  label: string;
}

function getLevelColor(level: number): string {
  if (level < 0.5) {
    // green → yellow
    const ratio = level / 0.5;
    const r = Math.round(76 + (234 - 76) * ratio);
    const g = Math.round(175 + (179 - 175) * ratio);
    const b = Math.round(80 + (8 - 80) * ratio);
    return `rgb(${r}, ${g}, ${b})`;
  }
  // yellow → red
  const ratio = (level - 0.5) / 0.5;
  const r = Math.round(234 + (220 - 234) * ratio);
  const g = Math.round(179 + (38 - 179) * ratio);
  const b = Math.round(8 + (38 - 8) * ratio);
  return `rgb(${r}, ${g}, ${b})`;
}

export function sanitizeAudioLevelForDisplay(level: number): number {
  if (!Number.isFinite(level)) {
    return 0;
  }
  return Math.max(0, Math.min(1, level));
}

export function AudioLevelMeter({ level, label }: AudioLevelMeterProps) {
  const clampedLevel = sanitizeAudioLevelForDisplay(level);
  const percentage = clampedLevel * 100;
  const roundedPercentage = Math.round(percentage);
  const color = getLevelColor(clampedLevel);

  return (
    ...
  );
}
```

これを以下のように変更:

1. 先頭 (L1 直前) に import 追加:
   ```tsx
   import { getLevelColor, sanitizeAudioLevel } from '../utils/audioLevelHelpers';

   ```
2. L6-L21 (`function getLevelColor` 全体) を削除
3. L23-L28 (`export function sanitizeAudioLevelForDisplay`) を削除
4. component 本体内の参照を変更:
   - `sanitizeAudioLevelForDisplay(level)` → `sanitizeAudioLevel(level)` (component 本体内 L31 相当)
   - `getLevelColor(clampedLevel)` はそのまま (関数名同じ、import で解決)

注意:
- `sanitizeAudioLevelForDisplay` は **export されていた** が、このプロジェクト内で他に使用されていない可能性が高い (grep で確認: `grep -rn sanitizeAudioLevelForDisplay src/` が `AudioLevelMeter.tsx` のみなら削除して良い)。**worker は必ず grep で確認すること**。もし他から import されていれば、その import 元も `sanitizeAudioLevel` に置換する。
- 結果: AudioLevelMeter.tsx は ~34 行 (props interface + import + component 本体のみ)
- trailing whitespace 禁止

## Step 3: 検証

```bash
grep -rn "sanitizeAudioLevelForDisplay" src/  # 残存していないことを確認
grep -rn "getLevelColor" src/  # AudioLevelMeter.tsx と audioLevelHelpers.ts のみであることを確認
npm run build  # tsc + vite build (1 関数 export 削除 + 1 export 追加 + 1 file extension)
bash scripts/agent-verify.sh src/components/AudioLevelMeter.tsx src/utils/audioLevelHelpers.ts
```

期待結果:
- npm run build: ✓ built
- 1932 modules transformed (Loop 143 と同水準、新 file なしのため数変動なし or 0 件減)
- agent-verify.sh: 全 OK
- `sanitizeAudioLevelForDisplay` の grep ヒットゼロ
- `getLevelColor` のヒットは 2 件 (AudioLevelMeter.tsx の import + audioLevelHelpers.ts の定義) のみ

## Step 4: refactor commit (1 件目)

```bash
git add src/components/AudioLevelMeter.tsx src/utils/audioLevelHelpers.ts
git commit -m "$(cat <<'EOF'
refactor(frontend): AudioLevelMeter の getLevelColor + sanitizeAudioLevelForDisplay を audioLevelHelpers.ts に統合 + 重複削除 (mjc-main-20260505-71 Loop 145, frontend 軸 17 件目 = 重複削除 + 既存 file 拡張軸 paradigm 新規確立, sanitizeAudioLevelForDisplay と sanitizeAudioLevel が完全重複 (DRY 違反) のため削除統合, getLevelColor は色 gradient display tier helper として audioLevelHelpers.ts に export 移動, AudioLevelMeter.tsx 56 行 → ~34 行 (-22 行), AGENTS.md priority 1 寄与, alternation 35 連続維持 (K(144) → frontend(145)))
EOF
)"
```

## Step 5: AGENT_LOG.md 末尾 (`---` の直後) に Loop 145 entry を追記

**手順**:
1. `tail -10 AGENT_LOG.md` で末尾を確認
2. 末尾の `---` 直後に以下のテンプレートで append:

```
[mjc-main-20260505-71 Loop 145 / 2026-05-06]
worker: mjc-worker-loop145-audio-level-helpers-consolidate (作業)
範囲: src/components/AudioLevelMeter.tsx (-22 行 = 純粋関数 2 件削除 + import 追加 + component 内参照書き換え) + src/utils/audioLevelHelpers.ts (+~16 行 = getLevelColor export 追加)
内容: AudioLevelMeter.tsx の getLevelColor (file-private) と sanitizeAudioLevelForDisplay (export) を audioLevelHelpers.ts に統合。frontend 軸 17 件目 = **重複削除 + 既存 file 拡張軸 paradigm 新規確立**。sanitizeAudioLevelForDisplay と既存 sanitizeAudioLevel が完全重複 (DRY 違反) のため削除統合。getLevelColor は色 gradient display tier helper として audioLevelHelpers.ts に export 移動。同じ entity (audio level) + 同じ tier (display) と判断 = 新 file 作成ではなく既存 file 拡張軸採用 = Loop 119-143 教訓継承 (paradigm 反復 < 責務階層精査) + paradigm 進化 (新 file 作成のみが正解ではない)。alternation pattern 35 連続維持 (K(144) → frontend(145))。AGENTS.md priority 1 寄与 (コード品質 + DRY)。
変更ファイル: src/components/AudioLevelMeter.tsx, src/utils/audioLevelHelpers.ts
検証: npm run build OK (tsc + vite build), bash scripts/agent-verify.sh OK
commit: <Step 4 の sha を埋める>
依存関係追加: なし
失敗理由: なし
次アクション: chore commit (本 entry 追記の commit) を続けて作成

---
```

注意:
- 末尾に `---` を必ず付ける (次の entry 用境界)
- 行末 trailing whitespace なし
- commit hash placeholder `<Step 4 の sha を埋める>` は Step 4 完了後の `git rev-parse --short HEAD` 結果で置換
- **重要**: append は **1 回のみ**。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む。

## Step 6: chore commit (2 件目)

```bash
git add AGENT_LOG.md
git commit -m "$(cat <<'EOF'
chore(agent-log): mjc-main-20260505-71 Loop 145 = AudioLevelMeter の getLevelColor + sanitizeAudioLevelForDisplay を audioLevelHelpers.ts に統合 + 重複削除 (frontend 軸 17 件目 = 重複削除 + 既存 file 拡張軸 paradigm 新規確立, DRY 違反解消, AGENTS.md priority 1 寄与, alternation 35 連続維持) のエントリ追記
EOF
)"
```

# 規模と所要時間

- 規模: SS = ~3-5 分完走見込み
- worker prompt 行数: 本 prompt は ~190 行 (重複削除 + 既存 file 拡張軸 paradigm 新規のため詳細記載)

# 上限明示

- ファイルは 3 つのみ編集: src/components/AudioLevelMeter.tsx + src/utils/audioLevelHelpers.ts + AGENT_LOG.md
- AGENT_LOG.md は **末尾に追記のみ** (先頭は絶対に触らない)
- AGENT_LOG.md への append は **1 回のみ** (重複追記禁止 = Loop 144 で fault precedent あり、繰り返し禁止)
- 新 file 作成なし
- test 件数の変動なし (frontend 軸 = 純粋関数移動のみ、機能不変)
- trailing whitespace 禁止

# 完了確認

```bash
git log --oneline -3
git status --short
wc -l src/components/AudioLevelMeter.tsx src/utils/audioLevelHelpers.ts AGENT_LOG.md
```

期待結果:
- 直近 2 commit が本 Loop 145 のもの (refactor + chore)
- working tree clean (untracked のみ残存 OK)
- AudioLevelMeter.tsx ~34 行 (元 56 - 22)
- audioLevelHelpers.ts ~31 行 (元 15 + 16)
- AGENT_LOG.md ~33,605 行前後 (33,588 + ~16)
