あなたは meet-jerky プロジェクトの worker (mjc-worker-loop114-meeting-detected-banner-helpers-extract) です。
Loop 114 = frontend 軸 5 件目 = MeetingDetectedBanner.tsx 純粋関数 4 件 を新 file `src/utils/meetingDetectedBannerHelpers.ts` に抽出する作業を担当します。

# 最初に必ず実施

1. `tail -350 AGENT_LOG.md` で末尾だけ読む (~32,800 行 / ~4MB あるので Read 全体禁止)。AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は **必ずファイル末尾** に行う。**先頭は絶対に触らない**。
2. `tail -10 AGENT_LOG.md` で末尾の `---` 区切りを確認 (新規追記は末尾の `---` 直後)
3. `cat src/utils/modelSelectorHelpers.ts` で precedent file (Loop 112 = frontend 軸 4 件目 = 最近接 precedent) を全文確認
4. `sed -n '460,530p' src/components/MeetingDetectedBanner.tsx` で抽出対象 4 関数 (L471-525) の現在の定義を確認
5. `sed -n '1,30p' src/components/MeetingDetectedBanner.tsx` で既存 import 群と先頭定数を確認
6. `sed -n '260,280p' src/components/MeetingDetectedBanner.tsx` で同 file 内 caller (L264 + L266 + L274) を確認

# 作業範囲 (担当範囲外の編集禁止)

## 1. 新規作成: `src/utils/meetingDetectedBannerHelpers.ts`

以下の 4 純粋関数を新 file に移動 (MeetingDetectedBanner.tsx L471-525 から):
- `getMeetingDetectedDisplayName` (export = file 外向け継続)
- `getMeetingDetectedSourceLabel` (export = file 外向け継続)
- `getMeetingDetectedHostLabel` (file 内 private = 新 file 内も private 保持)
- `getMeetingDetectedBannerDetail` (export = MeetingDetectedBanner.tsx から呼ぶため export 化)

新 file 内容案 (空行配置と import 順序は precedent に揃える):

```typescript
import type { MeetingAppDetectedPayload } from "../types";

export function getMeetingDetectedDisplayName(
  payload: MeetingAppDetectedPayload,
): string {
  return payload.service || payload.appName;
}

export function getMeetingDetectedSourceLabel(
  payload: MeetingAppDetectedPayload,
): string | null {
  const hostLabel = getMeetingDetectedHostLabel(payload.urlHost);
  if (payload.browserName && hostLabel) {
    return `${payload.browserName} URL`;
  }
  if (hostLabel) {
    return "ブラウザ URL";
  }
  if (payload.source === "browser") {
    return "ブラウザ URL";
  }
  if (payload.source === "app") {
    return "アプリ";
  }
  return null;
}

function getMeetingDetectedHostLabel(urlHost: string | undefined): string | null {
  if (!urlHost) {
    return null;
  }
  const trimmed = urlHost.trim();
  if (!trimmed) {
    return null;
  }
  try {
    const parsed = new URL(trimmed);
    return parsed.hostname || null;
  } catch {
    return (
      trimmed
        .split(/[/?#]/, 1)[0]
        .split("@")
        .pop()
        ?.replace(/:\d+$/, "") || null
    );
  }
}

export function getMeetingDetectedBannerDetail(
  payload: MeetingAppDetectedPayload,
  displayName: string,
): string {
  if (payload.source === "browser") {
    return `${displayName} · URL と音声アクティビティで確認`;
  }
  return `${displayName} · アプリ名と音声アクティビティで確認`;
}
```

行末の空白文字 (trailing whitespace) 禁止。空行末も含めて全行の末尾に whitespace を残さないこと。

## 2. 編集: `src/components/MeetingDetectedBanner.tsx`

- L471-525 (空行含む 4 関数定義 = 約 55 行) を **完全削除** (L470 の React component 末尾 `}` は残す)
- 既存 import 群 (L1-20) は全て残置 (`MeetingAppDetectedPayload` は L264/L266/L274 で型として使用継続のため L5 残置必須、ただし新 file への移動による未使用 type 化はしない = `MeetingAppDetectedPayload` は file 内 props として使用継続)
- import に以下を追加 (~/utils 配下で alphabetical 推奨 = L8-9 の `meetingStartRequest` の後 / L10 `meetingDetection` の前 または隣接位置に挿入。既存の `meetingDetection` import との namespace 衝突なし):

```typescript
import {
  getMeetingDetectedBannerDetail,
  getMeetingDetectedDisplayName,
  getMeetingDetectedSourceLabel,
} from "../utils/meetingDetectedBannerHelpers";
```

- 既存 caller (同 file 内 3 箇所: L264 getMeetingDetectedDisplayName / L266 getMeetingDetectedSourceLabel / L274 getMeetingDetectedBannerDetail) は **無変更** = 関数名は同一なので import 1 ブロック追加だけで動作
- `getMeetingDetectedHostLabel` は新 file 内 private 関数として保持 = 元 file からは消失 (元々 file 内 private で外部 caller ゼロのため影響なし)

# 検証

frontend 軸の場合:
- `npm run build` (tsc + vite build) を実行 = 型エラーなし + build ✓ 確認 (frontend test framework 未導入のため test 件数検証なし)
- `bash scripts/agent-verify.sh src/components/MeetingDetectedBanner.tsx src/utils/meetingDetectedBannerHelpers.ts` で全項目 OK 確認
- cargo test 不要 (rust 無変更)

# AGENT_LOG.md 末尾追記

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に以下のエントリを追記:

```
[mjc-main-20260505-59 Loop 114 / 2026-05-05]
worker: mjc-worker-loop114-meeting-detected-banner-helpers-extract (作業)
範囲: src/components/MeetingDetectedBanner.tsx (純粋関数 4 件削除 + 新 import 1 ブロック追加, ~50 行削減見込み) + src/utils/meetingDetectedBannerHelpers.ts (新規 ~58 行)
内容: MeetingDetectedBanner.tsx L471-525 の純粋関数 4 件 (getMeetingDetectedDisplayName + getMeetingDetectedSourceLabel + getMeetingDetectedHostLabel + getMeetingDetectedBannerDetail) を新 module utils/meetingDetectedBannerHelpers.ts に抽出。caller (同 file 内 3 箇所: L264 getMeetingDetectedDisplayName + L266 getMeetingDetectedSourceLabel + L274 getMeetingDetectedBannerDetail) は import 1 ブロック追加 + 既存呼び出しは無変更。getMeetingDetectedHostLabel は新 file 内 private 保持。component 本体 (export function MeetingDetectedBanner) は MeetingDetectedBanner.tsx 内に残置。frontend 軸 5 件目 = 大型 frontend file 責務分離 33 file 目 = scope 21 軸目開拓 (MeetingDetectedBanner component = 異 file = scope 多様性継続) = 純粋関数機能分離軸 frontend 版 paradigm 継続 (Loop 107/108/110/112 と同 paradigm = K 軸 (Loop 113) を挟んだので軸 reset 解釈可能 = 警告境界 reset)。MeetingDetectedBanner.tsx は 526 → ~471 行 (~10% 削減見込み)。file 専用 namespace `meetingDetectedBannerHelpers.ts` を採用 (共通化リスク回避設計、Loop 108/110/112 で確立した同 paradigm の継承 = 衝突回避設計 paradigm 5 件目)。メイン批判判断 = handoff 推奨候補 X1 を grep + Read 精査で「外部 caller ゼロ + 純粋関数 4 件 (副作用ある readPromptLiveCaptionStatus L40 は除外) + 規模 SS = handoff サマリの SessionList.tsx 不在を発見」確認手順で採用 = 連続 54 セッション目達成。
振る舞い: rust ファイル無変更のため cargo test 不要 (704 件件数完全不変は前 Loop 105 で確認済)。npm run build 全項目 OK = tsc 型チェック OK + vite build OK
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(frontend): MeetingDetectedBanner 純粋関数 helpers を utils/meetingDetectedBannerHelpers.ts に抽出
AGENTS.md priority: 1 (大型 frontend file 責務分離継続 + scope 21 軸目開拓)

---
```

(`<REFACTOR_HASH>` は refactor commit の実 hash で置換。chore commit hash は意図的に書かない = Loop 60-113 で 53 連続成功した 2-commit pattern)

# 2-commit pattern (worker 自律 連続 53 ループ成功 = 完全安定 pattern 化)

1. **refactor commit (1 件目)**: 抽出作業 = `git add src/components/MeetingDetectedBanner.tsx src/utils/meetingDetectedBannerHelpers.ts && git commit -m "refactor(frontend): MeetingDetectedBanner 純粋関数 helpers を utils/meetingDetectedBannerHelpers.ts に抽出 (mjc-main-20260505-59 Loop 114, frontend 軸 5 件目, 大型 frontend file 責務分離 33 file 目, scope 21 軸目開拓, AGENTS.md priority 1 寄与)"`
2. **refactor commit hash 取得**: `git log -1 --format=%H | head -c 7` で短縮 hash 取得
3. **AGENT_LOG.md 末尾追記**: 上記エントリの `<REFACTOR_HASH>` を実 hash に置換し、`tail -10 AGENT_LOG.md` で末尾確認後、Write ツールで一時 file に書いた後 `cat <tmp_file> >> AGENT_LOG.md` で末尾に append (Edit ツールでは末尾 `---` が unique にならない場合あり = Write 後 cat append 推奨)
4. **chore commit (2 件目)**: `git add AGENT_LOG.md && git commit -m "chore(agent-log): mjc-main-20260505-59 Loop 114 = utils/meetingDetectedBannerHelpers.ts 抽出 (構造分離 33 file 目, frontend 軸 5 件目 = scope 21 軸目開拓, 純粋関数機能分離軸 frontend 版 continuity = K 軸挟みで境界 reset)"` (chore commit 自身の hash は AGENT_LOG.md に書かない = 53 連続 precedent)

# 規模超過防止

- 編集対象 file: 2 つ (MeetingDetectedBanner.tsx + 新規 meetingDetectedBannerHelpers.ts)
- 削除対象: MeetingDetectedBanner.tsx L471-525 の指定範囲のみ (L1-470 + L526 = 残置)
- caller 修正: import 1 ブロック追加のみ = 既存呼び出しは無変更
- test 件数の上限: 704 件 (rust 無変更のため変動なし)
- frontend test 件数: 検証なし (framework 未導入)

# 注意点

- **行末の空白文字 (trailing whitespace) 禁止**
- 大型ファイルは Read 全体禁止 = `tail`/`head`/`grep`/`sed -n` で対象範囲のみ参照
- MeetingDetectedBanner.tsx 内の既存 import (`react`/`@tauri-apps`/`lucide-react`/`types`/`meetingStartRequest`/`meetingDetection`/`errorMessage`/`liveCaptionStatus`/`audioTrackLabels`) は全て本体 component で使用継続 = 削除禁止
- file 専用 namespace `meetingDetectedBannerHelpers.ts` を採用済 = 他 file との衝突回避済 (precedent: liveCaptionTrackHelpers / transcriptDisplayHelpers / transcriptViewFormatters / modelSelectorHelpers)
- 検証は `npm run build` で型エラーなし + ✓ built 確認
- 同 file 内 caller (L264/L266/L274) は無変更 = function 名は完全同一保持
- `MeetingAppDetectedPayload` type は import L5 として残置 (file 内 props/state で使用継続)
- `readPromptLiveCaptionStatus` (L40) は副作用 (console.error) を含むため抽出対象外 = file 内残置

# 完走後のメイン報告

完走時に以下を main agent に報告:
- refactor commit hash + chore commit hash
- MeetingDetectedBanner.tsx 削減後の行数 (`wc -l src/components/MeetingDetectedBanner.tsx`)
- meetingDetectedBannerHelpers.ts 行数 (`wc -l src/utils/meetingDetectedBannerHelpers.ts`)
- npm run build 結果 (built in XXXms / modules transformed N)
- agent-verify.sh 結果

以上。
