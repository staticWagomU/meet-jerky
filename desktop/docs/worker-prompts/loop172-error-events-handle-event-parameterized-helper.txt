あなたは meet-jerky の worker (mjc-worker-loop172-error-events-handle-event-parameterized-helper) です。

# 役割
mjc-main (mjc-main-20260505-85, Loop 172) からの 1 タスク = rust 軸 = parameterized helper 化 paradigm 反復 4 件目 = paradigm 系統樹拡張 7 件目 = test helper 集約軸 paradigm 反復 7 件目 = 既存 file 拡張軸 paradigm 12 件目 = 重複候補 grep 探索 paradigm 12 件目.

# 最初に必読
1. AGENT_LOG.md の **末尾 350 行のみ** を読め (tail -350 AGENT_LOG.md). **全体読込禁止 (~34,075 行)**.
2. 末尾 SUMMARY (mjc-main-20260505-84) で Loop 168/170 = parameterized helper 化 paradigm 反復 2/3 件目の precedent を確認.
3. AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**. **新規追記は必ずファイル末尾に行う**. **先頭は絶対に触らない**.

# 目的
elevenlabs_realtime.rs L172-190 の `scribe_error_events_are_queued_as_error_segments` test と openai_realtime.rs L182-200 の `error_events_are_queued_as_error_segments` test (両者は両 file の `ws_task::handle_event` を呼ぶ完全対称仕様 = 5 件 assertion 同型) を realtime_error_helpers.rs の既存 `#[cfg(test)] pub(crate) mod test_helpers` に **1 つの parameterized helper fn を追加**して集約する. 両 file の test fn 名は **完全保持**で中身を helper 呼び出しに置換 = cargo test 704 件件数完全不変保証.

# 編集対象 (4 file)

## (A) src-tauri/src/realtime_error_helpers.rs
**既存** `#[cfg(test)] pub(crate) mod test_helpers { ... }` の **末尾 (mod 閉じ `}` の直前)** に **1 つの新 helper fn を追加**:

```rust
    pub(crate) fn assert_handle_event_error_payload_creates_error_segment(
        handle_event: fn(
            &str,
            &Arc<Mutex<Vec<TranscriptionSegment>>>,
            &Option<String>,
            Option<TranscriptionSource>,
        ),
        error_payload: &str,
        speaker_label: &str,
        source: TranscriptionSource,
        expected_message: &str,
    ) {
        let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
        let speaker = Some(speaker_label.to_string());

        handle_event(error_payload, &pending, &speaker, Some(source));

        let segments = pending.lock();
        assert_eq!(segments.len(), 1);
        assert!(segments[0].text.contains(expected_message));
        assert_eq!(segments[0].speaker, speaker);
        assert_eq!(segments[0].source, Some(source));
        assert_eq!(segments[0].is_error, Some(true));
    }
```

**重要**:
- 既存 `Arc / Mutex / TranscriptionSegment / TranscriptionSource` の use は test_helpers module 内に既にあるため **追加 import 不要**.
- `TranscriptionSource` は `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize)]` 済み = `source` の 2 回使用 OK (Copy).
- 既存 `assert_push_error_*` helper 5 件は **無変更** (Loop 168 集約).

## (B) src-tauri/src/elevenlabs_realtime.rs L172-190
**現状** (19 行):

```rust
    #[test]
    fn scribe_error_events_are_queued_as_error_segments() {
        let pending = Arc::new(Mutex::new(Vec::<TranscriptionSegment>::new()));
        let speaker = Some("相手側".to_string());

        ws_task::handle_event(
            r#"{"message_type":"scribe_auth_error","message":"invalid api key"}"#,
            &pending,
            &speaker,
            Some(TranscriptionSource::SystemAudio),
        );

        let segments = pending.lock();
        assert_eq!(segments.len(), 1);
        assert!(segments[0].text.contains("invalid api key"));
        assert_eq!(segments[0].speaker, speaker);
        assert_eq!(segments[0].source, Some(TranscriptionSource::SystemAudio));
        assert_eq!(segments[0].is_error, Some(true));
    }
```

**置換後** (10 行 = test fn 名 / `#[test]` 完全保持):

```rust
    #[test]
    fn scribe_error_events_are_queued_as_error_segments() {
        crate::realtime_error_helpers::test_helpers::assert_handle_event_error_payload_creates_error_segment(
            ws_task::handle_event,
            r#"{"message_type":"scribe_auth_error","message":"invalid api key"}"#,
            "相手側",
            TranscriptionSource::SystemAudio,
            "invalid api key",
        );
    }
```

## (C) src-tauri/src/openai_realtime.rs L182-200
**現状** (19 行):

```rust
    #[test]
    fn error_events_are_queued_as_error_segments() {
        let pending = Arc::new(Mutex::new(Vec::<TranscriptionSegment>::new()));
        let speaker = Some("自分".to_string());

        ws_task::handle_event(
            r#"{"type":"error","error":{"message":"connection closed"}}"#,
            &pending,
            &speaker,
            Some(TranscriptionSource::Microphone),
        );

        let segments = pending.lock();
        assert_eq!(segments.len(), 1);
        assert!(segments[0].text.contains("connection closed"));
        assert_eq!(segments[0].speaker, speaker);
        assert_eq!(segments[0].source, Some(TranscriptionSource::Microphone));
        assert_eq!(segments[0].is_error, Some(true));
    }
```

**置換後** (10 行):

```rust
    #[test]
    fn error_events_are_queued_as_error_segments() {
        crate::realtime_error_helpers::test_helpers::assert_handle_event_error_payload_creates_error_segment(
            ws_task::handle_event,
            r#"{"type":"error","error":{"message":"connection closed"}}"#,
            "自分",
            TranscriptionSource::Microphone,
            "connection closed",
        );
    }
```

**注意**: 不要 import (`Arc`, `Mutex`, `TranscriptionSegment` 等が test module 内のみで使われていた場合) が **clippy で検出されたら削除**. ただし他の test (例: `handle_event_*`) で同じ import を使っているなら残す. cargo clippy --lib --tests -- -D warnings の指摘に従う.

## (D) AGENT_LOG.md
末尾に entry を 1 回だけ append. **append は 1 回のみ. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む** (Loop 144 fault precedent).

# AGENT_LOG.md entry テンプレート

**重要**: entry テンプレート冒頭に `---` を **含めない** (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める). Loop 166 boundary `---` 重複 fault precedent から.

```
[mjc-main Loop 172 / 2026-05-06]
worker: mjc-worker-loop172-error-events-handle-event-parameterized-helper (作業)
範囲: src-tauri/src/realtime_error_helpers.rs (test_helpers module 末尾に 1 helper fn 追加 = +約 20 行) + src-tauri/src/elevenlabs_realtime.rs L172-190 (test 中身を helper 呼び出しに置換 = test fn 名 / #[test] 完全保持 = 19 → 10 行 = -9 行) + src-tauri/src/openai_realtime.rs L182-200 (同上, 19 → 10 行 = -9 行)
内容: elevenlabs_realtime.rs L172-190 の scribe_error_events_are_queued_as_error_segments と openai_realtime.rs L182-200 の error_events_are_queued_as_error_segments は両 file の ws_task::handle_event を呼ぶ完全対称仕様 (両 handle_event のシグネチャ完全一致 + 5 件 assertion 同型). realtime_error_helpers.rs (= push_error の正規定義場所 + Loop 168 で test_helpers に push_error_* x5 helper 集約済) の test_helpers module に 1 つの parameterized helper fn (assert_handle_event_error_payload_creates_error_segment(handle_event: fn(...), error_payload, speaker_label, source, expected_message)) を追加 + 両 file の test 中身を helper 呼び出し 1 回に置換. test fn 名 / #[test] 完全保持 = test 件数 (704) 完全不変. callsite (test fn) 2 件は名前変更なし API 互換維持. **rust 軸 = 既存 file 拡張軸 paradigm 12 件目 = test helper 集約軸 paradigm 反復 7 件目 = paradigm 系統樹拡張 7 件目 = parameterized helper 化 paradigm 反復 4 件目 = 定型化期更なる延長**. 集約場所選定 = realtime_error_helpers (= push_error 正規定義場所 + Loop 168 で同 module 内に test_helpers 確立済 = 責務階層整合 + helper 本体と test_helpers の空間的並置). alternation pivot K(171) → rust(172) = 11 → 12. AGENTS.md priority 1 寄与 (DRY 解消 + 件数完全不変両立 + 正規定義場所並置).
振る舞い: cargo test (lib) 704 件件数完全不変、cargo clippy + cargo fmt + cargo build 全 OK. frontend 無変更.
verify: scripts/agent-verify.sh 全項目 OK
commits:
- (refactor commit hash) refactor(rust): error_events_are_queued_as_error_segments test 2 件を realtime_error_helpers::test_helpers の 1 parameterized helper に集約 (paradigm 系統樹拡張 7 件目)
AGENTS.md priority: 1 (paradigm 系統樹拡張 = parameterized helper 化 paradigm 反復 4 件目 = 真の DRY 解消 + 件数完全不変両立)
---
```

# 検証 (cd src-tauri 必須)

```sh
cd src-tauri
cargo test --lib 2>&1 | tail -20
# → "test result: ok. 704 passed; 0 failed" を確認

cargo clippy --lib --tests -- -D warnings 2>&1 | tail -10
# → 警告ゼロ

cargo fmt --check
# → OK (差分ゼロ)

cargo build --lib 2>&1 | tail -10
# → エラーなし
cd ..

bash scripts/agent-verify.sh src-tauri/src/realtime_error_helpers.rs src-tauri/src/elevenlabs_realtime.rs src-tauri/src/openai_realtime.rs AGENT_LOG.md
# → 全項目 OK
```

# コミット (2-commit pattern, 連続 28 ループ目達成見込み)

## commit 1: refactor (rust 編集 3 file)
```sh
git add src-tauri/src/realtime_error_helpers.rs src-tauri/src/elevenlabs_realtime.rs src-tauri/src/openai_realtime.rs
git commit -m "refactor(rust): error_events_are_queued_as_error_segments test 2 件を realtime_error_helpers::test_helpers の 1 parameterized helper に集約 (mjc-main Loop 172, rust 軸 = paradigm 系統樹拡張 7 件目 = parameterized helper 化 paradigm 反復 4 件目 = 既存 file 拡張軸 paradigm 12 件目, elevenlabs_realtime.rs L172-190 + openai_realtime.rs L182-200 の両 ws_task::handle_event を呼ぶ完全対称仕様 (handle_event シグネチャ完全一致 + 5 件 assertion 同型) test を引数化 helper で集約 + test fn 名 / #[test] 完全保持 = 件数 704 完全不変, callsite 2 件は中身を helper 呼び出しに置換 = DRY 違反解消, alternation pivot K(171) → rust(172) = 11 → 12, Loop 159 jst() / Loop 162 list_md_files() / Loop 164 audio_command_* / Loop 166 build_audio_drop_event_payload_* / Loop 168 push_error_* / Loop 170 engine_*_debug_format_* の paradigm 系統樹拡張 = parameterized helper 化 paradigm 反復 4 件目, AGENTS.md priority 1 寄与)"
```

## commit 2: chore (AGENT_LOG.md 追記)
```sh
git add AGENT_LOG.md
git commit -m "chore(agent-log): mjc-main Loop 172 = error_events_are_queued_as_error_segments test 2 件を realtime_error_helpers::test_helpers の 1 parameterized helper に集約 (rust 軸, paradigm 系統樹拡張 7 件目 = parameterized helper 化 paradigm 反復 4 件目, 既存 file 拡張軸 paradigm 12 件目 = test helper 集約軸 paradigm 反復 7 件目, elevenlabs_realtime.rs + openai_realtime.rs の両 ws_task::handle_event 呼び出し対称仕様 test を引数化 helper で集約, alternation pivot K(171) → rust(172) = 11 → 12, Loop 159/162/164/166/168/170 paradigm 系統樹の parameterized helper 化反復 4 件目, 重複候補 grep 探索 paradigm 12 件目達成) のエントリ追記"
```

# 規模超過防止

**担当範囲外の編集禁止**:
- 他 file (cloud_whisper.rs / realtime_ws_helpers.rs / 等) は触らない.
- 既存 push_error_* helper 5 件 (Loop 168 集約済) は **完全無変更**.
- engine_*_debug_format_* helper 2 件 (Loop 170 集約済 = realtime_audio_command::test_helpers) は **完全無変更**.
- audio_command_* helper 3 件 (Loop 164 集約済) は **完全無変更**.
- build_audio_drop_event_payload_* helper 5 件 (Loop 166 集約済 = audio_event::test_helpers) は **完全無変更**.

**test 件数上限**: cargo test --lib = **704 件完全不変** (test fn 名保持 + #[test] 数完全不変 = 件数増減ゼロ保証).

**大型 file Read 禁止**:
- elevenlabs_realtime.rs (~8.6KB / 約 280 行) = Read 全体 OK
- openai_realtime.rs (~24KB / 約 800 行) = **Read 全体禁止** = 必要な L182-200 周辺のみ tail/head/sed/Read with offset で参照
- realtime_error_helpers.rs (~6.3KB / 約 165 行) = Read 全体 OK

**行末空白文字 (trailing whitespace) 禁止** = `git diff --check` で確認.

# 完走後

mjc-main へ通知 (tmux send-keys 不要 = mjc-main が watchdog 経由で観測).

メインの確認手順:
- `git status --short` (working tree clean を主観測 = `git log --oneline -3` の即座のチェックは fault 誤判定リスク = Loop 158 precedent)
- AGENT_LOG.md 行数差分 = +13-15 行見込み

# 完走時間目安

規模 SS-S = ~10-15 分 (Loop 168 = 規模 S = ~15 分 / Loop 170 = 規模 S-M = 実測 ~2.5 分 ultra fast).

以上.
