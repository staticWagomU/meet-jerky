あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

必ず読んでください:
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness.md
- AGENT_LOG.md

担当範囲:
- src/components/MeetingDetectedBanner.tsx
- AGENT_LOG.md
- この worker prompt ファイル

目的:
会議検知バナーの自動非表示 `hideMeetingPromptWindow()` が失敗した場合、console error だけで終わらせず、既存の `listenerError` / alert UI で自動非表示失敗を明示してください。録音開始や停止の失敗ではなく、バナー表示制御の失敗として扱ってください。

実装条件:
- `PROMPT_AUTO_HIDE_MS` の timeout 内にある `hideMeetingPromptWindow().catch(...)` だけを狭く変更してください。
- `toErrorMessage(e)` を `msg` に取り、console error と `setListenerError` の両方に使ってください。
- 表示文言は `会議検知バナーの自動非表示に失敗しました: ${msg}` の形にしてください。
- hide 成功時の `clearPendingMeetingStartRequest()`、`setPendingAction(null)`、`setDetected(null)` は維持してください。
- `handleStartRecording`、dismiss handler、listener setup、event 名、pending request storage、CSS、Rust、依存関係、テスト基盤は変更しないでください。
- 他エージェントも同時に作業している可能性があります。他者の変更を戻さず、担当範囲外を編集しないでください。
- コミットは禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/components/MeetingDetectedBanner.tsx AGENT_LOG.md docs/worker-prompts/mj-worker-loop258-meeting-banner-auto-hide-error-20260507-071836.txt`

AGENT_LOG.md に、開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
