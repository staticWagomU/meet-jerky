あなたは meet-jerky の Tidy First 担当 worker (sonnet) です。
Loop 50 の作業を完走してください。

# 最初に必ず読むこと

1. **AGENT_LOG.md の末尾 350 行を `tail -350 AGENT_LOG.md` で読む** (~27,400+ 行 / ~2.5MB あるので Read 全体禁止、tail/head/grep のみ)。**先頭は絶対に触らない**。
2. AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**。新規追記は **必ずファイル末尾に行う**。具体手順: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に新規エントリを追記する。
3. docs/autonomous-main-prompt-claude.md / AGENTS.md / docs/architecture/transcription-refactor-plan.md は必要に応じ参照 (Read 全体可)。

# タスク (規模 S, ~175 行移動)

`src-tauri/src/transcription.rs` の tests mod から **MockEngine / MockStream 構造体 + impl + 3 tests** を `src-tauri/src/transcription_traits.rs` に移動する Tidy First (振る舞い不変)。

## 移動対象 (transcription.rs L52-226 の範囲、175 行)

以下を **完全に削除**して transcription_traits.rs の新規 tests mod に移植する:

1. **L52-58**: コメントヘッダ「TranscriptionEngine / TranscriptionStream trait テスト」(コメント全体)
2. **L60-61**: `use std::sync::atomic::AtomicUsize;` + `use std::sync::Arc;`
3. **L63-68**: `MockEngine` struct (rustdoc コメント L63-64 含む)
4. **L70-76**: `MockStream` struct
5. **L78-91**: `impl TranscriptionEngine for MockEngine`
6. **L93-123**: `impl TranscriptionStream for MockStream`
7. **L125-164**: `#[test] fn test_stream_lifecycle_feed_drain_finalize`
8. **L166-201**: `#[test] fn test_stream_config_speaker_propagates_to_segments`
9. **L203-226**: `#[test] fn test_feed_empty_samples_is_noop_in_mock`

## 同時整理

- **transcription.rs L50**: `use std::sync::atomic::Ordering;` を **削除**。grep 確認済 (Ordering は Mock tests 内のみで使用、移動後 unused になる)
- **transcription.rs**: 上記移動範囲の **直後にある「ensure_engine — エンジン種別ディスパッチ / 再ロード抑制」コメントヘッダ (L228-230) は残置** (候補 H = ensure_engine tests の locality 集約は別ループで実施)

## 移動先 (transcription_traits.rs、現状 48 行)

ファイル末尾に `#[cfg(test)] mod tests` を新規追加する形で挿入する:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::transcription_types::{TranscriptionSegment, TranscriptionSource};
    use std::sync::atomic::{AtomicUsize, Ordering};
    use std::sync::Arc;

    // ─────────────────────────────────────────
    // TranscriptionEngine / TranscriptionStream trait テスト
    // ─────────────────────────────────────────
    //
    // Whisper の実モデルをロードせずに trait の振る舞いを検証する。
    // モックエンジンが受け取ったサンプル数とライフサイクル (feed → drain →
    // finalize) を記録し、新 trait の契約が壊れていないことを確認する。

    /// テスト用モックエンジン。`feed` で受け取ったサンプル合計を記録し、
    /// `feed` 1 回ごとに 1 セグメントを出す。`finalize` 時には特殊セグメントを 1 つ追加する。
    struct MockEngine { ... }

    struct MockStream { ... }

    impl TranscriptionEngine for MockEngine { ... }

    impl TranscriptionStream for MockStream { ... }

    #[test]
    fn test_stream_lifecycle_feed_drain_finalize() { ... }

    #[test]
    fn test_stream_config_speaker_propagates_to_segments() { ... }

    #[test]
    fn test_feed_empty_samples_is_noop_in_mock() { ... }
}
```

注意:
- `use super::*;` で TranscriptionEngine / TranscriptionStream / StreamConfig は import 可能 (transcription_traits.rs 本体で pub 宣言)
- `TranscriptionSegment` / `TranscriptionSource` は transcription_traits.rs 本体 L3 で既に import 済だが **tests mod には届かない** ので tests mod 内で `use crate::transcription_types::{TranscriptionSegment, TranscriptionSource};` を再 import する
- AtomicUsize と Ordering は同じ `std::sync::atomic` 配下なので 1 行 `use std::sync::atomic::{AtomicUsize, Ordering};` でまとめて import 可能 (元の 2 行から整理)
- 移植時は **テスト本文を一字一句そのまま** 移す。リファクタしない、コメント変更しない。

# 規模超過防止 (厳守)

- **担当範囲外の編集禁止**: app_detection.rs / frontend / scripts / docs/ など、本タスク以外のファイルに触らない
- **test 件数の上限**: 新規テスト追加禁止。**既存 3 tests + Mock 構造体 + impl のみを移動**
- **transcription.rs から削除する範囲**: 上記 9 ブロック + L50 の `Ordering` use 文 のみ。tests mod の **L43-50 の他の use 文 (RequestedTranscriptionSources / transcription_error_payload / TranscriptionManager) は残置** (後続の ensure_engine tests / その他 tests で使用中)
- **L228 以降のコメントヘッダ「ensure_engine」とその後のテスト群は触らない**
- 既存の挙動・型・ロジック変更禁止 (Tidy First = 振る舞い不変)

# 検証 (順番厳守)

1. `cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src-tauri && cargo test --lib 2>&1 | tail -20` → **702 passed / 0 failed** を確認 (件数不変)
2. `cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src-tauri && cargo clippy --lib --tests -- -D warnings 2>&1 | tail -20` → 警告ゼロ
3. `cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src-tauri && cargo fmt --check` → OK
4. `cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop && bash scripts/agent-verify.sh src-tauri/src/transcription.rs src-tauri/src/transcription_traits.rs` → OK
5. **行末の空白文字 (trailing whitespace) 禁止**: `grep -nP " +$" src-tauri/src/transcription.rs src-tauri/src/transcription_traits.rs` → 出力なし

検証で 1 件でも失敗したら **commit 前に必ず修正**。失敗を放置しない。

# AGENT_LOG.md 追記

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に以下のエントリを追記 (Edit ツールでは末尾 `---` が unique にならないので、別ファイル `/tmp/loop50-log.md` に書いて `cat /tmp/loop50-log.md >> AGENT_LOG.md` で append する):

```
[mjc-main-20260505-25 Loop 50 / 2026-05-05 ~JST]

## What
- src-tauri/src/transcription.rs から MockEngine / MockStream 構造体 + impl + 3 tests (test_stream_lifecycle_feed_drain_finalize / test_stream_config_speaker_propagates_to_segments / test_feed_empty_samples_is_noop_in_mock) を src-tauri/src/transcription_traits.rs に移動
- transcription.rs L50 の `use std::sync::atomic::Ordering;` を削除 (移動後 unused、grep 確認済)
- transcription_traits.rs に新規 `#[cfg(test)] mod tests` 追加 (`use super::*;` + transcription_types + atomic + Arc)

## Why
- AGENTS.md 優先順位 1 = クラッシュ修正の予防的寄与 (TranscriptionEngine / TranscriptionStream トレイトの mock 実装と挙動テストの locality 完成 = トレイト契約と mock 検証の同居で理解性向上)
- variety pivot 軸 = test 軸 = Loop 47 から 3 ループ間隔 (Loop 48-49 で別軸 pivot 済) = 警告境界
- Mock 構造体 + impl は TranscriptionEngine / TranscriptionStream トレイト本体と locality 一致が自然 = Loop 32 / 38 / 42 / 47 precedent 継承

## How (Tidy First, behavior-preserving)
- 振る舞い不変 = cargo test --lib 件数不変 = 702 passed
- transcription.rs から L50 use 文 (Ordering) + L52-226 の 175 行を削除
- transcription_traits.rs に新規 tests mod 追加 (`use super::*;` で TranscriptionEngine / TranscriptionStream / StreamConfig アクセス、`use crate::transcription_types::{TranscriptionSegment, TranscriptionSource};` で型 import、`use std::sync::atomic::{AtomicUsize, Ordering};` + `use std::sync::Arc;`)
- transcription.rs: 611 → <移動後の実数> 行 / transcription_traits.rs: 48 → <移動後の実数> 行

## Verify
- cargo test --lib: 702 passed / 0 failed (件数不変)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- agent-verify.sh: OK
- trailing whitespace: なし

## commit
- commit `<ハッシュ>` (commit 後にハッシュを記入)
```

# Commit

検証 OK 後:

```
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop && bash scripts/agent-commit.sh "refactor(transcription): MockEngine/MockStream + 3 tests を transcription_traits.rs に移動 (Tidy First, mjc-main-20260505-25 Loop 50)" src-tauri/src/transcription.rs src-tauri/src/transcription_traits.rs AGENT_LOG.md
```

commit 成功後、commit hash を **AGENT_LOG.md の `commit ` 行に記入する別 commit (chore)** を作成:

```
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop && bash scripts/agent-commit.sh "chore(agent-log): Loop 50 commit hash <ハッシュ> を AGENT_LOG.md に記入" AGENT_LOG.md
```

# 完了条件

- 上記検証 5 つすべて OK
- 2 つの commit が main に乗っている (1: 実装 + AGENT_LOG.md、2: hash 反映 chore)
- 規模超過なし (transcription.rs / transcription_traits.rs / AGENT_LOG.md のみ変更)

完走したら短く完了報告 (~3 行)。
