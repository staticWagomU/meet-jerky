あなたは meet-jerky プロジェクトの worker (mjc-worker-loop151-speaker-normalize-extract) です。
Loop 151 = rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸 + tests 移動軸 = `src-tauri/src/transcript_bridge.rs` の **normalize_speaker** 1 関数 + その単体 test 10 件を新 file `src-tauri/src/speaker_normalize.rs` に抽出する作業を担当します。Loop 121 (throttle_key 抽出) と同 paradigm。

# 戦略 (重要、Loop 119/121/123 教訓継承)

- normalize_speaker は **`fn` (private) で transcript_bridge.rs 内で 1 callsite (L35 segment_to_append_args_at) のみ参照**
- 抽出後は **`pub fn` 化** + transcript_bridge.rs に `use crate::speaker_normalize::normalize_speaker;` 追加で同名参照を維持
- normalize_speaker の単体 test 10 件 (関数名 `normalize_speaker_*` で始まる) を新 file の `#[cfg(test)] mod tests` に移動 = test 件数完全不変 (移動 = 削除 + 追加で 0 増減)
- 残す test (segment_to_append_args 経由の test) は transcript_bridge.rs の tests module に残置
- alternation pattern: K(150) → rust(151) = **40 連続維持**
- paradigm: 純粋関数機能分離軸 rust 版 + 新 file 作成軸 + tests 移動軸 (Loop 95/119/121/123 と同 paradigm)
- scope **36 軸目開拓** (新 file `speaker_normalize.rs` = speaker normalization tier)
- **責務階層精査済 (メイン批判判断)**: normalize_speaker は話者ラベル正規化の独立 entity = transcript_bridge (segment 変換) とは責務階層が異なる純粋文字列処理 = 新 file 作成が妥当 (Loop 119/121 教訓継承)

# 最初に必ず実施

1. `tail -350 AGENT_LOG.md` で末尾だけ読む (~33,809 行 / ~4MB あるので Read 全体禁止)。AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は **必ずファイル末尾** に行う。**先頭は絶対に触らない**。
2. `tail -10 AGENT_LOG.md` で末尾の `---` 区切りを確認
3. `wc -l src-tauri/src/transcript_bridge.rs` で現在行数確認 (期待値: 826 行)
4. `sed -n '92,103p' src-tauri/src/transcript_bridge.rs` で normalize_speaker の本体 (doc + body 12 行) を確認
5. `grep -nE "fn normalize_speaker_" src-tauri/src/transcript_bridge.rs` で test 10 件の正確な行番号を確認
6. `cat src-tauri/src/app_detection_throttle_key.rs` で precedent 新 file 構造を参考
7. `sed -n '50,57p' src-tauri/src/lib.rs` で mod 登録順 (settings_permission / system_audio の周辺) を確認

# 作業範囲 (担当範囲外の編集禁止)

## 1. 新 file 作成: `src-tauri/src/speaker_normalize.rs`

内容 (~110 行 = doc comment + pub fn + tests 10 件):

```rust
//! 話者ラベル (speaker label) 正規化の純粋関数。
//!
//! `transcription.rs` 等で `"自分"` / `"相手側"` 等の文字列ラベルが
//! `TranscriptionSegment::speaker` (`Option<String>`) に格納される。
//! この純粋関数で前後の whitespace を trim + 空文字列 / `None` フォールバックを
//! `"不明"` に統一する。`transcript_bridge::segment_to_append_args*` から呼ばれ、
//! `Session::append_segment` の話者欄 (Markdown 仕様) に渡される。

/// 話者ラベルを正規化する。
///
/// - 前後の空白をトリム
/// - `None` または空文字列は `"不明"` にフォールバック
/// - それ以外は受け取った値をそのまま採用（`transcription.rs` 側で
///   既に `"自分"` / `"相手側"` が付与されているため）
pub fn normalize_speaker(raw: Option<&str>) -> String {
    match raw.map(str::trim) {
        Some(s) if !s.is_empty() => s.to_string(),
        _ => "不明".to_string(),
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    [移動した 10 件の test 本体をそのままここに記述]
}
```

**重要**:
- 元の `fn normalize_speaker` の doc comment は完全保持 (省略禁止)
- visibility は `pub fn` (元は `fn` = file-private、transcript_bridge.rs から `use` 経由で呼ばれるため `pub` 必須)
- tests 10 件は **transcript_bridge.rs から正確に切り出して移動** = assertion 内容 / コメント / 空白を完全コピー (改変禁止)
- 移動対象 10 件 (関数名 prefix `normalize_speaker_`):
  1. `normalize_speaker_returns_unknown_for_none`
  2. `normalize_speaker_returns_unknown_for_empty_string`
  3. `normalize_speaker_returns_unknown_for_whitespace_only`
  4. `normalize_speaker_passes_through_normal_label`
  5. `normalize_speaker_trims_surrounding_whitespace_only`
  6. `normalize_speaker_trims_unicode_full_width_space_u3000`
  7. `normalize_speaker_returns_unknown_for_only_unicode_full_width_spaces`
  8. `normalize_speaker_passes_through_nul_byte_label`
  9. `normalize_speaker_passes_through_control_character_label`
  10. `normalize_speaker_passes_through_zero_width_space_label`

## 2. `src-tauri/src/transcript_bridge.rs` の修正

### 2a. 冒頭に use 追加

```rust
// 修正前 (L4):
//   use crate::transcription_types::TranscriptionSegment;
// 修正後:
//   use crate::speaker_normalize::normalize_speaker;
//   use crate::transcription_types::TranscriptionSegment;
```

### 2b. fn normalize_speaker 削除 (L92-L103 = doc 含む 12 行)

L92-L103 範囲 (`/// 話者ラベルを正規化する` から `}` まで) を全削除。**前後の空行も適切に処理する** (連続空行 2 行になる場合は 1 行に圧縮)。

### 2c. tests module 内 normalize_speaker_* 10 件削除

`grep -nE "fn normalize_speaker_" src-tauri/src/transcript_bridge.rs` で正確な行番号を取得し、各 test (`#[test]` attribute から `}` までの 1 件分) を削除。10 件全て削除。

### 2d. caller (L35) は **変更不要**

`use crate::speaker_normalize::normalize_speaker;` を冒頭に追加するため、L35 の `normalize_speaker(segment.speaker.as_deref())` は同名で参照可能。

## 3. `src-tauri/src/lib.rs` に mod 登録追加

`mod settings_permission;` (L53) と `mod system_audio;` (L54) の間に `mod speaker_normalize;` を追加 (アルファベット順: settings_permission < speaker_normalize < system_audio)。

```
mod settings_permission;
mod speaker_normalize;  // ← 新規追加
mod system_audio;
```

# 検証

```bash
cd src-tauri && cargo fmt --check && cargo clippy --lib --tests -- -D warnings && cargo build --lib && cargo test --lib 2>&1 | tail -5 && cd ..
bash scripts/agent-verify.sh src-tauri/src/transcript_bridge.rs src-tauri/src/speaker_normalize.rs src-tauri/src/lib.rs
```

期待結果:
- cargo fmt --check: OK
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo build --lib: エラーなし
- cargo test --lib: **704 passed / 0 failed (件数完全不変)**
- agent-verify.sh: 全項目 OK

# AGENT_LOG.md 末尾追記 (1 回のみ、二重書き禁止)

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に **以下のエントリを 1 回だけ append** (Edit/Write を複数回繰り返すな、append が成功したら直ちに次へ進む = Loop 144 fault 教訓):

```
[mjc-main-20260505-75 Loop 151 / 2026-05-06]
worker: mjc-worker-loop151-speaker-normalize-extract (作業)
範囲: src-tauri/src/transcript_bridge.rs (純減 ~99 行 = fn 12 行 + tests 10 件 ~88 行 削除 + use 1 行 追加) + src-tauri/src/speaker_normalize.rs (新規 ~110 行 = doc + pub fn + tests 10 件) + src-tauri/src/lib.rs (mod 登録 1 行追加)
内容: transcript_bridge.rs の normalize_speaker (L92-L103 = doc + body 12 行) と tests module 内 normalize_speaker_* 10 件 (assertion 完全コピー = 件数完全不変) を新 file speaker_normalize.rs に移動。transcript_bridge.rs に `use crate::speaker_normalize::normalize_speaker;` 追加 = caller L35 と residual tests (segment_to_append_args 経由) は無変更で API 互換維持。lib.rs アルファベット順 (settings_permission の直後 + system_audio の直前) に mod 登録追加。**rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸 + tests 移動軸 = scope 36 軸目開拓 (speaker_normalize = 新 file = speaker normalization tier)**。**alternation pattern 40 連続成功** (frontend(149) → K(150) → rust(151))。**paradigm pivot (K → rust)**。Loop 119/121/123 = 同 paradigm 反復 + 責務階層精査済 (normalize_speaker は話者ラベル正規化の独立 entity = transcript_bridge と責務階層異なる = 新 file 作成軸が妥当)。
振る舞い: cargo test (lib) 704 件件数完全不変、cargo clippy + cargo fmt + cargo build 全 OK。frontend 無変更のため npm run build 不要。
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(rust): transcript_bridge の normalize_speaker を新 file speaker_normalize.rs に抽出 (tests 10 件も移動 + use 経由で API 互換維持)
AGENTS.md priority: 1 (大型 rust file 責務分離継続 + scope 36 軸目開拓)

---
```

(`<REFACTOR_HASH>` は refactor commit の実 hash で置換。chore commit hash は意図的に書かない = 2-commit pattern 連続 6 ループ成功した precedent)

# 2-commit pattern (worker 自律 連続 6 ループ成功 = Loop 144 fault からの完全復活継続中)

1. **refactor commit (1 件目)**: 全 rust 変更をまとめて add + commit
   ```
   git add src-tauri/src/transcript_bridge.rs src-tauri/src/speaker_normalize.rs src-tauri/src/lib.rs
   git commit -m "refactor(rust): transcript_bridge の normalize_speaker を新 file speaker_normalize.rs に抽出 (mjc-main-20260505-75 Loop 151, rust 軸 = 純粋関数機能分離軸 rust 版 + 新 file 作成軸 + tests 移動軸, scope 36 軸目開拓 = speaker normalization tier, 大型 rust file 責務分離 32 file 目, normalize_speaker pub fn 化 + tests 10 件 完全コピーで件数 完全不変, transcript_bridge.rs に use 追加で API 互換維持, AGENTS.md priority 1 寄与)"
   ```
2. **refactor commit hash 取得**: `git log -1 --format=%H | head -c 7`
3. **AGENT_LOG.md 末尾追記 (1 回のみ)**: 上記エントリの `<REFACTOR_HASH>` を実 hash に置換し、`tail -10 AGENT_LOG.md` で末尾確認後、Write ツールで一時 file に書いた後 `cat <tmp_file> >> AGENT_LOG.md` で末尾に append (Edit ツールでは末尾 `---` が unique にならない場合あり = Write 後 cat append 推奨)。**append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む (Loop 144 fault 教訓)**。
4. **chore commit (2 件目)**: `git add AGENT_LOG.md && git commit -m "chore(agent-log): mjc-main-20260505-75 Loop 151 = transcript_bridge の normalize_speaker を新 file speaker_normalize.rs に抽出 (構造分離 32 file 目, rust 軸, scope 36 軸目開拓 = speaker normalization tier, 純粋関数機能分離軸 rust 版 + 新 file 作成軸 + tests 移動軸, alternation 40 連続成功, normalize_speaker pub fn 化 + tests 10 件 完全コピーで件数完全不変, use 経由で API 互換維持)"` (chore commit 自身の hash は AGENT_LOG.md に書かない = 6 連続 precedent)

# 規模超過防止

- 編集対象 file: 3 つ (transcript_bridge.rs + speaker_normalize.rs (新規) + lib.rs) + AGENT_LOG.md
- transcript_bridge.rs 削減: ~100 行削減 (fn 12 行 + tests 10 件 ~88 行) + 1 行追加 (use) = 純減 ~99 行
- 新 file: ~110 行 (doc 6 行 + pub fn doc + body 12 行 + tests 10 件 ~88 行)
- lib.rs: 1 行追加
- test 件数の上限: 704 件 (件数完全不変、移動 = 削除 + 追加で 0 増減)
- cargo test 失敗 0 件

# 注意点 (worker prompt 必須要素 8 つ全て網羅)

1. **冒頭で「AGENT_LOG.md の末尾 350 行を読め」** (上記「最初に必ず実施」Step 1)
2. **「AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う」** (上記 Step 1 / AGENT_LOG.md 末尾追記節)
3. **「先頭は絶対に触らない」** (上記 Step 1)
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記 (上記 AGENT_LOG.md 末尾追記節)
5. **規模超過防止段落** = 担当範囲外の編集禁止 + test 件数の上限明示 (上記節)
6. **大型ファイルは Read 全体禁止 = tail/head/grep/sed -n 'X,Yp' で対象範囲のみ参照** (= AGENT_LOG.md / transcript_bridge.rs はサイズ大なので部分参照のみ)
7. **行末の空白文字 (trailing whitespace) 禁止**
8. **AGENT_LOG.md への append は 1 回のみ。Edit/Write を複数回繰り返さない。append が成功したら直ちに次へ進む** (Loop 144 fault 教訓継承 = Loop 146/147/148/149/150 で 5 連続成功実証 = 必須記載)

# 完走後のメイン報告

完走時に以下を main agent に報告:
- refactor commit hash + chore commit hash
- `wc -l src-tauri/src/transcript_bridge.rs src-tauri/src/speaker_normalize.rs` (期待: transcript_bridge.rs ~727 行 / speaker_normalize.rs ~110 行)
- cargo test 結果 (704 passed)
- agent-verify.sh 結果

以上。
