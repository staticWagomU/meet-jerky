あなたは作業担当エージェントです。

対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。
AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src/utils/audioDropCountPayload.ts
- AGENT_LOG.md

背景:
`audio-drop-count` payload の `dropped` は frontend で累計表示に加算されます。現在 `src/utils/audioDropCountPayload.ts` は有限な非負整数であれば受理しますが、JavaScript の安全整数範囲を超える値は正確に加算・表示できません。Rust 側の通常 emit は 100ms ごとの破棄数なので実用上巨大値は想定しにくい一方、payload 境界では安全整数に限定しておく方が録音状態診断として信頼できます。

実装内容:
- `isAudioDropCountPayload` で `dropped` に `Number.isSafeInteger` を要求してください。
- `getAudioDropCountPayloadIssue` も安全整数範囲外を明示できる理由文にしてください。
- `source` validation、event 名、payload field 名、React component 表示構造、Rust、依存関係は変更しないでください。
- `dropped >= 0` の契約は維持してください。
- 既存の「有限数ではない」「整数ではない」「0以上ではない」の理由が意味を失わないよう、判定順序を保ってください。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/utils/audioDropCountPayload.ts AGENT_LOG.md docs/worker-prompts/mj-research-loop320-next-value-slice-20260508-040854.txt docs/worker-prompts/mj-worker-loop320-audio-drop-safe-integer-20260508-041001.txt`

ログ:
AGENT_LOG.md に以下を追記してください。
- 開始日時
- 担当セッション
- 役割
- 作業範囲
- 指示内容
- 結果
- 変更ファイル
- 検証結果
- 依存関係追加の有無
- 失敗理由
- 次アクション
- ユーザー価値
- 非目標
- 未実機確認範囲

他エージェントも同時に作業している可能性があります。他者の変更を戻さず、担当範囲外を編集しないでください。コミットは禁止です。
最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
