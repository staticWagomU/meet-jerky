あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src/utils/audioDropCountPayload.ts
- AGENT_LOG.md

目的:
`audio-drop-count` のフロントエンド payload guard で、`dropped` が非整数の payload を不正として拒否するようにしてください。Rust 側の正規 payload は `usize` 由来なので、通常挙動は変えずに破損 payload の UI 反映を防ぐ小さな guard 強化です。

実装条件:
1. `src/utils/audioDropCountPayload.ts`
   - `isAudioDropCountPayload` の `dropped` 判定に `Number.isInteger(candidate.dropped)` を追加してください。
   - `source` 許可値、`typeof candidate.dropped === "number"`、`Number.isFinite(candidate.dropped)`、`candidate.dropped >= 0` は維持してください。
2. AGENT_LOG.md
   - 開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。
   - 変更ファイルには、この worker prompt と research prompt も含めてください。

禁止:
- コミットは禁止です。
- 依存追加は禁止です。
- `audioLevelPayload.ts`、`TranscriptView.tsx`、Rust 側、event 名、UI 文言は変更しないでください。
- テスト基盤の追加は禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/utils/audioDropCountPayload.ts AGENT_LOG.md docs/worker-prompts/mj-research-loop242-next-value-slice-20260507-032143.txt docs/worker-prompts/mj-worker-loop242-audio-drop-integer-guard-20260507-032143.txt`

残リスクには、JS/TS の単体テスト基盤がないため guard の入出力は build と diff review で確認すること、実機 `audio-drop-count` emit 経路は今回未確認であることを明記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
