あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src-tauri/src/app_detection_teams.rs
- src-tauri/src/app_detection.rs
- AGENT_LOG.md

目的:
Teams `/meet/<id>` URL fallback で、非数字の単一 segment を会議 URL として誤検知しないようにしてください。`/l/meetup-join/` と `/v2?meetingjoin=true` は変更しないでください。

背景:
- 既存実装は Teams `/meet/` 後を `has_single_non_empty_segment` で見るため、`/meet/free` や `/meet/abc-123` も分類され得ます。
- 既存 positive test は `teams.microsoft.com/meet/1234567890123` と `teams.live.com/meet/1234567890123` で numeric ID 前提です。
- Microsoft Support では Teams Free の Meeting ID を "numeric ID code" と説明しています: https://support.microsoft.com/en-us/office/join-a-meeting-in-microsoft-teams-free-047b93e5-1777-4289-a3be-0ed6ca3fa12a
- ただし実機 invite URL の全形式は未確認なので、残リスクに false negative 可能性を明記してください。

実装条件:
1. `src-tauri/src/app_detection.rs`
   - Teams URL negative tests に以下を `None` として追加してください:
     - `https://teams.microsoft.com/meet/free`
     - `https://teams.cloud.microsoft/meet/abcDEF`
     - `https://teams.live.com/meet/abc-123`
   - 既存 numeric positive tests は変更しないでください。
2. `src-tauri/src/app_detection_teams.rs`
   - Teams `/meet/` 専用 helper を追加してください。例:
     - trailing slash は既存同様許容
     - empty は reject
     - `/` を含む複数 segment は reject
     - ASCII digit のみ accept
   - work/school host と `teams.live.com` の `/meet/` 分岐だけこの helper に差し替えてください。
   - `has_non_empty_path_segments`、`query_has_param`、`/l/meetup-join/`、`/v2` 経路は変更しないでください。
3. AGENT_LOG.md
   - 開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。
   - 残リスクには、Teams `/meet/<id>` の非数字正式形式が存在する場合は false negative になる可能性、実機 invite URL は未確認であることを明記してください。

検証:
- `~/.cargo/bin/cargo fmt --manifest-path src-tauri/Cargo.toml --check`
- `~/.cargo/bin/cargo test --manifest-path src-tauri/Cargo.toml --lib classify_meeting_url`
- `~/.cargo/bin/cargo clippy --manifest-path src-tauri/Cargo.toml --lib --tests -- -D warnings`
- `git diff --check -- src-tauri/src/app_detection_teams.rs src-tauri/src/app_detection.rs AGENT_LOG.md`

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。コミットは禁止です。
