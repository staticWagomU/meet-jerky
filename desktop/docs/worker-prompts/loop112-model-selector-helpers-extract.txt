あなたは meet-jerky プロジェクトの worker (mjc-worker-loop112-model-selector-helpers-extract) です。
Loop 112 = frontend 軸 4 件目 = ModelSelector.tsx 純粋関数 2 件 を新 file `src/utils/modelSelectorHelpers.ts` に抽出する作業を担当します。

# 最初に必ず実施

1. `tail -350 AGENT_LOG.md` で末尾だけ読む (~32,800 行 / ~2.7MB あるので Read 全体禁止)
2. `tail -10 AGENT_LOG.md` で末尾の `---` 区切りを確認 (新規追記は **必ずファイル末尾** に行う、先頭は絶対に触らない)
3. `cat src/utils/transcriptDisplayHelpers.ts` で precedent file (Loop 110 = frontend 軸 3 件目 = 最近接 precedent) を全文確認
4. `head -35 src/components/ModelSelector.tsx` で抽出対象の 2 関数 (sanitizeProgress L18-23 + getModelDisplayName L25-33) の現在の定義を確認

# 作業範囲 (担当範囲外の編集禁止)

## 1. 新規作成: `src/utils/modelSelectorHelpers.ts`

以下の 2 純粋関数を export して新 file に移動 (ModelSelector.tsx L18-33 から):

```typescript
import type { ModelInfo } from "../types";

export function sanitizeProgress(progress: number): number {
  if (!Number.isFinite(progress)) {
    return 0;
  }
  return Math.max(0, Math.min(1, progress));
}

export function getModelDisplayName(
  models: ModelInfo[] | undefined,
  modelName: string | null,
): string | null {
  if (!modelName) {
    return null;
  }
  return models?.find((model) => model.name === modelName)?.displayName ?? modelName;
}
```

行末の空白文字 (trailing whitespace) 禁止。空行末も含めて全行の末尾に whitespace を残さないこと。

## 2. 編集: `src/components/ModelSelector.tsx`

- L18-33 の 2 純粋関数 (`sanitizeProgress` + `getModelDisplayName`) の定義を **完全削除** (interface ModelSelectorProps L12-16 + export function ModelSelector L35 以降は **残す = 削除しない**)
- 既存 import 群 (L1-10) は全て残置:
  - `import type { ModelInfo } from "../types";` (L5) は `useQuery<ModelInfo[]>` (L71) で使用継続のため残置必須
  - その他 react / tauri / tanstack 関連 import は全て本体 component で使用されているため残置
- import に以下を追加 (既存 import 群と適切な位置に挿入、~/utils 配下で alphabetical 推奨 = `errorMessage` の後 / `modelDownloadPayload` の前 または 後):

```typescript
import {
  getModelDisplayName,
  sanitizeProgress,
} from "../utils/modelSelectorHelpers";
```

- 既存 caller (同 file 内 4 箇所: L75 getModelDisplayName / L76 getModelDisplayName / L104 sanitizeProgress / L394 sanitizeProgress) は **無変更** = 関数名は同一なので import 1 行追加だけで動作 (DownloadStatus は同 file 内 component なので同じ import で参照可能)

# 検証

frontend 軸の場合:
- `npm run build` (tsc + vite build) を実行 = 型エラーなし + build ✓ 確認 (frontend test framework 未導入のため test 件数検証なし)
- `bash scripts/agent-verify.sh src/components/ModelSelector.tsx src/utils/modelSelectorHelpers.ts` で全項目 OK 確認
- cargo test 不要 (rust 無変更)

# AGENT_LOG.md 末尾追記

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に以下のエントリを追記:

```
[mjc-main-20260505-58 Loop 112 / 2026-05-05]
worker: mjc-worker-loop112-model-selector-helpers-extract (作業)
範囲: src/components/ModelSelector.tsx (純粋関数 2 件削除 + 新 import 1 行追加, ~15 行削減見込み) + src/utils/modelSelectorHelpers.ts (新規 ~16 行)
内容: ModelSelector.tsx L18-33 の純粋関数 2 件 (sanitizeProgress + getModelDisplayName) を新 module utils/modelSelectorHelpers.ts に抽出。caller (同 file 内 4 箇所: L75/L76 getModelDisplayName + L104/L394 sanitizeProgress) は import 1 行追加 + 既存呼び出しは無変更。component 本体 (ModelSelectorProps interface + export function ModelSelector + ModelOption + DownloadStatus) は ModelSelector.tsx 内に残置。frontend 軸 4 件目 = 大型 frontend file 責務分離 32 file 目 = scope 20 軸目開拓 (ModelSelector component = 異 file = scope 多様性継続) = 純粋関数機能分離軸 frontend 版 paradigm 継続 (Loop 107/108/110 と同 paradigm = K 軸 (Loop 111) を挟んだので軸 reset 解釈可能 = 警告境界 reset)。ModelSelector.tsx は 498 → ~483 行 (~3% 削減見込み)。file 専用 namespace `modelSelectorHelpers.ts` を採用 (共通化リスク回避設計、Loop 108/110 で確立した同 paradigm の継承 = 衝突回避設計 paradigm 4 件目)。メイン批判判断 = handoff 推奨候補 X1 を grep + Read 精査で「外部 caller 1 件 (TranscriptionControls.tsx) + 純粋関数 2 件 + 内部 React component 2 件 (抽出対象外) + 規模 SS 補正」確認手順で採用 = 連続 53 セッション目達成。
振る舞い: rust ファイル無変更のため cargo test 不要 (704 件件数完全不変は前 Loop 105 で確認済)。npm run build 全項目 OK = tsc 型チェック OK + vite build OK
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(frontend): ModelSelector 純粋関数 helpers を utils/modelSelectorHelpers.ts に抽出
AGENTS.md priority: 1 (大型 frontend file 責務分離継続 + scope 20 軸目開拓)

---
```

(`<REFACTOR_HASH>` は refactor commit の実 hash で置換。chore commit hash は意図的に書かない = Loop 60-111 で 51 連続成功した 2-commit pattern)

# 2-commit pattern (worker 自律 連続 51 ループ成功 = 完全安定 pattern 化)

1. **refactor commit (1 件目)**: 抽出作業 = `git add src/components/ModelSelector.tsx src/utils/modelSelectorHelpers.ts && git commit -m "refactor(frontend): ModelSelector 純粋関数 helpers を utils/modelSelectorHelpers.ts に抽出 (mjc-main-20260505-58 Loop 112, frontend 軸 4 件目, 大型 frontend file 責務分離 32 file 目, scope 20 軸目開拓, AGENTS.md priority 1 寄与)"`
2. **refactor commit hash 取得**: `git log -1 --format=%H | head -c 7` で短縮 hash 取得
3. **AGENT_LOG.md 末尾追記**: 上記エントリの `<REFACTOR_HASH>` を実 hash に置換し、`tail -10 AGENT_LOG.md` で末尾確認後、Write ツールで一時 file に書いた後 `cat <tmp_file> >> AGENT_LOG.md` で末尾に append (Edit ツールでは末尾 `---` が unique にならない場合あり = Write 後 cat append 推奨)
4. **chore commit (2 件目)**: `git add AGENT_LOG.md && git commit -m "chore(agent-log): mjc-main-20260505-58 Loop 112 = utils/modelSelectorHelpers.ts 抽出 (構造分離 32 file 目, frontend 軸 4 件目 = scope 20 軸目開拓, 純粋関数機能分離軸 frontend 版 continuity = K 軸挟みで境界 reset)"` (chore commit 自身の hash は AGENT_LOG.md に書かない = 51 連続 precedent)

# 規模超過防止

- 編集対象 file: 2 つ (ModelSelector.tsx + 新規 modelSelectorHelpers.ts)
- 削除対象: ModelSelector.tsx L18-33 の指定範囲のみ (L1-16 import + interface 群 + L35 以降 component 本体は **削除しない**)
- caller 修正: import 1 行追加のみ = 既存呼び出しは無変更
- test 件数の上限: 704 件 (rust 無変更のため変動なし)
- frontend test 件数: 検証なし (framework 未導入)

# 注意点

- **行末の空白文字 (trailing whitespace) 禁止**
- 大型ファイルは Read 全体禁止 = `tail`/`head`/`grep` で対象範囲のみ参照
- ModelSelector.tsx 内の既存 import (`react`/`@tauri-apps`/`@tanstack`/`types`/`errorMessage`/`modelDownloadPayload`) は全て本体 component で使用継続 = 削除禁止
- file 専用 namespace `modelSelectorHelpers.ts` を採用済 = 他 file との衝突回避済 (precedent: liveCaptionTrackHelpers / transcriptDisplayHelpers / transcriptViewFormatters)
- 検証は `npm run build` で型エラーなし + ✓ built 確認
- 同 file 内 caller (L75/L76/L104/L394) は無変更 = function 名は完全同一保持

# 完走後のメイン報告

完走時に以下を main agent に報告:
- refactor commit hash + chore commit hash
- ModelSelector.tsx 削減後の行数 (`wc -l src/components/ModelSelector.tsx`)
- modelSelectorHelpers.ts 行数 (`wc -l src/utils/modelSelectorHelpers.ts`)
- npm run build 結果 (built in XXXms / modules transformed N)
- agent-verify.sh 結果

以上。
