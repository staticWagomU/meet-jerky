あなたは meet-jerky の rust 軸 worker (mjc-worker-loop170-engine-debug-format-parameterized-helper) です.

# 目的 (= AGENTS.md priority 1 に直接寄与)

`elevenlabs_realtime.rs` と `openai_realtime.rs` の **engine_*_debug_format_* test 6 件** (両 file 3 件ずつ = 完全対称仕様 = engine 構造体名 + field 名 + model 値のみ違い) を、既存の `realtime_audio_command::test_helpers` モジュールに **2 つの parameterized helper fn** として集約し、callsite 6 件を helper 呼び出しに置換する.

= 既存 file 拡張軸 paradigm 11 件目 = test helper 集約軸 paradigm 反復 6 件目 = paradigm 系統樹拡張 6 件目 = parameterized helper 化 paradigm 反復 3 件目 = paradigm 反復による定型化期更なる延長 = 真の DRY 解消.

# 重要な制約

- test 件数 = 704 件、件数完全不変保証 (cargo test --lib --no-run + cargo test --lib で確認)
- 担当範囲外の編集は一切禁止. 編集対象は **3 file のみ** = `src-tauri/src/realtime_audio_command.rs` + `src-tauri/src/elevenlabs_realtime.rs` + `src-tauri/src/openai_realtime.rs`
- 行末の空白文字 (trailing whitespace) 禁止
- AGENT_LOG.md への append は 1 回のみ. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む
- entry テンプレート冒頭に `---` を含めない (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める)
- 大型 file は Read 全体禁止. tail/head/grep で対象範囲のみ参照

# Step 0: AGENT_LOG.md 末尾参照と運用前提確認

```
tail -350 AGENT_LOG.md
```

を実行して末尾を読む. **AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾. 新規追記は必ずファイル末尾に行う. 先頭は絶対に触らない**.

# Step 1: 対称仕様 test の対象範囲確認

以下 6 件の test fn が両 file に対称構造で存在することを確認する:

**elevenlabs_realtime.rs**:
- L193-205: `engine_default_debug_format_contains_type_name_and_empty_model_id_field`
  - assert: `ElevenLabsRealtimeEngine` 型名 + `model_id` field 名 + 空文字列 `""`
- L208-214: `engine_new_with_str_slice_debug_format_contains_provided_model_id_value`
  - assert: `ElevenLabsRealtimeEngine` + `model_id` + `"scribe_v2_realtime"`
- L217-223: `engine_new_with_owned_string_debug_format_contains_provided_model_id_value`
  - assert: `ElevenLabsRealtimeEngine` + `model_id` + `"scribe_v2_legacy"`

**openai_realtime.rs**:
- L610-622: `engine_default_debug_format_contains_type_name_and_empty_model_field`
  - assert: `OpenAIRealtimeEngine` 型名 + `model` field 名 + 空文字列 `""`
- L625-631: `engine_new_with_str_slice_debug_format_contains_provided_model_value`
  - assert: `OpenAIRealtimeEngine` + `model` + `"gpt-4o-mini-transcribe"`
- L634-640: `engine_new_with_owned_string_debug_format_contains_provided_model_value`
  - assert: `OpenAIRealtimeEngine` + `model` + `"gpt-4o-transcribe"`

両 file の test 内容は **type_name / field_name / model_value のみ違い、assert 構造は完全に同一** = parameterized helper 化が成立する.

`grep -n` で具体行を再確認する:

```
grep -nE 'engine_(default|new_with).*debug_format' src-tauri/src/elevenlabs_realtime.rs src-tauri/src/openai_realtime.rs
```

# Step 2: realtime_audio_command::test_helpers モジュールに 2 helper を追加

現状の `src-tauri/src/realtime_audio_command.rs` (47 行) は以下の構造:

```rust
//! ElevenLabs / OpenAI realtime stream の WS タスクへ送る音声コマンド。
...

#[derive(Debug)]
pub(crate) enum AudioCommand { ... }

#[cfg(test)]
pub(crate) mod test_helpers {
    use super::AudioCommand;

    pub fn assert_samples_variant_debug_format_contains_variant_name_and_payload_floats() { ... }
    pub fn assert_finalize_variant_debug_format_is_exact_variant_name() { ... }
    pub fn assert_samples_with_empty_vec_debug_format_contains_variant_name_and_empty_brackets() { ... }
}
```

この `test_helpers` モジュールの末尾 (`}` 直前) に以下 2 helper を追加する:

```rust
    /// Engine の Debug 出力が型名 + 指定 field 名 + 空文字列 (`""`) を含むことを検証する.
    ///
    /// `ElevenLabsRealtimeEngine::default()` / `OpenAIRealtimeEngine::default()` の test で共有.
    pub fn assert_engine_default_debug_format(
        formatted: &str,
        expected_type_name: &str,
        expected_field_name: &str,
    ) {
        assert!(
            formatted.contains(expected_type_name),
            "型名: {formatted}"
        );
        assert!(
            formatted.contains(expected_field_name),
            "field 名: {formatted}"
        );
        assert!(
            formatted.contains("\"\""),
            "空 String を Debug 出力: {formatted}"
        );
    }

    /// Engine の Debug 出力が型名 + 指定 field 名 + 指定 model 値 (引用符込み) を含むことを検証する.
    ///
    /// `Engine::new(&str)` / `Engine::new(String)` の test で共有.
    pub fn assert_engine_with_model_value_debug_format(
        formatted: &str,
        expected_type_name: &str,
        expected_field_name: &str,
        expected_model_value: &str,
    ) {
        assert!(formatted.contains(expected_type_name));
        assert!(formatted.contains(expected_field_name));
        assert!(formatted.contains(&format!("\"{expected_model_value}\"")));
    }
```

**注**: `pub fn` (= `pub(crate) mod test_helpers` 内の `pub fn` = test build 限定で同 crate 全 file から参照可能). 既存 helper と同じ visibility.

# Step 3: elevenlabs_realtime.rs の 3 test を helper 呼び出しに置換

`src-tauri/src/elevenlabs_realtime.rs` を Edit で以下のように修正:

**置換 1** (L192-205):
```rust
    #[test]
    fn engine_default_debug_format_contains_type_name_and_empty_model_id_field() {
        let engine = ElevenLabsRealtimeEngine::default();
        let formatted = format!("{engine:?}");
        assert!(
            formatted.contains("ElevenLabsRealtimeEngine"),
            "型名: {formatted}"
        );
        assert!(formatted.contains("model_id"), "field 名: {formatted}");
        assert!(
            formatted.contains("\"\""),
            "空 String を Debug 出力: {formatted}"
        );
    }
```
↓
```rust
    #[test]
    fn engine_default_debug_format_contains_type_name_and_empty_model_id_field() {
        let engine = ElevenLabsRealtimeEngine::default();
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_default_debug_format(
            &formatted,
            "ElevenLabsRealtimeEngine",
            "model_id",
        );
    }
```

**置換 2** (L207-214):
```rust
    #[test]
    fn engine_new_with_str_slice_debug_format_contains_provided_model_id_value() {
        let engine = ElevenLabsRealtimeEngine::new("scribe_v2_realtime");
        let formatted = format!("{engine:?}");
        assert!(formatted.contains("ElevenLabsRealtimeEngine"));
        assert!(formatted.contains("model_id"));
        assert!(formatted.contains("\"scribe_v2_realtime\""));
    }
```
↓
```rust
    #[test]
    fn engine_new_with_str_slice_debug_format_contains_provided_model_id_value() {
        let engine = ElevenLabsRealtimeEngine::new("scribe_v2_realtime");
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_with_model_value_debug_format(
            &formatted,
            "ElevenLabsRealtimeEngine",
            "model_id",
            "scribe_v2_realtime",
        );
    }
```

**置換 3** (L216-223):
```rust
    #[test]
    fn engine_new_with_owned_string_debug_format_contains_provided_model_id_value() {
        let engine = ElevenLabsRealtimeEngine::new(String::from("scribe_v2_legacy"));
        let formatted = format!("{engine:?}");
        assert!(formatted.contains("ElevenLabsRealtimeEngine"));
        assert!(formatted.contains("model_id"));
        assert!(formatted.contains("\"scribe_v2_legacy\""));
    }
```
↓
```rust
    #[test]
    fn engine_new_with_owned_string_debug_format_contains_provided_model_id_value() {
        let engine = ElevenLabsRealtimeEngine::new(String::from("scribe_v2_legacy"));
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_with_model_value_debug_format(
            &formatted,
            "ElevenLabsRealtimeEngine",
            "model_id",
            "scribe_v2_legacy",
        );
    }
```

**注**: test fn 名 / `#[test]` 属性 / engine インスタンス生成 / `format!("{engine:?}")` 呼び出しは完全保持 = 件数 (704) 完全不変保証.

# Step 4: openai_realtime.rs の 3 test を helper 呼び出しに置換

`src-tauri/src/openai_realtime.rs` を Edit で同様に修正:

**置換 1** (L609-622):
```rust
    #[test]
    fn engine_default_debug_format_contains_type_name_and_empty_model_field() {
        let engine = OpenAIRealtimeEngine::default();
        let formatted = format!("{engine:?}");
        assert!(
            formatted.contains("OpenAIRealtimeEngine"),
            "型名: {formatted}"
        );
        assert!(formatted.contains("model"), "field 名: {formatted}");
        assert!(
            formatted.contains("\"\""),
            "空 String を Debug 出力: {formatted}"
        );
    }
```
↓
```rust
    #[test]
    fn engine_default_debug_format_contains_type_name_and_empty_model_field() {
        let engine = OpenAIRealtimeEngine::default();
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_default_debug_format(
            &formatted,
            "OpenAIRealtimeEngine",
            "model",
        );
    }
```

**置換 2** (L624-631):
```rust
    #[test]
    fn engine_new_with_str_slice_debug_format_contains_provided_model_value() {
        let engine = OpenAIRealtimeEngine::new("gpt-4o-mini-transcribe");
        let formatted = format!("{engine:?}");
        assert!(formatted.contains("OpenAIRealtimeEngine"));
        assert!(formatted.contains("model"));
        assert!(formatted.contains("\"gpt-4o-mini-transcribe\""));
    }
```
↓
```rust
    #[test]
    fn engine_new_with_str_slice_debug_format_contains_provided_model_value() {
        let engine = OpenAIRealtimeEngine::new("gpt-4o-mini-transcribe");
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_with_model_value_debug_format(
            &formatted,
            "OpenAIRealtimeEngine",
            "model",
            "gpt-4o-mini-transcribe",
        );
    }
```

**置換 3** (L633-640):
```rust
    #[test]
    fn engine_new_with_owned_string_debug_format_contains_provided_model_value() {
        let engine = OpenAIRealtimeEngine::new(String::from("gpt-4o-transcribe"));
        let formatted = format!("{engine:?}");
        assert!(formatted.contains("OpenAIRealtimeEngine"));
        assert!(formatted.contains("model"));
        assert!(formatted.contains("\"gpt-4o-transcribe\""));
    }
```
↓
```rust
    #[test]
    fn engine_new_with_owned_string_debug_format_contains_provided_model_value() {
        let engine = OpenAIRealtimeEngine::new(String::from("gpt-4o-transcribe"));
        let formatted = format!("{engine:?}");
        crate::realtime_audio_command::test_helpers::assert_engine_with_model_value_debug_format(
            &formatted,
            "OpenAIRealtimeEngine",
            "model",
            "gpt-4o-transcribe",
        );
    }
```

# Step 5: 検証

```
cd src-tauri && cargo test --lib --no-run 2>&1 | tail -5
cd src-tauri && cargo test --lib 2>&1 | tail -20
cd src-tauri && cargo clippy --lib --tests -- -D warnings 2>&1 | tail -10
cd src-tauri && cargo fmt --check 2>&1 | tail -10
cd src-tauri && cargo build --lib 2>&1 | tail -5
```

期待結果:
- `cargo test --lib`: **704 passed; 0 failed** (件数完全不変保証)
- `cargo clippy --lib --tests -- -D warnings`: 警告ゼロ
- `cargo fmt --check`: 差分ゼロ (= 整形済)
- `cargo build --lib`: エラーなし

もし test 件数が 704 から増減したら **直ちに停止して報告**. test fn を削除/追加していない (= helper 呼び出しに置換のみ) ので 704 不変が原則.

# Step 6: 1st commit (refactor)

```
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
git add src-tauri/src/realtime_audio_command.rs src-tauri/src/elevenlabs_realtime.rs src-tauri/src/openai_realtime.rs
git commit -m "refactor(rust): engine_*_debug_format_* test 6 件を realtime_audio_command::test_helpers の 2 parameterized helper に集約 (mjc-main Loop 170, rust 軸 = paradigm 系統樹拡張 6 件目 = parameterized helper 化 paradigm 反復 3 件目 = 既存 file 拡張軸 paradigm 11 件目, elevenlabs_realtime.rs L193-222 + openai_realtime.rs L609-640 の type_name/field_name/model_value のみ違いの完全対称仕様 test を引数化 helper で集約 + test fn 名 / #[test] 完全保持 = 件数 704 完全不変, callsite 6 件は中身を helper 呼び出しに置換 = DRY 違反解消, alternation pivot K(169) → rust(170) = 9 → 10, Loop 159 jst() fn 集約 / Loop 162 list_md_files() fn 集約 / Loop 164 audio_command_* test logic 集約 / Loop 166 build_audio_drop_event_payload_* parameterized helper / Loop 168 push_error_* parameterized helper の paradigm 系統樹拡張 = parameterized helper 化 paradigm 反復 3 件目, AGENTS.md priority 1 寄与)"
```

# Step 7: AGENT_LOG.md 末尾追記

`tail -10 AGENT_LOG.md` で末尾の `---` 位置を確認. 末尾の `---` の **直後** に以下の entry を追記する.

**重要**: entry 冒頭に `---` を **含めない**. 前 entry の閉じ `---` を再利用. entry 末尾の `---` 1 個で締める.

entry テンプレート:
```
[Loop 170 @ 2026-05-06 ~JST] mjc-main Loop 170
- 軸: rust 軸
- 寄与: AGENTS.md priority 1 (パフォーマンス + 保守性 + 品質)
- paradigm: 既存 file 拡張軸 paradigm 11 件目 = test helper 集約軸 paradigm 反復 6 件目 = paradigm 系統樹拡張 6 件目 = parameterized helper 化 paradigm 反復 3 件目 = paradigm 反復による定型化期更なる延長
- 内容: engine_*_debug_format_* test 6 件 (elevenlabs_realtime.rs L193-222 + openai_realtime.rs L609-640) を realtime_audio_command::test_helpers の 2 parameterized helper (assert_engine_default_debug_format / assert_engine_with_model_value_debug_format) に集約
- 件数: cargo test 704 件件数完全不変
- alternation pivot: K(169) → rust(170) = 9 → 10 = pivot 加速継続
- precedent: Loop 159 jst() fn 集約 / Loop 162 list_md_files() fn 集約 / Loop 164 audio_command_* test logic 集約 / Loop 166 build_audio_drop_event_payload_* parameterized helper / Loop 168 push_error_* parameterized helper = paradigm 1-5 件目 → 本 Loop 170 = paradigm 6 件目達成
- 完走時間: 約 5-10 分
- commit: refactor + chore (= 2-commit pattern)
---
```

実行手順:
1. tail -10 AGENT_LOG.md で末尾確認
2. entry を一時 file に書き出す (cat > /tmp/loop170-entry.txt <<EOF ... EOF)
3. cat /tmp/loop170-entry.txt >> AGENT_LOG.md (1 回のみ)
4. tail -30 AGENT_LOG.md で追記が正しく反映されているか確認

**重要**: AGENT_LOG.md への append は 1 回のみ. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む.

# Step 8: 2nd commit (chore agent-log)

```
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
git add AGENT_LOG.md
git commit -m "chore(agent-log): mjc-main Loop 170 = engine_*_debug_format_* test 6 件を realtime_audio_command::test_helpers の 2 parameterized helper に集約 (rust 軸, paradigm 系統樹拡張 6 件目 = parameterized helper 化 paradigm 反復 3 件目, 既存 file 拡張軸 paradigm 11 件目 = test helper 集約軸 paradigm 反復 6 件目, elevenlabs_realtime.rs + openai_realtime.rs の type_name/field_name/model_value 違いの完全対称仕様 test を引数化 helper で集約, alternation pivot K(169) → rust(170) = 9 → 10, Loop 159/162/164/166/168 paradigm 系統樹の parameterized helper 化反復 3 件目, 重複候補 grep 探索 paradigm 11 件目達成) のエントリ追記"
```

# 完走判定

完走の最終確認:

```
git status --short
git log --oneline -3
cd src-tauri && cargo test --lib 2>&1 | grep 'test result:'
```

期待結果:
- `git status --short`: scripts/* の M 表示なし、handoff/worker-prompts の untracked のみ
- `git log --oneline -3`: 上 2 件が本 worker の commit (chore + refactor)
- `cargo test --lib`: `test result: ok. 704 passed; 0 failed; ...`

**完走報告**: 「Loop 170 完走. cargo test 704 件件数完全不変. paradigm 反復 3 件目達成.」

# 注意事項

- helper 名はあえて `assert_engine_default_debug_format` / `assert_engine_with_model_value_debug_format` の 2 つに分け、`engine_default` (空文字列前提) と `engine_with_model_value` (model 値前提) の 2 種類を区別. これにより null/empty case と value case の test 意図を helper 側で明確化.
- 引数 `expected_type_name` / `expected_field_name` / `expected_model_value` の名前は明示的にして cognitive load を下げる.
- `pub(crate) mod test_helpers` 内の `pub fn` (= 既存 3 helper と同じ visibility) なので path は `crate::realtime_audio_command::test_helpers::assert_*`.
- 既存 helper と完全に同じ書式 / インデント / 行末改行に揃える.
- callsite 置換時は `Edit` の old_string にコメント等を含めず、置換対象の test fn body だけを過不足なく指定する.
