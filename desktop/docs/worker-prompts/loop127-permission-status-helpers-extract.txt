あなたは meet-jerky プロジェクトの worker (claude sonnet) です。
mjc-main-20260505-64 の Loop 127 を担当します。

# 必読 (冒頭で必ず実施)

1. **AGENT_LOG.md の末尾 350 行を読め** (`tail -350 AGENT_LOG.md`)。**時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う。先頭は絶対に触らない。** 末尾の `---` 直後に新規エントリを追記すること。
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md を読め。
3. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md を読め。
4. **precedent file = src/utils/audioLevelHelpers.ts を cat 全体読み込みせよ** (Loop 125 frontend 軸 7 件目 直近 precedent = 規模 SS = 15 行で短小 = 同 R6 paradigm の 1 件目)。
5. **precedent file = src/utils/transcriptViewFormatters.ts を cat 全体読み込みせよ** (Loop 107 = 同 utils/ 配下命名スタイル参考)。

# タスク概要 (Loop 127 = frontend 軸 8 件目 = 純粋関数機能分離軸 frontend 版 5 件目)

## 目的

`src/routes/TranscriptView.tsx` (2442 行 Loop 125 後) から **2 純粋関数** を分離して **frontend file 責務分離 40 file 目** + **scope 27 軸目開拓 (新 file `src/utils/permissionStatusHelpers.ts`)** を達成する。

**メイン批判判断発掘 (Loop 119/121/123 教訓継承 = paradigm 反復 < 責務階層精査)**:
- 既存 `utils/macosPrivacySettings.ts` の責務 = 「macOS privacy settings 設定 + paths」 = OS 設定パス情報
- 抽出候補の責務 = 「permission status text label + UI row class 計算」 = **異階層** (UI 表示用 helper)
- ∴ **新 file `permissionStatusHelpers.ts` 作成軸採用** (既存 file 拡張軸ではない)
- R6 (TranscriptView.tsx 残 純粋関数 段階的分割抽出) の 2 件目 = Loop 125 audio level helpers の続編

## 成果物 (commit 順)

### Commit 1: refactor(frontend) — TranscriptView 純粋関数 permission status helpers を utils/permissionStatusHelpers.ts に抽出

#### 1A. 新 file `src/utils/permissionStatusHelpers.ts` を作成

以下の **2 純粋関数** を新 file へ移動 (export 化):

**純粋関数 (移動 + export 化)**:
- TranscriptView.tsx L80-98 `getPermissionStatusLabel(status: string | undefined, error: unknown, isChecking: boolean): string` → `export function`
- TranscriptView.tsx L100-107 `getPermissionRowClassName(status: string | undefined, error: unknown): string` → `export function`

両関数は **依存なし** (引数のみ参照、外部 import 不要) = export 化のみで完結。

**新 file の冒頭コメント**: なし (precedent = transcriptViewFormatters.ts / audioLevelHelpers.ts に倣う = コメントなしシンプルスタイル)

#### 1B. 元 file `src/routes/TranscriptView.tsx` の修正

- L80-107 を削除 (2 関数まるごと、関数間の空行も削除し他の関数との整合性確認)
- 既存 import section に `import { getPermissionStatusLabel, getPermissionRowClassName } from "../utils/permissionStatusHelpers";` を追加 (alphabetical 順を維持、Loop 125 の audioLevelHelpers の隣等)
- callsite 4 件は **無変更で動作する** (関数名そのまま、import 追加のみで参照解決):
  - L1720 `getPermissionStatusLabel(...)` (micPermissionStatusLabel)
  - L1725 `getPermissionStatusLabel(...)` (screenPermissionStatusLabel)
  - L1877 `className={getPermissionRowClassName(...)}`
  - L1906 `className={getPermissionRowClassName(...)}`

注意: 削除後の行番号は変動するため、`grep -n "function getPermissionStatusLabel\|function getPermissionRowClassName" src/routes/TranscriptView.tsx` で抽出範囲確定後に削除。

#### 1C. 検証 (Commit 1 前に必ず実行)

```
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
npm run build  # tsc + vite build 両方通ること
bash scripts/agent-verify.sh src/routes/TranscriptView.tsx src/utils/permissionStatusHelpers.ts
```

#### 1D. Commit 1 メッセージ (Conventional Commits)

```
refactor(frontend): TranscriptView の permission status helpers を utils/permissionStatusHelpers.ts に抽出 (mjc-main-20260505-64 Loop 127, frontend 軸 8 件目 = 純粋関数機能分離軸 frontend 版 5 件目, scope 27 軸目開拓, 大型 frontend file 責務分離 40 file 目, getPermissionStatusLabel + getPermissionRowClassName 抽出, R6 段階的分割 2 件目, AGENTS.md priority 1 寄与)
```

ファイルは `git add src/routes/TranscriptView.tsx src/utils/permissionStatusHelpers.ts` のみ。

Commit 1 完了後、`git log -1 --format=%H` で hash を取得して **記憶しておく**。

### Commit 2: chore(agent-log) — Loop 127 エントリ追記

#### 2A. AGENT_LOG.md 末尾追記

末尾の `---` 直後に以下のフォーマットでエントリ追記:

```
[mjc-main-20260505-64 Loop 127 / 2026-05-06]
worker: mjc-worker-loop127-permission-status-helpers-extract (作業)
範囲: src/routes/TranscriptView.tsx (-28 行 = 2 関数削除 + import 1 行追加 = 実質 -27 行) + src/utils/permissionStatusHelpers.ts (新規 ~22 行 = export 2 関数)
内容: TranscriptView.tsx L80-107 の getPermissionStatusLabel + getPermissionRowClassName を新 file utils/permissionStatusHelpers.ts に抽出。frontend 軸 8 件目 = 純粋関数機能分離軸 frontend 版 5 件目 = scope 27 軸目開拓 = 大型 frontend file 責務分離 40 file 目 = R6 段階的分割 2 件目 (Loop 125 audio level helpers の続編)。callsite 4 件 (L1720 + L1725 + L1877 + L1906) は import 追加のみで参照解決。alternation pattern 19 連続成功維持 (K(126) → frontend(127))。メイン批判判断 = 既存 utils/macosPrivacySettings.ts (OS settings paths) との責務階層精査で **異階層 = 新 file 作成軸採用** = Loop 119/121/123/125 教訓継承 (paradigm 反復 < 責務階層精査)。
変更ファイル: src/routes/TranscriptView.tsx, src/utils/permissionStatusHelpers.ts
検証: npm run build OK (tsc + vite build), bash scripts/agent-verify.sh OK
commit: <Commit 1 で取得した hash> refactor(frontend): TranscriptView の permission status helpers を utils/permissionStatusHelpers.ts に抽出
依存関係追加: なし
失敗理由: なし
次アクション: メイン側で SESSION SUMMARY 末尾追記 chore commit を予定
```

`<Commit 1 で取得した hash>` は **必ず実 hash の先頭 7 文字に置換**。

#### 2B. Commit 2 検証 + メッセージ

```
git add AGENT_LOG.md
git commit -m "chore(agent-log): mjc-main-20260505-64 Loop 127 = TranscriptView の permission status helpers を utils/permissionStatusHelpers.ts に抽出 (構造分離 40 file 目, frontend 軸 8 件目, scope 27 軸目開拓, 純粋関数機能分離軸 frontend 版 5 件目, alternation 19 連続成功, R6 段階的分割 2 件目)"
```

# 規模超過防止段落 (絶対遵守)

- **担当範囲は src/routes/TranscriptView.tsx + src/utils/permissionStatusHelpers.ts + AGENT_LOG.md のみ**。それ以外のファイル (src-tauri/, components/, hooks/, plan.md 等) は **絶対に触らない**。
- TranscriptView.tsx は 2442 行の大型 file = **Read 全体禁止** = `head -110 src/routes/TranscriptView.tsx | tail -35` 等の範囲指定で参照する。L80-107 抽出 + import 行追加 (上部) のみが目標。
- 検証は **npm run build (tsc + vite build) のみ** (frontend 軸 = cargo 不要)。
- **行末の空白文字 (trailing whitespace) 禁止** = エディタ設定 + `grep -nP " +$" file` で必ず確認。
- 抽出 2 関数以外の純粋関数 (残 25 件) は **本 Loop で触らない** (将来 Loop で段階的に対応)。

# 完走判定基準

- Commit 1 = refactor(frontend) 1 件
- Commit 2 = chore(agent-log) 1 件
- AGENT_LOG.md 末尾に Loop 127 entry 追加済
- npm run build OK + agent-verify.sh OK
- git status clean (handoff/* と worker-prompts/* untracked のみ残留)

# precedent (cat 全体推奨)

- src/utils/audioLevelHelpers.ts (Loop 125 = 15 行 = 規模 SS = 直近 precedent)
- src/utils/transcriptViewFormatters.ts (Loop 107 = 命名スタイル参考)
- src/utils/modelSelectorHelpers.ts (Loop 112 = 18 行 = 同 SS 規模)

# その他

- claude-agent-handoff-main.sh は触らない (interactive TUI 起動用)。
- `--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ。
- 完走後は端末で本 worker を終了 (claude -p print mode は最終出力一括方式 = stdout は完了まで空)。

頑張ってください。Loop 127 完走 = 累計 worker 完走 226/226 (100% 維持) を期待します。
