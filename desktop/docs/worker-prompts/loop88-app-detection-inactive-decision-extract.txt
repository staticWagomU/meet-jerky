あなたは meet-jerky の作業担当エージェント (Claude Code, print mode) = mjc-worker-app-detection-inactive-decision-loop88 です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。コミットは禁止しません (実施してください)。

# 必読 (順番厳守)

1. **AGENT_LOG.md の末尾 350 行を `tail -350 AGENT_LOG.md` で読んで状況を把握する**。AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う。**先頭は絶対に触らない**。
2. AGENTS.md / docs/product-concept.md / docs/agent-harness-claude.md (各 Read 全体可)
3. **大型ファイルは Read 全体禁止 = tail/head/grep で対象範囲のみ参照** (AGENT_LOG.md 31K+ 行、app_detection.rs ~3033 行 = ヒント参照のみ)
4. **直前 Loop 87 = `app_detection_notification.rs` 抽出が完璧完走済 = 同 shape (新 file + caller use 追加 + test 移動 + lib.rs mod 追加)** で本 Loop 88 を進めること。

# 担当範囲 (Loop 88 = 構造分離 14 件目 = 機能分類軸 2 件目)

`src-tauri/src/app_detection.rs` から **inactive 判定の純粋関数 2 件** を新 module `src-tauri/src/app_detection_inactive_decision.rs` に抽出する。

## 抽出対象 (production fn 2 件、純粋関数)

`app_detection.rs` の以下 2 fn を新 file へ移動 (~44 行 + doc コメント):

- L211-232: `fn should_warn_polling_stall(now_secs, last_seen_secs, last_warned_secs, expected_interval_secs, throttle_secs) -> Option<u64>` (`#[cfg_attr(not(target_os = "macos"), allow(dead_code))]` 付き)
- L237-258: `fn should_notify_meeting_inactive(now_secs, last_seen_secs, last_notified_secs, inactive_threshold_secs, throttle_secs) -> Option<u64>` (`#[allow(dead_code)]` 付き)

抽出後、新 file 内では visibility を `pub(crate)` に変更 (caller は同 crate 内 app_detection.rs のため)。

**重要 (state 非露出方針)**: state 依存 2 件 = `check_meeting_inactive_for_bundle` (L270-302) と `check_all_inactive_bundles` (L310-326) は **app_detection.rs に残す**。STATE / MEETING_INACTIVE_THRESHOLD / NOTIFICATION_THROTTLE / DetectionState の露出を回避し encapsulation を維持する設計判断。これらの fn の中身は本 Loop で一切触らない。

## 抽出対象 (axis test 20 件、`#[cfg(test)] mod tests` 内)

`app_detection.rs` の以下 20 test を新 file の `#[cfg(test)] mod tests` 内に移動:

- L1524: `fn should_warn_polling_stall_first_call_returns_none()`
- L1530: `fn should_warn_polling_stall_within_normal_range_returns_none()`
- L1536: `fn should_warn_polling_stall_stalled_returns_some_elapsed()`
- L1542: `fn should_warn_polling_stall_throttled_returns_none()`
- L1548: `fn should_warn_polling_stall_boundary_at_expected_times_three_returns_none()`
- L1558: `fn should_warn_polling_stall_clock_skew_last_warned_after_now_returns_none()`
- L1567: `fn should_warn_polling_stall_now_equals_last_seen_returns_none()`
- L1573: `fn should_warn_polling_stall_zero_expected_returns_some_when_elapsed_positive()`
- L1580: `fn should_warn_polling_stall_zero_throttle_returns_some_when_stalled()`
- L1587: `fn should_warn_polling_stall_now_secs_max_with_seen_returns_some_huge_elapsed()`
- L1597: `fn should_notify_meeting_inactive_first_call_returns_none()`
- L1603: `fn should_notify_meeting_inactive_within_active_range_returns_none()`
- L1609: `fn should_notify_meeting_inactive_inactive_returns_some_elapsed()`
- L1618: `fn should_notify_meeting_inactive_throttled_returns_none()`
- L1627: `fn should_notify_meeting_inactive_now_equals_last_seen_returns_none()`
- L1636: `fn should_notify_meeting_inactive_threshold_boundary_returns_some()`
- L1646: `fn should_notify_meeting_inactive_zero_threshold_returns_some_when_elapsed_positive()`
- L1656: `fn should_notify_meeting_inactive_zero_throttle_returns_some_when_inactive()`
- L1666: `fn should_notify_meeting_inactive_clock_skew_last_notified_after_now_returns_none()`
- L1676: `fn should_notify_meeting_inactive_now_secs_max_with_seen_returns_some_huge_elapsed()`

移動先での import: `use super::{should_warn_polling_stall, should_notify_meeting_inactive};`

各 test の `#[test]` attribute + 行末 `}` まで完全に移植する。test 内の各 `assert_eq!` / `assert!` の expected 値は完全に無変更で OK (純粋関数の振る舞いは保たれる)。

## caller 修正方針 (推奨 = 冒頭 use 追加 + 呼び出し側変更なし)

`app_detection.rs` 冒頭 use 文に下記 1 行を追加 (Loop 87 と同方式):

```rust
use crate::app_detection_inactive_decision::{should_notify_meeting_inactive, should_warn_polling_stall};
```

これで既存の呼び出し (L288 の `should_notify_meeting_inactive(...)` / L348 の `should_warn_polling_stall(...)`) はそのまま残せる。

## lib.rs 修正

`src-tauri/src/lib.rs` で alphabetical 順を維持して `mod app_detection_inactive_decision;` を追加。
位置: `mod app_detection_goto;` (L3) と `mod app_detection_notification;` (L4 = Loop 87 で挿入) の間に挿入。

## 新 file の構造 (推奨テンプレート)

```rust
//! 会議アプリ検知の inactive / polling stall 判定ロジック (純粋関数 2 件)。
//!
//! state 依存しない純粋関数として `should_warn_polling_stall` /
//! `should_notify_meeting_inactive` を提供する。state 依存の wrapper
//! (`check_meeting_inactive_for_bundle` / `check_all_inactive_bundles`)
//! は STATE 露出回避のため `app_detection.rs` 本体に残す。

/// browser_url_callback の発火間隔が想定より大幅に遅延しているかを判定する純粋関数。
/// `Some(elapsed)` は警告対象の経過秒数、`None` は警告不要。
#[cfg_attr(not(target_os = "macos"), allow(dead_code))]
pub(crate) fn should_warn_polling_stall(
    now_secs: u64,
    last_seen_secs: u64,
    last_warned_secs: u64,
    expected_interval_secs: u64,
    throttle_secs: u64,
) -> Option<u64> {
    // (元 L218-231 の本体を完全移植)
}

/// 監視中の会議アプリが長時間検知されない場合に「会議終了」通知を発火すべきかを判定する純粋関数。
/// `Some(elapsed)` は通知対象の経過秒数、`None` は通知不要。
#[allow(dead_code)]
pub(crate) fn should_notify_meeting_inactive(
    now_secs: u64,
    last_seen_secs: u64,
    last_notified_secs: u64,
    inactive_threshold_secs: u64,
    throttle_secs: u64,
) -> Option<u64> {
    // (元 L244-257 の本体を完全移植)
}

#[cfg(test)]
mod tests {
    use super::{should_notify_meeting_inactive, should_warn_polling_stall};

    // (元 L1524-1690 圏内の 20 test を完全移植、各々 #[test] attribute も保持)
}
```

# 規模超過防止 (重要)

- 担当範囲は **app_detection.rs L211-258 (純粋 fn 2 件) + L1524-1690 圏内の test 20 件 + 冒頭 use 文 1 行追加 + lib.rs mod 追加 + 新 file `app_detection_inactive_decision.rs`** に限定。**state 依存 fn (check_meeting_inactive_for_bundle / check_all_inactive_bundles) は触らない**。他の fn / test / file は触らない。
- **test 件数の上限**: cargo test --lib は **704 件全 pass** を維持 (件数完全不変)。test 移動のみで増減なし。
- 振る舞い不変: 純粋関数の expected 値は同一なので、test 内 assert は完全に無変更で OK。
- doc コメント (`#[cfg_attr]` / `#[allow]` 含む) を new file に正確に移植する。元の `///` doc も保持。

# 検証手順 (順番厳守、worker 自身で全実施)

1. `cd src-tauri && cargo build --lib` (エラーなし確認)
2. `cd src-tauri && cargo clippy --lib --tests -- -D warnings` (警告ゼロ確認)
3. `cd src-tauri && cargo fmt --check` (OK 確認)
4. `cd src-tauri && cargo test --lib 2>&1 | tail -20` (704 passed 確認、件数完全不変)
5. `bash scripts/agent-verify.sh src-tauri/src/app_detection.rs src-tauri/src/app_detection_inactive_decision.rs src-tauri/src/lib.rs` で総合検証

# 2-commit pattern (Loop 60-87 連続 28 ループ実証 + Loop 80/85/86/87 進化 pattern)

1. **refactor commit**: app_detection.rs + app_detection_inactive_decision.rs + lib.rs を staging して commit
   - メッセージ: `refactor(app-detection): inactive 判定の純粋関数 2 件を app_detection_inactive_decision.rs に抽出 (mjc-main-20260505-45 Loop 88, 機能分類軸 2 件目, 大型 rust file 責務分離 14 file 目, AGENTS.md priority 1 寄与)`
   - **commit 後、`git log -1 --format=%h` で実 hash を取得**
2. AGENT_LOG.md 末尾の `---` 直後に Loop 88 エントリを追記
   - **取得した refactor commit 実 hash を直接書く** (placeholder `<hash>` を書かない、Loop 80/85/86/87 precedent)
   - chore commit hash は **書かない** = メイン補正不要 pattern
3. **chore commit**: AGENT_LOG.md のみ staging して commit
   - メッセージ: `chore(agent-log): mjc-main-20260505-45 Loop 88 = app_detection_inactive_decision.rs 抽出 (構造分離 14 file 目, 機能分類軸 2 件目, 純粋関数のみ抽出 = state 露出回避 = encapsulation 維持) のエントリ追記`

# AGENT_LOG.md 末尾追記 手順 (重要)

1. `tail -10 AGENT_LOG.md` で末尾を確認 (末尾は `---` 区切り)
2. 末尾の `---` 直後に Loop 88 エントリを追記。形式 (precedent 踏襲):

```
[mjc-main-20260505-45 Loop 88 / 2026-05-05]
worker: mjc-worker-app-detection-inactive-decision-loop88 (作業)
範囲: src-tauri/src/app_detection.rs (純粋関数 2 件削除 + axis test 20 件削除 + 冒頭 use 文 1 行追加) + src-tauri/src/app_detection_inactive_decision.rs (新規 ~XX 行) + src-tauri/src/lib.rs (mod 追加)
内容: app_detection.rs L211-258 の should_warn_polling_stall / should_notify_meeting_inactive 純粋関数 2 件を新 module app_detection_inactive_decision.rs に抽出。state 依存 2 件 (check_meeting_inactive_for_bundle / check_all_inactive_bundles) は STATE 露出回避のため app_detection.rs に残す = encapsulation 維持の設計判断。axis test 20 件 (should_warn_polling_stall_* 10 件 + should_notify_meeting_inactive_* 10 件) を新 file 内 #[cfg(test)] mod tests に移動。caller 修正は冒頭 `use crate::app_detection_inactive_decision::{should_notify_meeting_inactive, should_warn_polling_stall};` を追加して既存呼び出し (L288 / L348) を無変更で残す方式。lib.rs alphabetical 順に `mod app_detection_inactive_decision;` を app_detection_goto と app_detection_notification の間に挿入。大型 rust file 責務分離 14 file 目 = 機能分類軸 2 件目 = Loop 87 notification 抽出の続編。
振る舞い: cargo test --lib 704 passed (件数完全不変、純粋関数の expected 値同一のため test assert 無変更)
clippy: 警告ゼロ (-D warnings)
fmt: OK
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <refactor 実 hash> refactor(app-detection): inactive 判定の純粋関数 2 件を app_detection_inactive_decision.rs に抽出
AGENTS.md priority: 1 (構造美の補強 = 機能分類軸の single source of truth 化、Loop 87 続編)

---
```

3. **trailing whitespace (行末の空白文字) を絶対に入れない**。エディタ設定で「保存時 trailing whitespace 削除」を有効化、または保存後 `grep -nE " +$" AGENT_LOG.md | tail -5` で確認 = 出力ゼロが正常。

# 完了報告

worker prompt 完了時 (1 ターンで報告) に、以下を簡潔にまとめる:

- 変更ファイル 3 件 (app_detection.rs / app_detection_inactive_decision.rs / lib.rs) + AGENT_LOG.md 1 件 = 4 file 変更
- cargo test 件数 (704 passed)、clippy 警告ゼロ、fmt OK
- refactor commit hash + chore commit hash 両方
- 残リスク (基本なし、振る舞い不変のため、純粋関数のみ抽出で state 非露出)
- 1 ターンで完結する形でまとめてください。
