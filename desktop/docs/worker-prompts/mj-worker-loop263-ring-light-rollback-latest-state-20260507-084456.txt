あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

必ず読んでください:
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness.md
- AGENT_LOG.md

担当範囲:
- src/routes/TranscriptView.tsx
- AGENT_LOG.md
- この worker prompt ファイル

目的:
Loop 263 のリングライト race guard について、古い表示要求が後から完了した場合の rollback が、最新状態が再び表示中になっているケースまで隠してしまうリスクを潰してください。

実装条件:
- 既存 Loop 263 差分と `ringLightVisibilityRequestIdRef` を戻さず、最小補正してください。
- `TranscriptView` 内に最新の desired visibility を保持する `useRef<boolean>` を追加してください。
- リングライト連動 effect のたびに、その ref へ `shouldShowRingLight` を保存してください。
- 古い表示要求が `set_ring_light_visible(true)` 完了後に stale だった場合、rollback の `set_ring_light_visible(false)` は「最新 desired visibility が false」の場合だけ実行してください。
- 最新 desired visibility が true の場合は、古い表示要求の完了は最終状態と矛盾しないため rollback しないでください。
- ユーザー向け `meetingError` は引き続き最新 request の失敗だけに出してください。
- `set_live_caption_window_visible` の既存 useEffect、録音/文字起こし開始停止ロジック、RingLightWindow.tsx、ringLight.ts、CSS、Rust、依存関係、テスト基盤は変更しないでください。
- コミットは禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/routes/TranscriptView.tsx AGENT_LOG.md docs/worker-prompts/mj-worker-loop263-ring-light-rollback-latest-state-20260507-084456.txt`

AGENT_LOG.md に、開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。Loop 263 の新機能補正なので、未実機確認範囲も明記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
