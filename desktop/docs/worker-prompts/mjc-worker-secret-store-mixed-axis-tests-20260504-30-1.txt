あなたは作業担当エージェント (Claude Code, print mode) です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

# 必読 (順序重要)
1. AGENTS.md
2. docs/product-concept.md
3. docs/agent-harness-claude.md
4. **AGENT_LOG.md の末尾 350 行を `tail -350 AGENT_LOG.md` で読む** = 末尾追記の場所を視覚的に学習する

# AGENT_LOG.md の追記運用 (絶対遵守)
- AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う。
- **先頭は絶対に触らない**。
- 具体手順: 編集前に `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に新エントリを追記する。
- AGENT_LOG.md は 21800+ 行 / 1.86MB+ あるため Read 全体禁止。tail/head/grep で対象範囲のみ参照すること。
- **行末の空白文字 (trailing whitespace) 禁止**。記述後に grep で確認する。

# 担当範囲 (絶対遵守)
- src-tauri/src/secret_store.rs の `mod tests` 末尾 (l.236 行 `}` 直前 / 既存 7 test の後) に test 関数 3 件のみ追加。
- 関数本体 (set_secret / get_secret / delete_secret / has_secret / set_*_api_key / clear_*_api_key / has_*_api_key) と enum SecretKey 定義と既存 test 7 件は **完全無変更**。
- 担当範囲外の他ファイル編集は禁止。
- AGENT_LOG.md への末尾追記 1 エントリ (本作業の記録)。
- それ以外の編集 (worker-prompts/handoff/scripts/その他 src/ ファイル) は一切禁止。

# 検証制約
- **Keychain 実通信禁止** (macOS 権限ダイアログ防止) = 実 Keychain を叩く test (set_secret 直接呼び出し / set_*_api_key("実値") 等) 禁止。
- 課金禁止 (OpenAI/ElevenLabs 実 API は禁止)。
- `cargo fmt --check` / `cargo clippy --lib -- -D warnings` / `cargo test --lib -- --test-threads=1` 全て pass 必須。
- 既存 560 件 + 新規 3 件 = 563 件 pass を目標。

# 実装内容 (3 軸 application = 混合 application パターン拡張: Debug 軸 + trait 軸 + format 軸)

既存 7 test の網羅軸:
- T1: set_openai_api_key_rejects_empty_input (空入力 reject)
- T2: set_elevenlabs_api_key_rejects_empty_input (同上)
- T3: secret_key_account_is_stable (account 文字列固定値)
- T4: secret_apis_error_clearly_on_unsupported_platforms (Linux 環境 only, cfg 分岐)
- T5: set_api_keys_return_consistent_japanese_message_for_empty_input (文言一致)
- T6: secret_key_accounts_are_distinct (account 重複なし)
- T7: set_api_keys_treat_various_whitespace_chars_as_empty (whitespace バリエーション)

未保護の軸 (本 Loop で 3 軸 application):

## T8 (Debug 出力対称軸 + 機密性軸)
名称: `secret_key_debug_output_uses_variant_name_without_leaking_account_string`
契約:
- `format!("{:?}", SecretKey::OpenAIApiKey)` の出力に `"OpenAIApiKey"` が含まれる
- `format!("{:?}", SecretKey::ElevenLabsApiKey)` の出力に `"ElevenLabsApiKey"` が含まれる
- どちらの Debug 出力にも account 文字列 (`"openai-api-key"` / `"elevenlabs-api-key"`) は **含まれない**
意義:
- `#[derive(Debug)]` 自動派生による現契約 (variant 名のみ出力 / フィールド出力なし) を CI 固定。
- 「将来 Debug を手動実装に切替えて account 値を含めてしまう誤改修」「ログ出力時に機密文字列が漏れる誤改修」を遮断。
- Debug 出力の安定性 + 機密値漏洩防止の二重保護。

実装例:
```rust
#[test]
fn secret_key_debug_output_uses_variant_name_without_leaking_account_string() {
    // Debug 出力は variant 名のみで構成され、account 文字列を含まない現契約を固定する。
    // 将来「Debug を手動実装に切替えて account 値を含めてしまう誤改修」「panic 時の
    // ログに機密文字列が漏れる誤改修」を遮断する装置。
    let openai_dbg = format!("{:?}", SecretKey::OpenAIApiKey);
    let elevenlabs_dbg = format!("{:?}", SecretKey::ElevenLabsApiKey);
    assert!(
        openai_dbg.contains("OpenAIApiKey"),
        "Debug 出力に variant 名がない: {openai_dbg}"
    );
    assert!(
        elevenlabs_dbg.contains("ElevenLabsApiKey"),
        "Debug 出力に variant 名がない: {elevenlabs_dbg}"
    );
    assert!(
        !openai_dbg.contains("openai-api-key"),
        "Debug 出力に account 文字列が漏出: {openai_dbg}"
    );
    assert!(
        !elevenlabs_dbg.contains("elevenlabs-api-key"),
        "Debug 出力に account 文字列が漏出: {elevenlabs_dbg}"
    );
}
```

## T9 (trait 対称軸 = Copy trait の executable specification)
名称: `secret_key_implements_copy_trait_so_value_remains_usable_after_assignment`
契約:
- `let key = SecretKey::OpenAIApiKey; let copied = key;` 後も `key.account()` が呼べる (move されない)
- `copied.account()` と `key.account()` が同じ値を返す
意義:
- `#[derive(Clone, Copy)]` の Copy trait 実装を CI 固定。
- 「将来 derive(Copy) を消して move semantics に変える誤改修」を遮断 (呼び出し元で `.clone()` 強制になり API 表面が壊れる)。
- 呼び出し元 (set_secret / get_secret 等で `key: SecretKey` を引数で受け取り、`entry_for(key)` と `key.account()` を順次使用) の API contract を保護。

実装例:
```rust
#[test]
fn secret_key_implements_copy_trait_so_value_remains_usable_after_assignment() {
    // SecretKey は #[derive(Copy)] により値型として扱える現契約を固定する。
    // 将来「Copy を消して move semantics に変える誤改修」が起きると、呼び出し元
    // (entry_for(key) と key.account() の順次使用) で borrow checker error が
    // 発生し API 表面が壊れるため遮断装置として機能する。
    let key = SecretKey::OpenAIApiKey;
    let copied = key; // Copy で move されない
    assert_eq!(
        copied.account(),
        "openai-api-key",
        "Copy 後の値が元と異なる"
    );
    assert_eq!(
        key.account(),
        "openai-api-key",
        "元の値が move されて使えなくなった"
    );
}
```

## T10 (format 対称軸 = account 文字列の format 不変条件)
名称: `secret_key_account_strings_use_lowercase_kebab_case_format_for_all_variants`
契約 (全 SecretKey variant について):
- account 文字列が空でない
- ASCII 小文字 + ハイフン (`-`) のみで構成される (大文字・数字・アンダースコア・空白なし)
- ハイフンを少なくとも 1 つ含む (kebab-case 区切り)
- ハイフンで開始しない / 終了しない
意義:
- 既存 T3 (secret_key_account_is_stable) は絶対値固定だが「format 規則」は明示していない。
- 将来 variant 追加 (例: `SecretKey::AnthropicApiKey`) 時に「PascalCase 命名」「数字混入」「ハイフン抜け」誤改修を遮断。
- Keychain account 命名規則の一貫性を CI 固定する装置。

実装例:
```rust
#[test]
fn secret_key_account_strings_use_lowercase_kebab_case_format_for_all_variants() {
    // account 文字列の format 規則 (lowercase kebab-case) を全 variant で固定する。
    // 既存 secret_key_account_is_stable は絶対値を固定するが、format 規則は
    // 明示していないため、将来 variant 追加時の命名一貫性を保証する装置として補完。
    for key in [SecretKey::OpenAIApiKey, SecretKey::ElevenLabsApiKey] {
        let account = key.account();
        assert!(!account.is_empty(), "account 文字列が空: {account}");
        assert!(
            account.chars().all(|c| c.is_ascii_lowercase() || c == '-'),
            "account に小文字・ハイフン以外の文字を含む: {account}"
        );
        assert!(
            account.contains('-'),
            "kebab-case 区切りのハイフンがない: {account}"
        );
        assert!(
            !account.starts_with('-'),
            "ハイフンで開始: {account}"
        );
        assert!(
            !account.ends_with('-'),
            "ハイフンで終了: {account}"
        );
    }
}
```

# 実装手順
1. 必読ドキュメントを順番に読む。AGENT_LOG.md は `tail -350 AGENT_LOG.md` で末尾のみ参照 (SESSION SUMMARY @ 2026-05-04 ~20:30 JST 部分が状況復元の起点)。
2. `src-tauri/src/secret_store.rs` 全体を Read (237 行で全体読み OK)。既存 test 7 件の文言・構造を確認。
3. mod tests 末尾 (現状 l.236 の `}` 直前) に T8 / T9 / T10 を順番に追加。実装例の文面を尊重して微調整可。
4. 検証 (順番):
   - `cd src-tauri && cargo fmt --check` (差分なし確認)
   - `cd src-tauri && cargo clippy --lib -- -D warnings` (警告ゼロ確認)
   - `cd src-tauri && cargo test --lib -- --test-threads=1` (560 → 563 passed 確認)
5. AGENT_LOG.md 末尾追記:
   - `tail -10 AGENT_LOG.md` で現在の末尾を確認
   - 末尾の `---` 直後に新エントリ (`## mjc-worker-secret-store-mixed-axis-tests-20260504-30-1` 見出し) を Edit ツールで追加
   - 内容: 開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果 (`test result: ok. 563 passed; 0 failed; ...`)、依存関係追加 (なし)、失敗理由 (なし)、次アクション (メインへ報告、commit 待ち)
6. **行末空白チェック**: `grep -nE ' +$' src-tauri/src/secret_store.rs` と `grep -nE ' +$' AGENT_LOG.md | tail -5` で trailing whitespace 0 件を確認。

# 完了報告 (1 ターンで完結)
- 変更ファイル一覧
- 検証結果 (test 件数、clippy 警告数、fmt 差分)
- 残リスク (なければ「なし」)
