あなたは作業担当エージェント (Claude Code, print mode) です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみ。

# 必読
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness-claude.md
- AGENT_LOG.md は 20120+ 行 / 1.7MB+ の巨大ファイル。**先頭は絶対に読まない・触らない**。`tail -350 AGENT_LOG.md` で末尾だけ参照する。

# AGENT_LOG.md 追記ルール (極めて重要)
- AGENT_LOG.md は **時系列順**: 最古ログが先頭、最新ログが末尾。
- **新規追記は必ずファイル末尾に行う**。先頭は絶対に触らない。
- 追記前に `tail -10 AGENT_LOG.md` で末尾を確認し、最終行が `---` (区切り) で終わっていることを確認。
- 追記場所は「ファイルの最終行の `---` の **後**」。新しい区切り線 `---` の直後にエントリ追加し、エントリの最後にも `---` を付ける。
- **先頭追記は事故**。再発させない。

# 担当範囲
src-tauri/src/transcript_bridge.rs の **mod tests に test 3 件のみ追加**。関数本体は **完全無変更**。
他ファイルは触らない。コミット禁止 (メインがやる)。

# 追加する test 3 件 (mod tests の末尾、Loop 2 で追加された最終 test `build_append_args_for_emission_at_returns_some_for_zero_start_with_is_error_some_false` の **直後**)

## T1: build_append_args_for_emission_falls_back_to_stream_started_when_zero_start_segment
- 目的: `build_append_args_for_emission` (cfg(test) wrapper, line 57-69, 内部で `build_append_args_for_emission_at(..., None)` 呼び出し) を経由した zero_start segment + 内部固定 observed=None で `unwrap_or(stream_started_at_secs)` の None 分岐が wrapper 経由で動く現契約を CI 固定。
- Loop 1 T1 (segment_to_append_args 低層) + Loop 2 T1 (build_append_args_for_emission_at 中層) と対称的に wrapper 高層で同契約を保護 = 3 層の二重保護構造完成。
- Setup:
  - segment.start_ms = 0, end_ms = 0, speaker = Some("自分"), text = "test"
  - `build_append_args_for_emission(&segment, Some(1_000), 1_040)` を呼ぶ (observed は内部で None 固定)
- Assert:
  - result.is_some() = true
  - result.unwrap().1 == 40 (= 1040 - 1000)
- コメント: 「wrapper 経由でも zero_start + 内部固定 observed=None で stream_started_at_secs フォールバックが効く現契約。Loop 1 T1 (segment_to_append_args 低層) + Loop 2 T1 (build_append_args_for_emission_at 中層) と対称的に wrapper 高層で同境界を 3 層保護。」

## T2: build_append_args_for_emission_at_handles_extreme_observed_at_secs_max_with_zero_start
- 目的: zero_start segment + observed_at_secs = Some(u64::MAX) + session_started_at_secs = Some(0) のとき、segment_abs_secs = u64::MAX → offset = u64::MAX - 0 = u64::MAX に飽和する現契約を CI 固定。saturating_sub の極端値挙動を emission helper 経由で保護。
- 既存 T5 (`segment_to_append_args_saturates_to_u64_max_for_extreme_start_ms`) は i64::MAX start_ms 経路、本 T2 は zero_start + observed=Some(u64::MAX) 経路 = 別軸の極端値。
- Setup:
  - segment.start_ms = 0, end_ms = 0, speaker = Some("自分"), text = "extreme"
  - `build_append_args_for_emission_at(&segment, Some(0), 0, Some(u64::MAX))` を呼ぶ
- Assert:
  - result.is_some() = true
  - result.unwrap().1 == u64::MAX (= u64::MAX - 0 = u64::MAX、saturating_sub は値変化なし)
- コメント: 「zero_start + observed=Some(u64::MAX) + session=Some(0) → segment_abs=u64::MAX → offset=u64::MAX (saturating_sub の no-op 範囲)。panic 化や別キャスト方式への誤改修検知。Loop 1 T5 (engine path 極端値) と対称な observed path 極端値。」

## T3: build_append_args_for_emission_at_saturates_to_zero_when_session_at_max_with_zero_start
- 目的: zero_start segment + session_started_at_secs = Some(u64::MAX) + stream=0 + observed=None のとき、segment_abs_secs = stream(0) → offset = 0 - u64::MAX → saturating_sub で 0 に飽和する現契約を CI 固定。clock 逆転の極端版。
- Setup:
  - segment.start_ms = 0, end_ms = 0, speaker = Some("相手側"), text = "boundary"
  - `build_append_args_for_emission_at(&segment, Some(u64::MAX), 0, None)` を呼ぶ
- Assert:
  - result.is_some() = true
  - result.unwrap().1 == 0 (= 0 - u64::MAX → saturating_sub → 0)
- コメント: 「session=Some(u64::MAX) + zero_start + stream=0 + observed=None → segment_abs=0 → offset=saturating_sub(0, u64::MAX)=0 (extreme clock 逆転)。Loop 2 T2 (通常範囲の clock 逆転) と対称な極端値版。?演算子は Some(u64::MAX) も通る (= 0 と混同しない) 契約も同時に固定。」

# 既存パターン踏襲 (厳守)
- 命名規則: 既存 test 命名規則を厳格に踏襲。
- 構造: struct literal → 関数呼び出し → assert_eq!/assert! の順 = 既存 test と完全対称。
- assert メッセージ: 日本語で現契約の固定理由を明記。
- T1/T2/T3 全て struct literal で明示構築。

# Tidy First 厳守
- 関数本体完全無変更 (mod tests のみに test 3 件追加)。
- ユーザー観測可能な振る舞い不変。
- 規模 XS = test 3 件のみ。test 4 件以上の追加は scope 超過、絶対禁止。

# 完了後の確認
1. `cargo test --lib --manifest-path src-tauri/Cargo.toml transcript_bridge::tests` で 3 件追加分が pass。元の 494 → 497 になるはず。
2. `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` でゼロ警告。
3. `cargo fmt --manifest-path src-tauri/Cargo.toml --check` で差分なし。
4. AGENT_LOG.md の **末尾に** 開始日時等を追記。`tail -10 AGENT_LOG.md` で末尾確認後に追記。**先頭は絶対に触らない**。
5. 1 ターン完結報告: 変更ファイル / 検証結果 / 残リスク。

# 残リスク事前明示
- なし (関数本体無変更、test 追加のみ)。
