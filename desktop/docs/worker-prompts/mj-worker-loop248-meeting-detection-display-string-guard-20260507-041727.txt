あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src/utils/meetingDetection.ts
- AGENT_LOG.md

目的:
`meeting-app-detected` の frontend payload guard で、会議検知プロンプトに表示される文字列フィールドが空でないだけでなく、制御文字なし・上限長内であることを確認してください。会議検知通知は録音開始導線に直結するため、破損 payload を UI 表示名として受け取らないようにします。

実装条件:
1. `src/utils/meetingDetection.ts`
   - `bundleId` / `appName` / `service` / `browserName` のような表示・識別文字列に、trim 後非空、C0/DEL 制御文字なし、trim 後長さ上限内の guard を適用してください。
   - 推奨上限は `bundleId` 256、`appName` / `service` / `browserName` 120 程度です。定数名で意図が分かるようにしてください。
   - `urlHost` の host-only 判定、privacy field 拒否 (`url` / `fullUrl` / `windowTitle`)、source ごとの必須/禁止フィールド契約は維持してください。
   - event 名、型定義、React component、Rust 側、UI 文言、依存関係、テスト基盤は変更しないでください。
2. AGENT_LOG.md
   - 開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。
   - 変更ファイルには、この worker prompt と research prompt も含めてください。
   - research は `AGENT_LOG.md` の古い summary 出力に引っ張られて最終候補が遅れたため、メイン側が `meetingDetection.ts` の payload guard を確認してこの最小候補を選んだことを記録してください。

禁止:
- コミットは禁止です。
- 依存追加は禁止です。
- `MeetingDetectedBanner.tsx`、`meetingDetectedBannerHelpers.ts`、Rust、型定義、UI 文言、テスト基盤、直近 Loop 242-247 の対象ファイルのうち担当外ファイルは変更しないでください。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/utils/meetingDetection.ts AGENT_LOG.md docs/worker-prompts/mj-research-loop248-next-value-slice-20260507-041727.txt docs/worker-prompts/mj-worker-loop248-meeting-detection-display-string-guard-20260507-041727.txt`

残リスクには、TS 単体テスト基盤がないため guard の入出力は build と diff review で確認すること、実機 `meeting-app-detected` emit 経路は今回未確認であることを明記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
