あなたは meet-jerky desktop の worker です。Loop 214 として、Whisper モデルダウンロードのイベント名 contract を小さく定数化してください。

前提:
- 作業ディレクトリ: /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
- 対象はこのリポジトリのみ。
- 課金、認証情報変更、破壊的操作、git reset --hard、未確認削除は禁止。
- 他の agent も同じ codebase にいる可能性があります。自分の担当外の差分を戻さず、衝突する場合は停止して報告してください。
- main agent は最終 review/verify/commit を行います。あなたは実装と自己検証まで行い、コミットしないでください。

目的:
- `model-download-progress` と `model-download-error` のイベント名 drift を防ぐため、Rust emit 側と React listen 側で定数を使う。
- payload shape、UI 文言、進捗計算、ダウンロード処理、エラー処理フローは変更しない。

変更範囲:
- `src-tauri/src/transcription_commands_model.rs`
- `src/utils/modelDownloadPayload.ts`
- `src/components/ModelSelector.tsx`
- `AGENT_LOG.md`

実装方針:
1. Rust:
   - `src-tauri/src/transcription_commands_model.rs` に以下の公開/非公開は既存スタイルに合わせた const を追加する。
     - `MODEL_DOWNLOAD_PROGRESS_EVENT = "model-download-progress"`
     - `MODEL_DOWNLOAD_ERROR_EVENT = "model-download-error"`
   - `app.emit(...)` の文字列リテラル 3 箇所を const 参照へ置き換える。
   - 既存 tests に、イベント名が固定値と一致する小さな test を追加する。
2. TypeScript:
   - `src/utils/modelDownloadPayload.ts` で同じイベント名 const を export する。
   - `src/components/ModelSelector.tsx` の `listen(...)` 文字列リテラル 2 箇所を import した const へ置き換える。
3. `AGENT_LOG.md`:
   - 末尾に Loop 214 の記録を追加する。
   - 実装内容と検証結果を書く。検証が失敗/skip した場合は正直に記録する。

検証:
- 可能なら以下を実行してください。
  - `PATH="/opt/homebrew/bin:$PATH" npm run build`
  - `~/.cargo/bin/cargo fmt --manifest-path src-tauri/Cargo.toml --check`
  - `~/.cargo/bin/cargo test --manifest-path src-tauri/Cargo.toml --lib transcription_commands_model`
  - `~/.cargo/bin/cargo clippy --manifest-path src-tauri/Cargo.toml --lib --tests -- -D warnings`
  - `git diff --check -- src-tauri/src/transcription_commands_model.rs src/components/ModelSelector.tsx src/utils/modelDownloadPayload.ts AGENT_LOG.md`

完了報告:
- 変更ファイル一覧
- 実装概要
- 実行した検証コマンドと結果
- 未解決リスクや意図的に触らなかった点
