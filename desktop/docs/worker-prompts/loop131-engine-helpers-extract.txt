あなたは meet-jerky の worker エージェントです。Loop 131 = TranscriptView.tsx Engine 系 2 純粋関数 (getRequiresLocalModel + getExternalApiProvider) を新 file `src/utils/transcriptionEngineHelpers.ts` に抽出します。

# 必読 (順序重要)

1. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
3. AGENT_LOG.md は時系列順 (最古ログ = 先頭、最新ログ = 末尾) = ~33,300 行 = **絶対に Read 全体しない**。`tail -350 AGENT_LOG.md` で末尾だけ参照。**先頭には絶対に触らない**。新規追記は必ずファイル末尾の `---` 直後に行う。
4. 既存 precedent (cat で全体確認すること、各 ~12-30 行で軽量):
   - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src/utils/audioLevelHelpers.ts` (Loop 125 = R6 段階的分割 1 件目)
   - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src/utils/permissionStatusHelpers.ts` (Loop 127 = R6 段階的分割 2 件目)
   - `cat /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/src/utils/trackStatusAriaLabels.ts` (Loop 129 = R6 段階的分割 3 件目)

# 範囲制限 (厳守)

- 編集対象は `src/routes/TranscriptView.tsx` (関数定義削除 + import 1 行追加) と新規 `src/utils/transcriptionEngineHelpers.ts` のみ。
- それ以外のファイルは触らない。
- `cargo test` 件数 (704 passed) は変動しない範囲のみ修正。
- TranscriptView.tsx 全体 (~2408 行) は **絶対に Read 全体しない** = 必要な範囲だけ `Read offset=... limit=...` で参照。
- 行末空白文字 (trailing whitespace) は **禁止**。
- `--no-verify` 禁止、`--dangerously-skip-permissions` は harness 内のみ。

# タスク詳細

## 抽出対象 (TranscriptView.tsx L345-L358)

```ts
function getRequiresLocalModel(engine: TranscriptionEngineType | undefined): boolean {
  return !engine || engine === "whisper";
}

function getExternalApiProvider(
  engine: TranscriptionEngineType | undefined,
): "OpenAI" | "ElevenLabs" | null {
  if (engine === "openAIRealtime") {
    return "OpenAI";
  }
  if (engine === "elevenLabsRealtime") {
    return "ElevenLabs";
  }
  return null;
}
```

## callsite (TranscriptView.tsx L637 + L640、各 1 件のみ)

```ts
const requiresLocalModel = getRequiresLocalModel(
  settings?.transcriptionEngine,
);
const externalApiProvider = getExternalApiProvider(
  settings?.transcriptionEngine,
);
```

## 手順

1. **新 file 作成**: `src/utils/transcriptionEngineHelpers.ts` を新規作成。内容:
   ```ts
   import type { TranscriptionEngineType } from "../types";

   export function getRequiresLocalModel(
     engine: TranscriptionEngineType | undefined,
   ): boolean {
     return !engine || engine === "whisper";
   }

   export function getExternalApiProvider(
     engine: TranscriptionEngineType | undefined,
   ): "OpenAI" | "ElevenLabs" | null {
     if (engine === "openAIRealtime") {
       return "OpenAI";
     }
     if (engine === "elevenLabsRealtime") {
       return "ElevenLabs";
     }
     return null;
   }
   ```

2. **TranscriptView.tsx 修正**:
   - L345-L358 の `function getRequiresLocalModel(...)` + `function getExternalApiProvider(...)` 定義 2 件を **削除** (間に空行があるなら整理)
   - 既存の utils import の近くに次の import を **追加**:
     ```ts
     import {
       getExternalApiProvider,
       getRequiresLocalModel,
     } from "../utils/transcriptionEngineHelpers";
     ```
   - **既存 utils import の正確な位置は `grep -n "from \"../utils/" src/routes/TranscriptView.tsx | head -10` で確認** してから挿入。alphabetical/既存 import の並びに合わせる (例: trackStatusAriaLabels の直前/直後)。
   - callsite (L637/L640 = 関数名は同じ) は import 追加だけで参照解決される = 修正不要。

# 検証

```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
npm run build  # tsc + vite build = ✓ 通ること
bash scripts/agent-verify.sh src/routes/TranscriptView.tsx src/utils/transcriptionEngineHelpers.ts
```

両方 OK のときのみ commit に進む。

# 2-commit pattern (Loop 60-130 の 71 連続 precedent と完全同形)

## commit 1 (refactor)

```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
git add src/routes/TranscriptView.tsx src/utils/transcriptionEngineHelpers.ts
git commit -m "refactor(frontend): TranscriptView の engine 機能特性 helpers を utils/transcriptionEngineHelpers.ts に抽出 (mjc-main-20260505-66 Loop 131, frontend 軸 10 件目 = 純粋関数機能分離軸 frontend 版 7 件目, scope 29 軸目開拓, 大型 frontend file 責務分離 42 file 目, getRequiresLocalModel + getExternalApiProvider 抽出, R6 段階的分割 4 件目, AGENTS.md priority 1 寄与)"
```

commit hash を取得 (`git log --oneline -1` の左端 7 文字) して `<COMMIT1_HASH>` として記録。

## AGENT_LOG.md 末尾追記

`tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に **append のみ** で追記。**先頭は絶対に触らない**。

追記内容 (テンポラリファイル `/tmp/loop131-entry.md` を Write してから `cat /tmp/loop131-entry.md >> AGENT_LOG.md`):

```
[mjc-main-20260505-66 Loop 131 / 2026-05-06]
worker: mjc-worker-loop131-engine-helpers-extract (作業)
範囲: src/routes/TranscriptView.tsx (関数定義 2 件削除 + import 1 行追加) + src/utils/transcriptionEngineHelpers.ts (新規 ~20 行 = import 1 行 + export 2 関数)
内容: TranscriptView.tsx L345-L358 の getRequiresLocalModel + getExternalApiProvider を新 file utils/transcriptionEngineHelpers.ts に抽出。frontend 軸 10 件目 = 純粋関数機能分離軸 frontend 版 7 件目 = scope 29 軸目開拓 = 大型 frontend file 責務分離 42 file 目 = R6 段階的分割 4 件目 (Loop 125 audio level + Loop 127 permission status + Loop 129 track ARIA の続編)。callsite 2 件 (L637 + L640、各 1 callsite) は import 追加のみで参照解決。alternation pattern 23 連続成功維持 (K(130) → frontend(131))。メイン批判判断 = 既存 utils/liveCaptionStatus.ts (display tier = engine → UI label) + utils/modelSelectorHelpers.ts (model display tier) との責務階層精査で **異階層 = 新 file 作成軸採用** = predicate/categorization tier の独立性確保 = Loop 119/121/123/125/127/129 教訓継承 (paradigm 反復 < 責務階層精査)。
変更ファイル: src/routes/TranscriptView.tsx, src/utils/transcriptionEngineHelpers.ts
検証: npm run build OK (tsc + vite build), bash scripts/agent-verify.sh OK
commit: <COMMIT1_HASH> refactor(frontend): TranscriptView の engine 機能特性 helpers を utils/transcriptionEngineHelpers.ts に抽出
依存関係追加: なし
失敗理由: なし
次アクション: chore commit (本 entry 追記の commit) を続けて作成

---
```

## commit 2 (chore)

```bash
git add AGENT_LOG.md
git commit -m "chore(agent-log): mjc-main-20260505-66 Loop 131 = TranscriptView の engine 機能特性 helpers を utils/transcriptionEngineHelpers.ts に抽出 (構造分離 42 file 目, frontend 軸 10 件目, scope 29 軸目開拓, 純粋関数機能分離軸 frontend 版 7 件目, alternation 23 連続成功, R6 段階的分割 4 件目)"
```

# 完了確認

`git log --oneline -3` で 2 commit 反映を確認:
- 最新 = chore(agent-log): ... Loop 131 ...
- その 1 つ前 = refactor(frontend): ... transcriptionEngineHelpers.ts ...

両方表示されたら作業完了。final answer 不要、停止せずメインエージェントに引き継がれる。
