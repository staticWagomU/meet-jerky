あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src/components/MeetingDetectedBanner.tsx
- AGENT_LOG.md

前提:
Loop 227 の直前 worker が `recoverLatestMeetingDetection` / `consumeLatestMeetingDetection` の不正 payload / invoke 失敗を `listenerError` に出す差分を作成済みです。コミット前レビューで、`recoverLatestMeetingDetection().catch(...)` の invoke 失敗時に `hasReceivedPromptContentRef.current` が false のままだと、既存の `showMeetingPromptWindow` effect 条件 `listenerError && hasReceivedPromptContentRef.current` を満たせず UI に出ない可能性が見つかりました。

目的:
最新会議検知通知の回収 invoke 失敗も、意図どおり会議検知バナーの UI 診断として表示されるようにする。

実装条件:
- `recoverLatestMeetingDetection().catch(...)` で listenerError を設定する直前に、必要な state/ref を live event の不正 payload 時と同じ粒度へ整えてください。
- 少なくとも `hasReceivedPromptContentRef.current = true` を設定し、必要なら `setDetected(null)` / `setPendingAction(null)` も揃えてください。
- `payload == null` 正常扱い、不正 payload 処理、consume 経路、listener 開始失敗、status listener、auto-hide、録音開始フロー、Rust 側、event 名は変更しないでください。
- 依存関係追加は禁止です。担当範囲外ファイルを編集しないでください。
- コミットは禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/components/MeetingDetectedBanner.tsx AGENT_LOG.md`

AGENT_LOG.md:
既存 Loop 227 entry を置き換えるのではなく、今回の修正分を追記してください。開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを記録してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
