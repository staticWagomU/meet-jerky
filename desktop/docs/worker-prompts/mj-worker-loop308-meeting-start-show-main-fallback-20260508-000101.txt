あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

必ず読む:
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness.md
- AGENT_LOG.md

担当範囲:
- src/components/MeetingDetectedBanner.tsx
- AGENT_LOG.md

目的:
会議検知プロンプトの「録音と文字起こしを開始」操作で `MEETING_START_REQUEST_EVENT` の受信側に依存しすぎないよう、イベント送信後に送信側からも `show_main_window` を呼ぶフォールバックを追加してください。これにより、録音開始予約は保存・送信できたが main window 表示 listener が遅延/未開始だった場合でも、メインウィンドウを表示しやすくします。

実装条件:
- 変更は `MeetingDetectedBanner.tsx` の小さな helper と `handleStartRecording` 内の呼び出しに限定してください。
- 録音を自動開始する処理は追加しないでください。pending request の保存と既存 `TranscriptView` 側の開始導線は維持してください。
- `MEETING_START_REQUEST_EVENT` の event 名、pending localStorage helper、Rust command 本体、App.tsx listener、TranscriptView、CSS、依存関係は変更しないでください。
- `show_main_window` の呼び出しに失敗した場合は、既存の `listenerError` と console にユーザーが理解できる文言を出し、pending request を勝手に消さないでください。イベント送信自体に失敗した場合の既存 rollback は維持してください。
- 課金、認証変更、外部通信、破壊的操作は禁止です。
- 他エージェントも同時に作業している可能性があります。他者の変更を戻さず、担当範囲外を編集しないでください。
- コミットは禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/components/MeetingDetectedBanner.tsx AGENT_LOG.md docs/worker-prompts/mj-worker-loop308-meeting-start-show-main-fallback-20260508-000101.txt`

AGENT_LOG.md には Loop 308 として、開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、次アクション、ユーザー価値、非目標、未実機確認範囲を簡潔に追記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
