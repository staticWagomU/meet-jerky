あなたは meet-jerky の worker (mjc-worker-loop190-status-recording-const) です。

# タスク (Loop 190 = frontend 軸 = const 集約 paradigm 反復 9 件目 = paradigm 完全成熟期完全成熟最終収束完成超越超越達成)

`"録音中"` 文字列リテラル 5 callsite (4 file) を `src/utils/statusLabels.ts` に新規追加する `STATUS_RECORDING_LABEL` const に置換する。

メイン批判検証で handoff prompt R32 「主要重複候補は事実上枯渇」評価との差異 (実際は 5 callsite/4 file = minor 評価より大きい価値) を検出済 = 本ループはメイン批判判断 連続 94 セッション目達成の実証作業。

## 範囲 (限定 = 規模超過防止)

**編集対象 (6 file のみ)**:
1. `src/utils/statusLabels.ts` (新規 const 追加 1 行)
2. `src/utils/liveCaptionTrackHelpers.ts` (**新規 import 追加** + L58 callsite)
3. `src/components/MicrophoneSection.tsx` (**新規 import 追加** + L45 callsite)
4. `src/components/LiveCaptionWindow.tsx` (**新規 import 追加** + L294 連結 literal を template literal 化)
5. `src/routes/TranscriptView.tsx` (L68 既存 import 拡張 + L1119 + L1203 callsite)
6. `AGENT_LOG.md` (末尾追記 1 entry)

**範囲外 (これらは触らない)**:
- 上記 6 file 以外のあらゆる file
- 上記 file 内であっても、以下に列挙した line 以外
- `"録音中・入力待ち"` 等の **別 entity literal** (本ループの scope 外、`"録音中"` 厳密 literal のみ集約)
- `"録音中"` 以外の文字列リテラル (例: `"開始中"`, `"停止中"`)
- rust コード (frontend 軸のため不要)

## 具体手順 (順序厳守)

### Step 1: statusLabels.ts に const 追加

`src/utils/statusLabels.ts` 末尾に以下 1 行を追加:

```typescript
export const STATUS_RECORDING_LABEL = "録音中";
```

**完成形 (7 const)**:
```typescript
export const STATUS_UNCHECKABLE_LABEL = "確認できません";
export const STATUS_CHECKING_LABEL = "確認中";
export const STATUS_UNDETERMINED_LABEL = "未確認";
export const STATUS_DENIED_LABEL = "未許可";
export const STATUS_UNREGISTERED_LABEL = "未登録";
export const STATUS_CHECKING_WITH_DOTS_LABEL = "確認中...";
export const STATUS_RECORDING_LABEL = "録音中";
```

### Step 2: liveCaptionTrackHelpers.ts を修正 (**新規 import 追加**)

liveCaptionTrackHelpers.ts は statusLabels import が **未存在** = 新規 import 追加が必要。

`grep -n "^import" src/utils/liveCaptionTrackHelpers.ts | head -10` で既存 import を確認し、適切な位置 (alphabetical または既存 utils import の近く) に以下を追加:

```typescript
import { STATUS_RECORDING_LABEL } from "./statusLabels";
```

L58 callsite:
```typescript
    normalizedLabel.includes("録音中") ||
```

これを:
```typescript
    normalizedLabel.includes(STATUS_RECORDING_LABEL) ||
```

### Step 3: MicrophoneSection.tsx を修正 (**新規 import 追加**)

MicrophoneSection.tsx は statusLabels import が **未存在** = 新規 import 追加が必要。

`grep -n "^import" src/components/MicrophoneSection.tsx | head -20` で既存 import を確認し、適切な位置 (alphabetical または既存 utils import の近く) に以下を追加:

```typescript
import { STATUS_RECORDING_LABEL } from "../utils/statusLabels";
```

L45 callsite:
```typescript
      ? "録音中"
```

これを:
```typescript
      ? STATUS_RECORDING_LABEL
```

### Step 4: LiveCaptionWindow.tsx を修正 (**新規 import 追加** + template literal 化)

LiveCaptionWindow.tsx は statusLabels import が **未存在** = 新規 import 追加が必要。

`grep -n "^import" src/components/LiveCaptionWindow.tsx | head -20` で既存 import を確認し、適切な位置 (alphabetical または既存 utils import の近く) に以下を追加:

```typescript
import { STATUS_RECORDING_LABEL } from "../utils/statusLabels";
```

L294 callsite (連結 literal = template literal 化必要):
```typescript
  const compactCaptionLabel = "録音中 · 字幕を表示中";
```

これを:
```typescript
  const compactCaptionLabel = `${STATUS_RECORDING_LABEL} · 字幕を表示中`;
```

### Step 5: TranscriptView.tsx を修正 (L68 既存 import 拡張)

L68 既存 import (alphabetical order):
```typescript
import { STATUS_CHECKING_LABEL, STATUS_UNCHECKABLE_LABEL } from "../utils/statusLabels";
```

これを (alphabetical order = STATUS_CHECKING_LABEL の前に STATUS_RECORDING_LABEL を入れるべきか? Loop 188 precedent では PermissionBanner.tsx が STATUS_CHECKING_LABEL, STATUS_CHECKING_WITH_DOTS_LABEL, STATUS_DENIED_LABEL, ... の alphabetical order で並んでいる. 同様に STATUS_RECORDING_LABEL は alphabetical 上 STATUS_CHECKING_* と STATUS_UNCHECKABLE_LABEL の間に位置):
```typescript
import { STATUS_CHECKING_LABEL, STATUS_RECORDING_LABEL, STATUS_UNCHECKABLE_LABEL } from "../utils/statusLabels";
```

L1119 callsite:
```typescript
        : "録音中"
```

これを:
```typescript
        : STATUS_RECORDING_LABEL
```

L1203 callsite:
```typescript
      ? "録音中"
```

これを:
```typescript
      ? STATUS_RECORDING_LABEL
```

### Step 6: 検証

```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
npm run build
```

期待値: tsc + vite build OK = エラーゼロ。

```bash
bash scripts/agent-verify.sh src/utils/statusLabels.ts src/utils/liveCaptionTrackHelpers.ts src/components/MicrophoneSection.tsx src/components/LiveCaptionWindow.tsx src/routes/TranscriptView.tsx
```

`agent-verify.sh` は frontend のみ skip 判定 = 全項目 OK 期待。

### Step 7: 1 番目の commit (refactor)

```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
git add src/utils/statusLabels.ts src/utils/liveCaptionTrackHelpers.ts src/components/MicrophoneSection.tsx src/components/LiveCaptionWindow.tsx src/routes/TranscriptView.tsx
git commit -m "refactor(frontend): STATUS_RECORDING_LABEL を src/utils/statusLabels.ts に集約 + 4 file 5 callsite を const 参照に統一 (mjc-main Loop 190, frontend 軸 = const 集約 paradigm 反復 9 件目 = paradigm 完全成熟期完全成熟最終収束完成超越超越達成, 既存 file 拡張軸 paradigm 20 件目 + 新規 import 追加軸 paradigm 反復 2 件目, '録音中' 5 callsite を統一 + DRY 違反解消, alternation pivot K(189) → frontend(190) = 29 → 30 = 節目 30 件突破, AGENTS.md priority 1 寄与)"
```

commit 完了後 `git log --oneline -1` で hash を取得 (使用は任意)。

### Step 8: AGENT_LOG.md に entry 追記

**重要 (worker prompt 必須要素 9)**: AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**。新規追記は **必ずファイル末尾**。**先頭は絶対に触らない**。

#### 8-1: 末尾確認

```bash
tail -10 AGENT_LOG.md
```

末尾は `---` (boundary 区切り) 1 行で終わっているはず。**この `---` の直後に新 entry を追加する**。

#### 8-2: entry テンプレート

以下の内容を temp file (`/tmp/loop190-entry.txt`) に Write し、`cat /tmp/loop190-entry.txt >> AGENT_LOG.md` で **1 回だけ** append。

**重要 (worker prompt 必須要素 9)**: 冒頭に `---` を **含めない**。前 entry の閉じ `---` を再利用する。**末尾の `---` 1 個で締める**。

```
[mjc-main Loop 190 / 2026-05-06]
worker: mjc-worker-loop190-status-recording-const (作業)
範囲: src/utils/statusLabels.ts (新規 const 追加 1 行) + src/utils/liveCaptionTrackHelpers.ts (新規 import 追加 + L58 callsite) + src/components/MicrophoneSection.tsx (新規 import 追加 + L45 callsite) + src/components/LiveCaptionWindow.tsx (新規 import 追加 + L294 template literal 化) + src/routes/TranscriptView.tsx (L68 既存 import 拡張 + L1119/L1203 callsite)
変更: STATUS_RECORDING_LABEL = "録音中" を statusLabels.ts に集約 + 4 file 5 callsite を const 参照に統一 = DRY 違反解消, callsite 5 件無変更で API 互換維持, ネット行数 +1 行のみ, **const 集約 paradigm 反復 9 件目達成 = paradigm 完全成熟期完全成熟最終収束完成超越超越達成 (Loop 174 → 176 → 178 → 180 → 182 → 184 → 186 → 188 → 190 = 9 連続 const 集約軸)** + **既存 file 拡張軸 paradigm 20 件目達成 (statusLabels.ts への 7 度目の拡張)** + **新規 import 追加軸 paradigm 反復 2 件目** (Loop 188 で 1 件目開拓 → Loop 190 で反復 2 件目 = 3 file = liveCaptionTrackHelpers.ts + MicrophoneSection.tsx + LiveCaptionWindow.tsx は statusLabels import 未存在で新規追加).
意義: AGENTS.md priority 1 (DRY 違反解消) 寄与. statusLabels.ts は 7 const で `"〜状態"` semantic + loading suffix + recording status semantic 完全網羅. alternation pivot K(189) → frontend(190) = 29 → 30 = **節目 30 件突破** = pivot 加速期維持. メイン批判判断 連続 94 セッション目達成 (handoff prompt R32 「主要重複候補は事実上枯渇」評価との差異 = 5 callsite/4 file 残存を grep で批判検証達成).
検証: npm run build (tsc + vite build) OK / agent-verify.sh で frontend のみ skip 判定 OK
備考: LiveCaptionWindow.tsx L294 は連結 literal `"録音中 · 字幕を表示中"` で template literal 化が必要だったが完全網羅で実施. 後継候補 = R31 (別 paradigm 開拓 = trait/generic/lifetime 探索) / R32 (const 集約軸の minor 候補探索 = `"開始中"` / `"停止中"` 等の ellipsis 混在候補) / K' (K 軸 2 連続 = Section 2.3 観測 49 件目).
---
```

**注意点**:
- 行末に空白 (trailing whitespace) を含めない
- 上記の `---` は **末尾の 1 個のみ** (boundary 区切り). 冒頭には `---` を入れない (前 entry の閉じ `---` を再利用)
- append は **Edit/Write を複数回繰り返さず、1 回で append が成功したら直ちに次へ進む** (Loop 144 fault precedent)

#### 8-3: append 実行

```bash
cat /tmp/loop190-entry.txt >> AGENT_LOG.md
```

### Step 9: 2 番目の commit (chore agent-log)

```bash
cd /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop
git add AGENT_LOG.md
git commit -m "chore(agent-log): mjc-main Loop 190 = STATUS_RECORDING_LABEL を statusLabels.ts に集約 (frontend 軸, const 集約 paradigm 反復 9 件目 = paradigm 完全成熟期完全成熟最終収束完成超越超越達成, 既存 file 拡張軸 paradigm 20 件目 + 新規 import 追加軸 paradigm 反復 2 件目, alternation pivot K(189) → frontend(190) = 29 → 30 = 節目 30 件突破, メイン批判判断 連続 94 セッション目達成) のエントリ追記"
```

### Step 10: 完了確認

```bash
git status --short
git log --oneline -3
```

`git status` は `working tree clean` (untracked の docs/handoff/* は無視可)。
`git log` の上 2 件が本ループの commit (refactor + chore agent-log)。

# 制約 (再掲、重要)

- `--no-verify` 禁止
- `--dangerously-skip-permissions` 禁止 (harness 内のみ)
- 行末空白 (trailing whitespace) 禁止
- AGENT_LOG.md への append は **1 回のみ**. Edit/Write を複数回繰り返さない
- entry テンプレート冒頭に `---` を含めない (boundary 重複 fault 防止 = Loop 166 precedent + 21 連続実証成功)
- 大型 file (AGENT_LOG.md ~34501 行) は Read 全体禁止 = `tail -10 AGENT_LOG.md` で末尾だけ確認
- TranscriptView.tsx は ~大型 file = **L60-70 / L1115-1125 / L1198-1210 だけ参照** = `sed -n '60,70p' src/routes/TranscriptView.tsx` 等で該当部のみ Read
- 範囲外の file (上記 6 file 以外) は **絶対に触らない**
- range 内 file でも、列挙した line 以外は触らない
- rust 系検証は skip 可 (frontend 軸のみ = cargo test 不要)
- AGENTS.md / docs/autonomous-main-prompt-claude.md / docs/agent-harness-claude.md を必ず参照
- 完了したら final answer で返事 (待機モード禁止 = 即座に 2-commit して終わる)

# 期待される完了状態 (2-commit pattern)

1. commit 1: `refactor(frontend): STATUS_RECORDING_LABEL を src/utils/statusLabels.ts に集約 + 4 file 5 callsite ...` = frontend 5 file 変更
2. commit 2: `chore(agent-log): mjc-main Loop 190 = ...` = AGENT_LOG.md 1 file 変更

`git status` working tree clean (untracked の docs/* は無視) を確認後、final answer で返事してください。

# AGENT_LOG.md 末尾を冒頭で必ず確認

冒頭で `tail -100 AGENT_LOG.md` を実行 → 直近 entry (Loop 189 + Loop 188 + ...) と末尾 boundary `---` の存在を確認してから着手すること。
