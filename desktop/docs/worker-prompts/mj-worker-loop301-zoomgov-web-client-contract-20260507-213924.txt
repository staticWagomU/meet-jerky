あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

必ず読む:
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness.md
- AGENT_LOG.md

担当範囲:
- src-tauri/src/app_detection.rs の Zoom URL classification tests
- AGENT_LOG.md の Loop 301 記録

目的:
ZoomGov の web client URL `https://<subdomain>.zoomgov.com/wc/<meeting_id>/join` が `classify_meeting_url` 経由で Zoom として検知される positive contract を固定してください。

根拠:
- `src-tauri/src/app_detection_zoom.rs` は `zoomgov.com` host と `/wc/<meeting_id>/join` 形式を受理する実装です。
- 既存 `classify_meeting_url_returns_service_and_host_only` には ZoomGov の `/j/<id>` と `/wc/join/<id>` はありますが、`/wc/<id>/join` の ZoomGov 統合テストが薄いです。
- 会議検知の網羅性と信頼性に関わる純粋関数テストで、production code 変更なしに固定できます。

実装方針:
- production code は原則変更しないでください。
- `src-tauri/src/app_detection.rs` の Zoom URL tests 付近に、`classify_meeting_url_accepts_zoomgov_web_client_meeting_id_join_url` のようなテストを追加してください。
- `https://agency.zoomgov.com/wc/1600991835/join` が `Some(MeetingUrlClassification { service: "Zoom", host: "agency.zoomgov.com" })` になることを確認してください。
- 可能なら trailing slash variant `https://agency.zoomgov.com/wc/1600991835/join/` も同じ test 内で固定してください。
- ZoomGov `/j/<id>`、`/wc/join/<id>`、Zoom commercial、他サービス、UI、依存関係は変更しないでください。

制約:
- 他エージェントも同時に作業している可能性があります。他者の変更を戻さず、担当範囲外を編集しないでください。
- 課金、認証情報変更、破壊的操作、git reset --hard、未確認削除は禁止です。
- コミットは禁止です。

検証:
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo test --manifest-path src-tauri/Cargo.toml --lib zoomgov`
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo clippy --manifest-path src-tauri/Cargo.toml --lib --tests -- -D warnings`
- `git diff --check -- src-tauri/src/app_detection.rs AGENT_LOG.md docs/worker-prompts/mj-worker-loop301-zoomgov-web-client-contract-20260507-213924.txt`

AGENT_LOG.md には、開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、次アクションを追記してください。
最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
