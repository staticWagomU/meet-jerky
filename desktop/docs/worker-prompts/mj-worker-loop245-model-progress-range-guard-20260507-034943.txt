あなたは作業担当エージェントです。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src/utils/modelDownloadPayload.ts
- AGENT_LOG.md

目的:
Whisper モデルダウンロード進捗 event の frontend payload guard で、破損した `progress` 値を UI 進捗として扱わないようにしてください。Rust 側の正規 emit は `0.0` 開始、ダウンロード比率、`1.0` 完了なので、`progress` は `0 <= progress <= 1` の有限数だけを受理する契約にします。

実装条件:
1. `src/utils/modelDownloadPayload.ts`
   - `isDownloadProgressPayload` で既存の `typeof candidate.progress === "number"` と `Number.isFinite(candidate.progress)` を維持したうえで、`candidate.progress >= 0` と `candidate.progress <= 1` を追加してください。
   - `model` の判定、`isDownloadErrorPayload`、event 名、型定義、`ModelSelector.tsx`、Rust 側は変更しないでください。
   - `sanitizeProgress` は UI 表示側の保険として残し、今回触らないでください。
2. AGENT_LOG.md
   - 開始日時、担当セッション、役割、作業範囲、指示内容、結果、変更ファイル、検証結果、依存関係追加の有無、失敗理由、残リスク、次アクションを追記してください。
   - 変更ファイルには、この worker prompt と research prompt も含めてください。
   - research は長い探索で最終出力が遅れたため、メイン側が Loop 243 の残リスクとコード確認からこの最小候補を選んだことを記録してください。

禁止:
- コミットは禁止です。
- 依存追加は禁止です。
- `src/components/ModelSelector.tsx`、Rust、型定義、event 名、UI 文言、テスト基盤、直近 Loop 240-244 の対象ファイルは変更しないでください。

検証:
- `PATH="/opt/homebrew/bin:$PATH" npm run build`
- `git diff --check -- src/utils/modelDownloadPayload.ts AGENT_LOG.md docs/worker-prompts/mj-research-loop245-next-value-slice-20260507-034943.txt docs/worker-prompts/mj-worker-loop245-model-progress-range-guard-20260507-034943.txt`

残リスクには、TS 単体テスト基盤がないため guard の入出力は build と diff review で確認すること、実機モデルダウンロード経路は今回未確認であることを明記してください。

最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
