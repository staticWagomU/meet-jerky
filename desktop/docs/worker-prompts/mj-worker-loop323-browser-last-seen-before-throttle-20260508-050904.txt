あなたは作業担当エージェントです。

対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。
AGENTS.md、docs/product-concept.md、docs/agent-harness.md、AGENT_LOG.md を読んでください。

担当範囲:
- src-tauri/src/app_detection.rs
- AGENT_LOG.md

背景:
`handle_browser_url_detection` は会議 URL / window title fallback を分類したあと、`last_seen` による 60 秒通知スロットリングで早期 return します。現在は `last_seen_secs` の更新がその後にあるため、ブラウザ会議を継続検知していてもスロットリング中の callback では inactive 判定用の最終検知時刻が更新されません。`meeting-app-detected` emit や通知頻度は増やさず、inactive 判定の信頼性だけを上げたいです。

実装内容:
- `handle_browser_url_detection` で、分類に成功し `STATE` が取得できた後、通知スロットリング判定で return する前に `state.last_seen_secs` を `throttle_key` / `now_secs` で更新してください。
- `now_secs` は関数冒頭で取得済みの値を再利用してください。
- 通知表示、`meeting-app-detected` emit、`latest_payload` 更新、`last_seen` の Instant ベース通知スロットリング挙動は維持してください。
- app launch 側 `handle_detection`、URL/window title 分類ルール、event 名、payload field 名、UI/TypeScript、Swift bridge、依存関係は変更しないでください。
- 必要なら小さな private helper を追加して、通知スロットリングで return する場合でも inactive 用時刻は更新済みになる契約を Rust unit test で固定してください。Tauri `AppHandle` が必要な大きい統合テストへ広げないでください。

検証:
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo test --manifest-path src-tauri/Cargo.toml --lib app_detection`
- `PATH="/opt/homebrew/bin:$PATH" ~/.cargo/bin/cargo clippy --manifest-path src-tauri/Cargo.toml --lib --tests -- -D warnings`
- `git diff --check -- src-tauri/src/app_detection.rs AGENT_LOG.md docs/worker-prompts/mj-research-loop323-next-value-slice-20260508-050904.txt docs/worker-prompts/mj-worker-loop323-browser-last-seen-before-throttle-20260508-050904.txt`

ログ:
AGENT_LOG.md に以下を追記してください。
- 開始日時
- 担当セッション
- 役割
- 作業範囲
- 指示内容
- 結果
- 変更ファイル
- 検証結果
- 依存関係追加の有無
- 失敗理由
- 次アクション
- ユーザー価値
- 非目標
- 未実機確認範囲

他エージェントも同時に作業している可能性があります。他者の変更を戻さず、担当範囲外を編集しないでください。コミットは禁止です。
最後に、変更内容、変更ファイル、検証結果、残リスクを簡潔に報告してください。
