あなたは meet-jerky の worker (mjc-worker-loop68-audio-sample-helpers) です。

# 最重要 (AGENT_LOG.md 編集ルール、必読)

1. **最初に `tail -350 AGENT_LOG.md` を読む** = 直近の precedent と SESSION SUMMARY を理解する。
2. **AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う**。
3. **先頭は絶対に触らない**。
4. **具体手順**: `tail -10 AGENT_LOG.md` で末尾を確認 → 末尾の `---` 直後に追記。

# タスク = audio.rs private pure helpers 抽出 → audio_sample_helpers.rs (新規)

## 抽出対象 (audio.rs から移動、計 ~40 行)

- `fn for_each_mono_sample<T, F>` (L237-256, ~20 行)
- `fn normalize_sample_to_f32<T>` (L258-263, ~6 行)
- `fn sanitize_sample` (L265-267, ~3 行)
- `fn calculate_rms_from_sum` (L269-279, ~11 行)

## visibility 化

全 4 fn を `pub(crate)` に昇格 (audio.rs から file 跨ぎ参照のため)。
ただし `sanitize_sample` は audio.rs 内部 (`normalize_sample_to_f32` から呼ばれる) のみで使われていれば private 維持可。
**grep で確認**: `cd src-tauri && grep -n 'sanitize_sample\|normalize_sample_to_f32\|for_each_mono_sample\|calculate_rms_from_sum' src/`
- audio.rs 外部から呼ばれている fn は pub(crate) に
- audio.rs 内部のみ + audio_sample_helpers.rs 内部のみで完結する fn は private 維持可

## 移動方針

- **関数本体は変更しない** (振る舞い完全不変)
- doc comment + #[cfg(test)] 等の attribute も維持
- import 文 (`use cpal::{FromSample, Sample};` 等の必要なもの) を audio_sample_helpers.rs 先頭に配置
- audio_sample_helpers.rs の頭にモジュールコメント (例 `//! 音声サンプル変換とモノラル化、RMS 計算の純粋ヘルパー。`)

## audio.rs 修正

- 抽出した 4 fn を audio.rs から削除
- audio.rs の caller に必要な use 文を追加 (例 `use crate::audio_sample_helpers::{for_each_mono_sample, calculate_rms_from_sum};`)
  - L212/228/549 で使う名前のみ import
  - rustfmt が自動で import 順序 sort するので手動順序気にしなくて OK

## tests 方針 (重要)

- audio.rs 内 #[cfg(test)] mod の test (test_normalize_sample_to_f32_for_i16 等 / test_for_each_mono_sample_keeps_f32_mono 等) は **audio.rs に残置**
- assert_close helper (audio.rs L570) も audio.rs に残置 (tests から呼ばれる helper、移動するとさらに規模膨らむ)
- audio.rs の tests mod 冒頭に `use crate::audio_sample_helpers::{for_each_mono_sample, normalize_sample_to_f32};` を追加
  - `use super::*;` がある場合、抽出先の fn は super:: 経由ではなくなるため明示 import が必要

## lib.rs 修正

- `mod audio_sample_helpers;` を追加 (アルファベット順 = `mod audio_event;` と `mod audio_traits;` の間)
- 現状: `mod audio; mod audio_event; mod audio_traits; mod audio_utils;`
- 追加後: `mod audio; mod audio_event; mod audio_sample_helpers; mod audio_traits; mod audio_utils;`

## precedent (cat 全体読み込み推奨)

- `cd src-tauri && cat src/audio_utils.rs` = 同じ scope (audio) の純粋 helper file precedent
- `cd src-tauri && cat src/audio_traits.rs` = 直近 Loop 66 で抽出された file precedent
- `cd src-tauri && cat src/session_store_types.rs` = 直近 Loop 67 で抽出された file precedent

## 規模制限 (超過厳禁)

- audio_sample_helpers.rs (新規) = ~50 行 (4 fn + use 文 + module コメント) を上限
- audio.rs 修正 = -40 行 + use 文追加 ~3 行
- lib.rs 修正 = +1 行 (mod 宣言のみ)
- **担当範囲外編集禁止** (frontend / docs / scripts / app_detection 関連は触らない)
- test 件数: **702 passed / 0 failed = 件数不変** (新規 test 追加禁止、削除禁止)

## 検証 (必ず通す)

- `cd src-tauri && cargo build --lib`: エラーなし
- `cd src-tauri && cargo test --lib`: 702 passed / 0 failed (件数不変)
- `cd src-tauri && cargo clippy --lib --tests -- -D warnings`: 警告ゼロ
- `cd src-tauri && cargo fmt --check`: OK
- `cd .. && bash scripts/agent-verify.sh src-tauri/src/audio.rs src-tauri/src/audio_sample_helpers.rs src-tauri/src/lib.rs`: OK
- grep 振る舞い不変確認:
  - `cd src-tauri && grep -n 'fn for_each_mono_sample\|fn normalize_sample_to_f32\|fn sanitize_sample\|fn calculate_rms_from_sum' src/audio.rs` → **完全に空** (全部移動済)
  - `cd src-tauri && grep -n 'fn for_each_mono_sample\|fn normalize_sample_to_f32\|fn calculate_rms_from_sum' src/audio_sample_helpers.rs` → 各 1 件
- **trailing whitespace**: なし (`grep -nE ' +$' src/audio_sample_helpers.rs src/audio.rs src/lib.rs` で確認、見つかれば削除)

## 2-commit pattern (Loop 60-67 連続 8 ループで実証、必ず実施)

### commit 1 (refactor commit)
```
git add src-tauri/src/audio_sample_helpers.rs src-tauri/src/audio.rs src-tauri/src/lib.rs
git commit -m "refactor(audio): private pure helpers (for_each_mono_sample / normalize_sample_to_f32 / sanitize_sample / calculate_rms_from_sum) を audio_sample_helpers.rs に抽出 (mjc-main-20260505-34 Loop 68, ~40 行抽出 + lib.rs mod 追加 + audio.rs caller use 文追加, audio_utils.rs precedent 直接展開, AGENTS.md priority 1 + 4 寄与)"
```

### commit 2 (chore: AGENT_LOG.md 反映)
```
# refactor commit hash を取得
HASH=$(git rev-parse HEAD)

# AGENT_LOG.md 末尾に Loop 68 entry を追記 (下記テンプレ使用)
# 必ず tail -10 で末尾の --- を確認してから書き込む

git add AGENT_LOG.md
git commit -m "chore(agent-log): Loop 68 commit hash $HASH を AGENT_LOG.md に記入 (worker amend 時の hash 更新漏れ補正、Loop 60-67 precedent 適用 9 連続)"
```

## AGENT_LOG.md エントリのテンプレ (末尾の --- 直後に追記)

```markdown

[mjc-main-20260505-34 Loop 68 / 2026-05-05 ~JST]

## What
- audio.rs から private pure helpers 4 件を新規ファイル audio_sample_helpers.rs に抽出 (~40 行)
  - `pub(crate) fn for_each_mono_sample<T, F>` (~20 行) = ステレオ→モノラル平均化 + on_sample callback
  - `pub(crate) fn normalize_sample_to_f32<T>` (~6 行) = T → f32 正規化 + sanitize 適用
  - `fn sanitize_sample` (~3 行) または `pub(crate)` (grep 結果次第) = audio_utils::sanitize_audio_sample wrapper
  - `pub(crate) fn calculate_rms_from_sum` (~11 行) = sum_squares + count → RMS
- lib.rs に `mod audio_sample_helpers;` 追加 (audio_event と audio_traits の間)
- audio.rs caller use 文追加 (`use crate::audio_sample_helpers::{...};`)
- tests は audio.rs 残置 + tests mod に明示 import 追加 (assert_close helper も残置)

## Why
- AGENTS.md 優先順位 1 = クラッシュ修正の予防的寄与 (audio.rs 989 行の段階的責務分離継続)
- AGENTS.md 優先順位 4 = リアルタイム文字起こし低遅延化への予防的寄与 (audio sample 処理の独立 file = 将来最適化容易化)
- locality 集約 = pure 純粋ロジックの単独 file 化
- audio_utils.rs precedent (sanitize_audio_sample / calculate_rms / resample_audio) 直接展開
- variety pivot = struct/trait 抽出 (Loop 66/67) → pure fn 抽出 (Loop 68) = pattern 切替

## How (Tidy First, behavior-preserving)
- 関数本体は変更せず、ファイル間移動のみ = 振る舞い完全不変
- visibility: 外部 caller があれば pub(crate) 化、なければ private 維持
- doc comment 維持
- 振る舞い不変 = 702 passed 件数不変

## Verify
- cargo build --lib: エラーなし
- cargo test --lib: 702 passed / 0 failed (件数不変)
- cargo clippy --lib --tests -- -D warnings: 警告ゼロ
- cargo fmt --check: OK
- agent-verify.sh: OK
- audio.rs から `fn for_each_mono_sample` / `fn normalize_sample_to_f32` / `fn calculate_rms_from_sum` を grep: 完全に空
- audio_sample_helpers.rs から同 fn を grep: 各 1 件確認
- trailing whitespace: なし

## commit
- (commit 1 hash placeholder = refactor commit)
```

(↑ commit 1 完了後に hash を上記 placeholder と置換、commit 2 で AGENT_LOG.md を再 add してから commit)

## トラブル時 (失敗フォールバック)

- cargo test 件数が変動した場合 → fn を全て元に戻して中断、メインに状況報告
- clippy 警告が出た場合 → fmt + warning 内容に従って修正、それでも消えなければメインに報告
- mod 順序で fmt が落ちる場合 → lib.rs の mod 宣言順を再確認 (alphabetical)
- assert_close helper が tests から見えなくなった場合 → audio.rs の tests mod 内 helper のスコープを確認

## 完走基準
1. 検証 (build + test + clippy + fmt + agent-verify.sh) 全 pass
2. 2-commit pattern で 2 commit 作成 (refactor + chore hash 反映)
3. AGENT_LOG.md 末尾に Loop 68 エントリ追記 (commit hash 含む)
4. trailing whitespace ゼロ
5. 規模上限 (audio_sample_helpers.rs ≤ 50 行) 遵守
