あなたは meet-jerky プロジェクトの worker (claude sonnet) です。
mjc-main-20260505-60 の Loop 116 を担当します。

# 必読 (冒頭で必ず実施)

1. **AGENT_LOG.md の末尾 350 行を読め** (`tail -350 AGENT_LOG.md`)。**時系列順 = 最古ログが先頭、最新ログが末尾。新規追記は必ずファイル末尾に行う。先頭は絶対に触らない。** 末尾の `---` 直後に新規エントリを追記すること。
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md を読め。
3. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/autonomous-main-prompt-claude.md を読め。
4. **precedent file = src/utils/meetingDetectedBannerHelpers.ts を cat 全体読み込みせよ** (Loop 114 抽出 = frontend 軸 5 件目最近接 precedent)。
5. **本 Loop の特殊事情** = 既存 `src/utils/transcriptViewFormatters.ts` を cat 全体読め (= 重複 2 件の import 元)。

# タスク概要 (Loop 116 = frontend 軸 6 件目 + DRY 重複解消混合)

## 目的

`src/routes/SessionList.tsx` (700 行) から 13 純粋関数を分離して **frontend file 責務分離 34 file 目** + **scope 22 軸目開拓 (`src/routes/` 配下)** を達成する。

**重要 (メイン批判判断発掘)**: 13 件のうち 2 件は既存 `src/utils/transcriptViewFormatters.ts` に **完全に同実装で存在 = 技術負債 = 重複解消必須**:
- SessionList.tsx L38 `getFileName(path)` = transcriptViewFormatters.ts:7 `getFileName(path)` (完全に同実装 = `path.split(/[\\/]/).pop() || path`)
- SessionList.tsx L74 `getSessionDisplayTitle(title)` = transcriptViewFormatters.ts:11 `getCompactSessionTitle(title)` (関数名違うだけで完全に同実装 = `.replace(/\s-\s\d{4}-\d{2}-\d{2}\s\d{2}:\d{2}$/, "").trim() || "無題の会議"`)

→ Loop 116 は **「11 件は新 file 抽出 + 重複 2 件は既存 transcriptViewFormatters から import (DRY 統合)」の混合 paradigm**。

## 成果物 (commit 順)

### Commit 1: refactor(frontend) — SessionList 純粋関数 helpers を utils/sessionListHelpers.ts に抽出 + 重複 2 件は transcriptViewFormatters 統合

#### 1A. 新 file `src/utils/sessionListHelpers.ts` を作成

以下の **11 純粋関数 + 2 type interface + 2 定数** を新 file へ移動 (export 化):

**定数 (移動)**:
- L23 `const SEARCH_QUERY_LABEL_MAX_LENGTH = 40;` → `export const`
- L24 `const SEARCH_EXCERPT_CONTEXT_LENGTH = 42;` → `export const`

**type interface (移動)**:
- L27-31 `interface TranscriptTrackCounts { self, other, unknown }` → `export interface`
- L33-36 `interface SessionStartedAtDisplay { label, iso }` → `export interface`

**純粋関数 (移動 + export 化)**:
- L42 `getSessionStartedAtDisplay(startedAtSecs: number): SessionStartedAtDisplay`
- L59 `formatSearchQueryForLabel(query: string): string`
- L66 `getSearchTerms(query: string): string[]`
- L81 `unescapeInlineMarkdownText(text: string): string`
- L85 `formatSearchExcerptText(text: string): string` (依存: unescapeInlineMarkdownText)
- L92 `getSearchMatchExcerpt(text: string, query: string): string | null` (依存: getSearchTerms + unescapeInlineMarkdownText + formatSearchExcerptText + SEARCH_EXCERPT_CONTEXT_LENGTH)
- L119 `escapeRegExp(text: string): string`
- L123 `renderHighlightedSearchExcerpt(text: string, query: string): ReactNode` (依存: getSearchTerms + escapeRegExp、`<mark>` JSX 返却 = 副作用なし純粋関数)
- L145 `hasTranscriptBody(searchText: string): boolean`
- L149 `getTranscriptTrackCounts(searchText: string): TranscriptTrackCounts` (依存: TranscriptTrackCounts)
- L164 `sessionMatchesQuery(session, startedAtLabel, query): boolean` (依存: getSearchTerms + getSessionDisplayTitle + getFileName + unescapeInlineMarkdownText + SessionSummary)

**重要な依存関係**:
- `sessionMatchesQuery` は `getFileName` + `getSessionDisplayTitle` の 2 件にも依存しているが、**これらは新 file には移動せず transcriptViewFormatters から import 形に変える** (= 重複解消)。
  - sessionListHelpers.ts では `import { getFileName, getCompactSessionTitle } from "./transcriptViewFormatters";` で取り込み、`sessionMatchesQuery` 内では `getCompactSessionTitle(session.title)` (元 `getSessionDisplayTitle(session.title)`) と `getFileName(session.path)` を呼び出す。
- `renderHighlightedSearchExcerpt` は ReactNode を返すため、`import { type ReactNode } from "react";` も必要。
- `SessionSummary` 型は `import type { SessionSummary } from "../hooks/useSessionList";` で取り込み。

**新 file の冒頭 (推定)**:
```ts
import { type ReactNode } from "react";
import type { SessionSummary } from "../hooks/useSessionList";
import { getCompactSessionTitle, getFileName } from "./transcriptViewFormatters";

export const SEARCH_QUERY_LABEL_MAX_LENGTH = 40;
export const SEARCH_EXCERPT_CONTEXT_LENGTH = 42;

export interface TranscriptTrackCounts {
  self: number;
  other: number;
  unknown: number;
}

export interface SessionStartedAtDisplay {
  label: string;
  iso: string | null;
}

// ... 11 純粋関数 ...
```

**規模目安**: 新 file ~160-180 行。

#### 1B. `src/routes/SessionList.tsx` を修正

**削除する関数定義** (13 件):
- L38-40 `getFileName`
- L42-57 `getSessionStartedAtDisplay`
- L59-64 `formatSearchQueryForLabel`
- L66-72 `getSearchTerms`
- L74-79 `getSessionDisplayTitle`
- L81-83 `unescapeInlineMarkdownText`
- L85-90 `formatSearchExcerptText`
- L92-117 `getSearchMatchExcerpt`
- L119-121 `escapeRegExp`
- L123-143 `renderHighlightedSearchExcerpt`
- L145-147 `hasTranscriptBody`
- L149-162 `getTranscriptTrackCounts`
- L164-182 `sessionMatchesQuery`

**削除する type / 定数**:
- L23-25 `const SEARCH_QUERY_LABEL_MAX_LENGTH` / `const SEARCH_EXCERPT_CONTEXT_LENGTH` (削除、新 file 側へ)
- L27-31 `interface TranscriptTrackCounts` (削除、新 file 側へ)
- L33-36 `interface SessionStartedAtDisplay` (削除、新 file 側へ)
- L25 `const EMPTY_SESSIONS: SessionSummary[] = [];` は **残す** (純粋関数ではないが SessionList コンポーネント内で使う定数 = この場所のままで OK)

**追加する import** (既存 import ブロックの直後):
```ts
import {
  formatSearchQueryForLabel,
  getSearchMatchExcerpt,
  getSessionStartedAtDisplay,
  getTranscriptTrackCounts,
  hasTranscriptBody,
  renderHighlightedSearchExcerpt,
  sessionMatchesQuery,
  type SessionStartedAtDisplay,
  type TranscriptTrackCounts,
} from "../utils/sessionListHelpers";
import {
  getCompactSessionTitle,
  getFileName,
} from "../utils/transcriptViewFormatters";
```

**呼び出し名のリネーム**:
- L174 `getSessionDisplayTitle(session.title)` → 不要 (sessionMatchesQuery 内に移動済 = SessionList.tsx 内で直接呼び出しは消える)
- L522 `getSessionDisplayTitle(session.title)` → `getCompactSessionTitle(session.title)` にリネーム呼び出し
- `getFileName` 呼び出し (L175, L223, L253, L523) は import 経由で **そのまま** = 名前変更不要

**注意**:
- L164-182 の `sessionMatchesQuery` 自体は新 file へ移動するが、関数本体内で `getSessionDisplayTitle(session.title)` を呼んでいるので、**新 file 側で `getCompactSessionTitle(session.title)` にリネーム呼び出し** が必要。
- L174-175 は元 `sessionMatchesQuery` 関数内なので、新 file の sessionMatchesQuery 内で同等の呼び出しに置換する。

**SessionList.tsx 削減見積もり**: -150 ~ -160 行 (700 → ~540-550 行)。

#### 1C. import 文の整理 + caller 修正の最終確認

`grep -n "getFileName\|getSessionDisplayTitle\|getSearchMatchExcerpt\|renderHighlightedSearchExcerpt\|sessionMatchesQuery\|getSessionStartedAtDisplay\|formatSearchQueryForLabel\|getSearchTerms\|unescapeInlineMarkdownText\|formatSearchExcerptText\|escapeRegExp\|hasTranscriptBody\|getTranscriptTrackCounts" src/routes/SessionList.tsx` で重複定義 / 未使用 import がないか最終確認。

#### 1D. 検証

- `npm run build` (tsc + vite build) で完走確認 (frontend test framework 未導入のため test 件数検証なし)
- vite v7.3.2 = ~1920 modules transformed が +1 module (新 file 追加) で ~1921 modules になる想定
- cargo は **触れていない** ので cargo test 不要 (704 件不変は前 Loop 105 観測値のまま継続)
- `bash scripts/agent-verify.sh src/routes/SessionList.tsx src/utils/sessionListHelpers.ts` で検証
- 検証 OK 後 `git add src/routes/SessionList.tsx src/utils/sessionListHelpers.ts && git commit -m "refactor(frontend): SessionList 純粋関数 helpers を utils/sessionListHelpers.ts に抽出 + 重複 2 件は transcriptViewFormatters 統合 (mjc-main-20260505-60 Loop 116, frontend 軸 6 件目 = scope 22 軸目開拓 src/routes/, 大型 frontend file 責務分離 34 file 目, 純粋関数機能分離軸 + DRY 軸混合, 重複 getFileName/getSessionDisplayTitle 解消, AGENTS.md priority 1 寄与)"`

**commit hash を `git log -1 --format=%h` で取得して、後続 commit 2 で AGENT_LOG.md 内に実 hash で書き込む。chore commit hash は書かない (placeholder 残留問題予防)**。

### Commit 2: chore(agent-log) — Loop 116 エントリ追記

`AGENT_LOG.md` の **末尾の `---` 直後** に以下のエントリを追記 (時系列順 = 末尾追記、先頭触らない、trailing whitespace 禁止):

```
[mjc-main-20260505-60 Loop 116 / 2026-05-06]
worker: mjc-worker-loop116-session-list-helpers-extract (作業)
範囲: src/routes/SessionList.tsx (純粋関数 13 件削除 + import 2 ブロック追加, ~150 行削減) + src/utils/sessionListHelpers.ts (新規 ~170 行) = 純粋関数 11 件抽出 + 重複 2 件 (getFileName/getSessionDisplayTitle) は既存 transcriptViewFormatters から import 統合 (DRY 軸混合)
内容: SessionList.tsx の純粋関数 13 件 (getSessionStartedAtDisplay + formatSearchQueryForLabel + getSearchTerms + unescapeInlineMarkdownText + formatSearchExcerptText + getSearchMatchExcerpt + escapeRegExp + renderHighlightedSearchExcerpt + hasTranscriptBody + getTranscriptTrackCounts + sessionMatchesQuery + getFileName + getSessionDisplayTitle) のうち、11 件を新 file `src/utils/sessionListHelpers.ts` (file 専用 namespace = 衝突回避設計 6 件目 precedent) に抽出、重複 2 件 (getFileName + getSessionDisplayTitle = transcriptViewFormatters.ts:7 + :11 と完全同実装の技術負債) は既存 transcriptViewFormatters から import 統合 = DRY 重複解消達成 = メイン批判判断発掘成果反映。frontend 軸 6 件目 (Loop 107/108/110/112/114 precedent) = scope 22 軸目開拓 (`src/routes/` 配下 = 異 directory = 多様性継続) + 大型 frontend file 責務分離 34 file 目 + 純粋関数機能分離軸 frontend 版 continuity。alternation pattern (K(115) → frontend(116)) = 軸 reset 解釈で 6 連続境界突破。
振る舞い: rust ファイル無変更のため cargo test 不要 (704 件件数完全不変は前 Loop 105 で確認済)。
verify: npm run build OK (vite v7.3.2 = ~1921 modules transformed = +1 module from Loop 114 = 新 file 追加分)。scripts/agent-verify.sh 全項目 OK。
commits:
- <REFACTOR_HASH> refactor(frontend): SessionList 純粋関数 helpers を utils/sessionListHelpers.ts に抽出 + 重複 2 件は transcriptViewFormatters 統合
AGENTS.md priority: 1 (大型 frontend file 責務分離 + DRY 重複解消 + 純粋関数機能分離 = Mac アプリ保守性向上)

---
```

**`<REFACTOR_HASH>` は実際の hash で置換** (例: `b6c0341`)。chore commit hash は **書かない**。

検証 OK 後 `git add AGENT_LOG.md && git commit -m "chore(agent-log): mjc-main-20260505-60 Loop 116 = utils/sessionListHelpers.ts 抽出 + 重複 2 件 transcriptViewFormatters 統合 (構造分離 34 file 目, frontend 軸 6 件目 = scope 22 軸目開拓 src/routes/, 純粋関数機能分離軸 + DRY 軸混合)"`

# 制約 (絶対遵守)

- 担当範囲は **src/routes/SessionList.tsx + src/utils/sessionListHelpers.ts + AGENT_LOG.md のみ**。他 file は触らない。
- transcriptViewFormatters.ts は **読むだけ** (=新規 export 追加なし、既存 export を import するだけ)。
- 行末の **trailing whitespace 禁止**。
- AGENT_LOG.md の **先頭は絶対に触らない**。**末尾の `---` 直後に追記**。
- `--no-verify` 禁止。`--dangerously-skip-permissions` は harness 内のみ。
- 規模超過防止: production 削減 ~150 行 + 新 file ~170 行が目安。これより大幅増は規模超過。
- 大型 file (AGENT_LOG.md = ~32,830 行) は **絶対に Read 全体禁止** = `tail -350 AGENT_LOG.md` で末尾だけ参照する。
- npm run build (tsc + vite build) を主検証として運用 (frontend test framework 未導入)。
- elevenlabs/openai 系の実 API 叩きは **厳禁** (課金禁止)。
- Keychain 実通信禁止 / Apple SpeechAnalyzer 実通信禁止。
- cargo は触れない (704 件不変前提)。
- TypeScript の `import type` / `export type` を適切に使う (純粋な型は type-only import に)。
- file 内の関数定義順序は依存関係順 (依存される側が先) を維持。

# 2-commit pattern (55 連続ループで実証済 = 完全安定)

1. refactor(frontend) commit を作成 (file 編集 + verify + git add + git commit)
2. `git log -1 --format=%h` で refactor hash 取得
3. AGENT_LOG.md 末尾エントリ内の `<REFACTOR_HASH>` placeholder を実 hash で置換書き込み (chore hash は書かない)
4. chore(agent-log) commit を作成 (AGENT_LOG.md edit + git add + git commit)

# 完走基準

- 2 commit 反映 (`git log --oneline -3` で確認)
- npm run build OK
- bash scripts/agent-verify.sh OK
- AGENT_LOG.md 末尾エントリ追記済 (先頭無変更)
- src/routes/SessionList.tsx ~540-550 行 (700 行 -150 行)
- src/utils/sessionListHelpers.ts 新規 ~170 行
- 重複 2 件 (getFileName + getSessionDisplayTitle) 解消 = transcriptViewFormatters から import 統合済
- 衝突回避: file 専用 namespace `sessionListHelpers.ts` (6 件目 precedent)
