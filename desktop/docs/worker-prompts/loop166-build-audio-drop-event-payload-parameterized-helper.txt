あなたは meet-jerky の worker サブエージェントです.
mjc-main-20260505-82 Loop 166 を実行します.

# 必読 (順序厳守)

1. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
3. **AGENT_LOG.md は 34002 行 / ~3.2MB ある**. **必ず `tail -350 AGENT_LOG.md` で末尾だけ参照する**. 先頭は絶対に触らない. AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾. **新規追記は必ずファイル末尾に行う**.
4. 末尾に `[SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-81` がある = 直前 session の状況メモ.

# タスク概要 (Loop 166 = rust 軸 = paradigm 系統樹拡張 = parameterized assertion helper 化 paradigm 4 件目)

`build_audio_drop_event_payload_*` test 10 件 (audio.rs x5 + system_audio.rs x5) を audio_event.rs に **`#[cfg(test)] pub(crate) mod test_helpers`** を新設して **5 つの parameterized helper fn** に集約する.

両 file の test 10 件は **test fn 名保持で中身を helper 呼び出し 1 行 (or 2 行) に置換** = test 件数 (704) 完全不変保証 + DRY 違反解消.

## 集約先

- **src/audio_event.rs** (現在 47 行 = build_audio_drop_event_payload の正規定義場所 + 既存 test 3 件あり). 末尾の `mod tests` の **直前** または **直後** に `#[cfg(test)] pub(crate) mod test_helpers { ... }` を追加.

## 集約対象 (10 件 = 5 paradigm x 2 file)

### audio.rs (L795-L858 内)

- L795-L809: `build_audio_drop_event_payload_includes_source_and_dropped_fields_with_microphone` (T1 microphone)
- L811-L816: `build_audio_drop_event_payload_serializes_zero_dropped_count_explicitly` (T2 microphone)
- L818-L827: `build_audio_drop_event_payload_handles_usize_max_boundary` (T3 microphone)
- L829-L843: `build_audio_drop_event_payload_passes_through_arbitrary_source_label` (T4 system_audio + any_other)
- L845-L858: `build_audio_drop_event_payload_has_exactly_two_top_level_fields` (T5 microphone)

### system_audio.rs (L365-L437 内, 全 #[cfg(target_os = "macos")] 付)

- L370-L384: `build_audio_drop_event_payload_includes_source_and_dropped_fields_with_system_audio` (T1 system_audio)
- L386-L392: `build_audio_drop_event_payload_serializes_zero_dropped_count_explicitly` (T2 system_audio)
- L394-L404: `build_audio_drop_event_payload_handles_usize_max_boundary` (T3 system_audio)
- L406-L421: `build_audio_drop_event_payload_passes_through_arbitrary_source_label` (T4 microphone + any_other)
- L423-L437: `build_audio_drop_event_payload_has_exactly_two_top_level_fields` (T5 system_audio)

## helper 5 つの設計 (audio_event.rs 内 #[cfg(test)] pub(crate) mod test_helpers)

```rust
#[cfg(test)]
pub(crate) mod test_helpers {
    use super::build_audio_drop_event_payload;

    pub(crate) fn assert_drop_payload_includes_source_and_dropped_fields(source: &str, dropped: usize) {
        let payload = build_audio_drop_event_payload(source, dropped);
        assert_eq!(
            payload.get("source").and_then(|v| v.as_str()),
            Some(source),
            "source field が文字列で含まれる契約"
        );
        assert_eq!(
            payload.get("dropped").and_then(|v| v.as_u64()),
            Some(dropped as u64),
            "dropped field が u64 として含まれる契約"
        );
    }

    pub(crate) fn assert_drop_payload_serializes_zero_dropped_count_explicitly(source: &str) {
        let payload = build_audio_drop_event_payload(source, 0);
        assert_eq!(payload.get("dropped").and_then(|v| v.as_u64()), Some(0));
    }

    pub(crate) fn assert_drop_payload_handles_usize_max_boundary(source: &str) {
        let payload = build_audio_drop_event_payload(source, usize::MAX);
        assert_eq!(
            payload.get("dropped").and_then(|v| v.as_u64()),
            Some(usize::MAX as u64),
            "usize::MAX が u64 として serde_json に渡せる契約"
        );
    }

    pub(crate) fn assert_drop_payload_passes_through_arbitrary_source_labels(label_a: &str, label_b: &str) {
        let payload = build_audio_drop_event_payload(label_a, 1);
        assert_eq!(
            payload.get("source").and_then(|v| v.as_str()),
            Some(label_a)
        );
        let payload2 = build_audio_drop_event_payload(label_b, 1);
        assert_eq!(
            payload2.get("source").and_then(|v| v.as_str()),
            Some(label_b),
            "source は arbitrary passthrough = source を判定する誤改修への検知装置"
        );
    }

    pub(crate) fn assert_drop_payload_has_exactly_two_top_level_fields(source: &str) {
        let payload = build_audio_drop_event_payload(source, 5);
        let obj = payload.as_object().expect("payload は JSON object");
        assert_eq!(
            obj.len(),
            2,
            "top-level field は exactly 2 つ (source + dropped) の契約: 実際 = {:?}",
            obj.keys().collect::<Vec<_>>()
        );
        assert!(obj.contains_key("source"));
        assert!(obj.contains_key("dropped"));
    }
}
```

## 両 file の test 中身置換 (test fn 名は完全保持)

### audio.rs 側 (`mod tests` 内, T1-T5)

T1: 中身を `crate::audio_event::test_helpers::assert_drop_payload_includes_source_and_dropped_fields("microphone", 42);` に置換
T2: `crate::audio_event::test_helpers::assert_drop_payload_serializes_zero_dropped_count_explicitly("microphone");`
T3: `crate::audio_event::test_helpers::assert_drop_payload_handles_usize_max_boundary("microphone");`
T4: `crate::audio_event::test_helpers::assert_drop_payload_passes_through_arbitrary_source_labels("system_audio", "any_other_label");`
T5: `crate::audio_event::test_helpers::assert_drop_payload_has_exactly_two_top_level_fields("microphone");`

### system_audio.rs 側 (`mod tests` 内, T1-T5, 全 #[cfg(target_os = "macos")] 維持)

T1: 中身を `crate::audio_event::test_helpers::assert_drop_payload_includes_source_and_dropped_fields("system_audio", 42);` に置換
T2: `crate::audio_event::test_helpers::assert_drop_payload_serializes_zero_dropped_count_explicitly("system_audio");`
T3: `crate::audio_event::test_helpers::assert_drop_payload_handles_usize_max_boundary("system_audio");`
T4: `crate::audio_event::test_helpers::assert_drop_payload_passes_through_arbitrary_source_labels("microphone", "any_other_label");`
T5: `crate::audio_event::test_helpers::assert_drop_payload_has_exactly_two_top_level_fields("system_audio");`

## 規模超過防止

- **担当範囲外編集禁止**: audio_event.rs / audio.rs (L790-L860) / system_audio.rs (L365-L438) のみ. 他 file 触らない.
- **test 件数の上限**: 704 件 (cargo test (lib) で完全不変必須). 増減したら fault.
- **既存 test 削除禁止**: 中身を helper 呼び出しに置換するだけ. test fn 名 / `#[test]` / `#[cfg(target_os = "macos")]` は **完全保持**.
- audio_event.rs 既存の 3 件の build_audio_drop_event_payload tests (L26-L46) は **触らない** = 別仕様 = 残置.

## 大型 file 注意

- audio.rs は 949 行. **全体 Read 禁止** = `sed -n '780,860p' src/audio.rs` または Read tool に offset+limit 指定で L790-L860 のみ参照.
- system_audio.rs は 438 行 (Read 全体 OK).
- audio_event.rs は 47 行 (Read 全体 OK).

## 検証 (worker 必須実行)

```fish
cd src-tauri
cargo test --lib 2>&1 | tail -30      # 704 passed 件数完全不変
cargo clippy --lib --tests -- -D warnings 2>&1 | tail -10
cargo fmt --check 2>&1 | tail -5
cargo build --lib 2>&1 | tail -10
cd ..
bash scripts/agent-verify.sh src-tauri/src/audio_event.rs src-tauri/src/audio.rs src-tauri/src/system_audio.rs
```

# 実行手順 (2-commit pattern, Loop 145-165 で 21 連続安定)

## Step 1: 実装
1. `tail -10 AGENT_LOG.md` で末尾形式確認 (`---` 区切り直前を確認)
2. audio_event.rs を Read (47 行)
3. audio.rs L790-L860 を Read で参照 (build_audio_drop_event_payload tests のみ)
4. system_audio.rs を Read (438 行)
5. audio_event.rs に Edit で `#[cfg(test)] pub(crate) mod test_helpers { ... }` を追加 (上記 5 つの helper fn)
6. audio.rs T1-T5 の中身を Edit で helper 呼び出しに置換 (test fn 名 / `#[test]` 保持)
7. system_audio.rs T1-T5 の中身を Edit で helper 呼び出しに置換 (test fn 名 / `#[cfg(target_os = "macos")]` / `#[test]` 保持)
8. 上記検証コマンド全て OK 確認 (cargo test 704 / clippy / fmt / build / agent-verify)

## Step 2: refactor commit
```fish
git add src-tauri/src/audio_event.rs src-tauri/src/audio.rs src-tauri/src/system_audio.rs
bash scripts/agent-commit.sh "refactor(rust): build_audio_drop_event_payload_* test 10 件を audio_event::test_helpers の 5 parameterized helper に集約 (mjc-main-20260505-82 Loop 166, rust 軸 = paradigm 系統樹拡張 = parameterized assertion helper 化 paradigm 4 件目, audio.rs T1-T5 + system_audio.rs T1-T5 の対称仕様 test を引数化 helper で集約 + test fn 名 / #[cfg(target_os = \"macos\")] 完全保持 = 件数 704 完全不変, callsite 10 件は中身を helper 呼び出し 1-2 行に置換 = DRY 違反解消, alternation pivot K(165) → rust(166) = 5 → 6, Loop 159 jst() fn 集約 / Loop 162 list_md_files() fn 集約 / Loop 164 audio_command_* test logic 集約 の paradigm 系統樹拡張 = fn 重複 → test 内容重複 → parameterized helper への paradigm 4 件目, AGENTS.md priority 1 寄与)" src-tauri/src/audio_event.rs src-tauri/src/audio.rs src-tauri/src/system_audio.rs
```

## Step 3: AGENT_LOG.md entry append
1. **append は 1 回のみ**. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む. (Loop 144 fault precedent)
2. 末尾追記内容: 下記 entry テンプレートを使用. 行末空白 (trailing whitespace) 禁止.

```
---
[mjc-main-20260505-82 Loop 166 / 2026-05-06]
worker: mjc-worker-loop166-build-audio-drop-event-payload-parameterized-helper (作業)
範囲: src-tauri/src/audio_event.rs (#[cfg(test)] pub(crate) mod test_helpers 新設, 5 helper fn 追加 = ~50 行追加) + src-tauri/src/audio.rs (T1-T5 = build_audio_drop_event_payload_* test 5 件の中身を helper 呼び出し 1-2 行に置換, test fn 名 / #[test] 完全保持) + src-tauri/src/system_audio.rs (T1-T5 = build_audio_drop_event_payload_* test 5 件の中身を helper 呼び出し 1-2 行に置換, test fn 名 / #[cfg(target_os = "macos")] / #[test] 完全保持)
内容: audio.rs T1-T5 と system_audio.rs T1-T5 の build_audio_drop_event_payload_* test 10 件は両 file で source label 違いで対称仕様 (audio.rs = "microphone" 中心 / system_audio.rs = "system_audio" 中心 + 全 #[cfg(target_os = "macos")]). audio_event.rs (= build_audio_drop_event_payload の正規定義場所 = 47 行 + 既存 test 3 件) に #[cfg(test)] pub(crate) mod test_helpers を新設し 5 つの parameterized helper fn (assert_drop_payload_includes_source_and_dropped_fields(source, dropped) / assert_drop_payload_serializes_zero_dropped_count_explicitly(source) / assert_drop_payload_handles_usize_max_boundary(source) / assert_drop_payload_passes_through_arbitrary_source_labels(label_a, label_b) / assert_drop_payload_has_exactly_two_top_level_fields(source)) を集約 + 両 file の test 10 件は test fn 名保持で中身を helper 呼び出し 1-2 行に置換 = test 件数 (704) 完全不変保証 + DRY 違反解消. callsite (test fn) 10 件は名前変更なし API 互換維持. paradigm 系統樹拡張 4 件目達成 (Loop 159 jst() fn 集約 → Loop 162 list_md_files() fn 集約 → Loop 164 audio_command_* test logic 集約 → Loop 166 parameterized assertion helper 化 = fn 重複 → test 内容重複 → parameterized helper). 既存 file 拡張軸 paradigm 9 件目 + 重複候補 grep 探索 paradigm 9 件目 + test helper 集約軸 paradigm 反復 4 件目. alternation pivot K(165) → rust(166) = 5 → 6. AGENTS.md priority 1 寄与 (DRY 解消 + 大型 file 構造改善).
振る舞い: cargo test (lib) 704 件件数完全不変、cargo clippy + cargo fmt + cargo build 全 OK. frontend 無変更.
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(rust): build_audio_drop_event_payload_* test 10 件を audio_event::test_helpers の 5 parameterized helper に集約 (paradigm 系統樹拡張 4 件目)
AGENTS.md priority: 1 (paradigm 系統樹拡張 = parameterized assertion helper 化 = 真の DRY 解消 + 件数完全不変両立)
---
```

3. **手順**: tmpfile (例: `/tmp/loop166_entry.md`) を Write で作成 → `cat /tmp/loop166_entry.md >> AGENT_LOG.md` で append (1 回のみ) → `tail -5 AGENT_LOG.md` で末尾確認.
4. `<REFACTOR_HASH>` は git log の Step 2 commit の short hash で書き換える.

## Step 4: chore commit
```fish
git add AGENT_LOG.md
bash scripts/agent-commit.sh "chore(agent-log): mjc-main-20260505-82 Loop 166 = build_audio_drop_event_payload_* test 10 件を audio_event::test_helpers の 5 parameterized helper に集約 (rust 軸, paradigm 系統樹拡張 4 件目 = parameterized assertion helper 化 paradigm, 既存 file 拡張軸 paradigm 9 件目 = test helper 集約軸 paradigm 反復 4 件目, audio.rs T1-T5 + system_audio.rs T1-T5 の対称仕様 test を引数化 helper で集約, alternation pivot K(165) → rust(166) = 5 → 6, Loop 159 jst() fn 集約 / Loop 162 list_md_files() fn 集約 / Loop 164 audio_command_* test logic 集約 の paradigm 系統樹拡張 = fn 重複 → test 内容重複 → parameterized helper, 重複候補 grep 探索 paradigm 9 件目達成) のエントリ追記" AGENT_LOG.md
```

# 制約事項 (絶対遵守)

- AGENT_LOG.md への append は **1 回のみ**. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む (Loop 144 fault precedent).
- AGENT_LOG.md 先頭に絶対追記しない. 末尾の `---` 区切り直後に追記.
- 行末空白 (trailing whitespace) 禁止.
- `--no-verify` 禁止. `--dangerously-skip-permissions` は harness 内のみ.
- 担当範囲 (audio_event.rs / audio.rs L790-L860 / system_audio.rs L365-L438) 外を触らない.
- test 件数 704 完全不変必須.
- audio_event.rs 既存 tests (L26-L46) は触らない.

# 完了基準

- 2-commit pattern (refactor + chore) で AGENT_LOG.md と code 両方が反映.
- cargo test 704 件件数完全不変 + cargo clippy + cargo fmt + cargo build 全 OK.
- agent-verify.sh 全項目 OK.
- working tree clean (untracked handoff/worker-prompts file を除く).
