あなたは作業担当エージェント (Claude Code, print mode) です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

# 必読 (順序遵守)

1. AGENTS.md
2. docs/product-concept.md
3. docs/agent-harness-claude.md
4. **AGENT_LOG.md の末尾 350 行のみを `tail -350 AGENT_LOG.md` で読む** (Read 全体禁止)
5. precedent (cat 全体): `cat src-tauri/src/transcription_commands_model.rs` (Loop 90 抽出, ~100 行 = 同 paradigm = 機能分類軸 = tauri commands 抽出)
6. precedent (cat 全体): `cat src-tauri/src/system_audio_format.rs` (Loop 92 直前抽出, ~165 行 = 直近 worker 自律学習 paradigm)

# AGENT_LOG.md 取り扱い (絶対遵守)

- AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾。
- 新規追記は **必ずファイル末尾** に行う。
- **先頭は絶対に触らない**。
- 具体手順:
  1. `tail -10 AGENT_LOG.md` で末尾を確認
  2. 末尾の `---` 直後に空行を 1 つ入れて新エントリ開始
  3. エントリ末尾に区切りの `---` を 1 行追加
- **行末の空白文字 (trailing whitespace) 禁止**

# 担当範囲 (これ以外は編集禁止)

- 新規作成: `src-tauri/src/secret_store_commands.rs` (~110-130 行)
- 編集: `src-tauri/src/secret_store.rs` (tauri commands 6 個 production ~45 行削除 + 関連 test 4 件削除 + 不要 import 整理)
- 編集: `src-tauri/src/lib.rs` (mod 追加 1 行 + invoke_handler の path 修正 6 件)
- 追記: `AGENT_LOG.md` (末尾エントリ追加のみ)

担当範囲外の編集は厳禁です。

# Loop 93 タスク内容 (構造分離 19 件目, 別 scope = secret_store 軸 1 件目 = pivot, 機能分類軸抽出)

## 背景

- Loop 91/92 = system_audio 軸 2 連続抽出達成 (PCM helpers + audio format 検証)。
- Loop 93 では別 scope へ pivot = secret_store.rs (303 行) の **tauri commands 6 個** (set_openai_api_key + clear_openai_api_key + has_openai_api_key + set_elevenlabs_api_key + clear_elevenlabs_api_key + has_elevenlabs_api_key) を別 module に抽出 = 構造分離 19 件目 = 新 scope = paradigm 切り替え (cfg(macos) 軸 → 機能分類軸)。
- secret_store.rs は内部 keychain helpers (entry_for + set_secret + get_secret + delete_secret + has_secret) と公開 tauri commands で機能境界が clear に分かれている。AGENTS.md priority 1 寄与。
- Loop 90 transcription_commands_model.rs (3 cmd + 2 helper + 5 test = 101 行) と完全同 paradigm。

## 抽出対象 (secret_store.rs から secret_store_commands.rs へ移動)

**Production (secret_store.rs L101-155, ~55 行)**:
- L101-107: `// ─────────────...` セパレータ + 「Tauri コマンド」ヘッダ doc コメント
- L109-119: `set_openai_api_key` (空文字 trim + 検証 + set_secret 呼び出し)
- L121-125: `clear_openai_api_key` (delete_secret 呼び出し)
- L127-131: `has_openai_api_key` (has_secret 呼び出し)
- L133-143: `set_elevenlabs_api_key` (空文字 trim + 検証 + set_secret 呼び出し)
- L145-149: `clear_elevenlabs_api_key` (delete_secret 呼び出し)
- L151-155: `has_elevenlabs_api_key` (has_secret 呼び出し)

**Test (secret_store.rs `mod tests` 内, command 軸の 4 件)**:
- `set_openai_api_key_rejects_empty_input` (~6 行)
- `set_elevenlabs_api_key_rejects_empty_input` (~6 行)
- `set_api_keys_return_consistent_japanese_message_for_empty_input` (~17 行)
- `set_api_keys_treat_various_whitespace_chars_as_empty` (~12 行)

これ以外の test (SecretKey 軸 5 件 = `secret_key_account_is_stable` / `secret_key_accounts_are_distinct` / `secret_key_debug_output_uses_variant_name_without_leaking_account_string` / `secret_key_implements_copy_trait_so_value_remains_usable_after_assignment` / `secret_key_account_strings_use_lowercase_kebab_case_format_for_all_variants` + 内部 helper 軸 1 件 = `secret_apis_error_clearly_on_unsupported_platforms`) は **絶対に触らない** (元 file 残留)。

## 抽出方式 (Loop 90 transcription_commands_model.rs precedent 踏襲)

### 1. 新規 file 作成 = `src-tauri/src/secret_store_commands.rs`

ファイル冒頭 docstring 例:

```
//! API キー (OpenAI / ElevenLabs) を OS のセキュアストアに保存・削除・存在確認する tauri コマンド群。
//!
//! `secret_store.rs` 本来責務 (Keychain 内部実装 + SecretKey 識別子 + 共通 set/get/delete/has helper)
//! から tauri 公開 API 軸を切り出して機能境界で分離する。
//! フロントエンドからの呼び出し経路 (invoke handler) は変わらない。
```

必要 import (移動):
```rust
use crate::secret_store::{delete_secret, has_secret, set_secret, SecretKey};
```

production 6 fn (set_openai_api_key + clear_openai_api_key + has_openai_api_key + set_elevenlabs_api_key + clear_elevenlabs_api_key + has_elevenlabs_api_key) をそのまま `#[tauri::command]` 属性付きで移動。`mod tests` を新 file 内に追加し、test 4 件を移動。test 内では `use super::*;` で 6 cmd を使えるようにする。

### 2. secret_store.rs 編集

- 該当 production section (L101-155) を削除 (Tauri コマンド section ヘッダ + 6 cmd)
- 不要になった import がないか cargo build で確認 (今は L1-99 が使う import のみ残るはず = 元々 use なし、`keyring::Entry` のみ cfg 内で参照)
- mod tests 内の関連 test 4 件を削除:
  - `set_openai_api_key_rejects_empty_input`
  - `set_elevenlabs_api_key_rejects_empty_input`
  - `set_api_keys_return_consistent_japanese_message_for_empty_input`
  - `set_api_keys_treat_various_whitespace_chars_as_empty`
- それ以外の test (SecretKey 軸 5 件 + 内部 helper 軸 1 件) は触らない

### 3. lib.rs 編集 (alphabetical mod + invoke_handler path)

- L27 `mod secret_store;` の **直後** に `mod secret_store_commands;` を 1 行追加 (alphabetical 順)
- invoke_handler L389-394 を以下に変更:
  - `secret_store::set_openai_api_key` → `secret_store_commands::set_openai_api_key`
  - `secret_store::clear_openai_api_key` → `secret_store_commands::clear_openai_api_key`
  - `secret_store::has_openai_api_key` → `secret_store_commands::has_openai_api_key`
  - `secret_store::set_elevenlabs_api_key` → `secret_store_commands::set_elevenlabs_api_key`
  - `secret_store::clear_elevenlabs_api_key` → `secret_store_commands::clear_elevenlabs_api_key`
  - `secret_store::has_elevenlabs_api_key` → `secret_store_commands::has_elevenlabs_api_key`

それ以外の lib.rs 修正は禁止。

## 規模超過防止 (重要)

- **production 6 fn / test 4 件以外は絶対に触らない**
- **担当範囲外の修正は禁止**
- **トータル test 件数は 704 = 完全不変** (移動のみ、削除/追加禁止)
- 振る舞い不変 = caller path 変更は lib.rs invoke_handler のみ、frontend `invoke('set_openai_api_key')` 等の名前は不変
- secret_store.rs 内の `set_secret` / `delete_secret` / `has_secret` は新 file から呼ばれるので **`pub fn` のまま維持** (`pub(crate)` への絞り込みは別 Loop で検討)

## 検証 (順序遵守)

1. `cd src-tauri && cargo fmt`
2. `cd src-tauri && cargo build --lib` → 不要 import の clippy warning や build error が出たら最小修正
3. `cd src-tauri && cargo clippy --lib --tests -- -D warnings`
4. `cd src-tauri && cargo test --lib` → **704 passed 不変** を確認
5. `bash scripts/agent-verify.sh src-tauri/src/secret_store.rs src-tauri/src/secret_store_commands.rs src-tauri/src/lib.rs`

# コミット (2-commit pattern, Loop 80/85/86/87/88/89/90/91/92 precedent)

## Commit 1: refactor

ステージ: `git add src-tauri/src/secret_store.rs src-tauri/src/secret_store_commands.rs src-tauri/src/lib.rs`

メッセージ:
```
refactor(secret-store): API キー保存/削除/存在確認の tauri commands を secret_store_commands.rs に抽出 (mjc-main-20260505-48 Loop 93, 機能分離軸 = secret_store 軸 1 件目, 大型 rust file 責務分離 19 file 目, AGENTS.md priority 1 寄与)
```

`git commit -m "..."` 後、**hash を `git log -1 --format=%H` で取得**。

## AGENT_LOG.md エントリ追記

末尾 `---` 直後に以下フォーマットで追記 (refactor commit hash は **取得した実 hash を直接書く**、chore commit hash は **書かない**):

```
[mjc-main-20260505-48 Loop 93 / 2026-05-05]
worker: mjc-worker-secret-store-commands-loop93 (作業)
範囲: src-tauri/src/secret_store.rs (tauri commands 6 個 production ~55 行削除 + test 4 件削除) + src-tauri/src/secret_store_commands.rs (新規 ~110-130 行) + src-tauri/src/lib.rs (mod 追加 1 行 + invoke_handler path 修正 6 件)
内容: secret_store.rs L101-155 の tauri commands 6 個 (set_openai_api_key + clear_openai_api_key + has_openai_api_key + set_elevenlabs_api_key + clear_elevenlabs_api_key + has_elevenlabs_api_key) を新 module secret_store_commands.rs に抽出。tests 4 件 (set_openai_api_key_rejects_empty_input + set_elevenlabs_api_key_rejects_empty_input + set_api_keys_return_consistent_japanese_message_for_empty_input + set_api_keys_treat_various_whitespace_chars_as_empty) を新 file 内 #[cfg(test)] mod tests に移動。lib.rs に mod secret_store_commands; 追加 + invoke_handler の secret_store::xxx を secret_store_commands::xxx に変更 (6 件)。secret_store.rs 内の SecretKey enum + entry_for + set_secret + get_secret + delete_secret + has_secret + 残 test 6 件は不変。大型 rust file 責務分離 19 file 目 = secret_store 軸 1 件目 (新 scope) = system_audio 軸 (Loop 91/92) からの pivot 達成 = paradigm 切り替え (cfg(macos) 軸 → 機能分類軸)。
振る舞い: cargo test --lib 704 passed (件数完全不変)
clippy: 警告ゼロ (-D warnings)
fmt: OK
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH_HERE> refactor(secret-store): API キー tauri commands を secret_store_commands.rs に抽出
AGENTS.md priority: 1 (構造美の補強 = secret_store 軸の機能境界分離 = 内部 keychain 実装と tauri 公開 API の責務分離)

---
```

`<REFACTOR_HASH_HERE>` は実 hash で置換 (Loop 80/85/86/87/88/89/90/91/92 precedent)。

## Commit 2: chore (AGENT_LOG.md 追記分)

ステージ: `git add AGENT_LOG.md`

メッセージ:
```
chore(agent-log): mjc-main-20260505-48 Loop 93 = secret_store_commands.rs 抽出 (構造分離 19 file 目, secret_store 軸 1 件目 = 新 scope, system_audio 軸からの pivot 達成 = 機能分類軸 paradigm) のエントリ追記
```

# 完走報告

最終出力で以下を簡潔に報告:
- 変更ファイル 4 件
- cargo test 704 passed 確認
- 2 commit hash
- 残リスク (あれば)

1 ターンで完結する形でまとめてください。
