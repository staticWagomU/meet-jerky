あなたは meet-jerky の worker サブエージェントです.
mjc-main-20260505-83 Loop 168 を実行します.

# 必読 (順序厳守)

1. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/AGENTS.md
2. /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop/docs/agent-harness-claude.md
3. **AGENT_LOG.md は ~34,026 行 / ~3.2MB ある**. **必ず `tail -350 AGENT_LOG.md` で末尾だけ参照する**. 先頭は絶対に触らない. AGENT_LOG.md は時系列順 = 最古ログが先頭、最新ログが末尾. **新規追記は必ずファイル末尾に行う**.
4. 末尾に `[SESSION SUMMARY @ 2026-05-06 ~JST] mjc-main-20260505-82` がある = 直前 session の状況メモ.

# タスク概要 (Loop 168 = rust 軸 = paradigm 系統樹拡張 5 件目 = parameterized helper 化 paradigm 反復 2 件目)

`push_error_*` test 10 件 (elevenlabs_realtime_ws_task.rs x5 + openai_realtime.rs x5) を `realtime_error_helpers.rs` に **`#[cfg(test)] pub(crate) mod test_helpers`** を新設して **5 つの parameterized helper fn** に集約する.

両 file の test 5 件は **engine 構造に一切依存せず**、`crate::realtime_error_helpers::push_error()` を直接呼ぶだけで、違いは `engine_label: &str` ("ElevenLabs" vs "OpenAI") のみ = 完全対称仕様.

両 file の test 10 件は **test fn 名保持で中身を helper 呼び出し 1 行に置換** = test 件数 (704) 完全不変保証 + DRY 違反解消.

## 集約先

- **src-tauri/src/realtime_error_helpers.rs** (現在 24 行 = `pub(crate) fn push_error(...)` の正規定義場所). 既存 fn の **直後** に `#[cfg(test)] pub(crate) mod test_helpers { ... }` を追加.

## 集約対象 (10 件 = 5 paradigm x 2 file)

### elevenlabs_realtime_ws_task.rs (`mod pending_timeout_tests` 内)

- L232: `push_error_format_prefix_and_suffix_are_fixed` (E1)
- L254: `push_error_sets_zero_timestamps_and_is_error_true` (E2)
- L275: `push_error_passes_source_through` (E3, 2 ケース内包)
- L309: `push_error_clones_speaker_field` (E4, 2 ケース内包)
- L342: `push_error_with_empty_and_multibyte_messages_preserve_text_format` (E5, 2 ケース内包)

### openai_realtime.rs (`mod tests` 内)

- L297: `push_error_format_prefix_and_suffix_are_fixed` (O1)
- L320: `push_error_sets_zero_timestamps_and_is_error_true` (O2)
- L342: `push_error_passes_source_through` (O3, 2 ケース内包)
- L376: `push_error_clones_speaker_field` (O4, 2 ケース内包)
- L409: `push_error_with_empty_and_multibyte_messages_preserve_text_format` (O5, 2 ケース内包)

## helper 5 つの設計 (realtime_error_helpers.rs 内 #[cfg(test)] pub(crate) mod test_helpers)

```rust
#[cfg(test)]
pub(crate) mod test_helpers {
    use std::sync::Arc;

    use parking_lot::Mutex;

    use crate::transcription_types::{TranscriptionSegment, TranscriptionSource};

    pub(crate) fn assert_push_error_format_prefix_and_suffix_are_fixed(engine_label: &str) {
        let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
        let speaker = Some("自分".to_string());

        super::push_error(
            engine_label,
            &pending,
            &speaker,
            Some(TranscriptionSource::Microphone),
            "connection closed".to_string(),
        );

        let segments = pending.lock();
        assert_eq!(segments.len(), 1);
        // prefix と suffix を含む完全一致で文言契約を強制 (engine_label は呼び出し側パラメータ).
        assert_eq!(
            segments[0].text,
            format!("[{engine_label} Realtime エラー: connection closed]")
        );
    }

    pub(crate) fn assert_push_error_sets_zero_timestamps_and_is_error_true(engine_label: &str) {
        let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
        let speaker = Some("自分".to_string());

        super::push_error(
            engine_label,
            &pending,
            &speaker,
            Some(TranscriptionSource::Microphone),
            "rate limit".to_string(),
        );

        let segments = pending.lock();
        assert_eq!(segments.len(), 1);
        // エラーセグメント identity 契約: 時刻情報なし + is_error=Some(true)
        assert_eq!(segments[0].start_ms, 0);
        assert_eq!(segments[0].end_ms, 0);
        assert_eq!(segments[0].is_error, Some(true));
    }

    pub(crate) fn assert_push_error_passes_source_through(engine_label: &str) {
        // ケース 1: Some(Microphone) は Some(Microphone) のまま渡る
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker = Some("自分".to_string());
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                Some(TranscriptionSource::Microphone),
                "err".to_string(),
            );
            assert_eq!(
                pending.lock()[0].source,
                Some(TranscriptionSource::Microphone)
            );
        }

        // ケース 2: None は None のまま渡る
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker = Some("自分".to_string());
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                None,
                "err".to_string(),
            );
            assert_eq!(pending.lock()[0].source, None);
        }
    }

    pub(crate) fn assert_push_error_clones_speaker_field(engine_label: &str) {
        // ケース 1: Some("自分") が clone されて渡る (元の所有権は保持される)
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker = Some("自分".to_string());
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                Some(TranscriptionSource::Microphone),
                "err".to_string(),
            );
            assert_eq!(pending.lock()[0].speaker, Some("自分".to_string()));
            // 元の speaker が clone 後も有効であることを確認 (move されていない)
            assert_eq!(speaker, Some("自分".to_string()));
        }

        // ケース 2: None は None のまま渡る
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker: Option<String> = None;
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                Some(TranscriptionSource::Microphone),
                "err".to_string(),
            );
            assert_eq!(pending.lock()[0].speaker, None);
        }
    }

    pub(crate) fn assert_push_error_with_empty_and_multibyte_messages_preserve_text_format(
        engine_label: &str,
    ) {
        // ケース 1: 空 message でも prefix/suffix が残り "[<engine_label> Realtime エラー: ]" になる
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker = Some("自分".to_string());
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                Some(TranscriptionSource::Microphone),
                "".to_string(),
            );
            assert_eq!(
                pending.lock()[0].text,
                format!("[{engine_label} Realtime エラー: ]")
            );
        }

        // ケース 2: 日本語マルチバイト message が prefix/suffix の間にそのまま埋め込まれる
        {
            let pending: Arc<Mutex<Vec<TranscriptionSegment>>> = Arc::new(Mutex::new(Vec::new()));
            let speaker = Some("自分".to_string());
            super::push_error(
                engine_label,
                &pending,
                &speaker,
                Some(TranscriptionSource::Microphone),
                "認証エラーが発生しました".to_string(),
            );
            assert_eq!(
                pending.lock()[0].text,
                format!("[{engine_label} Realtime エラー: 認証エラーが発生しました]")
            );
        }
    }
}
```

## 両 file の test 中身置換 (test fn 名 / `#[test]` は完全保持)

### elevenlabs_realtime_ws_task.rs 側 (`mod pending_timeout_tests` 内, E1-E5)

E1: 中身を `crate::realtime_error_helpers::test_helpers::assert_push_error_format_prefix_and_suffix_are_fixed("ElevenLabs");` に置換
E2: `crate::realtime_error_helpers::test_helpers::assert_push_error_sets_zero_timestamps_and_is_error_true("ElevenLabs");`
E3: `crate::realtime_error_helpers::test_helpers::assert_push_error_passes_source_through("ElevenLabs");`
E4: `crate::realtime_error_helpers::test_helpers::assert_push_error_clones_speaker_field("ElevenLabs");`
E5: `crate::realtime_error_helpers::test_helpers::assert_push_error_with_empty_and_multibyte_messages_preserve_text_format("ElevenLabs");`

### openai_realtime.rs 側 (`mod tests` 内, O1-O5)

O1: `crate::realtime_error_helpers::test_helpers::assert_push_error_format_prefix_and_suffix_are_fixed("OpenAI");`
O2: `crate::realtime_error_helpers::test_helpers::assert_push_error_sets_zero_timestamps_and_is_error_true("OpenAI");`
O3: `crate::realtime_error_helpers::test_helpers::assert_push_error_passes_source_through("OpenAI");`
O4: `crate::realtime_error_helpers::test_helpers::assert_push_error_clones_speaker_field("OpenAI");`
O5: `crate::realtime_error_helpers::test_helpers::assert_push_error_with_empty_and_multibyte_messages_preserve_text_format("OpenAI");`

両 file 内、`mod` 冒頭の `use` (Arc / Mutex / TranscriptionSegment / TranscriptionSource) は他 test (push_error_* 以外) で参照されているはずなので **削除しない** (使われなくなった import が出たら clippy が `unused_imports` で検出するので、その時のみ最小限削除).

## 規模超過防止

- **担当範囲外編集禁止**: `src-tauri/src/realtime_error_helpers.rs` / `src-tauri/src/elevenlabs_realtime_ws_task.rs` (`mod pending_timeout_tests` 内 push_error_* 5 件のみ) / `src-tauri/src/openai_realtime.rs` (`mod tests` 内 push_error_* 5 件のみ). 他 file 触らない.
- **test 件数の上限**: 704 件 (cargo test --lib で完全不変必須). 増減したら fault.
- **既存 test 削除禁止**: 中身を helper 呼び出しに置換するだけ. test fn 名 / `#[test]` は **完全保持**.
- 両 file の他 test (`pending_timeout_tests` の push_error 以外 / openai_realtime の handle_event_* 等) は **触らない**.

## 大型 file 注意

- `openai_realtime.rs` は 776 行. **全体 Read 禁止** = `sed -n '290,445p' src-tauri/src/openai_realtime.rs` または Read tool に offset+limit 指定で push_error_* test 範囲のみ参照.
- `elevenlabs_realtime_ws_task.rs` は 374 行 (Read 全体 OK).
- `realtime_error_helpers.rs` は 24 行 (Read 全体 OK).

## 検証 (worker 必須実行)

```fish
cd src-tauri
cargo test --lib 2>&1 | tail -30      # 704 passed 件数完全不変
cargo clippy --lib --tests -- -D warnings 2>&1 | tail -10
cargo fmt --check 2>&1 | tail -5
cargo build --lib 2>&1 | tail -10
cd ..
bash scripts/agent-verify.sh src-tauri/src/realtime_error_helpers.rs src-tauri/src/elevenlabs_realtime_ws_task.rs src-tauri/src/openai_realtime.rs
```

# 実行手順 (2-commit pattern, Loop 145-167 で 23 連続安定 = 節目 23 件突破)

## Step 1: 実装
1. `tail -10 AGENT_LOG.md` で末尾形式確認 (`---` 区切り直前を確認).
2. `realtime_error_helpers.rs` を Read (24 行).
3. `elevenlabs_realtime_ws_task.rs` を Read (374 行).
4. `openai_realtime.rs` の L290-L445 を Read で参照 (push_error_* test 範囲のみ).
5. `realtime_error_helpers.rs` に Edit で `#[cfg(test)] pub(crate) mod test_helpers { ... }` を追加 (上記 5 つの helper fn).
6. `elevenlabs_realtime_ws_task.rs` E1-E5 の中身を Edit で helper 呼び出しに置換 (test fn 名 / `#[test]` 保持).
7. `openai_realtime.rs` O1-O5 の中身を Edit で helper 呼び出しに置換 (test fn 名 / `#[test]` 保持).
8. 上記検証コマンド全て OK 確認 (cargo test 704 / clippy / fmt / build / agent-verify).

## Step 2: refactor commit
```fish
git add src-tauri/src/realtime_error_helpers.rs src-tauri/src/elevenlabs_realtime_ws_task.rs src-tauri/src/openai_realtime.rs
bash scripts/agent-commit.sh "refactor(rust): push_error_* test 10 件を realtime_error_helpers::test_helpers の 5 parameterized helper に集約 (mjc-main-20260505-83 Loop 168, rust 軸 = paradigm 系統樹拡張 5 件目 = parameterized helper 化 paradigm 反復 2 件目, elevenlabs_realtime_ws_task.rs E1-E5 + openai_realtime.rs O1-O5 の対称仕様 test を engine_label 引数化 helper で集約 + test fn 名 / #[test] 完全保持 = 件数 704 完全不変, callsite 10 件は中身を helper 呼び出し 1 行に置換 = DRY 違反解消, alternation pivot K(167) → rust(168) = 7 → 8, Loop 159 jst() fn 集約 / Loop 162 list_md_files() fn 集約 / Loop 164 audio_command_* test logic 集約 / Loop 166 build_audio_drop_event_payload_* parameterized helper 化 の paradigm 系統樹拡張 = parameterized helper 化 paradigm 反復 2 件目, AGENTS.md priority 1 寄与)" src-tauri/src/realtime_error_helpers.rs src-tauri/src/elevenlabs_realtime_ws_task.rs src-tauri/src/openai_realtime.rs
```

## Step 3: AGENT_LOG.md entry append
1. **append は 1 回のみ**. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む (Loop 144 fault precedent).
2. 末尾追記内容: 下記 entry テンプレートを使用. **冒頭に `---` を含めない** (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める). 行末空白 (trailing whitespace) 禁止.

```
[mjc-main-20260505-83 Loop 168 / 2026-05-06]
worker: mjc-worker-loop168-push-error-test-helper-consolidate (作業)
範囲: src-tauri/src/realtime_error_helpers.rs (#[cfg(test)] pub(crate) mod test_helpers 新設, 5 helper fn 追加 = ~95 行追加) + src-tauri/src/elevenlabs_realtime_ws_task.rs (E1-E5 = push_error_* test 5 件の中身を helper 呼び出し 1 行に置換, test fn 名 / #[test] 完全保持) + src-tauri/src/openai_realtime.rs (O1-O5 = push_error_* test 5 件の中身を helper 呼び出し 1 行に置換, test fn 名 / #[test] 完全保持)
内容: elevenlabs_realtime_ws_task.rs E1-E5 と openai_realtime.rs O1-O5 の push_error_* test 10 件は engine 構造に一切依存せず両 file で engine_label 違いの完全対称仕様 (elevenlabs = "ElevenLabs" 中心 / openai = "OpenAI" 中心). realtime_error_helpers.rs (= push_error の正規定義場所 = 24 行 + pub(crate) fn push_error のみ) に #[cfg(test)] pub(crate) mod test_helpers を新設し 5 つの parameterized helper fn (assert_push_error_format_prefix_and_suffix_are_fixed(engine_label) / assert_push_error_sets_zero_timestamps_and_is_error_true(engine_label) / assert_push_error_passes_source_through(engine_label) / assert_push_error_clones_speaker_field(engine_label) / assert_push_error_with_empty_and_multibyte_messages_preserve_text_format(engine_label)) を集約 + 両 file の test 10 件は test fn 名保持で中身を helper 呼び出し 1 行に置換 = test 件数 (704) 完全不変保証 + DRY 違反解消. callsite (test fn) 10 件は名前変更なし API 互換維持. paradigm 系統樹拡張 5 件目達成 (Loop 159 jst() fn 集約 → Loop 162 list_md_files() fn 集約 → Loop 164 audio_command_* test logic 集約 → Loop 166 build_audio_drop_event_payload_* parameterized helper → Loop 168 push_error_* parameterized helper = parameterized helper 化 paradigm 反復 2 件目 = paradigm 安定性実証). 既存 file 拡張軸 paradigm 10 件目 + 重複候補 grep 探索 paradigm 10 件目 + test helper 集約軸 paradigm 反復 5 件目. alternation pivot K(167) → rust(168) = 7 → 8. AGENTS.md priority 1 寄与 (DRY 解消 + helper の正規定義場所と並置).
振る舞い: cargo test (lib) 704 件件数完全不変、cargo clippy + cargo fmt + cargo build 全 OK. frontend 無変更.
verify: scripts/agent-verify.sh 全項目 OK
commits:
- <REFACTOR_HASH> refactor(rust): push_error_* test 10 件を realtime_error_helpers::test_helpers の 5 parameterized helper に集約 (paradigm 系統樹拡張 5 件目)
AGENTS.md priority: 1 (paradigm 系統樹拡張 = parameterized helper 化 paradigm 反復 2 件目 = 真の DRY 解消 + 件数完全不変両立 + 正規定義場所並置)
---
```

3. **手順**: tmpfile (例: `/tmp/loop168_entry.md`) を Write で作成 → `cat /tmp/loop168_entry.md >> AGENT_LOG.md` で append (1 回のみ) → `tail -5 AGENT_LOG.md` で末尾確認.
4. `<REFACTOR_HASH>` は git log の Step 2 commit の short hash で書き換える.

## Step 4: chore commit
```fish
git add AGENT_LOG.md
bash scripts/agent-commit.sh "chore(agent-log): mjc-main-20260505-83 Loop 168 = push_error_* test 10 件を realtime_error_helpers::test_helpers の 5 parameterized helper に集約 (rust 軸, paradigm 系統樹拡張 5 件目 = parameterized helper 化 paradigm 反復 2 件目, 既存 file 拡張軸 paradigm 10 件目 = test helper 集約軸 paradigm 反復 5 件目, elevenlabs_realtime_ws_task.rs E1-E5 + openai_realtime.rs O1-O5 の engine_label 違いの完全対称仕様 test を引数化 helper で集約, alternation pivot K(167) → rust(168) = 7 → 8, Loop 159/162/164/166 paradigm 系統樹の parameterized helper 化反復, 重複候補 grep 探索 paradigm 10 件目達成) のエントリ追記" AGENT_LOG.md
```

# 制約事項 (絶対遵守)

- AGENT_LOG.md への append は **1 回のみ**. Edit/Write を複数回繰り返さない. append が成功したら直ちに次へ進む (Loop 144 fault precedent).
- AGENT_LOG.md 先頭に絶対追記しない. 末尾の `---` 区切り直後に追記.
- entry テンプレート冒頭に `---` を含めない (前 entry の閉じ `---` を再利用、末尾 `---` 1 個で締める) (Loop 166 boundary fix precedent).
- 行末空白 (trailing whitespace) 禁止.
- `--no-verify` 禁止. `--dangerously-skip-permissions` は harness 内のみ.
- 担当範囲 (realtime_error_helpers.rs / elevenlabs_realtime_ws_task.rs の push_error_* 5 件 / openai_realtime.rs の push_error_* 5 件) 外を触らない.
- test 件数 704 完全不変必須.

# 完了基準

- 2-commit pattern (refactor + chore) で AGENT_LOG.md と code 両方が反映.
- cargo test 704 件件数完全不変 + cargo clippy + cargo fmt + cargo build 全 OK.
- agent-verify.sh 全項目 OK.
- working tree clean (untracked handoff/worker-prompts file を除く).
