あなたは作業担当エージェント (Claude Code, print mode) です。
対象は /Users/wagomu/dev/github.com/staticWagomU/meet-jerky/desktop のみです。

最初に必ず以下を読んでください:
- AGENTS.md
- docs/product-concept.md
- docs/agent-harness-claude.md
- AGENT_LOG.md は 21000+ 行 / 1.8MB+ あるので **必ず `tail -350 AGENT_LOG.md` で末尾だけ参照** してください。**Read で全体を読まないこと**。
- 末尾には `[SESSION SUMMARY @ 2026-05-04 ~18:05 JST] mjc-main-20260504-26 状況メモ` (直前セッションの 3 ループ詳細) があります。これが文脈の起点です。

# AGENT_LOG.md 追記の絶対ルール (mjc-main-21〜26 で 6 連続セッション事故ゼロ実績)
- AGENT_LOG.md は **時系列順 = 最古ログが先頭、最新ログが末尾**。
- 新規追記は **必ずファイル末尾** に行う。
- **先頭は絶対に触らない**。
- 具体手順: `tail -10 AGENT_LOG.md` で末尾の `---` 区切りを確認 → その直後に追記する。
- **行末の空白文字 (trailing whitespace) を含めない**。git --check warning を防ぐ。

# 担当範囲 (これ以外を絶対に編集しない)
- `src-tauri/src/session_manager.rs` の `mod tests` 末尾 (l.1078 直後) への test 関数 **3 件のみ** 追加
- `AGENT_LOG.md` への末尾追記 (本タスクのエントリ 1 つ)

担当範囲外 (関数本体 / 既存 test / 他ファイル / 他スクリプト) は **絶対に編集しない**。
コミットは禁止です。コミットはメインが行います。

# 規模超過防止 (重要)
- 追加する test は **3 件ちょうど**。多くも少なくもしない。
- 関数本体 (start_with_output / 他全関数) は **完全に無変更**。
- 既存 test は **完全に無変更**。
- 大型ファイル (AGENT_LOG.md / session_manager.rs) を Read で全体読まない。tail/head/grep で対象範囲のみ参照する。

# 背景 (mjc-main-20260504-27 Loop 1)
直前 commit `999fdbc` (mjc-main-26 Loop 3 = `permission_status_to_string` fail-safe 3 軸補強で 533 passed) の状態から、本 Loop は B 候補 (SessionManager::start_with_output 境界補強) を application します。

mjc-main-26 Loop 2 で `SessionManager::start` の title passthrough 3 軸を補強済 (df34f05)。本 Loop は **同関数族の `start_with_output`** に「責務分離の executable specification 化」+ 「同一関数の完全網羅戦略」パターンを application し、SessionManager 主要 5 経路 (start / start_with_output / append / finalize / discard) のうち start_with_output の output_dir 引数境界を保護する未着手軸を 1 commit で完成させます。

# 実装内容 (T1/T2/T3 の 3 件)

`src-tauri/src/session_manager.rs` の `start_with_output` シグネチャ (l.68-86) は:

```rust
pub fn start_with_output(
    &self,
    title: String,
    started_at: u64,
    output_dir: PathBuf,
    offset: FixedOffset,
) -> Result<(), SessionManagerError>
```

実装本体は output_dir に対して **一切の validation を行わない passthrough 設計** (path_for_session に直接委譲)。本 test は SessionManager 層が「output_dir の妥当性検証は呼び出し側 / OS 責務」と切り分けている契約を CI 固定する。

## T1: 空 PathBuf passthrough
```rust
#[test]
fn start_with_output_passes_empty_path_through_to_session_without_validation() {
    let manager = SessionManager::new();
    let result = manager.start_with_output(
        "title".to_string(),
        1_700_000_000,
        PathBuf::new(),
        jst(),
    );
    assert!(
        result.is_ok(),
        "SessionManager 層は output_dir の空 path に対する validation を行わない passthrough 契約: 呼び出し側責務として委譲する"
    );
    assert!(
        manager.is_active(),
        "空 path でも session は activate される: in-memory 状態と path 妥当性は独立した責務"
    );
}
```

## T2: traversal `../` 含む path passthrough
```rust
#[test]
fn start_with_output_passes_path_with_parent_traversal_through_without_sanitization() {
    let manager = SessionManager::new();
    let result = manager.start_with_output(
        "title".to_string(),
        1_700_000_000,
        PathBuf::from("../../etc"),
        jst(),
    );
    assert!(
        result.is_ok(),
        "SessionManager 層は path traversal (`../`) の sanitization を行わない passthrough 契約: traversal 検査は呼び出し側 / OS 責務"
    );
    assert!(
        manager.is_active(),
        "traversal を含む path でも session は activate される: validation 不在を CI 固定"
    );
}
```

## T3: 巨大 path (10_000 char component) passthrough
```rust
#[test]
fn start_with_output_passes_huge_path_through_to_session_without_size_limit() {
    let manager = SessionManager::new();
    let huge_component = "a".repeat(10_000);
    let result = manager.start_with_output(
        "title".to_string(),
        1_700_000_000,
        PathBuf::from(&huge_component),
        jst(),
    );
    assert!(
        result.is_ok(),
        "SessionManager 層は output_dir の長さに対する size limit を持たない passthrough 契約: size 制約は OS 責務"
    );
    assert!(
        manager.is_active(),
        "10_000 char path でも session は activate される: 将来の size limit 誤追加を遮断する装置"
    );
}
```

# 配置場所
- `src-tauri/src/session_manager.rs` の `mod tests` 末尾 = mjc-main-26 Loop 2 (df34f05) で追加された `start_passes_huge_title_through_to_session_without_truncation` の直後に追加。
- 具体的には `grep -n "start_passes_huge_title_through_to_session_without_truncation" src-tauri/src/session_manager.rs` で位置特定 → そのテスト関数の終端の `}` 直後 → ファイル末尾 `}` (mod tests 終端) より上に挿入。
- `jst()` ヘルパは既存 (mod tests 内で使用済) なので import 不要。
- `PathBuf` は既に `use` されている (l.10 周辺) ので追加 import 不要。

# 検証手順 (全て成功必須)
作業ディレクトリは `src-tauri` で実行:

1. `cd src-tauri && cargo fmt --check`
   - 差分なしを確認 (出力なし = OK)
   - 差分あれば `cargo fmt` で修正

2. `cd src-tauri && cargo clippy --lib -- -D warnings`
   - 警告ゼロを確認

3. `cd src-tauri && cargo test --lib -- --test-threads=1`
   - **533 → 536 passed** (+3 件、0 failed) を確認

# AGENT_LOG.md 追記内容 (本タスク完了後)

`tail -10 AGENT_LOG.md` で末尾の `---` を確認した上で、その直後に以下のセクションを追記してください (**ファイル末尾追記必須、行末空白禁止**):

```
## mjc-worker-start-with-output-passthrough-tests-20260504-27-1

- **開始日時 (JST)**: 2026-05-04
- **担当セッション**: mjc-worker-start-with-output-passthrough-tests-20260504-27-1
- **役割**: 作業担当 (worker, print mode)
- **セッション**: mjc-main-20260504-27 Loop 1

### 作業範囲
- `src-tauri/src/session_manager.rs` の `mod tests` 末尾への test 関数 3 件追加
- `AGENT_LOG.md` への末尾追記 (本エントリ)

### 指示内容
B 候補 (SessionManager::start_with_output 境界補強) として、output_dir 引数の passthrough 契約を CI 固定する test を 3 件追加:
- T1: 空 PathBuf passthrough
- T2: traversal `../` 含む path passthrough
- T3: 10_000 char 巨大 path passthrough

### 結果
- cargo fmt --check: <出力>
- cargo clippy --lib -- -D warnings: <出力>
- cargo test --lib -- --test-threads=1: <test result>

### 変更ファイル
- src-tauri/src/session_manager.rs (mod tests 末尾に T1/T2/T3 追加、関数本体・既存 test 無変更)
- AGENT_LOG.md (本エントリ末尾追記)

### 検証結果 (末尾 quote)
<実際のコマンド出力末尾>

### 依存関係追加
なし

### 失敗理由
なし

### 次アクション
メインへ報告、commit 待ち
---
```

# 出力フォーマット
1 ターンで完結する報告で、以下を含めてください:
- 変更したファイルとそのテスト件数
- cargo fmt / cargo clippy / cargo test の結果末尾
- AGENT_LOG.md 末尾追記が完了したことの確認 (`tail -30 AGENT_LOG.md` で末尾確認)
- 残リスク (想定なし)

以上です。
